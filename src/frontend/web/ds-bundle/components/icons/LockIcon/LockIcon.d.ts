import * as React from 'react';

/**
 * LockIcon — from web@0.0.0.
 */
export interface LockIconProps {
  [key: string]: unknown;
}

export declare const LockIcon: React.ComponentType<LockIconProps>;
