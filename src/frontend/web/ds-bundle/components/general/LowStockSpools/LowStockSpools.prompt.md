LowStockSpools from web. Use via `window.SpoolHub.LowStockSpools` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Preview

```jsx
() => <LowStockSpools spools={[]} loading={false} />
```
