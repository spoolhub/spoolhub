/* @ds-bundle: {"namespace":"SpoolHub","components":[{"name":"ActiveSpoolIcon","sourcePath":"components/icons/ActiveSpoolIcon/ActiveSpoolIcon.jsx"},{"name":"ActivityCard","sourcePath":"components/general/ActivityCard/ActivityCard.jsx"},{"name":"AddBrandModal","sourcePath":"components/general/AddBrandModal/AddBrandModal.jsx"},{"name":"AddPrinterModal","sourcePath":"components/general/AddPrinterModal/AddPrinterModal.jsx"},{"name":"AddSpoolForm","sourcePath":"components/general/AddSpoolForm/AddSpoolForm.jsx"},{"name":"AddSpoolProfileModal","sourcePath":"components/general/AddSpoolProfileModal/AddSpoolProfileModal.jsx"},{"name":"AmsConflictModal","sourcePath":"components/general/AmsConflictModal/AmsConflictModal.jsx"},{"name":"AndroidScanner","sourcePath":"components/scan/AndroidScanner/AndroidScanner.jsx"},{"name":"BrandCard","sourcePath":"components/general/BrandCard/BrandCard.jsx"},{"name":"BrandFilterBar","sourcePath":"components/general/BrandFilterBar/BrandFilterBar.jsx"},{"name":"BrandLogo","sourcePath":"components/brandcard/BrandLogo/BrandLogo.jsx"},{"name":"DarkModeToggle","sourcePath":"components/general/DarkModeToggle/DarkModeToggle.jsx"},{"name":"DesktopScanner","sourcePath":"components/scan/DesktopScanner/DesktopScanner.jsx"},{"name":"FilamentCard","sourcePath":"components/general/FilamentCard/FilamentCard.jsx"},{"name":"FilamentFilterDropdown","sourcePath":"components/general/FilamentFilterDropdown/FilamentFilterDropdown.jsx"},{"name":"FilamentIcon","sourcePath":"components/icons/FilamentIcon/FilamentIcon.jsx"},{"name":"Header","sourcePath":"components/general/Header/Header.jsx"},{"name":"InfoCircleIcon","sourcePath":"components/icons/InfoCircleIcon/InfoCircleIcon.jsx"},{"name":"InfoIcon","sourcePath":"components/icons/InfoIcon/InfoIcon.jsx"},{"name":"IphoneScanner","sourcePath":"components/scan/IphoneScanner/IphoneScanner.jsx"},{"name":"LanguageSelector","sourcePath":"components/general/LanguageSelector/LanguageSelector.jsx"},{"name":"LockIcon","sourcePath":"components/icons/LockIcon/LockIcon.jsx"},{"name":"LowStockSpools","sourcePath":"components/general/LowStockSpools/LowStockSpools.jsx"},{"name":"MaterialTag","sourcePath":"components/general/MaterialTag/MaterialTag.jsx"},{"name":"MetricCard","sourcePath":"components/general/MetricCard/MetricCard.jsx"},{"name":"NfcIcon","sourcePath":"components/icons/NfcIcon/NfcIcon.jsx"},{"name":"NfcScanModal","sourcePath":"components/general/NfcScanModal/NfcScanModal.jsx"},{"name":"Pagination","sourcePath":"components/general/Pagination/Pagination.jsx"},{"name":"PenIcon","sourcePath":"components/icons/PenIcon/PenIcon.jsx"},{"name":"PrinterIcon","sourcePath":"components/icons/PrinterIcon/PrinterIcon.jsx"},{"name":"PrinterPicker","sourcePath":"components/general/PrinterPicker/PrinterPicker.jsx"},{"name":"PrintHistoryList","sourcePath":"components/printhistory/PrintHistoryList/PrintHistoryList.jsx"},{"name":"RecentActivity","sourcePath":"components/general/RecentActivity/RecentActivity.jsx"},{"name":"ReloadIcon","sourcePath":"components/icons/ReloadIcon/ReloadIcon.jsx"},{"name":"ScanResult","sourcePath":"components/scan/ScanResult/ScanResult.jsx"},{"name":"ScanView","sourcePath":"components/general/ScanView/ScanView.jsx"},{"name":"Sidebar","sourcePath":"components/general/Sidebar/Sidebar.jsx"},{"name":"SpoolCard","sourcePath":"components/general/SpoolCard/SpoolCard.jsx"},{"name":"SpoolDetail","sourcePath":"components/general/SpoolDetail/SpoolDetail.jsx"},{"name":"SpoolDetailsForm","sourcePath":"components/general/SpoolDetailsForm/SpoolDetailsForm.jsx"},{"name":"SpoolEditor","sourcePath":"components/general/SpoolEditor/SpoolEditor.jsx"},{"name":"SpoolFilterDropdown","sourcePath":"components/general/SpoolFilterDropdown/SpoolFilterDropdown.jsx"},{"name":"SpoolHubLogo","sourcePath":"components/icons/SpoolHubLogo/SpoolHubLogo.jsx"},{"name":"SpoolIcon","sourcePath":"components/icons/SpoolIcon/SpoolIcon.jsx"},{"name":"SpoolOutlineIcon","sourcePath":"components/icons/SpoolOutlineIcon/SpoolOutlineIcon.jsx"},{"name":"SpoolScanPopup","sourcePath":"components/scan/SpoolScanPopup/SpoolScanPopup.jsx"},{"name":"SpoolSearchBar","sourcePath":"components/general/SpoolSearchBar/SpoolSearchBar.jsx"},{"name":"StatusRing","sourcePath":"components/general/StatusRing/StatusRing.jsx"},{"name":"UnassignedTagPanel","sourcePath":"components/general/UnassignedTagPanel/UnassignedTagPanel.jsx"},{"name":"UpdatesIcon","sourcePath":"components/icons/UpdatesIcon/UpdatesIcon.jsx"},{"name":"UsbOffIcon","sourcePath":"components/icons/UsbOffIcon/UsbOffIcon.jsx"}],"sourceHashes":{"components/icons/ActiveSpoolIcon/ActiveSpoolIcon.jsx":"4b91365699cc","components/icons/ActiveSpoolIcon/ActiveSpoolIcon.d.ts":"a9126dc1a3d5","components/icons/ActiveSpoolIcon/ActiveSpoolIcon.prompt.md":"b1647d0b4df3","components/general/ActivityCard/ActivityCard.jsx":"5ae269a98156","components/general/ActivityCard/ActivityCard.d.ts":"22e460ea1b0f","components/general/ActivityCard/ActivityCard.prompt.md":"28c32a1a387b","components/general/AddBrandModal/AddBrandModal.jsx":"73e13a44b2ac","components/general/AddBrandModal/AddBrandModal.d.ts":"9c23570ebe15","components/general/AddBrandModal/AddBrandModal.prompt.md":"f8a7d38e2653","components/general/AddPrinterModal/AddPrinterModal.jsx":"1473afb43286","components/general/AddPrinterModal/AddPrinterModal.d.ts":"9ed0c81ffd80","components/general/AddPrinterModal/AddPrinterModal.prompt.md":"85be21d58f05","components/general/AddSpoolForm/AddSpoolForm.jsx":"3235a25439a8","components/general/AddSpoolForm/AddSpoolForm.d.ts":"d8bd588f7d88","components/general/AddSpoolForm/AddSpoolForm.prompt.md":"14274f55a729","components/general/AddSpoolProfileModal/AddSpoolProfileModal.jsx":"7281fd326143","components/general/AddSpoolProfileModal/AddSpoolProfileModal.d.ts":"95192f8c584e","components/general/AddSpoolProfileModal/AddSpoolProfileModal.prompt.md":"9d08854a4f03","components/general/AmsConflictModal/AmsConflictModal.jsx":"8426f9781b57","components/general/AmsConflictModal/AmsConflictModal.d.ts":"86ebaf617298","components/general/AmsConflictModal/AmsConflictModal.prompt.md":"8197dce44056","components/scan/AndroidScanner/AndroidScanner.jsx":"84ec6eaee980","components/scan/AndroidScanner/AndroidScanner.d.ts":"ae20f990b152","components/scan/AndroidScanner/AndroidScanner.prompt.md":"2bc82ff2e763","components/general/BrandCard/BrandCard.jsx":"1ed8f6d283fc","components/general/BrandCard/BrandCard.d.ts":"1de9f9c4cb06","components/general/BrandCard/BrandCard.prompt.md":"ff131889042f","components/general/BrandFilterBar/BrandFilterBar.jsx":"e0226d36bc85","components/general/BrandFilterBar/BrandFilterBar.d.ts":"2844bdcee99c","components/general/BrandFilterBar/BrandFilterBar.prompt.md":"e029c135f713","components/brandcard/BrandLogo/BrandLogo.jsx":"0bed5f992043","components/brandcard/BrandLogo/BrandLogo.d.ts":"ac6eaf9096cf","components/brandcard/BrandLogo/BrandLogo.prompt.md":"9254d8616b56","components/general/DarkModeToggle/DarkModeToggle.jsx":"0ac85572e000","components/general/DarkModeToggle/DarkModeToggle.d.ts":"f8d274f85d1b","components/general/DarkModeToggle/DarkModeToggle.prompt.md":"ccabbb21ed10","components/scan/DesktopScanner/DesktopScanner.jsx":"0f923bfee8c2","components/scan/DesktopScanner/DesktopScanner.d.ts":"64a286d4c507","components/scan/DesktopScanner/DesktopScanner.prompt.md":"a757006090b2","components/general/FilamentCard/FilamentCard.jsx":"8ce0a7220c02","components/general/FilamentCard/FilamentCard.d.ts":"b661bd8f8836","components/general/FilamentCard/FilamentCard.prompt.md":"9498ce3dbde5","components/general/FilamentFilterDropdown/FilamentFilterDropdown.jsx":"a53bdf9760b8","components/general/FilamentFilterDropdown/FilamentFilterDropdown.d.ts":"613992d925c0","components/general/FilamentFilterDropdown/FilamentFilterDropdown.prompt.md":"2b65dbf8a6df","components/icons/FilamentIcon/FilamentIcon.jsx":"0a4b563c8cc1","components/icons/FilamentIcon/FilamentIcon.d.ts":"64fca42b4582","components/icons/FilamentIcon/FilamentIcon.prompt.md":"8d39cd5894cb","components/general/Header/Header.jsx":"5c4d79c33ca2","components/general/Header/Header.d.ts":"64ff6a9cb19f","components/general/Header/Header.prompt.md":"7afa8fe208a7","components/icons/InfoCircleIcon/InfoCircleIcon.jsx":"a940eabd59f1","components/icons/InfoCircleIcon/InfoCircleIcon.d.ts":"26f387bd3f44","components/icons/InfoCircleIcon/InfoCircleIcon.prompt.md":"5ac646f72476","components/icons/InfoIcon/InfoIcon.jsx":"e0b3c36b0ab1","components/icons/InfoIcon/InfoIcon.d.ts":"89c176f7fddb","components/icons/InfoIcon/InfoIcon.prompt.md":"d29d6949b2ff","components/scan/IphoneScanner/IphoneScanner.jsx":"247f66ee79d9","components/scan/IphoneScanner/IphoneScanner.d.ts":"90b71b258eab","components/scan/IphoneScanner/IphoneScanner.prompt.md":"267257ae4c02","components/general/LanguageSelector/LanguageSelector.jsx":"cbeb56190597","components/general/LanguageSelector/LanguageSelector.d.ts":"91a2d5ebe973","components/general/LanguageSelector/LanguageSelector.prompt.md":"8fa4450236e1","components/icons/LockIcon/LockIcon.jsx":"44b0e075ad0e","components/icons/LockIcon/LockIcon.d.ts":"f85fee30c1a6","components/icons/LockIcon/LockIcon.prompt.md":"a5b48e7f623e","components/general/LowStockSpools/LowStockSpools.jsx":"6e463766864e","components/general/LowStockSpools/LowStockSpools.d.ts":"fa654f4e0b3b","components/general/LowStockSpools/LowStockSpools.prompt.md":"bc954f1e459e","components/general/MaterialTag/MaterialTag.jsx":"9bb2a9a26805","components/general/MaterialTag/MaterialTag.d.ts":"62e619f996ef","components/general/MaterialTag/MaterialTag.prompt.md":"f7c9efd7b6d2","components/general/MetricCard/MetricCard.jsx":"caf5a0898433","components/general/MetricCard/MetricCard.d.ts":"7d2b759bc43f","components/general/MetricCard/MetricCard.prompt.md":"9db21ea8be73","components/icons/NfcIcon/NfcIcon.jsx":"1047b87c491a","components/icons/NfcIcon/NfcIcon.d.ts":"151692e4e913","components/icons/NfcIcon/NfcIcon.prompt.md":"f752d89cc385","components/general/NfcScanModal/NfcScanModal.jsx":"9589b10c6c9b","components/general/NfcScanModal/NfcScanModal.d.ts":"0c4cba154e51","components/general/NfcScanModal/NfcScanModal.prompt.md":"e45ada13fa63","components/general/Pagination/Pagination.jsx":"61a545dccac9","components/general/Pagination/Pagination.d.ts":"fc0742144dfd","components/general/Pagination/Pagination.prompt.md":"0a99ee34db63","components/icons/PenIcon/PenIcon.jsx":"ca152fefa264","components/icons/PenIcon/PenIcon.d.ts":"d590465669a7","components/icons/PenIcon/PenIcon.prompt.md":"bb4f1e069465","components/icons/PrinterIcon/PrinterIcon.jsx":"e6adab8eebd4","components/icons/PrinterIcon/PrinterIcon.d.ts":"e9f0c5971074","components/icons/PrinterIcon/PrinterIcon.prompt.md":"7eb47eebed76","components/general/PrinterPicker/PrinterPicker.jsx":"654859eb76bc","components/general/PrinterPicker/PrinterPicker.d.ts":"d0b8d86db77c","components/general/PrinterPicker/PrinterPicker.prompt.md":"a0ae9410900f","components/printhistory/PrintHistoryList/PrintHistoryList.jsx":"d1de91b324ac","components/printhistory/PrintHistoryList/PrintHistoryList.d.ts":"d76df369d282","components/printhistory/PrintHistoryList/PrintHistoryList.prompt.md":"f6c3d3bac912","components/general/RecentActivity/RecentActivity.jsx":"db4272c8fba4","components/general/RecentActivity/RecentActivity.d.ts":"1cc5f7bac5af","components/general/RecentActivity/RecentActivity.prompt.md":"ea108057c59c","components/icons/ReloadIcon/ReloadIcon.jsx":"a52090ad7299","components/icons/ReloadIcon/ReloadIcon.d.ts":"8a79bda48811","components/icons/ReloadIcon/ReloadIcon.prompt.md":"9e964357be8d","components/scan/ScanResult/ScanResult.jsx":"2d2a36379593","components/scan/ScanResult/ScanResult.d.ts":"f02b20bdb3af","components/scan/ScanResult/ScanResult.prompt.md":"f2568f7b87fe","components/general/ScanView/ScanView.jsx":"99a3299df557","components/general/ScanView/ScanView.d.ts":"8a36d426d68d","components/general/ScanView/ScanView.prompt.md":"f449e0a54306","components/general/Sidebar/Sidebar.jsx":"2003ae3c83e4","components/general/Sidebar/Sidebar.d.ts":"a0939b2554cc","components/general/Sidebar/Sidebar.prompt.md":"81dda4d806dd","components/general/SpoolCard/SpoolCard.jsx":"3f8b65a327d6","components/general/SpoolCard/SpoolCard.d.ts":"5387017dc5d7","components/general/SpoolCard/SpoolCard.prompt.md":"2896bbf68e3b","components/general/SpoolDetail/SpoolDetail.jsx":"9dd877718963","components/general/SpoolDetail/SpoolDetail.d.ts":"47a2a9b0f9b1","components/general/SpoolDetail/SpoolDetail.prompt.md":"5817bc545cf4","components/general/SpoolDetailsForm/SpoolDetailsForm.jsx":"847f1d758844","components/general/SpoolDetailsForm/SpoolDetailsForm.d.ts":"f3f93f7d501f","components/general/SpoolDetailsForm/SpoolDetailsForm.prompt.md":"4f91b09c1322","components/general/SpoolEditor/SpoolEditor.jsx":"f72304a2cc08","components/general/SpoolEditor/SpoolEditor.d.ts":"baa46fcba41c","components/general/SpoolEditor/SpoolEditor.prompt.md":"6f2dc1b77ac3","components/general/SpoolFilterDropdown/SpoolFilterDropdown.jsx":"cba30a08efe1","components/general/SpoolFilterDropdown/SpoolFilterDropdown.d.ts":"a9a99405f8f6","components/general/SpoolFilterDropdown/SpoolFilterDropdown.prompt.md":"8c81623343b8","components/icons/SpoolHubLogo/SpoolHubLogo.jsx":"ea14ce10b0c3","components/icons/SpoolHubLogo/SpoolHubLogo.d.ts":"5d0917ffa5c4","components/icons/SpoolHubLogo/SpoolHubLogo.prompt.md":"ce180ce918b7","components/icons/SpoolIcon/SpoolIcon.jsx":"81f85ce951c6","components/icons/SpoolIcon/SpoolIcon.d.ts":"9b45d9181c4c","components/icons/SpoolIcon/SpoolIcon.prompt.md":"f3922ef5bc28","components/icons/SpoolOutlineIcon/SpoolOutlineIcon.jsx":"8b2aabd09eb1","components/icons/SpoolOutlineIcon/SpoolOutlineIcon.d.ts":"a654c9777512","components/icons/SpoolOutlineIcon/SpoolOutlineIcon.prompt.md":"4ac4a65672d5","components/scan/SpoolScanPopup/SpoolScanPopup.jsx":"bcb3e9c645d2","components/scan/SpoolScanPopup/SpoolScanPopup.d.ts":"d4be48987bf7","components/scan/SpoolScanPopup/SpoolScanPopup.prompt.md":"d96581282850","components/general/SpoolSearchBar/SpoolSearchBar.jsx":"34add7f55158","components/general/SpoolSearchBar/SpoolSearchBar.d.ts":"be0f218f1c4b","components/general/SpoolSearchBar/SpoolSearchBar.prompt.md":"175bba41e93e","components/general/StatusRing/StatusRing.jsx":"616a3b5d2a68","components/general/StatusRing/StatusRing.d.ts":"161efe43b686","components/general/StatusRing/StatusRing.prompt.md":"427f67df8337","components/general/UnassignedTagPanel/UnassignedTagPanel.jsx":"a0f80b0400f7","components/general/UnassignedTagPanel/UnassignedTagPanel.d.ts":"0d5e491fd338","components/general/UnassignedTagPanel/UnassignedTagPanel.prompt.md":"6c0d97c02a8b","components/icons/UpdatesIcon/UpdatesIcon.jsx":"61c9671ea953","components/icons/UpdatesIcon/UpdatesIcon.d.ts":"4bcaa66db16b","components/icons/UpdatesIcon/UpdatesIcon.prompt.md":"cf177bb48e0a","components/icons/UsbOffIcon/UsbOffIcon.jsx":"811a01706f4e","components/icons/UsbOffIcon/UsbOffIcon.d.ts":"ee28e080d711","components/icons/UsbOffIcon/UsbOffIcon.prompt.md":"127d8ec843ed"},"inlinedExternals":["@microsoft/signalr","axios","cookie","flag-icons","html-parse-stringify","i18next","react-i18next","react-router","react-router-dom","set-cookie-parser","use-sync-external-store","void-elements"],"builtBy":"cc-design-sync"} */
var __import_meta_env__={"VITE_API_URL":"","DEV":true,"PROD":false,"SSR":false,"MODE":"development"};
var SpoolHub = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
    get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
  }) : x)(function(x) {
    if (typeof require !== "undefined") return require.apply(this, arguments);
    throw Error('Dynamic require of "' + x + '" is not supported');
  });
  var __commonJS = (cb, mod) => function __require2() {
    try {
      return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
    } catch (e2) {
      throw mod = 0, e2;
    }
  };
  var __export = (target, all3) => {
    for (var name in all3)
      __defProp(target, name, { get: all3[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // shim:react-shim
  var require_react_shim = __commonJS({
    "shim:react-shim"(exports, module) {
      var R = window.React;
      function jsx55(t2, p, k) {
        return R.createElement(t2, k === void 0 ? p : Object.assign({ key: k }, p));
      }
      module.exports = R;
      module.exports.jsx = jsx55;
      module.exports.jsxs = jsx55;
      module.exports.jsxDEV = jsx55;
      module.exports.Fragment = R.Fragment;
    }
  });

  // shim:react-dom-shim
  var require_react_dom_shim = __commonJS({
    "shim:react-dom-shim"(exports, module) {
      var D = window.ReactDOM;
      var n = function() {
      };
      module.exports = Object.assign({ preload: n, preinit: n, preconnect: n, prefetchDNS: n, preloadModule: n, preinitModule: n }, D);
    }
  });

  // node_modules/void-elements/index.js
  var require_void_elements = __commonJS({
    "node_modules/void-elements/index.js"(exports, module) {
      module.exports = {
        "area": true,
        "base": true,
        "br": true,
        "col": true,
        "embed": true,
        "hr": true,
        "img": true,
        "input": true,
        "link": true,
        "meta": true,
        "param": true,
        "source": true,
        "track": true,
        "wbr": true
      };
    }
  });

  // node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
  var require_use_sync_external_store_shim_development = __commonJS({
    "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js"(exports) {
      "use strict";
      (function() {
        function is(x, y) {
          return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
        }
        function useSyncExternalStore$2(subscribe, getSnapshot) {
          didWarnOld18Alpha || void 0 === React14.startTransition || (didWarnOld18Alpha = true, console.error(
            "You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release."
          ));
          var value = getSnapshot();
          if (!didWarnUncachedGetSnapshot) {
            var cachedValue = getSnapshot();
            objectIs(value, cachedValue) || (console.error(
              "The result of getSnapshot should be cached to avoid an infinite loop"
            ), didWarnUncachedGetSnapshot = true);
          }
          cachedValue = useState33({
            inst: { value, getSnapshot }
          });
          var inst = cachedValue[0].inst, forceUpdate = cachedValue[1];
          useLayoutEffect4(
            function() {
              inst.value = value;
              inst.getSnapshot = getSnapshot;
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            },
            [subscribe, value, getSnapshot]
          );
          useEffect29(
            function() {
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
              return subscribe(function() {
                checkIfSnapshotChanged(inst) && forceUpdate({ inst });
              });
            },
            [subscribe]
          );
          useDebugValue(value);
          return value;
        }
        function checkIfSnapshotChanged(inst) {
          var latestGetSnapshot = inst.getSnapshot;
          inst = inst.value;
          try {
            var nextValue = latestGetSnapshot();
            return !objectIs(inst, nextValue);
          } catch (error) {
            return true;
          }
        }
        function useSyncExternalStore$1(subscribe, getSnapshot) {
          return getSnapshot();
        }
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
        var React14 = require_react_shim(), objectIs = "function" === typeof Object.is ? Object.is : is, useState33 = React14.useState, useEffect29 = React14.useEffect, useLayoutEffect4 = React14.useLayoutEffect, useDebugValue = React14.useDebugValue, didWarnOld18Alpha = false, didWarnUncachedGetSnapshot = false, shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$1 : useSyncExternalStore$2;
        exports.useSyncExternalStore = void 0 !== React14.useSyncExternalStore ? React14.useSyncExternalStore : shim;
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
      })();
    }
  });

  // node_modules/use-sync-external-store/shim/index.js
  var require_shim = __commonJS({
    "node_modules/use-sync-external-store/shim/index.js"(exports, module) {
      "use strict";
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_use_sync_external_store_shim_development();
      }
    }
  });

  // src/components/index.ts
  var index_exports = {};
  __export(index_exports, {
    ActiveSpoolIcon: () => ActiveSpoolIcon,
    ActivityCard: () => ActivityCard,
    AddBrandModal: () => AddBrandModal,
    AddPrinterModal: () => AddPrinterModal,
    AddSpoolForm: () => AddSpoolForm,
    AddSpoolProfileModal: () => AddSpoolProfileModal,
    AmsConflictModal: () => AmsConflictModal,
    AndroidScanner: () => AndroidScanner,
    BrandCard: () => BrandCard,
    BrandFilterBar: () => BrandFilterBar,
    BrandLogo: () => BrandLogo,
    ConnectionBanner: () => ConnectionBanner,
    DarkModeToggle: () => DarkModeToggle,
    DesktopScanner: () => DesktopScanner,
    FilamentCard: () => FilamentCard,
    FilamentFilterDropdown: () => FilamentFilterDropdown,
    FilamentIcon: () => FilamentIcon,
    Header: () => Header,
    InfoCircleIcon: () => InfoCircleIcon,
    InfoIcon: () => InfoIcon,
    IphoneScanner: () => IphoneScanner,
    LanguageSelector: () => LanguageSelector,
    LockIcon: () => LockIcon,
    LowStockSpools: () => LowStockSpools,
    MaterialTag: () => MaterialTag,
    MetricCard: () => MetricCard,
    NfcIcon: () => NfcIcon,
    NfcScanModal: () => NfcScanModal,
    Pagination: () => Pagination,
    PenIcon: () => PenIcon,
    PreviewProvider: () => PreviewProvider,
    PrintHistoryList: () => PrintHistoryList,
    PrinterCard: () => PrinterCard,
    PrinterIcon: () => PrinterIcon,
    PrinterPicker: () => PrinterPicker,
    RecentActivity: () => RecentActivity,
    ReloadIcon: () => ReloadIcon,
    ScanResult: () => ScanResult,
    ScanView: () => ScanView,
    Sidebar: () => Sidebar,
    SpoolCard: () => SpoolCard,
    SpoolDetail: () => SpoolDetail,
    SpoolDetailsForm: () => SpoolDetailsForm,
    SpoolEditor: () => SpoolEditor,
    SpoolFilterDropdown: () => SpoolFilterDropdown,
    SpoolHubLogo: () => SpoolHubLogo,
    SpoolIcon: () => SpoolIcon,
    SpoolOutlineIcon: () => SpoolOutlineIcon,
    SpoolScanPopup: () => SpoolScanPopup,
    SpoolSearchBar: () => SpoolSearchBar,
    StatusRing: () => StatusRing,
    UnassignedTagPanel: () => UnassignedTagPanel,
    UpdatesIcon: () => UpdatesIcon,
    UsbOffIcon: () => UsbOffIcon
  });

  // node_modules/react-router/dist/development/chunk-QUQL4437.mjs
  var React = __toESM(require_react_shim(), 1);
  var React2 = __toESM(require_react_shim(), 1);
  var React3 = __toESM(require_react_shim(), 1);
  var React4 = __toESM(require_react_shim(), 1);
  var React9 = __toESM(require_react_shim(), 1);
  var React8 = __toESM(require_react_shim(), 1);
  var React7 = __toESM(require_react_shim(), 1);
  var React6 = __toESM(require_react_shim(), 1);
  var React5 = __toESM(require_react_shim(), 1);
  var React10 = __toESM(require_react_shim(), 1);
  var React11 = __toESM(require_react_shim(), 1);
  var import_meta = {};
  var PopStateEventType = "popstate";
  function isLocation(obj) {
    return typeof obj === "object" && obj != null && "pathname" in obj && "search" in obj && "hash" in obj && "state" in obj && "key" in obj;
  }
  function createBrowserHistory(options = {}) {
    function createBrowserLocation(window2, globalHistory) {
      let maskedLocation = globalHistory.state?.masked;
      let { pathname, search, hash } = maskedLocation || window2.location;
      return createLocation(
        "",
        { pathname, search, hash },
        // state defaults to `null` because `window.history.state` does
        globalHistory.state && globalHistory.state.usr || null,
        globalHistory.state && globalHistory.state.key || "default",
        maskedLocation ? {
          pathname: window2.location.pathname,
          search: window2.location.search,
          hash: window2.location.hash
        } : void 0
      );
    }
    function createBrowserHref(window2, to) {
      return typeof to === "string" ? to : createPath(to);
    }
    return getUrlBasedHistory(
      createBrowserLocation,
      createBrowserHref,
      null,
      options
    );
  }
  function invariant(value, message) {
    if (value === false || value === null || typeof value === "undefined") {
      throw new Error(message);
    }
  }
  function warning(cond, message) {
    if (!cond) {
      if (typeof console !== "undefined") console.warn(message);
      try {
        throw new Error(message);
      } catch (e2) {
      }
    }
  }
  function createKey() {
    return Math.random().toString(36).substring(2, 10);
  }
  function getHistoryState(location, index) {
    return {
      usr: location.state,
      key: location.key,
      idx: index,
      masked: location.mask ? {
        pathname: location.pathname,
        search: location.search,
        hash: location.hash
      } : void 0
    };
  }
  function createLocation(current, to, state = null, key, mask) {
    let location = {
      pathname: typeof current === "string" ? current : current.pathname,
      search: "",
      hash: "",
      ...typeof to === "string" ? parsePath(to) : to,
      state,
      // TODO: This could be cleaned up.  push/replace should probably just take
      // full Locations now and avoid the need to run through this flow at all
      // But that's a pretty big refactor to the current test suite so going to
      // keep as is for the time being and just let any incoming keys take precedence
      key: to && to.key || key || createKey(),
      mask
    };
    return location;
  }
  function createPath({
    pathname = "/",
    search = "",
    hash = ""
  }) {
    if (search && search !== "?")
      pathname += search.charAt(0) === "?" ? search : "?" + search;
    if (hash && hash !== "#")
      pathname += hash.charAt(0) === "#" ? hash : "#" + hash;
    return pathname;
  }
  function parsePath(path) {
    let parsedPath = {};
    if (path) {
      let hashIndex = path.indexOf("#");
      if (hashIndex >= 0) {
        parsedPath.hash = path.substring(hashIndex);
        path = path.substring(0, hashIndex);
      }
      let searchIndex = path.indexOf("?");
      if (searchIndex >= 0) {
        parsedPath.search = path.substring(searchIndex);
        path = path.substring(0, searchIndex);
      }
      if (path) {
        parsedPath.pathname = path;
      }
    }
    return parsedPath;
  }
  function getUrlBasedHistory(getLocation, createHref2, validateLocation, options = {}) {
    let { window: window2 = document.defaultView, v5Compat = false } = options;
    let globalHistory = window2.history;
    let action = "POP";
    let listener = null;
    let index = getIndex();
    if (index == null) {
      index = 0;
      globalHistory.replaceState({ ...globalHistory.state, idx: index }, "");
    }
    function getIndex() {
      let state = globalHistory.state || { idx: null };
      return state.idx;
    }
    function handlePop() {
      action = "POP";
      let nextIndex = getIndex();
      let delta = nextIndex == null ? null : nextIndex - index;
      index = nextIndex;
      if (listener) {
        listener({ action, location: history.location, delta });
      }
    }
    function push(to, state) {
      action = "PUSH";
      let location = isLocation(to) ? to : createLocation(history.location, to, state);
      if (validateLocation) validateLocation(location, to);
      index = getIndex() + 1;
      let historyState = getHistoryState(location, index);
      let url = history.createHref(location.mask || location);
      try {
        globalHistory.pushState(historyState, "", url);
      } catch (error) {
        if (error instanceof DOMException && error.name === "DataCloneError") {
          throw error;
        }
        window2.location.assign(url);
      }
      if (v5Compat && listener) {
        listener({ action, location: history.location, delta: 1 });
      }
    }
    function replace2(to, state) {
      action = "REPLACE";
      let location = isLocation(to) ? to : createLocation(history.location, to, state);
      if (validateLocation) validateLocation(location, to);
      index = getIndex();
      let historyState = getHistoryState(location, index);
      let url = history.createHref(location.mask || location);
      globalHistory.replaceState(historyState, "", url);
      if (v5Compat && listener) {
        listener({ action, location: history.location, delta: 0 });
      }
    }
    function createURL(to) {
      return createBrowserURLImpl(window2, to);
    }
    let history = {
      get action() {
        return action;
      },
      get location() {
        return getLocation(window2, globalHistory);
      },
      listen(fn) {
        if (listener) {
          throw new Error("A history only accepts one active listener");
        }
        window2.addEventListener(PopStateEventType, handlePop);
        listener = fn;
        return () => {
          window2.removeEventListener(PopStateEventType, handlePop);
          listener = null;
        };
      },
      createHref(to) {
        return createHref2(window2, to);
      },
      createURL,
      encodeLocation(to) {
        let url = createURL(to);
        return {
          pathname: url.pathname,
          search: url.search,
          hash: url.hash
        };
      },
      push,
      replace: replace2,
      go(n) {
        return globalHistory.go(n);
      }
    };
    return history;
  }
  function createBrowserURLImpl(windowImpl, to, isAbsolute = false) {
    let base = "http://localhost";
    if (windowImpl) {
      base = windowImpl.location.origin !== "null" ? windowImpl.location.origin : windowImpl.location.href;
    }
    invariant(base, "No window.location.(origin|href) available to create URL");
    let href = typeof to === "string" ? to : createPath(to);
    href = href.replace(/ $/, "%20");
    if (!isAbsolute && href.startsWith("//")) {
      href = base + href;
    }
    return new URL(href, base);
  }
  var _map;
  _map = /* @__PURE__ */ new WeakMap();
  function matchRoutes(routes, locationArg, basename = "/") {
    return matchRoutesImpl(routes, locationArg, basename, false);
  }
  function matchRoutesImpl(routes, locationArg, basename, allowPartial, precomputedBranches) {
    let location = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
    let pathname = stripBasename(location.pathname || "/", basename);
    if (pathname == null) {
      return null;
    }
    let branches = precomputedBranches ?? flattenAndRankRoutes(routes);
    let matches = null;
    let decoded = decodePath(pathname);
    for (let i = 0; matches == null && i < branches.length; ++i) {
      matches = matchRouteBranch(
        branches[i],
        decoded,
        allowPartial
      );
    }
    return matches;
  }
  function convertRouteMatchToUiMatch(match, loaderData) {
    let { route, pathname, params } = match;
    return {
      id: route.id,
      pathname,
      params,
      data: loaderData[route.id],
      loaderData: loaderData[route.id],
      handle: route.handle
    };
  }
  function flattenAndRankRoutes(routes) {
    let branches = flattenRoutes(routes);
    rankRouteBranches(branches);
    return branches;
  }
  function flattenRoutes(routes, branches = [], parentsMeta = [], parentPath = "", _hasParentOptionalSegments = false) {
    let flattenRoute = (route, index, hasParentOptionalSegments = _hasParentOptionalSegments, relativePath) => {
      let meta = {
        relativePath: relativePath === void 0 ? route.path || "" : relativePath,
        caseSensitive: route.caseSensitive === true,
        childrenIndex: index,
        route
      };
      if (meta.relativePath.startsWith("/")) {
        if (!meta.relativePath.startsWith(parentPath) && hasParentOptionalSegments) {
          return;
        }
        invariant(
          meta.relativePath.startsWith(parentPath),
          `Absolute route path "${meta.relativePath}" nested under path "${parentPath}" is not valid. An absolute child route path must start with the combined path of all its parent routes.`
        );
        meta.relativePath = meta.relativePath.slice(parentPath.length);
      }
      let path = joinPaths([parentPath, meta.relativePath]);
      let routesMeta = parentsMeta.concat(meta);
      if (route.children && route.children.length > 0) {
        invariant(
          // Our types know better, but runtime JS may not!
          // @ts-expect-error
          route.index !== true,
          `Index routes must not have child routes. Please remove all child routes from route path "${path}".`
        );
        flattenRoutes(
          route.children,
          branches,
          routesMeta,
          path,
          hasParentOptionalSegments
        );
      }
      if (route.path == null && !route.index) {
        return;
      }
      branches.push({
        path,
        score: computeScore(path, route.index),
        routesMeta
      });
    };
    routes.forEach((route, index) => {
      if (route.path === "" || !route.path?.includes("?")) {
        flattenRoute(route, index);
      } else {
        for (let exploded of explodeOptionalSegments(route.path)) {
          flattenRoute(route, index, true, exploded);
        }
      }
    });
    return branches;
  }
  function explodeOptionalSegments(path) {
    let segments = path.split("/");
    if (segments.length === 0) return [];
    let [first, ...rest] = segments;
    let isOptional = first.endsWith("?");
    let required = first.replace(/\?$/, "");
    if (rest.length === 0) {
      return isOptional ? [required, ""] : [required];
    }
    let restExploded = explodeOptionalSegments(rest.join("/"));
    let result = [];
    result.push(
      ...restExploded.map(
        (subpath) => subpath === "" ? required : [required, subpath].join("/")
      )
    );
    if (isOptional) {
      result.push(...restExploded);
    }
    return result.map(
      (exploded) => path.startsWith("/") && exploded === "" ? "/" : exploded
    );
  }
  function rankRouteBranches(branches) {
    branches.sort(
      (a, b) => a.score !== b.score ? b.score - a.score : compareIndexes(
        a.routesMeta.map((meta) => meta.childrenIndex),
        b.routesMeta.map((meta) => meta.childrenIndex)
      )
    );
  }
  var paramRe = /^:[\w-]+$/;
  var dynamicSegmentValue = 3;
  var indexRouteValue = 2;
  var emptySegmentValue = 1;
  var staticSegmentValue = 10;
  var splatPenalty = -2;
  var isSplat = (s) => s === "*";
  function computeScore(path, index) {
    let segments = path.split("/");
    let initialScore = segments.length;
    if (segments.some(isSplat)) {
      initialScore += splatPenalty;
    }
    if (index) {
      initialScore += indexRouteValue;
    }
    return segments.filter((s) => !isSplat(s)).reduce(
      (score, segment) => score + (paramRe.test(segment) ? dynamicSegmentValue : segment === "" ? emptySegmentValue : staticSegmentValue),
      initialScore
    );
  }
  function compareIndexes(a, b) {
    let siblings = a.length === b.length && a.slice(0, -1).every((n, i) => n === b[i]);
    return siblings ? (
      // If two routes are siblings, we should try to match the earlier sibling
      // first. This allows people to have fine-grained control over the matching
      // behavior by simply putting routes with identical paths in the order they
      // want them tried.
      a[a.length - 1] - b[b.length - 1]
    ) : (
      // Otherwise, it doesn't really make sense to rank non-siblings by index,
      // so they sort equally.
      0
    );
  }
  function matchRouteBranch(branch, pathname, allowPartial = false) {
    let { routesMeta } = branch;
    let matchedParams = {};
    let matchedPathname = "/";
    let matches = [];
    for (let i = 0; i < routesMeta.length; ++i) {
      let meta = routesMeta[i];
      let end = i === routesMeta.length - 1;
      let remainingPathname = matchedPathname === "/" ? pathname : pathname.slice(matchedPathname.length) || "/";
      let match = matchPath(
        { path: meta.relativePath, caseSensitive: meta.caseSensitive, end },
        remainingPathname
      );
      let route = meta.route;
      if (!match && end && allowPartial && !routesMeta[routesMeta.length - 1].route.index) {
        match = matchPath(
          {
            path: meta.relativePath,
            caseSensitive: meta.caseSensitive,
            end: false
          },
          remainingPathname
        );
      }
      if (!match) {
        return null;
      }
      Object.assign(matchedParams, match.params);
      matches.push({
        // TODO: Can this as be avoided?
        params: matchedParams,
        pathname: joinPaths([matchedPathname, match.pathname]),
        pathnameBase: normalizePathname(
          joinPaths([matchedPathname, match.pathnameBase])
        ),
        route
      });
      if (match.pathnameBase !== "/") {
        matchedPathname = joinPaths([matchedPathname, match.pathnameBase]);
      }
    }
    return matches;
  }
  function matchPath(pattern, pathname) {
    if (typeof pattern === "string") {
      pattern = { path: pattern, caseSensitive: false, end: true };
    }
    let [matcher, compiledParams] = compilePath(
      pattern.path,
      pattern.caseSensitive,
      pattern.end
    );
    let match = pathname.match(matcher);
    if (!match) return null;
    let matchedPathname = match[0];
    let pathnameBase = matchedPathname.replace(/(.)\/+$/, "$1");
    let captureGroups = match.slice(1);
    let params = compiledParams.reduce(
      (memo2, { paramName, isOptional }, index) => {
        if (paramName === "*") {
          let splatValue = captureGroups[index] || "";
          pathnameBase = matchedPathname.slice(0, matchedPathname.length - splatValue.length).replace(/(.)\/+$/, "$1");
        }
        const value = captureGroups[index];
        if (isOptional && !value) {
          memo2[paramName] = void 0;
        } else {
          memo2[paramName] = (value || "").replace(/%2F/g, "/");
        }
        return memo2;
      },
      {}
    );
    return {
      params,
      pathname: matchedPathname,
      pathnameBase,
      pattern
    };
  }
  function compilePath(path, caseSensitive = false, end = true) {
    warning(
      path === "*" || !path.endsWith("*") || path.endsWith("/*"),
      `Route path "${path}" will be treated as if it were "${path.replace(/\*$/, "/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${path.replace(/\*$/, "/*")}".`
    );
    let params = [];
    let regexpSource = "^" + path.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(
      /\/:([\w-]+)(\?)?/g,
      (match, paramName, isOptional, index, str) => {
        params.push({ paramName, isOptional: isOptional != null });
        if (isOptional) {
          let nextChar = str.charAt(index + match.length);
          if (nextChar && nextChar !== "/") {
            return "/([^\\/]*)";
          }
          return "(?:/([^\\/]*))?";
        }
        return "/([^\\/]+)";
      }
    ).replace(/\/([\w-]+)\?(\/|$)/g, "(/$1)?$2");
    if (path.endsWith("*")) {
      params.push({ paramName: "*" });
      regexpSource += path === "*" || path === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$";
    } else if (end) {
      regexpSource += "\\/*$";
    } else if (path !== "" && path !== "/") {
      regexpSource += "(?:(?=\\/|$))";
    } else {
    }
    let matcher = new RegExp(regexpSource, caseSensitive ? void 0 : "i");
    return [matcher, params];
  }
  function decodePath(value) {
    try {
      return value.split("/").map((v) => decodeURIComponent(v).replace(/\//g, "%2F")).join("/");
    } catch (error) {
      warning(
        false,
        `The URL path "${value}" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (${error}).`
      );
      return value;
    }
  }
  function stripBasename(pathname, basename) {
    if (basename === "/") return pathname;
    if (!pathname.toLowerCase().startsWith(basename.toLowerCase())) {
      return null;
    }
    let startIndex = basename.endsWith("/") ? basename.length - 1 : basename.length;
    let nextChar = pathname.charAt(startIndex);
    if (nextChar && nextChar !== "/") {
      return null;
    }
    return pathname.slice(startIndex) || "/";
  }
  var ABSOLUTE_URL_REGEX = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
  function resolvePath(to, fromPathname = "/") {
    let {
      pathname: toPathname,
      search = "",
      hash = ""
    } = typeof to === "string" ? parsePath(to) : to;
    let pathname;
    if (toPathname) {
      toPathname = removeDoubleSlashes(toPathname);
      if (toPathname.startsWith("/")) {
        pathname = resolvePathname(toPathname.substring(1), "/");
      } else {
        pathname = resolvePathname(toPathname, fromPathname);
      }
    } else {
      pathname = fromPathname;
    }
    return {
      pathname,
      search: normalizeSearch(search),
      hash: normalizeHash(hash)
    };
  }
  function resolvePathname(relativePath, fromPathname) {
    let segments = removeTrailingSlash(fromPathname).split("/");
    let relativeSegments = relativePath.split("/");
    relativeSegments.forEach((segment) => {
      if (segment === "..") {
        if (segments.length > 1) segments.pop();
      } else if (segment !== ".") {
        segments.push(segment);
      }
    });
    return segments.length > 1 ? segments.join("/") : "/";
  }
  function getInvalidPathError(char, field, dest, path) {
    return `Cannot include a '${char}' character in a manually specified \`to.${field}\` field [${JSON.stringify(
      path
    )}].  Please separate it out to the \`to.${dest}\` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.`;
  }
  function getPathContributingMatches(matches) {
    return matches.filter(
      (match, index) => index === 0 || match.route.path && match.route.path.length > 0
    );
  }
  function getResolveToMatches(matches) {
    let pathMatches = getPathContributingMatches(matches);
    return pathMatches.map(
      (match, idx) => idx === pathMatches.length - 1 ? match.pathname : match.pathnameBase
    );
  }
  function resolveTo(toArg, routePathnames, locationPathname, isPathRelative = false) {
    let to;
    if (typeof toArg === "string") {
      to = parsePath(toArg);
    } else {
      to = { ...toArg };
      invariant(
        !to.pathname || !to.pathname.includes("?"),
        getInvalidPathError("?", "pathname", "search", to)
      );
      invariant(
        !to.pathname || !to.pathname.includes("#"),
        getInvalidPathError("#", "pathname", "hash", to)
      );
      invariant(
        !to.search || !to.search.includes("#"),
        getInvalidPathError("#", "search", "hash", to)
      );
    }
    let isEmptyPath = toArg === "" || to.pathname === "";
    let toPathname = isEmptyPath ? "/" : to.pathname;
    let from;
    if (toPathname == null) {
      from = locationPathname;
    } else {
      let routePathnameIndex = routePathnames.length - 1;
      if (!isPathRelative && toPathname.startsWith("..")) {
        let toSegments = toPathname.split("/");
        while (toSegments[0] === "..") {
          toSegments.shift();
          routePathnameIndex -= 1;
        }
        to.pathname = toSegments.join("/");
      }
      from = routePathnameIndex >= 0 ? routePathnames[routePathnameIndex] : "/";
    }
    let path = resolvePath(to, from);
    let hasExplicitTrailingSlash = toPathname && toPathname !== "/" && toPathname.endsWith("/");
    let hasCurrentTrailingSlash = (isEmptyPath || toPathname === ".") && locationPathname.endsWith("/");
    if (!path.pathname.endsWith("/") && (hasExplicitTrailingSlash || hasCurrentTrailingSlash)) {
      path.pathname += "/";
    }
    return path;
  }
  var removeDoubleSlashes = (path) => path.replace(/\/\/+/g, "/");
  var joinPaths = (paths) => removeDoubleSlashes(paths.join("/"));
  var removeTrailingSlash = (path) => path.replace(/\/+$/, "");
  var normalizePathname = (pathname) => removeTrailingSlash(pathname).replace(/^\/*/, "/");
  var normalizeSearch = (search) => !search || search === "?" ? "" : search.startsWith("?") ? search : "?" + search;
  var normalizeHash = (hash) => !hash || hash === "#" ? "" : hash.startsWith("#") ? hash : "#" + hash;
  var ErrorResponseImpl = class {
    constructor(status, statusText, data2, internal = false) {
      this.status = status;
      this.statusText = statusText || "";
      this.internal = internal;
      if (data2 instanceof Error) {
        this.data = data2.toString();
        this.error = data2;
      } else {
        this.data = data2;
      }
    }
  };
  function isRouteErrorResponse(error) {
    return error != null && typeof error.status === "number" && typeof error.statusText === "string" && typeof error.internal === "boolean" && "data" in error;
  }
  function getRoutePattern(matches) {
    let parts = matches.map((m) => m.route.path).filter(Boolean);
    return joinPaths(parts) || "/";
  }
  var isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
  function parseToInfo(_to, basename) {
    let to = _to;
    if (typeof to !== "string" || !ABSOLUTE_URL_REGEX.test(to)) {
      return {
        absoluteURL: void 0,
        isExternal: false,
        to
      };
    }
    let absoluteURL = to;
    let isExternal = false;
    if (isBrowser) {
      try {
        let currentUrl = new URL(window.location.href);
        let targetUrl = to.startsWith("//") ? new URL(currentUrl.protocol + to) : new URL(to);
        let path = stripBasename(targetUrl.pathname, basename);
        if (targetUrl.origin === currentUrl.origin && path != null) {
          to = path + targetUrl.search + targetUrl.hash;
        } else {
          isExternal = true;
        }
      } catch (e2) {
        warning(
          false,
          `<Link to="${to}"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.`
        );
      }
    }
    return {
      absoluteURL,
      isExternal,
      to
    };
  }
  var objectProtoNames = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
  var validMutationMethodsArr = [
    "POST",
    "PUT",
    "PATCH",
    "DELETE"
  ];
  var validMutationMethods = new Set(
    validMutationMethodsArr
  );
  var validRequestMethodsArr = [
    "GET",
    ...validMutationMethodsArr
  ];
  var validRequestMethods = new Set(validRequestMethodsArr);
  var _routes;
  var _branches;
  var _hmrRoutes;
  var _hmrBranches;
  _routes = /* @__PURE__ */ new WeakMap();
  _branches = /* @__PURE__ */ new WeakMap();
  _hmrRoutes = /* @__PURE__ */ new WeakMap();
  _hmrBranches = /* @__PURE__ */ new WeakMap();
  var DataRouterContext = React.createContext(null);
  DataRouterContext.displayName = "DataRouter";
  var DataRouterStateContext = React.createContext(null);
  DataRouterStateContext.displayName = "DataRouterState";
  var RSCRouterContext = React.createContext(false);
  function useIsRSCRouterContext() {
    return React.useContext(RSCRouterContext);
  }
  var ViewTransitionContext = React.createContext({
    isTransitioning: false
  });
  ViewTransitionContext.displayName = "ViewTransition";
  var FetchersContext = React.createContext(
    /* @__PURE__ */ new Map()
  );
  FetchersContext.displayName = "Fetchers";
  var AwaitContext = React.createContext(null);
  AwaitContext.displayName = "Await";
  var NavigationContext = React.createContext(
    null
  );
  NavigationContext.displayName = "Navigation";
  var LocationContext = React.createContext(
    null
  );
  LocationContext.displayName = "Location";
  var RouteContext = React.createContext({
    outlet: null,
    matches: [],
    isDataRoute: false
  });
  RouteContext.displayName = "Route";
  var RouteErrorContext = React.createContext(null);
  RouteErrorContext.displayName = "RouteError";
  var ENABLE_DEV_WARNINGS = true;
  var ERROR_DIGEST_BASE = "REACT_ROUTER_ERROR";
  var ERROR_DIGEST_REDIRECT = "REDIRECT";
  var ERROR_DIGEST_ROUTE_ERROR_RESPONSE = "ROUTE_ERROR_RESPONSE";
  function decodeRedirectErrorDigest(digest) {
    if (digest.startsWith(`${ERROR_DIGEST_BASE}:${ERROR_DIGEST_REDIRECT}:{`)) {
      try {
        let parsed = JSON.parse(digest.slice(28));
        if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string" && typeof parsed.location === "string" && typeof parsed.reloadDocument === "boolean" && typeof parsed.replace === "boolean") {
          return parsed;
        }
      } catch {
      }
    }
  }
  function decodeRouteErrorResponseDigest(digest) {
    if (digest.startsWith(
      `${ERROR_DIGEST_BASE}:${ERROR_DIGEST_ROUTE_ERROR_RESPONSE}:{`
    )) {
      try {
        let parsed = JSON.parse(digest.slice(40));
        if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string") {
          return new ErrorResponseImpl(
            parsed.status,
            parsed.statusText,
            parsed.data
          );
        }
      } catch {
      }
    }
  }
  function useHref(to, { relative } = {}) {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useHref() may be used only in the context of a <Router> component.`
    );
    let { basename, navigator: navigator2 } = React2.useContext(NavigationContext);
    let { hash, pathname, search } = useResolvedPath(to, { relative });
    let joinedPathname = pathname;
    if (basename !== "/") {
      joinedPathname = pathname === "/" ? basename : joinPaths([basename, pathname]);
    }
    return navigator2.createHref({ pathname: joinedPathname, search, hash });
  }
  function useInRouterContext() {
    return React2.useContext(LocationContext) != null;
  }
  function useLocation() {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useLocation() may be used only in the context of a <Router> component.`
    );
    return React2.useContext(LocationContext).location;
  }
  var navigateEffectWarning = `You should call navigate() in a React.useEffect(), not when your component is first rendered.`;
  function useIsomorphicLayoutEffect(cb) {
    let isStatic = React2.useContext(NavigationContext).static;
    if (!isStatic) {
      React2.useLayoutEffect(cb);
    }
  }
  function useNavigate() {
    let { isDataRoute } = React2.useContext(RouteContext);
    return isDataRoute ? useNavigateStable() : useNavigateUnstable();
  }
  function useNavigateUnstable() {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useNavigate() may be used only in the context of a <Router> component.`
    );
    let dataRouterContext = React2.useContext(DataRouterContext);
    let { basename, navigator: navigator2 } = React2.useContext(NavigationContext);
    let { matches } = React2.useContext(RouteContext);
    let { pathname: locationPathname } = useLocation();
    let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
    let activeRef = React2.useRef(false);
    useIsomorphicLayoutEffect(() => {
      activeRef.current = true;
    });
    let navigate = React2.useCallback(
      (to, options = {}) => {
        warning(activeRef.current, navigateEffectWarning);
        if (!activeRef.current) return;
        if (typeof to === "number") {
          navigator2.go(to);
          return;
        }
        let path = resolveTo(
          to,
          JSON.parse(routePathnamesJson),
          locationPathname,
          options.relative === "path"
        );
        if (dataRouterContext == null && basename !== "/") {
          path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
        }
        (!!options.replace ? navigator2.replace : navigator2.push)(
          path,
          options.state,
          options
        );
      },
      [
        basename,
        navigator2,
        routePathnamesJson,
        locationPathname,
        dataRouterContext
      ]
    );
    return navigate;
  }
  var OutletContext = React2.createContext(null);
  function useResolvedPath(to, { relative } = {}) {
    let { matches } = React2.useContext(RouteContext);
    let { pathname: locationPathname } = useLocation();
    let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
    return React2.useMemo(
      () => resolveTo(
        to,
        JSON.parse(routePathnamesJson),
        locationPathname,
        relative === "path"
      ),
      [to, routePathnamesJson, locationPathname, relative]
    );
  }
  function useRoutesImpl(routes, locationArg, dataRouterOpts) {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useRoutes() may be used only in the context of a <Router> component.`
    );
    let { navigator: navigator2 } = React2.useContext(NavigationContext);
    let { matches: parentMatches } = React2.useContext(RouteContext);
    let routeMatch = parentMatches[parentMatches.length - 1];
    let parentParams = routeMatch ? routeMatch.params : {};
    let parentPathname = routeMatch ? routeMatch.pathname : "/";
    let parentPathnameBase = routeMatch ? routeMatch.pathnameBase : "/";
    let parentRoute = routeMatch && routeMatch.route;
    if (ENABLE_DEV_WARNINGS) {
      let parentPath = parentRoute && parentRoute.path || "";
      warningOnce(
        parentPathname,
        !parentRoute || parentPath.endsWith("*") || parentPath.endsWith("*?"),
        `You rendered descendant <Routes> (or called \`useRoutes()\`) at "${parentPathname}" (under <Route path="${parentPath}">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won't match anymore and therefore the child routes will never render.

Please change the parent <Route path="${parentPath}"> to <Route path="${parentPath === "/" ? "*" : `${parentPath}/*`}">.`
      );
    }
    let locationFromContext = useLocation();
    let location;
    if (locationArg) {
      let parsedLocationArg = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
      invariant(
        parentPathnameBase === "/" || parsedLocationArg.pathname?.startsWith(parentPathnameBase),
        `When overriding the location using \`<Routes location>\` or \`useRoutes(routes, location)\`, the location pathname must begin with the portion of the URL pathname that was matched by all parent routes. The current pathname base is "${parentPathnameBase}" but pathname "${parsedLocationArg.pathname}" was given in the \`location\` prop.`
      );
      location = parsedLocationArg;
    } else {
      location = locationFromContext;
    }
    let pathname = location.pathname || "/";
    let remainingPathname = pathname;
    if (parentPathnameBase !== "/") {
      let parentSegments = parentPathnameBase.replace(/^\//, "").split("/");
      let segments = pathname.replace(/^\//, "").split("/");
      remainingPathname = "/" + segments.slice(parentSegments.length).join("/");
    }
    let matches = dataRouterOpts && dataRouterOpts.state.matches.length ? (
      // If we're in a data router, use the matches we've already identified but ensure
      // we have the latest route instances from the manifest in case elements have changed
      dataRouterOpts.state.matches.map(
        (m) => Object.assign(m, {
          route: dataRouterOpts.manifest[m.route.id] || m.route
        })
      )
    ) : matchRoutes(routes, { pathname: remainingPathname });
    if (ENABLE_DEV_WARNINGS) {
      warning(
        parentRoute || matches != null,
        `No routes matched location "${location.pathname}${location.search}${location.hash}" `
      );
      warning(
        matches == null || matches[matches.length - 1].route.element !== void 0 || matches[matches.length - 1].route.Component !== void 0 || matches[matches.length - 1].route.lazy !== void 0,
        `Matched leaf route at location "${location.pathname}${location.search}${location.hash}" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.`
      );
    }
    let renderedMatches = _renderMatches(
      matches && matches.map(
        (match) => Object.assign({}, match, {
          params: Object.assign({}, parentParams, match.params),
          pathname: joinPaths([
            parentPathnameBase,
            // Re-encode pathnames that were decoded inside matchRoutes.
            // Pre-encode `%`, `?` and `#` ahead of `encodeLocation` because it uses
            // `new URL()` internally and we need to prevent it from treating
            // them as separators
            navigator2.encodeLocation ? navigator2.encodeLocation(
              match.pathname.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")
            ).pathname : match.pathname
          ]),
          pathnameBase: match.pathnameBase === "/" ? parentPathnameBase : joinPaths([
            parentPathnameBase,
            // Re-encode pathnames that were decoded inside matchRoutes
            // Pre-encode `%`, `?` and `#` ahead of `encodeLocation` because it uses
            // `new URL()` internally and we need to prevent it from treating
            // them as separators
            navigator2.encodeLocation ? navigator2.encodeLocation(
              match.pathnameBase.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")
            ).pathname : match.pathnameBase
          ])
        })
      ),
      parentMatches,
      dataRouterOpts
    );
    if (locationArg && renderedMatches) {
      return /* @__PURE__ */ React2.createElement(
        LocationContext.Provider,
        {
          value: {
            location: {
              pathname: "/",
              search: "",
              hash: "",
              state: null,
              key: "default",
              mask: void 0,
              ...location
            },
            navigationType: "POP"
            /* Pop */
          }
        },
        renderedMatches
      );
    }
    return renderedMatches;
  }
  function DefaultErrorComponent() {
    let error = useRouteError();
    let message = isRouteErrorResponse(error) ? `${error.status} ${error.statusText}` : error instanceof Error ? error.message : JSON.stringify(error);
    let stack = error instanceof Error ? error.stack : null;
    let lightgrey = "rgba(200,200,200, 0.5)";
    let preStyles = { padding: "0.5rem", backgroundColor: lightgrey };
    let codeStyles = { padding: "2px 4px", backgroundColor: lightgrey };
    let devInfo = null;
    if (ENABLE_DEV_WARNINGS) {
      console.error(
        "Error handled by React Router default ErrorBoundary:",
        error
      );
      devInfo = /* @__PURE__ */ React2.createElement(React2.Fragment, null, /* @__PURE__ */ React2.createElement("p", null, "\u{1F4BF} Hey developer \u{1F44B}"), /* @__PURE__ */ React2.createElement("p", null, "You can provide a way better UX than this when your app throws errors by providing your own ", /* @__PURE__ */ React2.createElement("code", { style: codeStyles }, "ErrorBoundary"), " or", " ", /* @__PURE__ */ React2.createElement("code", { style: codeStyles }, "errorElement"), " prop on your route."));
    }
    return /* @__PURE__ */ React2.createElement(React2.Fragment, null, /* @__PURE__ */ React2.createElement("h2", null, "Unexpected Application Error!"), /* @__PURE__ */ React2.createElement("h3", { style: { fontStyle: "italic" } }, message), stack ? /* @__PURE__ */ React2.createElement("pre", { style: preStyles }, stack) : null, devInfo);
  }
  var defaultErrorElement = /* @__PURE__ */ React2.createElement(DefaultErrorComponent, null);
  var RenderErrorBoundary = class extends React2.Component {
    constructor(props) {
      super(props);
      this.state = {
        location: props.location,
        revalidation: props.revalidation,
        error: props.error
      };
    }
    static getDerivedStateFromError(error) {
      return { error };
    }
    static getDerivedStateFromProps(props, state) {
      if (state.location !== props.location || state.revalidation !== "idle" && props.revalidation === "idle") {
        return {
          error: props.error,
          location: props.location,
          revalidation: props.revalidation
        };
      }
      return {
        error: props.error !== void 0 ? props.error : state.error,
        location: state.location,
        revalidation: props.revalidation || state.revalidation
      };
    }
    componentDidCatch(error, errorInfo) {
      if (this.props.onError) {
        this.props.onError(error, errorInfo);
      } else {
        console.error(
          "React Router caught the following error during render",
          error
        );
      }
    }
    render() {
      let error = this.state.error;
      if (this.context && typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
        const decoded = decodeRouteErrorResponseDigest(error.digest);
        if (decoded) error = decoded;
      }
      let result = error !== void 0 ? /* @__PURE__ */ React2.createElement(RouteContext.Provider, { value: this.props.routeContext }, /* @__PURE__ */ React2.createElement(
        RouteErrorContext.Provider,
        {
          value: error,
          children: this.props.component
        }
      )) : this.props.children;
      if (this.context) {
        return /* @__PURE__ */ React2.createElement(RSCErrorHandler, { error }, result);
      }
      return result;
    }
  };
  RenderErrorBoundary.contextType = RSCRouterContext;
  var errorRedirectHandledMap = /* @__PURE__ */ new WeakMap();
  function RSCErrorHandler({
    children,
    error
  }) {
    let { basename } = React2.useContext(NavigationContext);
    if (typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
      let redirect2 = decodeRedirectErrorDigest(error.digest);
      if (redirect2) {
        let existingRedirect = errorRedirectHandledMap.get(error);
        if (existingRedirect) throw existingRedirect;
        let parsed = parseToInfo(redirect2.location, basename);
        if (isBrowser && !errorRedirectHandledMap.get(error)) {
          if (parsed.isExternal || redirect2.reloadDocument) {
            window.location.href = parsed.absoluteURL || parsed.to;
          } else {
            const redirectPromise = Promise.resolve().then(
              () => window.__reactRouterDataRouter.navigate(parsed.to, {
                replace: redirect2.replace
              })
            );
            errorRedirectHandledMap.set(error, redirectPromise);
            throw redirectPromise;
          }
        }
        return /* @__PURE__ */ React2.createElement(
          "meta",
          {
            httpEquiv: "refresh",
            content: `0;url=${parsed.absoluteURL || parsed.to}`
          }
        );
      }
    }
    return children;
  }
  function RenderedRoute({ routeContext, match, children }) {
    let dataRouterContext = React2.useContext(DataRouterContext);
    if (dataRouterContext && dataRouterContext.static && dataRouterContext.staticContext && (match.route.errorElement || match.route.ErrorBoundary)) {
      dataRouterContext.staticContext._deepestRenderedBoundaryId = match.route.id;
    }
    return /* @__PURE__ */ React2.createElement(RouteContext.Provider, { value: routeContext }, children);
  }
  function _renderMatches(matches, parentMatches = [], dataRouterOpts) {
    let dataRouterState = dataRouterOpts?.state;
    if (matches == null) {
      if (!dataRouterState) {
        return null;
      }
      if (dataRouterState.errors) {
        matches = dataRouterState.matches;
      } else if (parentMatches.length === 0 && !dataRouterState.initialized && dataRouterState.matches.length > 0) {
        matches = dataRouterState.matches;
      } else {
        return null;
      }
    }
    let renderedMatches = matches;
    let errors = dataRouterState?.errors;
    if (errors != null) {
      let errorIndex = renderedMatches.findIndex(
        (m) => m.route.id && errors?.[m.route.id] !== void 0
      );
      invariant(
        errorIndex >= 0,
        `Could not find a matching route for errors on route IDs: ${Object.keys(
          errors
        ).join(",")}`
      );
      renderedMatches = renderedMatches.slice(
        0,
        Math.min(renderedMatches.length, errorIndex + 1)
      );
    }
    let renderFallback = false;
    let fallbackIndex = -1;
    if (dataRouterOpts && dataRouterState) {
      renderFallback = dataRouterState.renderFallback;
      for (let i = 0; i < renderedMatches.length; i++) {
        let match = renderedMatches[i];
        if (match.route.HydrateFallback || match.route.hydrateFallbackElement) {
          fallbackIndex = i;
        }
        if (match.route.id) {
          let { loaderData, errors: errors2 } = dataRouterState;
          let needsToRunLoader = match.route.loader && !loaderData.hasOwnProperty(match.route.id) && (!errors2 || errors2[match.route.id] === void 0);
          if (match.route.lazy || needsToRunLoader) {
            if (dataRouterOpts.isStatic) {
              renderFallback = true;
            }
            if (fallbackIndex >= 0) {
              renderedMatches = renderedMatches.slice(0, fallbackIndex + 1);
            } else {
              renderedMatches = [renderedMatches[0]];
            }
            break;
          }
        }
      }
    }
    let onErrorHandler = dataRouterOpts?.onError;
    let onError = dataRouterState && onErrorHandler ? (error, errorInfo) => {
      onErrorHandler(error, {
        location: dataRouterState.location,
        params: dataRouterState.matches?.[0]?.params ?? {},
        pattern: getRoutePattern(dataRouterState.matches),
        errorInfo
      });
    } : void 0;
    return renderedMatches.reduceRight(
      (outlet, match, index) => {
        let error;
        let shouldRenderHydrateFallback = false;
        let errorElement = null;
        let hydrateFallbackElement = null;
        if (dataRouterState) {
          error = errors && match.route.id ? errors[match.route.id] : void 0;
          errorElement = match.route.errorElement || defaultErrorElement;
          if (renderFallback) {
            if (fallbackIndex < 0 && index === 0) {
              warningOnce(
                "route-fallback",
                false,
                "No `HydrateFallback` element provided to render during initial hydration"
              );
              shouldRenderHydrateFallback = true;
              hydrateFallbackElement = null;
            } else if (fallbackIndex === index) {
              shouldRenderHydrateFallback = true;
              hydrateFallbackElement = match.route.hydrateFallbackElement || null;
            }
          }
        }
        let matches2 = parentMatches.concat(renderedMatches.slice(0, index + 1));
        let getChildren = () => {
          let children;
          if (error) {
            children = errorElement;
          } else if (shouldRenderHydrateFallback) {
            children = hydrateFallbackElement;
          } else if (match.route.Component) {
            children = /* @__PURE__ */ React2.createElement(match.route.Component, null);
          } else if (match.route.element) {
            children = match.route.element;
          } else {
            children = outlet;
          }
          return /* @__PURE__ */ React2.createElement(
            RenderedRoute,
            {
              match,
              routeContext: {
                outlet,
                matches: matches2,
                isDataRoute: dataRouterState != null
              },
              children
            }
          );
        };
        return dataRouterState && (match.route.ErrorBoundary || match.route.errorElement || index === 0) ? /* @__PURE__ */ React2.createElement(
          RenderErrorBoundary,
          {
            location: dataRouterState.location,
            revalidation: dataRouterState.revalidation,
            component: errorElement,
            error,
            children: getChildren(),
            routeContext: { outlet: null, matches: matches2, isDataRoute: true },
            onError
          }
        ) : getChildren();
      },
      null
    );
  }
  function getDataRouterConsoleError(hookName) {
    return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
  }
  function useDataRouterContext(hookName) {
    let ctx = React2.useContext(DataRouterContext);
    invariant(ctx, getDataRouterConsoleError(hookName));
    return ctx;
  }
  function useDataRouterState(hookName) {
    let state = React2.useContext(DataRouterStateContext);
    invariant(state, getDataRouterConsoleError(hookName));
    return state;
  }
  function useRouteContext(hookName) {
    let route = React2.useContext(RouteContext);
    invariant(route, getDataRouterConsoleError(hookName));
    return route;
  }
  function useCurrentRouteId(hookName) {
    let route = useRouteContext(hookName);
    let thisRoute = route.matches[route.matches.length - 1];
    invariant(
      thisRoute.route.id,
      `${hookName} can only be used on routes that contain a unique "id"`
    );
    return thisRoute.route.id;
  }
  function useRouteId() {
    return useCurrentRouteId(
      "useRouteId"
      /* UseRouteId */
    );
  }
  function useNavigation() {
    let state = useDataRouterState(
      "useNavigation"
      /* UseNavigation */
    );
    return React2.useMemo(() => {
      let { matches, historyAction, ...rest } = state.navigation;
      return rest;
    }, [state.navigation]);
  }
  function useMatches() {
    let { matches, loaderData } = useDataRouterState(
      "useMatches"
      /* UseMatches */
    );
    return React2.useMemo(
      () => matches.map((m) => convertRouteMatchToUiMatch(m, loaderData)),
      [matches, loaderData]
    );
  }
  function useRouteError() {
    let error = React2.useContext(RouteErrorContext);
    let state = useDataRouterState(
      "useRouteError"
      /* UseRouteError */
    );
    let routeId = useCurrentRouteId(
      "useRouteError"
      /* UseRouteError */
    );
    if (error !== void 0) {
      return error;
    }
    return state.errors?.[routeId];
  }
  function useNavigateStable() {
    let { router } = useDataRouterContext(
      "useNavigate"
      /* UseNavigateStable */
    );
    let id = useCurrentRouteId(
      "useNavigate"
      /* UseNavigateStable */
    );
    let activeRef = React2.useRef(false);
    useIsomorphicLayoutEffect(() => {
      activeRef.current = true;
    });
    let navigate = React2.useCallback(
      async (to, options = {}) => {
        warning(activeRef.current, navigateEffectWarning);
        if (!activeRef.current) return;
        if (typeof to === "number") {
          await router.navigate(to);
        } else {
          await router.navigate(to, { fromRouteId: id, ...options });
        }
      },
      [router, id]
    );
    return navigate;
  }
  var alreadyWarned = {};
  function warningOnce(key, cond, message) {
    if (!cond && !alreadyWarned[key]) {
      alreadyWarned[key] = true;
      warning(false, message);
    }
  }
  var USE_OPTIMISTIC = "useOptimistic";
  var useOptimisticImpl = React3[USE_OPTIMISTIC];
  var MemoizedDataRoutes = React3.memo(DataRoutes2);
  function DataRoutes2({
    routes,
    manifest,
    future,
    state,
    isStatic,
    onError
  }) {
    return useRoutesImpl(routes, void 0, {
      manifest,
      state,
      isStatic,
      onError,
      future
    });
  }
  function Router({
    basename: basenameProp = "/",
    children = null,
    location: locationProp,
    navigationType = "POP",
    navigator: navigator2,
    static: staticProp = false,
    useTransitions
  }) {
    invariant(
      !useInRouterContext(),
      `You cannot render a <Router> inside another <Router>. You should never have more than one in your app.`
    );
    let basename = basenameProp.replace(/^\/*/, "/");
    let navigationContext = React3.useMemo(
      () => ({
        basename,
        navigator: navigator2,
        static: staticProp,
        useTransitions,
        future: {}
      }),
      [basename, navigator2, staticProp, useTransitions]
    );
    if (typeof locationProp === "string") {
      locationProp = parsePath(locationProp);
    }
    let {
      pathname = "/",
      search = "",
      hash = "",
      state = null,
      key = "default",
      mask
    } = locationProp;
    let locationContext = React3.useMemo(() => {
      let trailingPathname = stripBasename(pathname, basename);
      if (trailingPathname == null) {
        return null;
      }
      return {
        location: {
          pathname: trailingPathname,
          search,
          hash,
          state,
          key,
          mask
        },
        navigationType
      };
    }, [basename, pathname, search, hash, state, key, navigationType, mask]);
    warning(
      locationContext != null,
      `<Router basename="${basename}"> is not able to match the URL "${pathname}${search}${hash}" because it does not start with the basename, so the <Router> won't render anything.`
    );
    if (locationContext == null) {
      return null;
    }
    return /* @__PURE__ */ React3.createElement(NavigationContext.Provider, { value: navigationContext }, /* @__PURE__ */ React3.createElement(LocationContext.Provider, { children, value: locationContext }));
  }
  var defaultMethod = "get";
  var defaultEncType = "application/x-www-form-urlencoded";
  function isHtmlElement(object) {
    return typeof HTMLElement !== "undefined" && object instanceof HTMLElement;
  }
  function isButtonElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "button";
  }
  function isFormElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "form";
  }
  function isInputElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "input";
  }
  function isModifiedEvent(event) {
    return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
  }
  function shouldProcessLinkClick(event, target) {
    return event.button === 0 && // Ignore everything but left clicks
    (!target || target === "_self") && // Let browser handle "target=_blank" etc.
    !isModifiedEvent(event);
  }
  function createSearchParams(init2 = "") {
    return new URLSearchParams(
      typeof init2 === "string" || Array.isArray(init2) || init2 instanceof URLSearchParams ? init2 : Object.keys(init2).reduce((memo2, key) => {
        let value = init2[key];
        return memo2.concat(
          Array.isArray(value) ? value.map((v) => [key, v]) : [[key, value]]
        );
      }, [])
    );
  }
  function getSearchParamsForLocation(locationSearch, defaultSearchParams) {
    let searchParams = createSearchParams(locationSearch);
    if (defaultSearchParams) {
      defaultSearchParams.forEach((_, key) => {
        if (!searchParams.has(key)) {
          defaultSearchParams.getAll(key).forEach((value) => {
            searchParams.append(key, value);
          });
        }
      });
    }
    return searchParams;
  }
  var _formDataSupportsSubmitter = null;
  function isFormDataSubmitterSupported() {
    if (_formDataSupportsSubmitter === null) {
      try {
        new FormData(
          document.createElement("form"),
          // @ts-expect-error if FormData supports the submitter parameter, this will throw
          0
        );
        _formDataSupportsSubmitter = false;
      } catch (e2) {
        _formDataSupportsSubmitter = true;
      }
    }
    return _formDataSupportsSubmitter;
  }
  var supportedFormEncTypes = /* @__PURE__ */ new Set([
    "application/x-www-form-urlencoded",
    "multipart/form-data",
    "text/plain"
  ]);
  function getFormEncType(encType) {
    if (encType != null && !supportedFormEncTypes.has(encType)) {
      warning(
        false,
        `"${encType}" is not a valid \`encType\` for \`<Form>\`/\`<fetcher.Form>\` and will default to "${defaultEncType}"`
      );
      return null;
    }
    return encType;
  }
  function getFormSubmissionInfo(target, basename) {
    let method;
    let action;
    let encType;
    let formData;
    let body;
    if (isFormElement(target)) {
      let attr = target.getAttribute("action");
      action = attr ? stripBasename(attr, basename) : null;
      method = target.getAttribute("method") || defaultMethod;
      encType = getFormEncType(target.getAttribute("enctype")) || defaultEncType;
      formData = new FormData(target);
    } else if (isButtonElement(target) || isInputElement(target) && (target.type === "submit" || target.type === "image")) {
      let form = target.form;
      if (form == null) {
        throw new Error(
          `Cannot submit a <button> or <input type="submit"> without a <form>`
        );
      }
      let attr = target.getAttribute("formaction") || form.getAttribute("action");
      action = attr ? stripBasename(attr, basename) : null;
      method = target.getAttribute("formmethod") || form.getAttribute("method") || defaultMethod;
      encType = getFormEncType(target.getAttribute("formenctype")) || getFormEncType(form.getAttribute("enctype")) || defaultEncType;
      formData = new FormData(form, target);
      if (!isFormDataSubmitterSupported()) {
        let { name, type, value } = target;
        if (type === "image") {
          let prefix = name ? `${name}.` : "";
          formData.append(`${prefix}x`, "0");
          formData.append(`${prefix}y`, "0");
        } else if (name) {
          formData.append(name, value);
        }
      }
    } else if (isHtmlElement(target)) {
      throw new Error(
        `Cannot submit element that is not <form>, <button>, or <input type="submit|image">`
      );
    } else {
      method = defaultMethod;
      action = null;
      encType = defaultEncType;
      body = target;
    }
    if (formData && encType === "text/plain") {
      body = formData;
      formData = void 0;
    }
    return { action, method: method.toLowerCase(), encType, formData, body };
  }
  var objectProtoNames2 = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
  var ESCAPE_LOOKUP = {
    "&": "\\u0026",
    ">": "\\u003e",
    "<": "\\u003c",
    "\u2028": "\\u2028",
    "\u2029": "\\u2029"
  };
  var ESCAPE_REGEX = /[&><\u2028\u2029]/g;
  function escapeHtml(html) {
    return html.replace(ESCAPE_REGEX, (match) => ESCAPE_LOOKUP[match]);
  }
  function invariant2(value, message) {
    if (value === false || value === null || typeof value === "undefined") {
      throw new Error(message);
    }
  }
  function singleFetchUrl(reqUrl, basename, trailingSlashAware, extension) {
    let url = typeof reqUrl === "string" ? new URL(
      reqUrl,
      // This can be called during the SSR flow via PrefetchPageLinksImpl so
      // don't assume window is available
      typeof window === "undefined" ? "server://singlefetch/" : window.location.origin
    ) : reqUrl;
    if (trailingSlashAware) {
      if (url.pathname.endsWith("/")) {
        url.pathname = `${url.pathname}_.${extension}`;
      } else {
        url.pathname = `${url.pathname}.${extension}`;
      }
    } else {
      if (url.pathname === "/") {
        url.pathname = `_root.${extension}`;
      } else if (basename && stripBasename(url.pathname, basename) === "/") {
        url.pathname = `${removeTrailingSlash(basename)}/_root.${extension}`;
      } else {
        url.pathname = `${removeTrailingSlash(url.pathname)}.${extension}`;
      }
    }
    return url;
  }
  async function loadRouteModule(route, routeModulesCache) {
    if (route.id in routeModulesCache) {
      return routeModulesCache[route.id];
    }
    try {
      let routeModule = await import(
        /* @vite-ignore */
        /* webpackIgnore: true */
        route.module
      );
      routeModulesCache[route.id] = routeModule;
      return routeModule;
    } catch (error) {
      console.error(
        `Error loading route module \`${route.module}\`, reloading page...`
      );
      console.error(error);
      if (window.__reactRouterContext && window.__reactRouterContext.isSpaMode && // @ts-expect-error
      import_meta.hot) {
        throw error;
      }
      window.location.reload();
      return new Promise(() => {
      });
    }
  }
  function isPageLinkDescriptor(object) {
    return object != null && typeof object.page === "string";
  }
  function isHtmlLinkDescriptor(object) {
    if (object == null) {
      return false;
    }
    if (object.href == null) {
      return object.rel === "preload" && typeof object.imageSrcSet === "string" && typeof object.imageSizes === "string";
    }
    return typeof object.rel === "string" && typeof object.href === "string";
  }
  async function getKeyedPrefetchLinks(matches, manifest, routeModules) {
    let links = await Promise.all(
      matches.map(async (match) => {
        let route = manifest.routes[match.route.id];
        if (route) {
          let mod = await loadRouteModule(route, routeModules);
          return mod.links ? mod.links() : [];
        }
        return [];
      })
    );
    return dedupeLinkDescriptors(
      links.flat(1).filter(isHtmlLinkDescriptor).filter((link) => link.rel === "stylesheet" || link.rel === "preload").map(
        (link) => link.rel === "stylesheet" ? { ...link, rel: "prefetch", as: "style" } : { ...link, rel: "prefetch" }
      )
    );
  }
  function getNewMatchesForLinks(page, nextMatches, currentMatches, manifest, location, mode) {
    let isNew = (match, index) => {
      if (!currentMatches[index]) return true;
      return match.route.id !== currentMatches[index].route.id;
    };
    let matchPathChanged = (match, index) => {
      return (
        // param change, /users/123 -> /users/456
        currentMatches[index].pathname !== match.pathname || // splat param changed, which is not present in match.path
        // e.g. /files/images/avatar.jpg -> files/finances.xls
        currentMatches[index].route.path?.endsWith("*") && currentMatches[index].params["*"] !== match.params["*"]
      );
    };
    if (mode === "assets") {
      return nextMatches.filter(
        (match, index) => isNew(match, index) || matchPathChanged(match, index)
      );
    }
    if (mode === "data") {
      return nextMatches.filter((match, index) => {
        let manifestRoute = manifest.routes[match.route.id];
        if (!manifestRoute || !manifestRoute.hasLoader) {
          return false;
        }
        if (isNew(match, index) || matchPathChanged(match, index)) {
          return true;
        }
        if (match.route.shouldRevalidate) {
          let routeChoice = match.route.shouldRevalidate({
            currentUrl: new URL(
              location.pathname + location.search + location.hash,
              window.origin
            ),
            currentParams: currentMatches[0]?.params || {},
            nextUrl: new URL(page, window.origin),
            nextParams: match.params,
            defaultShouldRevalidate: true
          });
          if (typeof routeChoice === "boolean") {
            return routeChoice;
          }
        }
        return true;
      });
    }
    return [];
  }
  function getModuleLinkHrefs(matches, manifest, { includeHydrateFallback } = {}) {
    return dedupeHrefs(
      matches.map((match) => {
        let route = manifest.routes[match.route.id];
        if (!route) return [];
        let hrefs = [route.module];
        if (route.clientActionModule) {
          hrefs = hrefs.concat(route.clientActionModule);
        }
        if (route.clientLoaderModule) {
          hrefs = hrefs.concat(route.clientLoaderModule);
        }
        if (includeHydrateFallback && route.hydrateFallbackModule) {
          hrefs = hrefs.concat(route.hydrateFallbackModule);
        }
        if (route.imports) {
          hrefs = hrefs.concat(route.imports);
        }
        return hrefs;
      }).flat(1)
    );
  }
  function dedupeHrefs(hrefs) {
    return [...new Set(hrefs)];
  }
  function sortKeys(obj) {
    let sorted = {};
    let keys = Object.keys(obj).sort();
    for (let key of keys) {
      sorted[key] = obj[key];
    }
    return sorted;
  }
  function dedupeLinkDescriptors(descriptors, preloads) {
    let set = /* @__PURE__ */ new Set();
    let preloadsSet = new Set(preloads);
    return descriptors.reduce((deduped, descriptor) => {
      let alreadyModulePreload = preloads && !isPageLinkDescriptor(descriptor) && descriptor.as === "script" && descriptor.href && preloadsSet.has(descriptor.href);
      if (alreadyModulePreload) {
        return deduped;
      }
      let key = JSON.stringify(sortKeys(descriptor));
      if (!set.has(key)) {
        set.add(key);
        deduped.push({ key, link: descriptor });
      }
      return deduped;
    }, []);
  }
  function useDataRouterContext2() {
    let context = React8.useContext(DataRouterContext);
    invariant2(
      context,
      "You must render this element inside a <DataRouterContext.Provider> element"
    );
    return context;
  }
  function useDataRouterStateContext() {
    let context = React8.useContext(DataRouterStateContext);
    invariant2(
      context,
      "You must render this element inside a <DataRouterStateContext.Provider> element"
    );
    return context;
  }
  var FrameworkContext = React8.createContext(void 0);
  FrameworkContext.displayName = "FrameworkContext";
  function useFrameworkContext() {
    let context = React8.useContext(FrameworkContext);
    invariant2(
      context,
      "You must render this element inside a <HydratedRouter> element"
    );
    return context;
  }
  function usePrefetchBehavior(prefetch, theirElementProps) {
    let frameworkContext = React8.useContext(FrameworkContext);
    let [maybePrefetch, setMaybePrefetch] = React8.useState(false);
    let [shouldPrefetch, setShouldPrefetch] = React8.useState(false);
    let { onFocus, onBlur, onMouseEnter, onMouseLeave, onTouchStart } = theirElementProps;
    let ref = React8.useRef(null);
    React8.useEffect(() => {
      if (prefetch === "render") {
        setShouldPrefetch(true);
      }
      if (prefetch === "viewport") {
        let callback = (entries) => {
          entries.forEach((entry) => {
            setShouldPrefetch(entry.isIntersecting);
          });
        };
        let observer = new IntersectionObserver(callback, { threshold: 0.5 });
        if (ref.current) observer.observe(ref.current);
        return () => {
          observer.disconnect();
        };
      }
    }, [prefetch]);
    React8.useEffect(() => {
      if (maybePrefetch) {
        let id = setTimeout(() => {
          setShouldPrefetch(true);
        }, 100);
        return () => {
          clearTimeout(id);
        };
      }
    }, [maybePrefetch]);
    let setIntent = () => {
      setMaybePrefetch(true);
    };
    let cancelIntent = () => {
      setMaybePrefetch(false);
      setShouldPrefetch(false);
    };
    if (!frameworkContext) {
      return [false, ref, {}];
    }
    if (prefetch !== "intent") {
      return [shouldPrefetch, ref, {}];
    }
    return [
      shouldPrefetch,
      ref,
      {
        onFocus: composeEventHandlers(onFocus, setIntent),
        onBlur: composeEventHandlers(onBlur, cancelIntent),
        onMouseEnter: composeEventHandlers(onMouseEnter, setIntent),
        onMouseLeave: composeEventHandlers(onMouseLeave, cancelIntent),
        onTouchStart: composeEventHandlers(onTouchStart, setIntent)
      }
    ];
  }
  function composeEventHandlers(theirHandler, ourHandler) {
    return (event) => {
      theirHandler && theirHandler(event);
      if (!event.defaultPrevented) {
        ourHandler(event);
      }
    };
  }
  function PrefetchPageLinks({ page, ...linkProps }) {
    let rsc = useIsRSCRouterContext();
    let { router } = useDataRouterContext2();
    let matches = React8.useMemo(
      () => matchRoutes(router.routes, page, router.basename),
      [router.routes, page, router.basename]
    );
    if (!matches) {
      return null;
    }
    if (rsc) {
      return /* @__PURE__ */ React8.createElement(RSCPrefetchPageLinksImpl, { page, matches, ...linkProps });
    }
    return /* @__PURE__ */ React8.createElement(PrefetchPageLinksImpl, { page, matches, ...linkProps });
  }
  function useKeyedPrefetchLinks(matches) {
    let { manifest, routeModules } = useFrameworkContext();
    let [keyedPrefetchLinks, setKeyedPrefetchLinks] = React8.useState([]);
    React8.useEffect(() => {
      let interrupted = false;
      void getKeyedPrefetchLinks(matches, manifest, routeModules).then(
        (links) => {
          if (!interrupted) {
            setKeyedPrefetchLinks(links);
          }
        }
      );
      return () => {
        interrupted = true;
      };
    }, [matches, manifest, routeModules]);
    return keyedPrefetchLinks;
  }
  function RSCPrefetchPageLinksImpl({
    page,
    matches: nextMatches,
    ...linkProps
  }) {
    let location = useLocation();
    let { future } = useFrameworkContext();
    let { basename } = useDataRouterContext2();
    let dataHrefs = React8.useMemo(() => {
      if (page === location.pathname + location.search + location.hash) {
        return [];
      }
      let url = singleFetchUrl(
        page,
        basename,
        future.v8_trailingSlashAwareDataRequests,
        "rsc"
      );
      let hasSomeRoutesWithShouldRevalidate = false;
      let targetRoutes = [];
      for (let match of nextMatches) {
        if (typeof match.route.shouldRevalidate === "function") {
          hasSomeRoutesWithShouldRevalidate = true;
        } else {
          targetRoutes.push(match.route.id);
        }
      }
      if (hasSomeRoutesWithShouldRevalidate && targetRoutes.length > 0) {
        url.searchParams.set("_routes", targetRoutes.join(","));
      }
      return [url.pathname + url.search];
    }, [
      basename,
      future.v8_trailingSlashAwareDataRequests,
      page,
      location,
      nextMatches
    ]);
    return /* @__PURE__ */ React8.createElement(React8.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "prefetch", as: "fetch", href, ...linkProps })));
  }
  function PrefetchPageLinksImpl({
    page,
    matches: nextMatches,
    ...linkProps
  }) {
    let location = useLocation();
    let { future, manifest, routeModules } = useFrameworkContext();
    let { basename } = useDataRouterContext2();
    let { loaderData, matches } = useDataRouterStateContext();
    let newMatchesForData = React8.useMemo(
      () => getNewMatchesForLinks(
        page,
        nextMatches,
        matches,
        manifest,
        location,
        "data"
      ),
      [page, nextMatches, matches, manifest, location]
    );
    let newMatchesForAssets = React8.useMemo(
      () => getNewMatchesForLinks(
        page,
        nextMatches,
        matches,
        manifest,
        location,
        "assets"
      ),
      [page, nextMatches, matches, manifest, location]
    );
    let dataHrefs = React8.useMemo(() => {
      if (page === location.pathname + location.search + location.hash) {
        return [];
      }
      let routesParams = /* @__PURE__ */ new Set();
      let foundOptOutRoute = false;
      nextMatches.forEach((m) => {
        let manifestRoute = manifest.routes[m.route.id];
        if (!manifestRoute || !manifestRoute.hasLoader) {
          return;
        }
        if (!newMatchesForData.some((m2) => m2.route.id === m.route.id) && m.route.id in loaderData && routeModules[m.route.id]?.shouldRevalidate) {
          foundOptOutRoute = true;
        } else if (manifestRoute.hasClientLoader) {
          foundOptOutRoute = true;
        } else {
          routesParams.add(m.route.id);
        }
      });
      if (routesParams.size === 0) {
        return [];
      }
      let url = singleFetchUrl(
        page,
        basename,
        future.v8_trailingSlashAwareDataRequests,
        "data"
      );
      if (foundOptOutRoute && routesParams.size > 0) {
        url.searchParams.set(
          "_routes",
          nextMatches.filter((m) => routesParams.has(m.route.id)).map((m) => m.route.id).join(",")
        );
      }
      return [url.pathname + url.search];
    }, [
      basename,
      future.v8_trailingSlashAwareDataRequests,
      loaderData,
      location,
      manifest,
      newMatchesForData,
      nextMatches,
      page,
      routeModules
    ]);
    let moduleHrefs = React8.useMemo(
      () => getModuleLinkHrefs(newMatchesForAssets, manifest),
      [newMatchesForAssets, manifest]
    );
    let keyedPrefetchLinks = useKeyedPrefetchLinks(newMatchesForAssets);
    return /* @__PURE__ */ React8.createElement(React8.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "prefetch", as: "fetch", href, ...linkProps })), moduleHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "modulepreload", href, ...linkProps })), keyedPrefetchLinks.map(({ key, link }) => (
      // these don't spread `linkProps` because they are full link descriptors
      // already with their own props
      /* @__PURE__ */ React8.createElement(
        "link",
        {
          key,
          nonce: linkProps.nonce,
          ...link,
          crossOrigin: link.crossOrigin ?? linkProps.crossOrigin
        }
      )
    )));
  }
  function mergeRefs(...refs) {
    return (value) => {
      refs.forEach((ref) => {
        if (typeof ref === "function") {
          ref(value);
        } else if (ref != null) {
          ref.current = value;
        }
      });
    };
  }
  var isBrowser2 = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
  try {
    if (isBrowser2) {
      window.__reactRouterVersion = // @ts-expect-error
      "7.16.0";
    }
  } catch (e2) {
  }
  function BrowserRouter({
    basename,
    children,
    useTransitions,
    window: window2
  }) {
    let historyRef = React10.useRef();
    if (historyRef.current == null) {
      historyRef.current = createBrowserHistory({ window: window2, v5Compat: true });
    }
    let history = historyRef.current;
    let [state, setStateImpl] = React10.useState({
      action: history.action,
      location: history.location
    });
    let setState = React10.useCallback(
      (newState) => {
        if (useTransitions === false) {
          setStateImpl(newState);
        } else {
          React10.startTransition(() => setStateImpl(newState));
        }
      },
      [useTransitions]
    );
    React10.useLayoutEffect(() => history.listen(setState), [history, setState]);
    return /* @__PURE__ */ React10.createElement(
      Router,
      {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        useTransitions
      }
    );
  }
  function HistoryRouter({
    basename,
    children,
    history,
    useTransitions
  }) {
    let [state, setStateImpl] = React10.useState({
      action: history.action,
      location: history.location
    });
    let setState = React10.useCallback(
      (newState) => {
        if (useTransitions === false) {
          setStateImpl(newState);
        } else {
          React10.startTransition(() => setStateImpl(newState));
        }
      },
      [useTransitions]
    );
    React10.useLayoutEffect(() => history.listen(setState), [history, setState]);
    return /* @__PURE__ */ React10.createElement(
      Router,
      {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        useTransitions
      }
    );
  }
  HistoryRouter.displayName = "unstable_HistoryRouter";
  var ABSOLUTE_URL_REGEX2 = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
  var Link = React10.forwardRef(
    function LinkWithRef({
      onClick,
      discover = "render",
      prefetch = "none",
      relative,
      reloadDocument,
      replace: replace2,
      mask,
      state,
      target,
      to,
      preventScrollReset,
      viewTransition,
      defaultShouldRevalidate,
      ...rest
    }, forwardedRef) {
      let { basename, navigator: navigator2, useTransitions } = React10.useContext(NavigationContext);
      let isAbsolute = typeof to === "string" && ABSOLUTE_URL_REGEX2.test(to);
      let parsed = parseToInfo(to, basename);
      to = parsed.to;
      let href = useHref(to, { relative });
      let location = useLocation();
      let maskedHref = null;
      if (mask) {
        let resolved = resolveTo(
          mask,
          [],
          location.mask ? location.mask.pathname : "/",
          true
        );
        if (basename !== "/") {
          resolved.pathname = resolved.pathname === "/" ? basename : joinPaths([basename, resolved.pathname]);
        }
        maskedHref = navigator2.createHref(resolved);
      }
      let [shouldPrefetch, prefetchRef, prefetchHandlers] = usePrefetchBehavior(
        prefetch,
        rest
      );
      let internalOnClick = useLinkClickHandler(to, {
        replace: replace2,
        mask,
        state,
        target,
        preventScrollReset,
        relative,
        viewTransition,
        defaultShouldRevalidate,
        useTransitions
      });
      function handleClick(event) {
        if (onClick) onClick(event);
        if (!event.defaultPrevented) {
          internalOnClick(event);
        }
      }
      let isSpaLink = !(parsed.isExternal || reloadDocument);
      let link = (
        // eslint-disable-next-line jsx-a11y/anchor-has-content
        /* @__PURE__ */ React10.createElement(
          "a",
          {
            ...rest,
            ...prefetchHandlers,
            href: (isSpaLink ? maskedHref : void 0) || parsed.absoluteURL || href,
            onClick: isSpaLink ? handleClick : onClick,
            ref: mergeRefs(forwardedRef, prefetchRef),
            target,
            "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
          }
        )
      );
      return shouldPrefetch && !isAbsolute ? /* @__PURE__ */ React10.createElement(React10.Fragment, null, link, /* @__PURE__ */ React10.createElement(PrefetchPageLinks, { page: href })) : link;
    }
  );
  Link.displayName = "Link";
  var NavLink = React10.forwardRef(
    function NavLinkWithRef({
      "aria-current": ariaCurrentProp = "page",
      caseSensitive = false,
      className: classNameProp = "",
      end = false,
      style: styleProp,
      to,
      viewTransition,
      children,
      ...rest
    }, ref) {
      let path = useResolvedPath(to, { relative: rest.relative });
      let location = useLocation();
      let routerState = React10.useContext(DataRouterStateContext);
      let { navigator: navigator2, basename } = React10.useContext(NavigationContext);
      let isTransitioning = routerState != null && // Conditional usage is OK here because the usage of a data router is static
      // eslint-disable-next-line react-hooks/rules-of-hooks
      useViewTransitionState(path) && viewTransition === true;
      let toPathname = navigator2.encodeLocation ? navigator2.encodeLocation(path).pathname : path.pathname;
      let locationPathname = location.pathname;
      let nextLocationPathname = routerState && routerState.navigation && routerState.navigation.location ? routerState.navigation.location.pathname : null;
      if (!caseSensitive) {
        locationPathname = locationPathname.toLowerCase();
        nextLocationPathname = nextLocationPathname ? nextLocationPathname.toLowerCase() : null;
        toPathname = toPathname.toLowerCase();
      }
      if (nextLocationPathname && basename) {
        nextLocationPathname = stripBasename(nextLocationPathname, basename) || nextLocationPathname;
      }
      const endSlashPosition = toPathname !== "/" && toPathname.endsWith("/") ? toPathname.length - 1 : toPathname.length;
      let isActive = locationPathname === toPathname || !end && locationPathname.startsWith(toPathname) && locationPathname.charAt(endSlashPosition) === "/";
      let isPending = nextLocationPathname != null && (nextLocationPathname === toPathname || !end && nextLocationPathname.startsWith(toPathname) && nextLocationPathname.charAt(toPathname.length) === "/");
      let renderProps = {
        isActive,
        isPending,
        isTransitioning
      };
      let ariaCurrent = isActive ? ariaCurrentProp : void 0;
      let className;
      if (typeof classNameProp === "function") {
        className = classNameProp(renderProps);
      } else {
        className = [
          classNameProp,
          isActive ? "active" : null,
          isPending ? "pending" : null,
          isTransitioning ? "transitioning" : null
        ].filter(Boolean).join(" ");
      }
      let style = typeof styleProp === "function" ? styleProp(renderProps) : styleProp;
      return /* @__PURE__ */ React10.createElement(
        Link,
        {
          ...rest,
          "aria-current": ariaCurrent,
          className,
          ref,
          style,
          to,
          viewTransition
        },
        typeof children === "function" ? children(renderProps) : children
      );
    }
  );
  NavLink.displayName = "NavLink";
  var Form = React10.forwardRef(
    ({
      discover = "render",
      fetcherKey,
      navigate,
      reloadDocument,
      replace: replace2,
      state,
      method = defaultMethod,
      action,
      onSubmit,
      relative,
      preventScrollReset,
      viewTransition,
      defaultShouldRevalidate,
      ...props
    }, forwardedRef) => {
      let { useTransitions } = React10.useContext(NavigationContext);
      let submit = useSubmit();
      let formAction = useFormAction(action, { relative });
      let formMethod = method.toLowerCase() === "get" ? "get" : "post";
      let isAbsolute = typeof action === "string" && ABSOLUTE_URL_REGEX2.test(action);
      let submitHandler = (event) => {
        onSubmit && onSubmit(event);
        if (event.defaultPrevented) return;
        event.preventDefault();
        let submitter = event.nativeEvent.submitter;
        let submitMethod = submitter?.getAttribute("formmethod") || method;
        let doSubmit = () => submit(submitter || event.currentTarget, {
          fetcherKey,
          method: submitMethod,
          navigate,
          replace: replace2,
          state,
          relative,
          preventScrollReset,
          viewTransition,
          defaultShouldRevalidate
        });
        if (useTransitions && navigate !== false) {
          React10.startTransition(() => doSubmit());
        } else {
          doSubmit();
        }
      };
      return /* @__PURE__ */ React10.createElement(
        "form",
        {
          ref: forwardedRef,
          method: formMethod,
          action: formAction,
          onSubmit: reloadDocument ? onSubmit : submitHandler,
          ...props,
          "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
        }
      );
    }
  );
  Form.displayName = "Form";
  function ScrollRestoration({
    getKey,
    storageKey,
    ...props
  }) {
    let remixContext = React10.useContext(FrameworkContext);
    let { basename } = React10.useContext(NavigationContext);
    let location = useLocation();
    let matches = useMatches();
    useScrollRestoration({ getKey, storageKey });
    let ssrKey = React10.useMemo(
      () => {
        if (!remixContext || !getKey) return null;
        let userKey = getScrollRestorationKey(
          location,
          matches,
          basename,
          getKey
        );
        return userKey !== location.key ? userKey : null;
      },
      // Nah, we only need this the first time for the SSR render
      // eslint-disable-next-line react-hooks/exhaustive-deps
      []
    );
    if (!remixContext || remixContext.isSpaMode) {
      return null;
    }
    let restoreScroll = ((storageKey2, restoreKey) => {
      if (!window.history.state || !window.history.state.key) {
        let key = Math.random().toString(32).slice(2);
        window.history.replaceState({ key }, "");
      }
      try {
        let positions = JSON.parse(sessionStorage.getItem(storageKey2) || "{}");
        let storedY = positions[restoreKey || window.history.state.key];
        if (typeof storedY === "number") {
          window.scrollTo(0, storedY);
        }
      } catch (error) {
        console.error(error);
        sessionStorage.removeItem(storageKey2);
      }
    }).toString();
    return /* @__PURE__ */ React10.createElement(
      "script",
      {
        ...props,
        suppressHydrationWarning: true,
        dangerouslySetInnerHTML: {
          __html: `(${restoreScroll})(${escapeHtml(
            JSON.stringify(storageKey || SCROLL_RESTORATION_STORAGE_KEY)
          )}, ${escapeHtml(JSON.stringify(ssrKey))})`
        }
      }
    );
  }
  ScrollRestoration.displayName = "ScrollRestoration";
  function getDataRouterConsoleError2(hookName) {
    return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
  }
  function useDataRouterContext3(hookName) {
    let ctx = React10.useContext(DataRouterContext);
    invariant(ctx, getDataRouterConsoleError2(hookName));
    return ctx;
  }
  function useDataRouterState2(hookName) {
    let state = React10.useContext(DataRouterStateContext);
    invariant(state, getDataRouterConsoleError2(hookName));
    return state;
  }
  function useLinkClickHandler(to, {
    target,
    replace: replaceProp,
    mask,
    state,
    preventScrollReset,
    relative,
    viewTransition,
    defaultShouldRevalidate,
    useTransitions
  } = {}) {
    let navigate = useNavigate();
    let location = useLocation();
    let path = useResolvedPath(to, { relative });
    return React10.useCallback(
      (event) => {
        if (shouldProcessLinkClick(event, target)) {
          event.preventDefault();
          let replace2 = replaceProp !== void 0 ? replaceProp : createPath(location) === createPath(path);
          let doNavigate = () => navigate(to, {
            replace: replace2,
            mask,
            state,
            preventScrollReset,
            relative,
            viewTransition,
            defaultShouldRevalidate
          });
          if (useTransitions) {
            React10.startTransition(() => doNavigate());
          } else {
            doNavigate();
          }
        }
      },
      [
        location,
        navigate,
        path,
        replaceProp,
        mask,
        state,
        target,
        to,
        preventScrollReset,
        relative,
        viewTransition,
        defaultShouldRevalidate,
        useTransitions
      ]
    );
  }
  function useSearchParams(defaultInit) {
    warning(
      typeof URLSearchParams !== "undefined",
      `You cannot use the \`useSearchParams\` hook in a browser that does not support the URLSearchParams API. If you need to support Internet Explorer 11, we recommend you load a polyfill such as https://github.com/ungap/url-search-params.`
    );
    let defaultSearchParamsRef = React10.useRef(createSearchParams(defaultInit));
    let hasSetSearchParamsRef = React10.useRef(false);
    let location = useLocation();
    let searchParams = React10.useMemo(
      () => (
        // Only merge in the defaults if we haven't yet called setSearchParams.
        // Once we call that we want those to take precedence, otherwise you can't
        // remove a param with setSearchParams({}) if it has an initial value
        getSearchParamsForLocation(
          location.search,
          hasSetSearchParamsRef.current ? null : defaultSearchParamsRef.current
        )
      ),
      [location.search]
    );
    let navigate = useNavigate();
    let setSearchParams = React10.useCallback(
      (nextInit, navigateOptions) => {
        const newSearchParams = createSearchParams(
          typeof nextInit === "function" ? nextInit(new URLSearchParams(searchParams)) : nextInit
        );
        hasSetSearchParamsRef.current = true;
        navigate("?" + newSearchParams, navigateOptions);
      },
      [navigate, searchParams]
    );
    return [searchParams, setSearchParams];
  }
  var fetcherId = 0;
  var getUniqueFetcherId = () => `__${String(++fetcherId)}__`;
  function useSubmit() {
    let { router } = useDataRouterContext3(
      "useSubmit"
      /* UseSubmit */
    );
    let { basename } = React10.useContext(NavigationContext);
    let currentRouteId = useRouteId();
    let routerFetch = router.fetch;
    let routerNavigate = router.navigate;
    return React10.useCallback(
      async (target, options = {}) => {
        let { action, method, encType, formData, body } = getFormSubmissionInfo(
          target,
          basename
        );
        if (options.navigate === false) {
          let key = options.fetcherKey || getUniqueFetcherId();
          await routerFetch(key, currentRouteId, options.action || action, {
            defaultShouldRevalidate: options.defaultShouldRevalidate,
            preventScrollReset: options.preventScrollReset,
            formData,
            body,
            formMethod: options.method || method,
            formEncType: options.encType || encType,
            flushSync: options.flushSync
          });
        } else {
          await routerNavigate(options.action || action, {
            defaultShouldRevalidate: options.defaultShouldRevalidate,
            preventScrollReset: options.preventScrollReset,
            formData,
            body,
            formMethod: options.method || method,
            formEncType: options.encType || encType,
            replace: options.replace,
            state: options.state,
            fromRouteId: currentRouteId,
            flushSync: options.flushSync,
            viewTransition: options.viewTransition
          });
        }
      },
      [routerFetch, routerNavigate, basename, currentRouteId]
    );
  }
  function useFormAction(action, { relative } = {}) {
    let { basename } = React10.useContext(NavigationContext);
    let routeContext = React10.useContext(RouteContext);
    invariant(routeContext, "useFormAction must be used inside a RouteContext");
    let [match] = routeContext.matches.slice(-1);
    let path = { ...useResolvedPath(action ? action : ".", { relative }) };
    let location = useLocation();
    if (action == null) {
      path.search = location.search;
      let params = new URLSearchParams(path.search);
      let indexValues = params.getAll("index");
      let hasNakedIndexParam = indexValues.some((v) => v === "");
      if (hasNakedIndexParam) {
        params.delete("index");
        indexValues.filter((v) => v).forEach((v) => params.append("index", v));
        let qs = params.toString();
        path.search = qs ? `?${qs}` : "";
      }
    }
    if ((!action || action === ".") && match.route.index) {
      path.search = path.search ? path.search.replace(/^\?/, "?index&") : "?index";
    }
    if (basename !== "/") {
      path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
    }
    return createPath(path);
  }
  var SCROLL_RESTORATION_STORAGE_KEY = "react-router-scroll-positions";
  var savedScrollPositions = {};
  function getScrollRestorationKey(location, matches, basename, getKey) {
    let key = null;
    if (getKey) {
      if (basename !== "/") {
        key = getKey(
          {
            ...location,
            pathname: stripBasename(location.pathname, basename) || location.pathname
          },
          matches
        );
      } else {
        key = getKey(location, matches);
      }
    }
    if (key == null) {
      key = location.key;
    }
    return key;
  }
  function useScrollRestoration({
    getKey,
    storageKey
  } = {}) {
    let { router } = useDataRouterContext3(
      "useScrollRestoration"
      /* UseScrollRestoration */
    );
    let { restoreScrollPosition, preventScrollReset } = useDataRouterState2(
      "useScrollRestoration"
      /* UseScrollRestoration */
    );
    let { basename } = React10.useContext(NavigationContext);
    let location = useLocation();
    let matches = useMatches();
    let navigation = useNavigation();
    React10.useEffect(() => {
      window.history.scrollRestoration = "manual";
      return () => {
        window.history.scrollRestoration = "auto";
      };
    }, []);
    usePageHide(
      React10.useCallback(() => {
        if (navigation.state === "idle") {
          let key = getScrollRestorationKey(location, matches, basename, getKey);
          savedScrollPositions[key] = window.scrollY;
        }
        try {
          sessionStorage.setItem(
            storageKey || SCROLL_RESTORATION_STORAGE_KEY,
            JSON.stringify(savedScrollPositions)
          );
        } catch (error) {
          warning(
            false,
            `Failed to save scroll positions in sessionStorage, <ScrollRestoration /> will not work properly (${error}).`
          );
        }
        window.history.scrollRestoration = "auto";
      }, [navigation.state, getKey, basename, location, matches, storageKey])
    );
    if (typeof document !== "undefined") {
      React10.useLayoutEffect(() => {
        try {
          let sessionPositions = sessionStorage.getItem(
            storageKey || SCROLL_RESTORATION_STORAGE_KEY
          );
          if (sessionPositions) {
            savedScrollPositions = JSON.parse(sessionPositions);
          }
        } catch (e2) {
        }
      }, [storageKey]);
      React10.useLayoutEffect(() => {
        let disableScrollRestoration = router?.enableScrollRestoration(
          savedScrollPositions,
          () => window.scrollY,
          getKey ? (location2, matches2) => getScrollRestorationKey(location2, matches2, basename, getKey) : void 0
        );
        return () => disableScrollRestoration && disableScrollRestoration();
      }, [router, basename, getKey]);
      React10.useLayoutEffect(() => {
        if (restoreScrollPosition === false) {
          return;
        }
        if (typeof restoreScrollPosition === "number") {
          window.scrollTo(0, restoreScrollPosition);
          return;
        }
        try {
          if (location.hash) {
            let el = document.getElementById(
              decodeURIComponent(location.hash.slice(1))
            );
            if (el) {
              el.scrollIntoView();
              return;
            }
          }
        } catch {
          warning(
            false,
            `"${location.hash.slice(
              1
            )}" is not a decodable element ID. The view will not scroll to it.`
          );
        }
        if (preventScrollReset === true) {
          return;
        }
        window.scrollTo(0, 0);
      }, [location, restoreScrollPosition, preventScrollReset]);
    }
  }
  function usePageHide(callback, options) {
    let { capture } = options || {};
    React10.useEffect(() => {
      let opts = capture != null ? { capture } : void 0;
      window.addEventListener("pagehide", callback, opts);
      return () => {
        window.removeEventListener("pagehide", callback, opts);
      };
    }, [callback, capture]);
  }
  function useViewTransitionState(to, { relative } = {}) {
    let vtContext = React10.useContext(ViewTransitionContext);
    invariant(
      vtContext != null,
      "`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?"
    );
    let { basename } = useDataRouterContext3(
      "useViewTransitionState"
      /* useViewTransitionState */
    );
    let path = useResolvedPath(to, { relative });
    if (!vtContext.isTransitioning) {
      return false;
    }
    let currentPath = stripBasename(vtContext.currentLocation.pathname, basename) || vtContext.currentLocation.pathname;
    let nextPath = stripBasename(vtContext.nextLocation.pathname, basename) || vtContext.nextLocation.pathname;
    return matchPath(path.pathname, nextPath) != null || matchPath(path.pathname, currentPath) != null;
  }

  // src/components/PreviewProvider.tsx
  var import_jsx_runtime = __toESM(require_react_shim(), 1);
  function PreviewProvider({ children }) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BrowserRouter, { children });
  }

  // node_modules/react-i18next/dist/es/Trans.js
  var import_react3 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/TransWithoutContext.js
  var import_react = __toESM(require_react_shim(), 1);

  // node_modules/i18next/dist/esm/i18next.js
  var isString = (obj) => typeof obj === "string";
  var defer = () => {
    let res;
    let rej;
    const promise = new Promise((resolve, reject) => {
      res = resolve;
      rej = reject;
    });
    promise.resolve = res;
    promise.reject = rej;
    return promise;
  };
  var makeString = (object) => {
    if (object == null) return "";
    return String(object);
  };
  var copy = (a, s, t2) => {
    a.forEach((m) => {
      if (s[m]) t2[m] = s[m];
    });
  };
  var lastOfPathSeparatorRegExp = /###/g;
  var cleanKey = (key) => key && key.includes("###") ? key.replace(lastOfPathSeparatorRegExp, ".") : key;
  var canNotTraverseDeeper = (object) => !object || isString(object);
  var getLastOfPath = (object, path, Empty) => {
    const stack = !isString(path) ? path : path.split(".");
    let stackIndex = 0;
    while (stackIndex < stack.length - 1) {
      if (canNotTraverseDeeper(object)) return {};
      const key = cleanKey(stack[stackIndex]);
      if (!object[key] && Empty) object[key] = new Empty();
      if (Object.prototype.hasOwnProperty.call(object, key)) {
        object = object[key];
      } else {
        object = {};
      }
      ++stackIndex;
    }
    if (canNotTraverseDeeper(object)) return {};
    return {
      obj: object,
      k: cleanKey(stack[stackIndex])
    };
  };
  var setPath = (object, path, newValue) => {
    const {
      obj,
      k
    } = getLastOfPath(object, path, Object);
    if (obj !== void 0 || path.length === 1) {
      obj[k] = newValue;
      return;
    }
    let e2 = path[path.length - 1];
    let p = path.slice(0, path.length - 1);
    let last = getLastOfPath(object, p, Object);
    while (last.obj === void 0 && p.length) {
      e2 = `${p[p.length - 1]}.${e2}`;
      p = p.slice(0, p.length - 1);
      last = getLastOfPath(object, p, Object);
      if (last?.obj && typeof last.obj[`${last.k}.${e2}`] !== "undefined") {
        last.obj = void 0;
      }
    }
    last.obj[`${last.k}.${e2}`] = newValue;
  };
  var pushPath = (object, path, newValue, concat) => {
    const {
      obj,
      k
    } = getLastOfPath(object, path, Object);
    obj[k] = obj[k] || [];
    obj[k].push(newValue);
  };
  var getPath = (object, path) => {
    const {
      obj,
      k
    } = getLastOfPath(object, path);
    if (!obj) return void 0;
    if (!Object.prototype.hasOwnProperty.call(obj, k)) return void 0;
    return obj[k];
  };
  var getPathWithDefaults = (data2, defaultData, key) => {
    const value = getPath(data2, key);
    if (value !== void 0) {
      return value;
    }
    return getPath(defaultData, key);
  };
  var deepExtend = (target, source, overwrite) => {
    for (const prop in source) {
      if (prop !== "__proto__" && prop !== "constructor") {
        if (prop in target) {
          if (isString(target[prop]) || target[prop] instanceof String || isString(source[prop]) || source[prop] instanceof String) {
            if (overwrite) target[prop] = source[prop];
          } else {
            deepExtend(target[prop], source[prop], overwrite);
          }
        } else {
          target[prop] = source[prop];
        }
      }
    }
    return target;
  };
  var regexEscape = (str) => str.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
  var _entityMap = {
    "&": "&amp;",
    "<": "&lt;",
    ">": "&gt;",
    '"': "&quot;",
    "'": "&#39;",
    "/": "&#x2F;"
  };
  var escape = (data2) => {
    if (isString(data2)) {
      return data2.replace(/[&<>"'\/]/g, (s) => _entityMap[s]);
    }
    return data2;
  };
  var RegExpCache = class {
    constructor(capacity) {
      this.capacity = capacity;
      this.regExpMap = /* @__PURE__ */ new Map();
      this.regExpQueue = [];
    }
    getRegExp(pattern) {
      const regExpFromCache = this.regExpMap.get(pattern);
      if (regExpFromCache !== void 0) {
        return regExpFromCache;
      }
      const regExpNew = new RegExp(pattern);
      if (this.regExpQueue.length === this.capacity) {
        this.regExpMap.delete(this.regExpQueue.shift());
      }
      this.regExpMap.set(pattern, regExpNew);
      this.regExpQueue.push(pattern);
      return regExpNew;
    }
  };
  var chars = [" ", ",", "?", "!", ";"];
  var looksLikeObjectPathRegExpCache = new RegExpCache(20);
  var looksLikeObjectPath = (key, nsSeparator, keySeparator) => {
    nsSeparator = nsSeparator || "";
    keySeparator = keySeparator || "";
    const possibleChars = chars.filter((c) => !nsSeparator.includes(c) && !keySeparator.includes(c));
    if (possibleChars.length === 0) return true;
    const r = looksLikeObjectPathRegExpCache.getRegExp(`(${possibleChars.map((c) => c === "?" ? "\\?" : c).join("|")})`);
    let matched = !r.test(key);
    if (!matched) {
      const ki = key.indexOf(keySeparator);
      if (ki > 0 && !r.test(key.substring(0, ki))) {
        matched = true;
      }
    }
    return matched;
  };
  var deepFind = (obj, path, keySeparator = ".") => {
    if (!obj) return void 0;
    if (obj[path]) {
      if (!Object.prototype.hasOwnProperty.call(obj, path)) return void 0;
      return obj[path];
    }
    const tokens = path.split(keySeparator);
    let current = obj;
    for (let i = 0; i < tokens.length; ) {
      if (!current || typeof current !== "object") {
        return void 0;
      }
      let next;
      let nextPath = "";
      for (let j = i; j < tokens.length; ++j) {
        if (j !== i) {
          nextPath += keySeparator;
        }
        nextPath += tokens[j];
        next = current[nextPath];
        if (next !== void 0) {
          if (["string", "number", "boolean"].includes(typeof next) && j < tokens.length - 1) {
            continue;
          }
          i += j - i + 1;
          break;
        }
      }
      current = next;
    }
    return current;
  };
  var getCleanedCode = (code) => code?.replace(/_/g, "-");
  var consoleLogger = {
    type: "logger",
    log(args) {
      this.output("log", args);
    },
    warn(args) {
      this.output("warn", args);
    },
    error(args) {
      this.output("error", args);
    },
    output(type, args) {
      console?.[type]?.apply?.(console, args);
    }
  };
  var Logger = class _Logger {
    constructor(concreteLogger, options = {}) {
      this.init(concreteLogger, options);
    }
    init(concreteLogger, options = {}) {
      this.prefix = options.prefix || "i18next:";
      this.logger = concreteLogger || consoleLogger;
      this.options = options;
      this.debug = options.debug;
    }
    log(...args) {
      return this.forward(args, "log", "", true);
    }
    warn(...args) {
      return this.forward(args, "warn", "", true);
    }
    error(...args) {
      return this.forward(args, "error", "");
    }
    deprecate(...args) {
      return this.forward(args, "warn", "WARNING DEPRECATED: ", true);
    }
    forward(args, lvl, prefix, debugOnly) {
      if (debugOnly && !this.debug) return null;
      args = args.map((a) => isString(a) ? a.replace(/[\r\n\x00-\x1F\x7F]/g, " ") : a);
      if (isString(args[0])) args[0] = `${prefix}${this.prefix} ${args[0]}`;
      return this.logger[lvl](args);
    }
    create(moduleName) {
      return new _Logger(this.logger, {
        ...{
          prefix: `${this.prefix}:${moduleName}:`
        },
        ...this.options
      });
    }
    clone(options) {
      options = options || this.options;
      options.prefix = options.prefix || this.prefix;
      return new _Logger(this.logger, options);
    }
  };
  var baseLogger = new Logger();
  var EventEmitter = class {
    constructor() {
      this.observers = {};
    }
    on(events, listener) {
      events.split(" ").forEach((event) => {
        if (!this.observers[event]) this.observers[event] = /* @__PURE__ */ new Map();
        const numListeners = this.observers[event].get(listener) || 0;
        this.observers[event].set(listener, numListeners + 1);
      });
      return this;
    }
    off(event, listener) {
      if (!this.observers[event]) return;
      if (!listener) {
        delete this.observers[event];
        return;
      }
      this.observers[event].delete(listener);
    }
    once(event, listener) {
      const wrapper = (...args) => {
        listener(...args);
        this.off(event, wrapper);
      };
      this.on(event, wrapper);
      return this;
    }
    emit(event, ...args) {
      if (this.observers[event]) {
        const cloned = Array.from(this.observers[event].entries());
        cloned.forEach(([observer, numTimesAdded]) => {
          for (let i = 0; i < numTimesAdded; i++) {
            observer(...args);
          }
        });
      }
      if (this.observers["*"]) {
        const cloned = Array.from(this.observers["*"].entries());
        cloned.forEach(([observer, numTimesAdded]) => {
          for (let i = 0; i < numTimesAdded; i++) {
            observer(event, ...args);
          }
        });
      }
    }
  };
  var ResourceStore = class extends EventEmitter {
    constructor(data2, options = {
      ns: ["translation"],
      defaultNS: "translation"
    }) {
      super();
      this.data = data2 || {};
      this.options = options;
      if (this.options.keySeparator === void 0) {
        this.options.keySeparator = ".";
      }
      if (this.options.ignoreJSONStructure === void 0) {
        this.options.ignoreJSONStructure = true;
      }
    }
    addNamespaces(ns) {
      if (!this.options.ns.includes(ns)) {
        this.options.ns.push(ns);
      }
    }
    removeNamespaces(ns) {
      const index = this.options.ns.indexOf(ns);
      if (index > -1) {
        this.options.ns.splice(index, 1);
      }
    }
    getResource(lng, ns, key, options = {}) {
      const keySeparator = options.keySeparator !== void 0 ? options.keySeparator : this.options.keySeparator;
      const ignoreJSONStructure = options.ignoreJSONStructure !== void 0 ? options.ignoreJSONStructure : this.options.ignoreJSONStructure;
      let path;
      if (lng.includes(".")) {
        path = lng.split(".");
      } else {
        path = [lng, ns];
        if (key) {
          if (Array.isArray(key)) {
            path.push(...key);
          } else if (isString(key) && keySeparator) {
            path.push(...key.split(keySeparator));
          } else {
            path.push(key);
          }
        }
      }
      const result = getPath(this.data, path);
      if (!result && !ns && !key && lng.includes(".")) {
        lng = path[0];
        ns = path[1];
        key = path.slice(2).join(".");
      }
      if (result || !ignoreJSONStructure || !isString(key)) return result;
      return deepFind(this.data?.[lng]?.[ns], key, keySeparator);
    }
    addResource(lng, ns, key, value, options = {
      silent: false
    }) {
      const keySeparator = options.keySeparator !== void 0 ? options.keySeparator : this.options.keySeparator;
      let path = [lng, ns];
      if (key) path = path.concat(keySeparator ? key.split(keySeparator) : key);
      if (lng.includes(".")) {
        path = lng.split(".");
        value = ns;
        ns = path[1];
      }
      this.addNamespaces(ns);
      setPath(this.data, path, value);
      if (!options.silent) this.emit("added", lng, ns, key, value);
    }
    addResources(lng, ns, resources, options = {
      silent: false
    }) {
      for (const m in resources) {
        if (isString(resources[m]) || Array.isArray(resources[m])) this.addResource(lng, ns, m, resources[m], {
          silent: true
        });
      }
      if (!options.silent) this.emit("added", lng, ns, resources);
    }
    addResourceBundle(lng, ns, resources, deep, overwrite, options = {
      silent: false,
      skipCopy: false
    }) {
      let path = [lng, ns];
      if (lng.includes(".")) {
        path = lng.split(".");
        deep = resources;
        resources = ns;
        ns = path[1];
      }
      this.addNamespaces(ns);
      let pack = getPath(this.data, path) || {};
      if (!options.skipCopy) resources = JSON.parse(JSON.stringify(resources));
      if (deep) {
        deepExtend(pack, resources, overwrite);
      } else {
        pack = {
          ...pack,
          ...resources
        };
      }
      setPath(this.data, path, pack);
      if (!options.silent) this.emit("added", lng, ns, resources);
    }
    removeResourceBundle(lng, ns) {
      if (this.hasResourceBundle(lng, ns)) {
        delete this.data[lng][ns];
      }
      this.removeNamespaces(ns);
      this.emit("removed", lng, ns);
    }
    hasResourceBundle(lng, ns) {
      return this.getResource(lng, ns) !== void 0;
    }
    getResourceBundle(lng, ns) {
      if (!ns) ns = this.options.defaultNS;
      return this.getResource(lng, ns);
    }
    getDataByLanguage(lng) {
      return this.data[lng];
    }
    hasLanguageSomeTranslations(lng) {
      const data2 = this.getDataByLanguage(lng);
      const n = data2 && Object.keys(data2) || [];
      return !!n.find((v) => data2[v] && Object.keys(data2[v]).length > 0);
    }
    toJSON() {
      return this.data;
    }
  };
  var postProcessor = {
    processors: {},
    addPostProcessor(module) {
      this.processors[module.name] = module;
    },
    handle(processors, value, key, options, translator) {
      processors.forEach((processor) => {
        value = this.processors[processor]?.process(value, key, options, translator) ?? value;
      });
      return value;
    }
  };
  var PATH_KEY = /* @__PURE__ */ Symbol("i18next/PATH_KEY");
  function createProxy() {
    const state = [];
    const handler = /* @__PURE__ */ Object.create(null);
    let proxy;
    handler.get = (target, key) => {
      proxy?.revoke?.();
      if (key === PATH_KEY) return state;
      state.push(key);
      proxy = Proxy.revocable(target, handler);
      return proxy.proxy;
    };
    return Proxy.revocable(/* @__PURE__ */ Object.create(null), handler).proxy;
  }
  function keysFromSelector(selector, opts) {
    const {
      [PATH_KEY]: path
    } = selector(createProxy());
    const keySeparator = opts?.keySeparator ?? ".";
    const nsSeparator = opts?.nsSeparator ?? ":";
    const strict = opts?.enableSelector === "strict";
    if (path.length > 1 && nsSeparator) {
      const ns = opts?.ns;
      const nsList = strict ? Array.isArray(ns) ? ns : ns ? [ns] : null : Array.isArray(ns) ? ns : null;
      if (nsList) {
        const candidates = strict ? nsList : nsList.length > 1 ? nsList.slice(1) : [];
        if (candidates.includes(path[0])) {
          return `${path[0]}${nsSeparator}${path.slice(1).join(keySeparator)}`;
        }
      }
    }
    return path.join(keySeparator);
  }
  var shouldHandleAsObject = (res) => !isString(res) && typeof res !== "boolean" && typeof res !== "number";
  var Translator = class _Translator extends EventEmitter {
    constructor(services, options = {}) {
      super();
      copy(["resourceStore", "languageUtils", "pluralResolver", "interpolator", "backendConnector", "i18nFormat", "utils"], services, this);
      this.options = options;
      if (this.options.keySeparator === void 0) {
        this.options.keySeparator = ".";
      }
      this.logger = baseLogger.create("translator");
      this.checkedLoadedFor = {};
    }
    changeLanguage(lng) {
      if (lng) this.language = lng;
    }
    exists(key, o = {
      interpolation: {}
    }) {
      const opt = {
        ...o
      };
      if (key == null) return false;
      const resolved = this.resolve(key, opt);
      if (resolved?.res === void 0) return false;
      const isObject3 = shouldHandleAsObject(resolved.res);
      if (opt.returnObjects === false && isObject3) {
        return false;
      }
      return true;
    }
    extractFromKey(key, opt) {
      let nsSeparator = opt.nsSeparator !== void 0 ? opt.nsSeparator : this.options.nsSeparator;
      if (nsSeparator === void 0) nsSeparator = ":";
      const keySeparator = opt.keySeparator !== void 0 ? opt.keySeparator : this.options.keySeparator;
      let namespaces = opt.ns || this.options.defaultNS || [];
      const wouldCheckForNsInKey = nsSeparator && key.includes(nsSeparator);
      const seemsNaturalLanguage = !this.options.userDefinedKeySeparator && !opt.keySeparator && !this.options.userDefinedNsSeparator && !opt.nsSeparator && !looksLikeObjectPath(key, nsSeparator, keySeparator);
      if (wouldCheckForNsInKey && !seemsNaturalLanguage) {
        const m = key.match(this.interpolator.nestingRegexp);
        if (m && m.length > 0) {
          return {
            key,
            namespaces: isString(namespaces) ? [namespaces] : namespaces
          };
        }
        const parts = key.split(nsSeparator);
        if (nsSeparator !== keySeparator || nsSeparator === keySeparator && this.options.ns.includes(parts[0])) namespaces = parts.shift();
        key = parts.join(keySeparator);
      }
      return {
        key,
        namespaces: isString(namespaces) ? [namespaces] : namespaces
      };
    }
    translate(keys, o, lastKey) {
      let opt = typeof o === "object" ? {
        ...o
      } : o;
      if (typeof opt !== "object" && this.options.overloadTranslationOptionHandler) {
        opt = this.options.overloadTranslationOptionHandler(arguments);
      }
      if (typeof opt === "object") opt = {
        ...opt
      };
      if (!opt) opt = {};
      if (keys == null) return "";
      if (typeof keys === "function") keys = keysFromSelector(keys, {
        ...this.options,
        ...opt
      });
      if (!Array.isArray(keys)) keys = [String(keys)];
      keys = keys.map((k) => typeof k === "function" ? keysFromSelector(k, {
        ...this.options,
        ...opt
      }) : String(k));
      const returnDetails = opt.returnDetails !== void 0 ? opt.returnDetails : this.options.returnDetails;
      const keySeparator = opt.keySeparator !== void 0 ? opt.keySeparator : this.options.keySeparator;
      const {
        key,
        namespaces
      } = this.extractFromKey(keys[keys.length - 1], opt);
      const namespace = namespaces[namespaces.length - 1];
      let nsSeparator = opt.nsSeparator !== void 0 ? opt.nsSeparator : this.options.nsSeparator;
      if (nsSeparator === void 0) nsSeparator = ":";
      const lng = opt.lng || this.language;
      const appendNamespaceToCIMode = opt.appendNamespaceToCIMode || this.options.appendNamespaceToCIMode;
      if (lng?.toLowerCase() === "cimode") {
        if (appendNamespaceToCIMode) {
          if (returnDetails) {
            return {
              res: `${namespace}${nsSeparator}${key}`,
              usedKey: key,
              exactUsedKey: key,
              usedLng: lng,
              usedNS: namespace,
              usedParams: this.getUsedParamsDetails(opt)
            };
          }
          return `${namespace}${nsSeparator}${key}`;
        }
        if (returnDetails) {
          return {
            res: key,
            usedKey: key,
            exactUsedKey: key,
            usedLng: lng,
            usedNS: namespace,
            usedParams: this.getUsedParamsDetails(opt)
          };
        }
        return key;
      }
      const resolved = this.resolve(keys, opt);
      let res = resolved?.res;
      const resUsedKey = resolved?.usedKey || key;
      const resExactUsedKey = resolved?.exactUsedKey || key;
      const noObject = ["[object Number]", "[object Function]", "[object RegExp]"];
      const joinArrays = opt.joinArrays !== void 0 ? opt.joinArrays : this.options.joinArrays;
      const handleAsObjectInI18nFormat = !this.i18nFormat || this.i18nFormat.handleAsObject;
      const needsPluralHandling = opt.count !== void 0 && !isString(opt.count);
      const hasDefaultValue = _Translator.hasDefaultValue(opt);
      const defaultValueSuffix = needsPluralHandling ? this.pluralResolver.getSuffix(lng, opt.count, opt) : "";
      const defaultValueSuffixOrdinalFallback = opt.ordinal && needsPluralHandling ? this.pluralResolver.getSuffix(lng, opt.count, {
        ordinal: false
      }) : "";
      const needsZeroSuffixLookup = needsPluralHandling && !opt.ordinal && opt.count === 0;
      const defaultValue = needsZeroSuffixLookup && opt[`defaultValue${this.options.pluralSeparator}zero`] || opt[`defaultValue${defaultValueSuffix}`] || opt[`defaultValue${defaultValueSuffixOrdinalFallback}`] || opt.defaultValue;
      let resForObjHndl = res;
      if (handleAsObjectInI18nFormat && !res && hasDefaultValue) {
        resForObjHndl = defaultValue;
      }
      const handleAsObject = shouldHandleAsObject(resForObjHndl);
      const resType = Object.prototype.toString.apply(resForObjHndl);
      if (handleAsObjectInI18nFormat && resForObjHndl && handleAsObject && !noObject.includes(resType) && !(isString(joinArrays) && Array.isArray(resForObjHndl))) {
        if (!opt.returnObjects && !this.options.returnObjects) {
          if (!this.options.returnedObjectHandler) {
            this.logger.warn("accessing an object - but returnObjects options is not enabled!");
          }
          const r = this.options.returnedObjectHandler ? this.options.returnedObjectHandler(resUsedKey, resForObjHndl, {
            ...opt,
            ns: namespaces
          }) : `key '${key} (${this.language})' returned an object instead of string.`;
          if (returnDetails) {
            resolved.res = r;
            resolved.usedParams = this.getUsedParamsDetails(opt);
            return resolved;
          }
          return r;
        }
        if (keySeparator) {
          const resTypeIsArray = Array.isArray(resForObjHndl);
          const copy2 = resTypeIsArray ? [] : {};
          const newKeyToUse = resTypeIsArray ? resExactUsedKey : resUsedKey;
          for (const m in resForObjHndl) {
            if (Object.prototype.hasOwnProperty.call(resForObjHndl, m)) {
              const deepKey = `${newKeyToUse}${keySeparator}${m}`;
              if (hasDefaultValue && !res) {
                copy2[m] = this.translate(deepKey, {
                  ...opt,
                  defaultValue: shouldHandleAsObject(defaultValue) ? defaultValue[m] : void 0,
                  ...{
                    joinArrays: false,
                    ns: namespaces
                  }
                });
              } else {
                copy2[m] = this.translate(deepKey, {
                  ...opt,
                  ...{
                    joinArrays: false,
                    ns: namespaces
                  }
                });
              }
              if (copy2[m] === deepKey) copy2[m] = resForObjHndl[m];
            }
          }
          res = copy2;
        }
      } else if (handleAsObjectInI18nFormat && isString(joinArrays) && Array.isArray(res)) {
        res = res.join(joinArrays);
        if (res) res = this.extendTranslation(res, keys, opt, lastKey);
      } else {
        let usedDefault = false;
        let usedKey = false;
        if (!this.isValidLookup(res) && hasDefaultValue) {
          usedDefault = true;
          res = defaultValue;
        }
        if (!this.isValidLookup(res)) {
          usedKey = true;
          res = key;
        }
        const missingKeyNoValueFallbackToKey = opt.missingKeyNoValueFallbackToKey || this.options.missingKeyNoValueFallbackToKey;
        const resForMissing = missingKeyNoValueFallbackToKey && usedKey ? void 0 : res;
        const updateMissing = hasDefaultValue && defaultValue !== res && this.options.updateMissing;
        if (usedKey || usedDefault || updateMissing) {
          this.logger.log(updateMissing ? "updateKey" : "missingKey", lng, namespace, needsPluralHandling && !updateMissing ? `${key}${this.pluralResolver.getSuffix(lng, opt.count, opt)}` : key, updateMissing ? defaultValue : res);
          if (keySeparator) {
            const fk = this.resolve(key, {
              ...opt,
              keySeparator: false
            });
            if (fk && fk.res) this.logger.warn("Seems the loaded translations were in flat JSON format instead of nested. Either set keySeparator: false on init or make sure your translations are published in nested format.");
          }
          let lngs = [];
          const fallbackLngs = this.languageUtils.getFallbackCodes(this.options.fallbackLng, opt.lng || this.language);
          if (this.options.saveMissingTo === "fallback" && fallbackLngs && fallbackLngs[0]) {
            for (let i = 0; i < fallbackLngs.length; i++) {
              lngs.push(fallbackLngs[i]);
            }
          } else if (this.options.saveMissingTo === "all") {
            lngs = this.languageUtils.toResolveHierarchy(opt.lng || this.language);
          } else {
            lngs.push(opt.lng || this.language);
          }
          const send = (l, k, specificDefaultValue) => {
            const defaultForMissing = hasDefaultValue && specificDefaultValue !== res ? specificDefaultValue : resForMissing;
            if (this.options.missingKeyHandler) {
              this.options.missingKeyHandler(l, namespace, k, defaultForMissing, updateMissing, opt);
            } else if (this.backendConnector?.saveMissing) {
              this.backendConnector.saveMissing(l, namespace, k, defaultForMissing, updateMissing, opt);
            }
            this.emit("missingKey", l, namespace, k, res);
          };
          if (this.options.saveMissing) {
            if (this.options.saveMissingPlurals && needsPluralHandling) {
              lngs.forEach((language) => {
                const suffixes = this.pluralResolver.getSuffixes(language, opt);
                if (needsZeroSuffixLookup && opt[`defaultValue${this.options.pluralSeparator}zero`] && !suffixes.includes(`${this.options.pluralSeparator}zero`)) {
                  suffixes.push(`${this.options.pluralSeparator}zero`);
                }
                suffixes.forEach((suffix) => {
                  send([language], key + suffix, opt[`defaultValue${suffix}`] || defaultValue);
                });
              });
            } else {
              send(lngs, key, defaultValue);
            }
          }
        }
        res = this.extendTranslation(res, keys, opt, resolved, lastKey);
        if (usedKey && res === key && this.options.appendNamespaceToMissingKey) {
          res = `${namespace}${nsSeparator}${key}`;
        }
        if ((usedKey || usedDefault) && this.options.parseMissingKeyHandler) {
          res = this.options.parseMissingKeyHandler(this.options.appendNamespaceToMissingKey ? `${namespace}${nsSeparator}${key}` : key, usedDefault ? res : void 0, opt);
        }
      }
      if (returnDetails) {
        resolved.res = res;
        resolved.usedParams = this.getUsedParamsDetails(opt);
        return resolved;
      }
      return res;
    }
    extendTranslation(res, key, opt, resolved, lastKey) {
      if (this.i18nFormat?.parse) {
        res = this.i18nFormat.parse(res, {
          ...this.options.interpolation.defaultVariables,
          ...opt
        }, opt.lng || this.language || resolved.usedLng, resolved.usedNS, resolved.usedKey, {
          resolved
        });
      } else if (!opt.skipInterpolation) {
        if (opt.interpolation) this.interpolator.init({
          ...opt,
          ...{
            interpolation: {
              ...this.options.interpolation,
              ...opt.interpolation
            }
          }
        });
        const skipOnVariables = isString(res) && (opt?.interpolation?.skipOnVariables !== void 0 ? opt.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables);
        let nestBef;
        if (skipOnVariables) {
          const nb = res.match(this.interpolator.nestingRegexp);
          nestBef = nb && nb.length;
        }
        let data2 = opt.replace && !isString(opt.replace) ? opt.replace : opt;
        if (this.options.interpolation.defaultVariables) data2 = {
          ...this.options.interpolation.defaultVariables,
          ...data2
        };
        res = this.interpolator.interpolate(res, data2, opt.lng || this.language || resolved.usedLng, opt);
        if (skipOnVariables) {
          const na = res.match(this.interpolator.nestingRegexp);
          const nestAft = na && na.length;
          if (nestBef < nestAft) opt.nest = false;
        }
        if (!opt.lng && resolved && resolved.res) opt.lng = this.language || resolved.usedLng;
        if (opt.nest !== false) res = this.interpolator.nest(res, (...args) => {
          if (lastKey?.[0] === args[0] && !opt.context) {
            this.logger.warn(`It seems you are nesting recursively key: ${args[0]} in key: ${key[0]}`);
            return null;
          }
          return this.translate(...args, key);
        }, opt);
        if (opt.interpolation) this.interpolator.reset();
      }
      const postProcess = opt.postProcess || this.options.postProcess;
      const postProcessorNames = isString(postProcess) ? [postProcess] : postProcess;
      if (res != null && postProcessorNames?.length && opt.applyPostProcessor !== false) {
        res = postProcessor.handle(postProcessorNames, res, key, this.options && this.options.postProcessPassResolved ? {
          i18nResolved: {
            ...resolved,
            usedParams: this.getUsedParamsDetails(opt)
          },
          ...opt
        } : opt, this);
      }
      return res;
    }
    resolve(keys, opt = {}) {
      let found;
      let usedKey;
      let exactUsedKey;
      let usedLng;
      let usedNS;
      if (isString(keys)) keys = [keys];
      if (Array.isArray(keys)) keys = keys.map((k) => typeof k === "function" ? keysFromSelector(k, {
        ...this.options,
        ...opt
      }) : k);
      keys.forEach((k) => {
        if (this.isValidLookup(found)) return;
        const extracted = this.extractFromKey(k, opt);
        const key = extracted.key;
        usedKey = key;
        let namespaces = extracted.namespaces;
        if (this.options.fallbackNS) namespaces = namespaces.concat(this.options.fallbackNS);
        const needsPluralHandling = opt.count !== void 0 && !isString(opt.count);
        const needsZeroSuffixLookup = needsPluralHandling && !opt.ordinal && opt.count === 0;
        const needsContextHandling = opt.context !== void 0 && (isString(opt.context) || typeof opt.context === "number") && opt.context !== "";
        const codes = opt.lngs ? opt.lngs : this.languageUtils.toResolveHierarchy(opt.lng || this.language, opt.fallbackLng);
        namespaces.forEach((ns) => {
          if (this.isValidLookup(found)) return;
          usedNS = ns;
          if (!this.checkedLoadedFor[`${codes[0]}-${ns}`] && this.utils?.hasLoadedNamespace && !this.utils?.hasLoadedNamespace(usedNS)) {
            this.checkedLoadedFor[`${codes[0]}-${ns}`] = true;
            this.logger.warn(`key "${usedKey}" for languages "${codes.join(", ")}" won't get resolved as namespace "${usedNS}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
          }
          codes.forEach((code) => {
            if (this.isValidLookup(found)) return;
            usedLng = code;
            const finalKeys = [key];
            if (this.i18nFormat?.addLookupKeys) {
              this.i18nFormat.addLookupKeys(finalKeys, key, code, ns, opt);
            } else {
              let pluralSuffix;
              if (needsPluralHandling) pluralSuffix = this.pluralResolver.getSuffix(code, opt.count, opt);
              const zeroSuffix = `${this.options.pluralSeparator}zero`;
              const ordinalPrefix = `${this.options.pluralSeparator}ordinal${this.options.pluralSeparator}`;
              if (needsPluralHandling) {
                if (opt.ordinal && pluralSuffix.startsWith(ordinalPrefix)) {
                  finalKeys.push(key + pluralSuffix.replace(ordinalPrefix, this.options.pluralSeparator));
                }
                finalKeys.push(key + pluralSuffix);
                if (needsZeroSuffixLookup) {
                  finalKeys.push(key + zeroSuffix);
                }
              }
              if (needsContextHandling) {
                const contextKey = `${key}${this.options.contextSeparator || "_"}${opt.context}`;
                finalKeys.push(contextKey);
                if (needsPluralHandling) {
                  if (opt.ordinal && pluralSuffix.startsWith(ordinalPrefix)) {
                    finalKeys.push(contextKey + pluralSuffix.replace(ordinalPrefix, this.options.pluralSeparator));
                  }
                  finalKeys.push(contextKey + pluralSuffix);
                  if (needsZeroSuffixLookup) {
                    finalKeys.push(contextKey + zeroSuffix);
                  }
                }
              }
            }
            let possibleKey;
            while (possibleKey = finalKeys.pop()) {
              if (!this.isValidLookup(found)) {
                exactUsedKey = possibleKey;
                found = this.getResource(code, ns, possibleKey, opt);
              }
            }
          });
        });
      });
      return {
        res: found,
        usedKey,
        exactUsedKey,
        usedLng,
        usedNS
      };
    }
    isValidLookup(res) {
      return res !== void 0 && !(!this.options.returnNull && res === null) && !(!this.options.returnEmptyString && res === "");
    }
    getResource(code, ns, key, options = {}) {
      if (this.i18nFormat?.getResource) return this.i18nFormat.getResource(code, ns, key, options);
      return this.resourceStore.getResource(code, ns, key, options);
    }
    getUsedParamsDetails(options = {}) {
      const optionsKeys = ["defaultValue", "ordinal", "context", "replace", "lng", "lngs", "fallbackLng", "ns", "keySeparator", "nsSeparator", "returnObjects", "returnDetails", "joinArrays", "postProcess", "interpolation"];
      const useOptionsReplaceForData = options.replace && !isString(options.replace);
      let data2 = useOptionsReplaceForData ? options.replace : options;
      if (useOptionsReplaceForData && typeof options.count !== "undefined") {
        data2.count = options.count;
      }
      if (this.options.interpolation.defaultVariables) {
        data2 = {
          ...this.options.interpolation.defaultVariables,
          ...data2
        };
      }
      if (!useOptionsReplaceForData) {
        data2 = {
          ...data2
        };
        for (const key of optionsKeys) {
          delete data2[key];
        }
      }
      return data2;
    }
    static hasDefaultValue(options) {
      const prefix = "defaultValue";
      for (const option in options) {
        if (Object.prototype.hasOwnProperty.call(options, option) && option.startsWith(prefix) && void 0 !== options[option]) {
          return true;
        }
      }
      return false;
    }
  };
  var LanguageUtil = class {
    constructor(options) {
      this.options = options;
      this.supportedLngs = this.options.supportedLngs || false;
      this.logger = baseLogger.create("languageUtils");
    }
    getScriptPartFromCode(code) {
      code = getCleanedCode(code);
      if (!code || !code.includes("-")) return null;
      const p = code.split("-");
      if (p.length === 2) return null;
      p.pop();
      if (p[p.length - 1].toLowerCase() === "x") return null;
      return this.formatLanguageCode(p.join("-"));
    }
    getLanguagePartFromCode(code) {
      code = getCleanedCode(code);
      if (!code || !code.includes("-")) return code;
      const p = code.split("-");
      return this.formatLanguageCode(p[0]);
    }
    formatLanguageCode(code) {
      if (isString(code) && code.includes("-")) {
        let formattedCode;
        try {
          formattedCode = Intl.getCanonicalLocales(code)[0];
        } catch (e2) {
        }
        if (formattedCode && this.options.lowerCaseLng) {
          formattedCode = formattedCode.toLowerCase();
        }
        if (formattedCode) return formattedCode;
        if (this.options.lowerCaseLng) {
          return code.toLowerCase();
        }
        return code;
      }
      return this.options.cleanCode || this.options.lowerCaseLng ? code.toLowerCase() : code;
    }
    isSupportedCode(code) {
      if (this.options.load === "languageOnly" || this.options.nonExplicitSupportedLngs) {
        code = this.getLanguagePartFromCode(code);
      }
      return !this.supportedLngs || !this.supportedLngs.length || this.supportedLngs.includes(code);
    }
    getBestMatchFromCodes(codes) {
      if (!codes) return null;
      let found;
      codes.forEach((code) => {
        if (found) return;
        const cleanedLng = this.formatLanguageCode(code);
        if (!this.options.supportedLngs || this.isSupportedCode(cleanedLng)) found = cleanedLng;
      });
      if (!found && this.options.supportedLngs) {
        codes.forEach((code) => {
          if (found) return;
          const lngScOnly = this.getScriptPartFromCode(code);
          if (this.isSupportedCode(lngScOnly)) return found = lngScOnly;
          const lngOnly = this.getLanguagePartFromCode(code);
          if (this.isSupportedCode(lngOnly)) return found = lngOnly;
          found = this.options.supportedLngs.find((supportedLng) => {
            if (supportedLng === lngOnly) return true;
            if (!supportedLng.includes("-") && !lngOnly.includes("-")) return false;
            if (supportedLng.includes("-") && !lngOnly.includes("-") && supportedLng.slice(0, supportedLng.indexOf("-")) === lngOnly) return true;
            if (supportedLng.startsWith(lngOnly) && lngOnly.length > 1) return true;
            return false;
          });
        });
      }
      if (!found) found = this.getFallbackCodes(this.options.fallbackLng)[0];
      return found;
    }
    getFallbackCodes(fallbacks, code) {
      if (!fallbacks) return [];
      if (typeof fallbacks === "function") fallbacks = fallbacks(code);
      if (isString(fallbacks)) fallbacks = [fallbacks];
      if (Array.isArray(fallbacks)) return fallbacks;
      if (!code) return fallbacks.default || [];
      let found = fallbacks[code];
      if (!found) found = fallbacks[this.getScriptPartFromCode(code)];
      if (!found) found = fallbacks[this.formatLanguageCode(code)];
      if (!found) found = fallbacks[this.getLanguagePartFromCode(code)];
      if (!found) found = fallbacks.default;
      return found || [];
    }
    toResolveHierarchy(code, fallbackCode) {
      const fallbackCodes = this.getFallbackCodes((fallbackCode === false ? [] : fallbackCode) || this.options.fallbackLng || [], code);
      const codes = [];
      const addCode = (c) => {
        if (!c) return;
        if (this.isSupportedCode(c)) {
          codes.push(c);
        } else {
          this.logger.warn(`rejecting language code not found in supportedLngs: ${c}`);
        }
      };
      if (isString(code) && (code.includes("-") || code.includes("_"))) {
        if (this.options.load !== "languageOnly") addCode(this.formatLanguageCode(code));
        if (this.options.load !== "languageOnly" && this.options.load !== "currentOnly") addCode(this.getScriptPartFromCode(code));
        if (this.options.load !== "currentOnly") addCode(this.getLanguagePartFromCode(code));
      } else if (isString(code)) {
        addCode(this.formatLanguageCode(code));
      }
      fallbackCodes.forEach((fc) => {
        if (!codes.includes(fc)) addCode(this.formatLanguageCode(fc));
      });
      return codes;
    }
  };
  var suffixesOrder = {
    zero: 0,
    one: 1,
    two: 2,
    few: 3,
    many: 4,
    other: 5
  };
  var dummyRule = {
    select: (count) => count === 1 ? "one" : "other",
    resolvedOptions: () => ({
      pluralCategories: ["one", "other"]
    })
  };
  var PluralResolver = class {
    constructor(languageUtils, options = {}) {
      this.languageUtils = languageUtils;
      this.options = options;
      this.logger = baseLogger.create("pluralResolver");
      this.pluralRulesCache = {};
    }
    clearCache() {
      this.pluralRulesCache = {};
    }
    getRule(code, options = {}) {
      const cleanedCode = getCleanedCode(code === "dev" ? "en" : code);
      const type = options.ordinal ? "ordinal" : "cardinal";
      const cacheKey = JSON.stringify({
        cleanedCode,
        type
      });
      if (cacheKey in this.pluralRulesCache) {
        return this.pluralRulesCache[cacheKey];
      }
      let rule;
      try {
        rule = new Intl.PluralRules(cleanedCode, {
          type
        });
      } catch (err) {
        if (typeof Intl === "undefined") {
          this.logger.error("No Intl support, please use an Intl polyfill!");
          return dummyRule;
        }
        if (!code.match(/-|_/)) return dummyRule;
        const lngPart = this.languageUtils.getLanguagePartFromCode(code);
        rule = this.getRule(lngPart, options);
      }
      this.pluralRulesCache[cacheKey] = rule;
      return rule;
    }
    needsPlural(code, options = {}) {
      let rule = this.getRule(code, options);
      if (!rule) rule = this.getRule("dev", options);
      return rule?.resolvedOptions().pluralCategories.length > 1;
    }
    getPluralFormsOfKey(code, key, options = {}) {
      return this.getSuffixes(code, options).map((suffix) => `${key}${suffix}`);
    }
    getSuffixes(code, options = {}) {
      let rule = this.getRule(code, options);
      if (!rule) rule = this.getRule("dev", options);
      if (!rule) return [];
      return rule.resolvedOptions().pluralCategories.sort((pluralCategory1, pluralCategory2) => suffixesOrder[pluralCategory1] - suffixesOrder[pluralCategory2]).map((pluralCategory) => `${this.options.prepend}${options.ordinal ? `ordinal${this.options.prepend}` : ""}${pluralCategory}`);
    }
    getSuffix(code, count, options = {}) {
      const rule = this.getRule(code, options);
      if (rule) {
        return `${this.options.prepend}${options.ordinal ? `ordinal${this.options.prepend}` : ""}${rule.select(count)}`;
      }
      this.logger.warn(`no plural rule found for: ${code}`);
      return this.getSuffix("dev", count, options);
    }
  };
  var deepFindWithDefaults = (data2, defaultData, key, keySeparator = ".", ignoreJSONStructure = true) => {
    let path = getPathWithDefaults(data2, defaultData, key);
    if (!path && ignoreJSONStructure && isString(key)) {
      path = deepFind(data2, key, keySeparator);
      if (path === void 0) path = deepFind(defaultData, key, keySeparator);
    }
    return path;
  };
  var regexSafe = (val) => val.replace(/\$/g, "$$$$");
  var Interpolator = class {
    constructor(options = {}) {
      this.logger = baseLogger.create("interpolator");
      this.options = options;
      this.format = options?.interpolation?.format || ((value) => value);
      this.init(options);
    }
    init(options = {}) {
      if (!options.interpolation) options.interpolation = {
        escapeValue: true
      };
      const {
        escape: escape$1,
        escapeValue,
        useRawValueToEscape,
        prefix,
        prefixEscaped,
        suffix,
        suffixEscaped,
        formatSeparator,
        unescapeSuffix,
        unescapePrefix,
        nestingPrefix,
        nestingPrefixEscaped,
        nestingSuffix,
        nestingSuffixEscaped,
        nestingOptionsSeparator,
        maxReplaces,
        alwaysFormat
      } = options.interpolation;
      this.escape = escape$1 !== void 0 ? escape$1 : escape;
      this.escapeValue = escapeValue !== void 0 ? escapeValue : true;
      this.useRawValueToEscape = useRawValueToEscape !== void 0 ? useRawValueToEscape : false;
      this.prefix = prefix ? regexEscape(prefix) : prefixEscaped || "{{";
      this.suffix = suffix ? regexEscape(suffix) : suffixEscaped || "}}";
      this.formatSeparator = formatSeparator || ",";
      this.unescapePrefix = unescapeSuffix ? "" : unescapePrefix ? regexEscape(unescapePrefix) : "-";
      this.unescapeSuffix = this.unescapePrefix ? "" : unescapeSuffix ? regexEscape(unescapeSuffix) : "";
      this.nestingPrefix = nestingPrefix ? regexEscape(nestingPrefix) : nestingPrefixEscaped || regexEscape("$t(");
      this.nestingSuffix = nestingSuffix ? regexEscape(nestingSuffix) : nestingSuffixEscaped || regexEscape(")");
      this.nestingOptionsSeparator = nestingOptionsSeparator || ",";
      this.maxReplaces = maxReplaces || 1e3;
      this.alwaysFormat = alwaysFormat !== void 0 ? alwaysFormat : false;
      this.resetRegExp();
    }
    reset() {
      if (this.options) this.init(this.options);
    }
    resetRegExp() {
      const getOrResetRegExp = (existingRegExp, pattern) => {
        if (existingRegExp?.source === pattern) {
          existingRegExp.lastIndex = 0;
          return existingRegExp;
        }
        return new RegExp(pattern, "g");
      };
      this.regexp = getOrResetRegExp(this.regexp, `${this.prefix}(.+?)${this.suffix}`);
      this.regexpUnescape = getOrResetRegExp(this.regexpUnescape, `${this.prefix}${this.unescapePrefix}(.+?)${this.unescapeSuffix}${this.suffix}`);
      this.nestingRegexp = getOrResetRegExp(this.nestingRegexp, `${this.nestingPrefix}((?:[^()"']+|"[^"]*"|'[^']*'|\\((?:[^()]|"[^"]*"|'[^']*')*\\))*?)${this.nestingSuffix}`);
    }
    interpolate(str, data2, lng, options) {
      let match;
      let value;
      let replaces;
      const defaultData = this.options && this.options.interpolation && this.options.interpolation.defaultVariables || {};
      const handleFormat = (key) => {
        if (!key.includes(this.formatSeparator)) {
          const path = deepFindWithDefaults(data2, defaultData, key, this.options.keySeparator, this.options.ignoreJSONStructure);
          return this.alwaysFormat ? this.format(path, void 0, lng, {
            ...options,
            ...data2,
            interpolationkey: key
          }) : path;
        }
        const p = key.split(this.formatSeparator);
        const k = p.shift().trim();
        const f = p.join(this.formatSeparator).trim();
        return this.format(deepFindWithDefaults(data2, defaultData, k, this.options.keySeparator, this.options.ignoreJSONStructure), f, lng, {
          ...options,
          ...data2,
          interpolationkey: k
        });
      };
      this.resetRegExp();
      if (!this.escapeValue && typeof str === "string" && /\$t\([^)]*\{[^}]*\{\{/.test(str)) {
        this.logger.warn("nesting options string contains interpolated variables with escapeValue: false \u2014 if any of those values are attacker-controlled they can inject additional nesting options (e.g. redirect lng/ns). Sanitise untrusted input before passing it to t(), or keep escapeValue: true.");
      }
      const missingInterpolationHandler = options?.missingInterpolationHandler || this.options.missingInterpolationHandler;
      const skipOnVariables = options?.interpolation?.skipOnVariables !== void 0 ? options.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables;
      const todos = [{
        regex: this.regexpUnescape,
        safeValue: (val) => regexSafe(val)
      }, {
        regex: this.regexp,
        safeValue: (val) => this.escapeValue ? regexSafe(this.escape(val)) : regexSafe(val)
      }];
      todos.forEach((todo) => {
        replaces = 0;
        while (match = todo.regex.exec(str)) {
          const matchedVar = match[1].trim();
          value = handleFormat(matchedVar);
          if (value === void 0) {
            if (typeof missingInterpolationHandler === "function") {
              const temp = missingInterpolationHandler(str, match, options);
              value = isString(temp) ? temp : "";
            } else if (options && Object.prototype.hasOwnProperty.call(options, matchedVar)) {
              value = "";
            } else if (skipOnVariables) {
              value = match[0];
              continue;
            } else {
              this.logger.warn(`missed to pass in variable ${matchedVar} for interpolating ${str}`);
              value = "";
            }
          } else if (!isString(value) && !this.useRawValueToEscape) {
            value = makeString(value);
          }
          const safeValue = todo.safeValue(value);
          str = str.replace(match[0], safeValue);
          if (skipOnVariables) {
            todo.regex.lastIndex += value.length;
            todo.regex.lastIndex -= match[0].length;
          } else {
            todo.regex.lastIndex = 0;
          }
          replaces++;
          if (replaces >= this.maxReplaces) {
            break;
          }
        }
      });
      return str;
    }
    nest(str, fc, options = {}) {
      let match;
      let value;
      let clonedOptions;
      const handleHasOptions = (key, inheritedOptions) => {
        const sep = this.nestingOptionsSeparator;
        if (!key.includes(sep)) return key;
        const c = key.split(new RegExp(`${regexEscape(sep)}[ ]*{`));
        let optionsString = `{${c[1]}`;
        key = c[0];
        optionsString = this.interpolate(optionsString, clonedOptions);
        const matchedSingleQuotes = optionsString.match(/'/g);
        const matchedDoubleQuotes = optionsString.match(/"/g);
        if ((matchedSingleQuotes?.length ?? 0) % 2 === 0 && !matchedDoubleQuotes || (matchedDoubleQuotes?.length ?? 0) % 2 !== 0) {
          optionsString = optionsString.replace(/'/g, '"');
        }
        try {
          clonedOptions = JSON.parse(optionsString);
          if (inheritedOptions) clonedOptions = {
            ...inheritedOptions,
            ...clonedOptions
          };
        } catch (e2) {
          this.logger.warn(`failed parsing options string in nesting for key ${key}`, e2);
          return `${key}${sep}${optionsString}`;
        }
        if (clonedOptions.defaultValue && clonedOptions.defaultValue.includes(this.prefix)) delete clonedOptions.defaultValue;
        return key;
      };
      while (match = this.nestingRegexp.exec(str)) {
        let formatters = [];
        clonedOptions = {
          ...options
        };
        clonedOptions = clonedOptions.replace && !isString(clonedOptions.replace) ? clonedOptions.replace : clonedOptions;
        clonedOptions.applyPostProcessor = false;
        delete clonedOptions.defaultValue;
        const keyEndIndex = /{.*}/.test(match[1]) ? match[1].lastIndexOf("}") + 1 : match[1].indexOf(this.formatSeparator);
        if (keyEndIndex !== -1) {
          formatters = match[1].slice(keyEndIndex).split(this.formatSeparator).map((elem) => elem.trim()).filter(Boolean);
          match[1] = match[1].slice(0, keyEndIndex);
        }
        value = fc(handleHasOptions.call(this, match[1].trim(), clonedOptions), clonedOptions);
        if (value && match[0] === str && !isString(value)) return value;
        if (!isString(value)) value = makeString(value);
        if (!value) {
          this.logger.warn(`missed to resolve ${match[1]} for nesting ${str}`);
          value = "";
        }
        if (formatters.length) {
          value = formatters.reduce((v, f) => this.format(v, f, options.lng, {
            ...options,
            interpolationkey: match[1].trim()
          }), value.trim());
        }
        str = str.replace(match[0], value);
        this.regexp.lastIndex = 0;
      }
      return str;
    }
  };
  var parseFormatStr = (formatStr) => {
    let formatName = formatStr.toLowerCase().trim();
    const formatOptions = {};
    if (formatStr.includes("(")) {
      const p = formatStr.split("(");
      formatName = p[0].toLowerCase().trim();
      const optStr = p[1].slice(0, -1);
      if (formatName === "currency" && !optStr.includes(":")) {
        if (!formatOptions.currency) formatOptions.currency = optStr.trim();
      } else if (formatName === "relativetime" && !optStr.includes(":")) {
        if (!formatOptions.range) formatOptions.range = optStr.trim();
      } else {
        const opts = optStr.split(";");
        opts.forEach((opt) => {
          if (opt) {
            const [key, ...rest] = opt.split(":");
            const val = rest.join(":").trim().replace(/^'+|'+$/g, "");
            const trimmedKey = key.trim();
            if (!formatOptions[trimmedKey]) formatOptions[trimmedKey] = val;
            if (val === "false") formatOptions[trimmedKey] = false;
            if (val === "true") formatOptions[trimmedKey] = true;
            if (!isNaN(val)) formatOptions[trimmedKey] = parseInt(val, 10);
          }
        });
      }
    }
    return {
      formatName,
      formatOptions
    };
  };
  var createCachedFormatter = (fn) => {
    const cache2 = {};
    return (v, l, o) => {
      let optForCache = o;
      if (o && o.interpolationkey && o.formatParams && o.formatParams[o.interpolationkey] && o[o.interpolationkey]) {
        optForCache = {
          ...optForCache,
          [o.interpolationkey]: void 0
        };
      }
      const key = l + JSON.stringify(optForCache);
      let frm = cache2[key];
      if (!frm) {
        frm = fn(getCleanedCode(l), o);
        cache2[key] = frm;
      }
      return frm(v);
    };
  };
  var createNonCachedFormatter = (fn) => (v, l, o) => fn(getCleanedCode(l), o)(v);
  var Formatter = class {
    constructor(options = {}) {
      this.logger = baseLogger.create("formatter");
      this.options = options;
      this.init(options);
    }
    init(services, options = {
      interpolation: {}
    }) {
      this.formatSeparator = options.interpolation.formatSeparator || ",";
      const cf = options.cacheInBuiltFormats ? createCachedFormatter : createNonCachedFormatter;
      this.formats = {
        number: cf((lng, opt) => {
          const formatter = new Intl.NumberFormat(lng, {
            ...opt
          });
          return (val) => formatter.format(val);
        }),
        currency: cf((lng, opt) => {
          const formatter = new Intl.NumberFormat(lng, {
            ...opt,
            style: "currency"
          });
          return (val) => formatter.format(val);
        }),
        datetime: cf((lng, opt) => {
          const formatter = new Intl.DateTimeFormat(lng, {
            ...opt
          });
          return (val) => formatter.format(val);
        }),
        relativetime: cf((lng, opt) => {
          const formatter = new Intl.RelativeTimeFormat(lng, {
            ...opt
          });
          return (val) => formatter.format(val, opt.range || "day");
        }),
        list: cf((lng, opt) => {
          const formatter = new Intl.ListFormat(lng, {
            ...opt
          });
          return (val) => formatter.format(val);
        })
      };
    }
    add(name, fc) {
      this.formats[name.toLowerCase().trim()] = fc;
    }
    addCached(name, fc) {
      this.formats[name.toLowerCase().trim()] = createCachedFormatter(fc);
    }
    format(value, format, lng, options = {}) {
      if (!format) return value;
      if (value == null) return value;
      const formats = format.split(this.formatSeparator);
      if (formats.length > 1 && formats[0].indexOf("(") > 1 && !formats[0].includes(")") && formats.find((f) => f.includes(")"))) {
        const lastIndex = formats.findIndex((f) => f.includes(")"));
        formats[0] = [formats[0], ...formats.splice(1, lastIndex)].join(this.formatSeparator);
      }
      const result = formats.reduce((mem, f) => {
        const {
          formatName,
          formatOptions
        } = parseFormatStr(f);
        if (this.formats[formatName]) {
          let formatted = mem;
          try {
            const valOptions = options?.formatParams?.[options.interpolationkey] || {};
            const l = valOptions.locale || valOptions.lng || options.locale || options.lng || lng;
            formatted = this.formats[formatName](mem, l, {
              ...formatOptions,
              ...options,
              ...valOptions
            });
          } catch (error) {
            this.logger.warn(error);
          }
          return formatted;
        } else {
          this.logger.warn(`there was no format function for ${formatName}`);
        }
        return mem;
      }, value);
      return result;
    }
  };
  var removePending = (q, name) => {
    if (q.pending[name] !== void 0) {
      delete q.pending[name];
      q.pendingCount--;
    }
  };
  var Connector = class extends EventEmitter {
    constructor(backend, store, services, options = {}) {
      super();
      this.backend = backend;
      this.store = store;
      this.services = services;
      this.languageUtils = services.languageUtils;
      this.options = options;
      this.logger = baseLogger.create("backendConnector");
      this.waitingReads = [];
      this.maxParallelReads = options.maxParallelReads || 10;
      this.readingCalls = 0;
      this.maxRetries = options.maxRetries >= 0 ? options.maxRetries : 5;
      this.retryTimeout = options.retryTimeout >= 1 ? options.retryTimeout : 350;
      this.state = {};
      this.queue = [];
      this.backend?.init?.(services, options.backend, options);
    }
    queueLoad(languages, namespaces, options, callback) {
      const toLoad = {};
      const pending = {};
      const toLoadLanguages = {};
      const toLoadNamespaces = {};
      languages.forEach((lng) => {
        let hasAllNamespaces = true;
        namespaces.forEach((ns) => {
          const name = `${lng}|${ns}`;
          if (!options.reload && this.store.hasResourceBundle(lng, ns)) {
            this.state[name] = 2;
          } else if (this.state[name] < 0) ;
          else if (this.state[name] === 1) {
            if (pending[name] === void 0) pending[name] = true;
          } else {
            this.state[name] = 1;
            hasAllNamespaces = false;
            if (pending[name] === void 0) pending[name] = true;
            if (toLoad[name] === void 0) toLoad[name] = true;
            if (toLoadNamespaces[ns] === void 0) toLoadNamespaces[ns] = true;
          }
        });
        if (!hasAllNamespaces) toLoadLanguages[lng] = true;
      });
      if (Object.keys(toLoad).length || Object.keys(pending).length) {
        this.queue.push({
          pending,
          pendingCount: Object.keys(pending).length,
          loaded: {},
          errors: [],
          callback
        });
      }
      return {
        toLoad: Object.keys(toLoad),
        pending: Object.keys(pending),
        toLoadLanguages: Object.keys(toLoadLanguages),
        toLoadNamespaces: Object.keys(toLoadNamespaces)
      };
    }
    loaded(name, err, data2) {
      const s = name.split("|");
      const lng = s[0];
      const ns = s[1];
      if (err) this.emit("failedLoading", lng, ns, err);
      if (!err && data2) {
        this.store.addResourceBundle(lng, ns, data2, void 0, void 0, {
          skipCopy: true
        });
      }
      this.state[name] = err ? -1 : 2;
      if (err && data2) this.state[name] = 0;
      const loaded = {};
      this.queue.forEach((q) => {
        pushPath(q.loaded, [lng], ns);
        removePending(q, name);
        if (err) q.errors.push(err);
        if (q.pendingCount === 0 && !q.done) {
          Object.keys(q.loaded).forEach((l) => {
            if (!loaded[l]) loaded[l] = {};
            const loadedKeys = q.loaded[l];
            if (loadedKeys.length) {
              loadedKeys.forEach((n) => {
                if (loaded[l][n] === void 0) loaded[l][n] = true;
              });
            }
          });
          q.done = true;
          if (q.errors.length) {
            q.callback(q.errors);
          } else {
            q.callback();
          }
        }
      });
      this.emit("loaded", loaded);
      this.queue = this.queue.filter((q) => !q.done);
    }
    read(lng, ns, fcName, tried = 0, wait = this.retryTimeout, callback) {
      if (!lng.length) return callback(null, {});
      if (this.readingCalls >= this.maxParallelReads) {
        this.waitingReads.push({
          lng,
          ns,
          fcName,
          tried,
          wait,
          callback
        });
        return;
      }
      this.readingCalls++;
      const resolver = (err, data2) => {
        this.readingCalls--;
        if (this.waitingReads.length > 0) {
          const next = this.waitingReads.shift();
          this.read(next.lng, next.ns, next.fcName, next.tried, next.wait, next.callback);
        }
        if (err && data2 && tried < this.maxRetries) {
          setTimeout(() => {
            this.read(lng, ns, fcName, tried + 1, wait * 2, callback);
          }, wait);
          return;
        }
        callback(err, data2);
      };
      const fc = this.backend[fcName].bind(this.backend);
      if (fc.length === 2) {
        try {
          const r = fc(lng, ns);
          if (r && typeof r.then === "function") {
            r.then((data2) => resolver(null, data2)).catch(resolver);
          } else {
            resolver(null, r);
          }
        } catch (err) {
          resolver(err);
        }
        return;
      }
      return fc(lng, ns, resolver);
    }
    prepareLoading(languages, namespaces, options = {}, callback) {
      if (!this.backend) {
        this.logger.warn("No backend was added via i18next.use. Will not load resources.");
        return callback && callback();
      }
      if (isString(languages)) languages = this.languageUtils.toResolveHierarchy(languages);
      if (isString(namespaces)) namespaces = [namespaces];
      const toLoad = this.queueLoad(languages, namespaces, options, callback);
      if (!toLoad.toLoad.length) {
        if (!toLoad.pending.length) callback();
        return null;
      }
      toLoad.toLoad.forEach((name) => {
        this.loadOne(name);
      });
    }
    load(languages, namespaces, callback) {
      this.prepareLoading(languages, namespaces, {}, callback);
    }
    reload(languages, namespaces, callback) {
      this.prepareLoading(languages, namespaces, {
        reload: true
      }, callback);
    }
    loadOne(name, prefix = "") {
      const s = name.split("|");
      const lng = s[0];
      const ns = s[1];
      this.read(lng, ns, "read", void 0, void 0, (err, data2) => {
        if (err) this.logger.warn(`${prefix}loading namespace ${ns} for language ${lng} failed`, err);
        if (!err && data2) this.logger.log(`${prefix}loaded namespace ${ns} for language ${lng}`, data2);
        this.loaded(name, err, data2);
      });
    }
    saveMissing(languages, namespace, key, fallbackValue, isUpdate, options = {}, clb = () => {
    }) {
      if (this.services?.utils?.hasLoadedNamespace && !this.services?.utils?.hasLoadedNamespace(namespace)) {
        this.logger.warn(`did not save key "${key}" as the namespace "${namespace}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
        return;
      }
      if (key === void 0 || key === null || key === "") return;
      if (this.backend?.create) {
        const opts = {
          ...options,
          isUpdate
        };
        const fc = this.backend.create.bind(this.backend);
        if (fc.length < 6) {
          try {
            let r;
            if (fc.length === 5) {
              r = fc(languages, namespace, key, fallbackValue, opts);
            } else {
              r = fc(languages, namespace, key, fallbackValue);
            }
            if (r && typeof r.then === "function") {
              r.then((data2) => clb(null, data2)).catch(clb);
            } else {
              clb(null, r);
            }
          } catch (err) {
            clb(err);
          }
        } else {
          fc(languages, namespace, key, fallbackValue, clb, opts);
        }
      }
      if (!languages || !languages[0]) return;
      this.store.addResource(languages[0], namespace, key, fallbackValue);
    }
  };
  var get = () => ({
    debug: false,
    initAsync: true,
    ns: ["translation"],
    defaultNS: ["translation"],
    fallbackLng: ["dev"],
    fallbackNS: false,
    supportedLngs: false,
    nonExplicitSupportedLngs: false,
    load: "all",
    preload: false,
    keySeparator: ".",
    nsSeparator: ":",
    pluralSeparator: "_",
    contextSeparator: "_",
    enableSelector: false,
    partialBundledLanguages: false,
    saveMissing: false,
    updateMissing: false,
    saveMissingTo: "fallback",
    saveMissingPlurals: true,
    missingKeyHandler: false,
    missingInterpolationHandler: false,
    postProcess: false,
    postProcessPassResolved: false,
    returnNull: false,
    returnEmptyString: true,
    returnObjects: false,
    joinArrays: false,
    returnedObjectHandler: false,
    parseMissingKeyHandler: false,
    appendNamespaceToMissingKey: false,
    appendNamespaceToCIMode: false,
    overloadTranslationOptionHandler: (args) => {
      let ret = {};
      if (typeof args[1] === "object") ret = args[1];
      if (isString(args[1])) ret.defaultValue = args[1];
      if (isString(args[2])) ret.tDescription = args[2];
      if (typeof args[2] === "object" || typeof args[3] === "object") {
        const options = args[3] || args[2];
        Object.keys(options).forEach((key) => {
          ret[key] = options[key];
        });
      }
      return ret;
    },
    interpolation: {
      escapeValue: true,
      prefix: "{{",
      suffix: "}}",
      formatSeparator: ",",
      unescapePrefix: "-",
      nestingPrefix: "$t(",
      nestingSuffix: ")",
      nestingOptionsSeparator: ",",
      maxReplaces: 1e3,
      skipOnVariables: true
    },
    cacheInBuiltFormats: true
  });
  var transformOptions = (options) => {
    if (isString(options.ns)) options.ns = [options.ns];
    if (isString(options.fallbackLng)) options.fallbackLng = [options.fallbackLng];
    if (isString(options.fallbackNS)) options.fallbackNS = [options.fallbackNS];
    if (options.supportedLngs && !options.supportedLngs.includes("cimode")) {
      options.supportedLngs = options.supportedLngs.concat(["cimode"]);
    }
    return options;
  };
  var noop = () => {
  };
  var bindMemberFunctions = (inst) => {
    const mems = Object.getOwnPropertyNames(Object.getPrototypeOf(inst));
    mems.forEach((mem) => {
      if (typeof inst[mem] === "function") {
        inst[mem] = inst[mem].bind(inst);
      }
    });
  };
  var I18n = class _I18n extends EventEmitter {
    constructor(options = {}, callback) {
      super();
      this.options = transformOptions(options);
      this.services = {};
      this.logger = baseLogger;
      this.modules = {
        external: []
      };
      bindMemberFunctions(this);
      if (callback && !this.isInitialized && !options.isClone) {
        if (!this.options.initAsync) {
          this.init(options, callback);
          return this;
        }
        setTimeout(() => {
          this.init(options, callback);
        }, 0);
      }
    }
    init(options = {}, callback) {
      this.isInitializing = true;
      if (typeof options === "function") {
        callback = options;
        options = {};
      }
      if (options.defaultNS == null && options.ns) {
        if (isString(options.ns)) {
          options.defaultNS = options.ns;
        } else if (!options.ns.includes("translation")) {
          options.defaultNS = options.ns[0];
        }
      }
      const defOpts = get();
      this.options = {
        ...defOpts,
        ...this.options,
        ...transformOptions(options)
      };
      this.options.interpolation = {
        ...defOpts.interpolation,
        ...this.options.interpolation
      };
      if (options.keySeparator !== void 0) {
        this.options.userDefinedKeySeparator = options.keySeparator;
      }
      if (options.nsSeparator !== void 0) {
        this.options.userDefinedNsSeparator = options.nsSeparator;
      }
      if (typeof this.options.overloadTranslationOptionHandler !== "function") {
        this.options.overloadTranslationOptionHandler = defOpts.overloadTranslationOptionHandler;
      }
      const createClassOnDemand = (ClassOrObject) => {
        if (!ClassOrObject) return null;
        if (typeof ClassOrObject === "function") return new ClassOrObject();
        return ClassOrObject;
      };
      if (!this.options.isClone) {
        if (this.modules.logger) {
          baseLogger.init(createClassOnDemand(this.modules.logger), this.options);
        } else {
          baseLogger.init(null, this.options);
        }
        let formatter;
        if (this.modules.formatter) {
          formatter = this.modules.formatter;
        } else {
          formatter = Formatter;
        }
        const lu = new LanguageUtil(this.options);
        this.store = new ResourceStore(this.options.resources, this.options);
        const s = this.services;
        s.logger = baseLogger;
        s.resourceStore = this.store;
        s.languageUtils = lu;
        s.pluralResolver = new PluralResolver(lu, {
          prepend: this.options.pluralSeparator
        });
        if (formatter) {
          s.formatter = createClassOnDemand(formatter);
          if (s.formatter.init) s.formatter.init(s, this.options);
          this.options.interpolation.format = s.formatter.format.bind(s.formatter);
        }
        s.interpolator = new Interpolator(this.options);
        s.utils = {
          hasLoadedNamespace: this.hasLoadedNamespace.bind(this)
        };
        s.backendConnector = new Connector(createClassOnDemand(this.modules.backend), s.resourceStore, s, this.options);
        s.backendConnector.on("*", (event, ...args) => {
          this.emit(event, ...args);
        });
        if (this.modules.languageDetector) {
          s.languageDetector = createClassOnDemand(this.modules.languageDetector);
          if (s.languageDetector.init) s.languageDetector.init(s, this.options.detection, this.options);
        }
        if (this.modules.i18nFormat) {
          s.i18nFormat = createClassOnDemand(this.modules.i18nFormat);
          if (s.i18nFormat.init) s.i18nFormat.init(this);
        }
        this.translator = new Translator(this.services, this.options);
        this.translator.on("*", (event, ...args) => {
          this.emit(event, ...args);
        });
        this.modules.external.forEach((m) => {
          if (m.init) m.init(this);
        });
      }
      this.format = this.options.interpolation.format;
      if (!callback) callback = noop;
      if (this.options.fallbackLng && !this.services.languageDetector && !this.options.lng) {
        const codes = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
        if (codes.length > 0 && codes[0] !== "dev") this.options.lng = codes[0];
      }
      if (!this.services.languageDetector && !this.options.lng) {
        this.logger.warn("init: no languageDetector is used and no lng is defined");
      }
      const storeApi = ["getResource", "hasResourceBundle", "getResourceBundle", "getDataByLanguage"];
      storeApi.forEach((fcName) => {
        this[fcName] = (...args) => this.store[fcName](...args);
      });
      const storeApiChained = ["addResource", "addResources", "addResourceBundle", "removeResourceBundle"];
      storeApiChained.forEach((fcName) => {
        this[fcName] = (...args) => {
          this.store[fcName](...args);
          return this;
        };
      });
      const deferred = defer();
      const load = () => {
        const finish = (err, t2) => {
          this.isInitializing = false;
          if (this.isInitialized && !this.initializedStoreOnce) this.logger.warn("init: i18next is already initialized. You should call init just once!");
          this.isInitialized = true;
          if (!this.options.isClone) this.logger.log("initialized", this.options);
          this.emit("initialized", this.options);
          deferred.resolve(t2);
          callback(err, t2);
        };
        if ((this.languages || this.isLanguageChangingTo) && !this.isInitialized) return finish(null, this.t.bind(this));
        this.changeLanguage(this.options.lng, finish);
      };
      if (this.options.resources || !this.options.initAsync) {
        load();
      } else {
        setTimeout(load, 0);
      }
      return deferred;
    }
    loadResources(language, callback = noop) {
      let usedCallback = callback;
      const usedLng = isString(language) ? language : this.language;
      if (typeof language === "function") usedCallback = language;
      if (!this.options.resources || this.options.partialBundledLanguages) {
        if (usedLng?.toLowerCase() === "cimode" && (!this.options.preload || this.options.preload.length === 0)) return usedCallback();
        const toLoad = [];
        const append2 = (lng) => {
          if (!lng) return;
          if (lng === "cimode") return;
          const lngs = this.services.languageUtils.toResolveHierarchy(lng);
          lngs.forEach((l) => {
            if (l === "cimode") return;
            if (!toLoad.includes(l)) toLoad.push(l);
          });
        };
        if (!usedLng) {
          const fallbacks = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
          fallbacks.forEach((l) => append2(l));
        } else {
          append2(usedLng);
        }
        this.options.preload?.forEach?.((l) => append2(l));
        this.services.backendConnector.load(toLoad, this.options.ns, (e2) => {
          if (!e2 && !this.resolvedLanguage && this.language) this.setResolvedLanguage(this.language);
          usedCallback(e2);
        });
      } else {
        usedCallback(null);
      }
    }
    reloadResources(lngs, ns, callback) {
      const deferred = defer();
      if (typeof lngs === "function") {
        callback = lngs;
        lngs = void 0;
      }
      if (typeof ns === "function") {
        callback = ns;
        ns = void 0;
      }
      if (!lngs) lngs = this.languages;
      if (!ns) ns = this.options.ns;
      if (!callback) callback = noop;
      this.services.backendConnector.reload(lngs, ns, (err) => {
        deferred.resolve();
        callback(err);
      });
      return deferred;
    }
    use(module) {
      if (!module) throw new Error("You are passing an undefined module! Please check the object you are passing to i18next.use()");
      if (!module.type) throw new Error("You are passing a wrong module! Please check the object you are passing to i18next.use()");
      if (module.type === "backend") {
        this.modules.backend = module;
      }
      if (module.type === "logger" || module.log && module.warn && module.error) {
        this.modules.logger = module;
      }
      if (module.type === "languageDetector") {
        this.modules.languageDetector = module;
      }
      if (module.type === "i18nFormat") {
        this.modules.i18nFormat = module;
      }
      if (module.type === "postProcessor") {
        postProcessor.addPostProcessor(module);
      }
      if (module.type === "formatter") {
        this.modules.formatter = module;
      }
      if (module.type === "3rdParty") {
        this.modules.external.push(module);
      }
      return this;
    }
    setResolvedLanguage(l) {
      if (!l || !this.languages) return;
      if (["cimode", "dev"].includes(l)) return;
      for (let li = 0; li < this.languages.length; li++) {
        const lngInLngs = this.languages[li];
        if (["cimode", "dev"].includes(lngInLngs)) continue;
        if (this.store.hasLanguageSomeTranslations(lngInLngs)) {
          this.resolvedLanguage = lngInLngs;
          break;
        }
      }
      if (!this.resolvedLanguage && !this.languages.includes(l) && this.store.hasLanguageSomeTranslations(l)) {
        this.resolvedLanguage = l;
        this.languages.unshift(l);
      }
    }
    changeLanguage(lng, callback) {
      this.isLanguageChangingTo = lng;
      const deferred = defer();
      this.emit("languageChanging", lng);
      const setLngProps = (l) => {
        this.language = l;
        this.languages = this.services.languageUtils.toResolveHierarchy(l);
        this.resolvedLanguage = void 0;
        this.setResolvedLanguage(l);
      };
      const done = (err, l) => {
        if (l) {
          if (this.isLanguageChangingTo === lng) {
            setLngProps(l);
            this.translator.changeLanguage(l);
            this.isLanguageChangingTo = void 0;
            this.emit("languageChanged", l);
            this.logger.log("languageChanged", l);
          }
        } else {
          this.isLanguageChangingTo = void 0;
        }
        deferred.resolve((...args) => this.t(...args));
        if (callback) callback(err, (...args) => this.t(...args));
      };
      const setLng = (lngs) => {
        if (!lng && !lngs && this.services.languageDetector) lngs = [];
        const fl = isString(lngs) ? lngs : lngs && lngs[0];
        const l = this.store.hasLanguageSomeTranslations(fl) ? fl : this.services.languageUtils.getBestMatchFromCodes(isString(lngs) ? [lngs] : lngs);
        if (l) {
          if (!this.language) {
            setLngProps(l);
          }
          if (!this.translator.language) this.translator.changeLanguage(l);
          this.services.languageDetector?.cacheUserLanguage?.(l);
        }
        this.loadResources(l, (err) => {
          done(err, l);
        });
      };
      if (!lng && this.services.languageDetector && !this.services.languageDetector.async) {
        setLng(this.services.languageDetector.detect());
      } else if (!lng && this.services.languageDetector && this.services.languageDetector.async) {
        if (this.services.languageDetector.detect.length === 0) {
          this.services.languageDetector.detect().then(setLng);
        } else {
          this.services.languageDetector.detect(setLng);
        }
      } else {
        setLng(lng);
      }
      return deferred;
    }
    getFixedT(lng, ns, keyPrefix, fixedOpts) {
      const scopeNs = fixedOpts?.scopeNs;
      const fixedT = (key, opts, ...rest) => {
        let o;
        if (typeof opts !== "object") {
          o = this.options.overloadTranslationOptionHandler([key, opts].concat(rest));
        } else {
          o = {
            ...opts
          };
        }
        o.lng = o.lng || fixedT.lng;
        o.lngs = o.lngs || fixedT.lngs;
        const explicitCallNs = o.ns !== void 0 && o.ns !== null;
        o.ns = o.ns || fixedT.ns;
        if (o.keyPrefix !== "") o.keyPrefix = o.keyPrefix || keyPrefix || fixedT.keyPrefix;
        const selectorOpts = {
          ...this.options,
          ...o
        };
        if (Array.isArray(scopeNs) && !explicitCallNs) selectorOpts.ns = scopeNs;
        if (typeof o.keyPrefix === "function") o.keyPrefix = keysFromSelector(o.keyPrefix, selectorOpts);
        const keySeparator = this.options.keySeparator || ".";
        let resultKey;
        if (o.keyPrefix && Array.isArray(key)) {
          resultKey = key.map((k) => {
            if (typeof k === "function") k = keysFromSelector(k, selectorOpts);
            return `${o.keyPrefix}${keySeparator}${k}`;
          });
        } else {
          if (typeof key === "function") key = keysFromSelector(key, selectorOpts);
          resultKey = o.keyPrefix ? `${o.keyPrefix}${keySeparator}${key}` : key;
        }
        return this.t(resultKey, o);
      };
      if (isString(lng)) {
        fixedT.lng = lng;
      } else {
        fixedT.lngs = lng;
      }
      fixedT.ns = ns;
      fixedT.keyPrefix = keyPrefix;
      return fixedT;
    }
    t(...args) {
      return this.translator?.translate(...args);
    }
    exists(...args) {
      return this.translator?.exists(...args);
    }
    setDefaultNamespace(ns) {
      this.options.defaultNS = ns;
    }
    hasLoadedNamespace(ns, options = {}) {
      if (!this.isInitialized) {
        this.logger.warn("hasLoadedNamespace: i18next was not initialized", this.languages);
        return false;
      }
      if (!this.languages || !this.languages.length) {
        this.logger.warn("hasLoadedNamespace: i18n.languages were undefined or empty", this.languages);
        return false;
      }
      const lng = options.lng || this.resolvedLanguage || this.languages[0];
      const fallbackLng = this.options ? this.options.fallbackLng : false;
      const lastLng = this.languages[this.languages.length - 1];
      if (lng.toLowerCase() === "cimode") return true;
      const loadNotPending = (l, n) => {
        const loadState = this.services.backendConnector.state[`${l}|${n}`];
        return loadState === -1 || loadState === 0 || loadState === 2;
      };
      if (options.precheck) {
        const preResult = options.precheck(this, loadNotPending);
        if (preResult !== void 0) return preResult;
      }
      if (this.hasResourceBundle(lng, ns)) return true;
      if (!this.services.backendConnector.backend || this.options.resources && !this.options.partialBundledLanguages) return true;
      if (loadNotPending(lng, ns) && (!fallbackLng || loadNotPending(lastLng, ns))) return true;
      return false;
    }
    loadNamespaces(ns, callback) {
      const deferred = defer();
      if (!this.options.ns) {
        if (callback) callback();
        return Promise.resolve();
      }
      if (isString(ns)) ns = [ns];
      ns.forEach((n) => {
        if (!this.options.ns.includes(n)) this.options.ns.push(n);
      });
      this.loadResources((err) => {
        deferred.resolve();
        if (callback) callback(err);
      });
      return deferred;
    }
    loadLanguages(lngs, callback) {
      const deferred = defer();
      if (isString(lngs)) lngs = [lngs];
      const preloaded = this.options.preload || [];
      const newLngs = lngs.filter((lng) => !preloaded.includes(lng) && this.services.languageUtils.isSupportedCode(lng));
      if (!newLngs.length) {
        if (callback) callback();
        return Promise.resolve();
      }
      this.options.preload = preloaded.concat(newLngs);
      this.loadResources((err) => {
        deferred.resolve();
        if (callback) callback(err);
      });
      return deferred;
    }
    dir(lng) {
      if (!lng) lng = this.resolvedLanguage || (this.languages?.length > 0 ? this.languages[0] : this.language);
      if (!lng) return "rtl";
      try {
        const l = new Intl.Locale(lng);
        if (l && l.getTextInfo) {
          const ti = l.getTextInfo();
          if (ti && ti.direction) return ti.direction;
        }
      } catch (e2) {
      }
      const rtlLngs = ["ar", "shu", "sqr", "ssh", "xaa", "yhd", "yud", "aao", "abh", "abv", "acm", "acq", "acw", "acx", "acy", "adf", "ads", "aeb", "aec", "afb", "ajp", "apc", "apd", "arb", "arq", "ars", "ary", "arz", "auz", "avl", "ayh", "ayl", "ayn", "ayp", "bbz", "pga", "he", "iw", "ps", "pbt", "pbu", "pst", "prp", "prd", "ug", "ur", "ydd", "yds", "yih", "ji", "yi", "hbo", "men", "xmn", "fa", "jpr", "peo", "pes", "prs", "dv", "sam", "ckb"];
      const languageUtils = this.services?.languageUtils || new LanguageUtil(get());
      if (lng.toLowerCase().indexOf("-latn") > 1) return "ltr";
      return rtlLngs.includes(languageUtils.getLanguagePartFromCode(lng)) || lng.toLowerCase().indexOf("-arab") > 1 ? "rtl" : "ltr";
    }
    static createInstance(options = {}, callback) {
      const instance2 = new _I18n(options, callback);
      instance2.createInstance = _I18n.createInstance;
      return instance2;
    }
    cloneInstance(options = {}, callback = noop) {
      const forkResourceStore = options.forkResourceStore;
      if (forkResourceStore) delete options.forkResourceStore;
      const mergedOptions = {
        ...this.options,
        ...options,
        ...{
          isClone: true
        }
      };
      const clone = new _I18n(mergedOptions);
      if (options.debug !== void 0 || options.prefix !== void 0) {
        clone.logger = clone.logger.clone(options);
      }
      const membersToCopy = ["store", "services", "language"];
      membersToCopy.forEach((m) => {
        clone[m] = this[m];
      });
      clone.services = {
        ...this.services
      };
      clone.services.utils = {
        hasLoadedNamespace: clone.hasLoadedNamespace.bind(clone)
      };
      if (forkResourceStore) {
        const clonedData = Object.keys(this.store.data).reduce((prev, l) => {
          prev[l] = {
            ...this.store.data[l]
          };
          prev[l] = Object.keys(prev[l]).reduce((acc, n) => {
            acc[n] = {
              ...prev[l][n]
            };
            return acc;
          }, prev[l]);
          return prev;
        }, {});
        clone.store = new ResourceStore(clonedData, mergedOptions);
        clone.services.resourceStore = clone.store;
      }
      if (options.interpolation) {
        const defOpts = get();
        const mergedInterpolation = {
          ...defOpts.interpolation,
          ...this.options.interpolation,
          ...options.interpolation
        };
        const mergedForInterpolator = {
          ...mergedOptions,
          interpolation: mergedInterpolation
        };
        clone.services.interpolator = new Interpolator(mergedForInterpolator);
      }
      clone.translator = new Translator(clone.services, mergedOptions);
      clone.translator.on("*", (event, ...args) => {
        clone.emit(event, ...args);
      });
      clone.init(mergedOptions, callback);
      clone.translator.options = mergedOptions;
      clone.translator.backendConnector.services.utils = {
        hasLoadedNamespace: clone.hasLoadedNamespace.bind(clone)
      };
      return clone;
    }
    toJSON() {
      return {
        options: this.options,
        store: this.store,
        language: this.language,
        languages: this.languages,
        resolvedLanguage: this.resolvedLanguage
      };
    }
  };
  var instance = I18n.createInstance();
  var createInstance = instance.createInstance;
  var dir = instance.dir;
  var init = instance.init;
  var loadResources = instance.loadResources;
  var reloadResources = instance.reloadResources;
  var use = instance.use;
  var changeLanguage = instance.changeLanguage;
  var getFixedT = instance.getFixedT;
  var t = instance.t;
  var exists = instance.exists;
  var setDefaultNamespace = instance.setDefaultNamespace;
  var hasLoadedNamespace = instance.hasLoadedNamespace;
  var loadNamespaces = instance.loadNamespaces;
  var loadLanguages = instance.loadLanguages;

  // node_modules/html-parse-stringify/dist/html-parse-stringify.module.js
  var import_void_elements = __toESM(require_void_elements());

  // node_modules/react-i18next/dist/es/utils.js
  var warn = (i18n, code, msg, rest) => {
    const args = [msg, {
      code,
      ...rest || {}
    }];
    if (i18n?.services?.logger?.forward) {
      return i18n.services.logger.forward(args, "warn", "react-i18next::", true);
    }
    if (isString2(args[0])) args[0] = `react-i18next:: ${args[0]}`;
    if (i18n?.services?.logger?.warn) {
      i18n.services.logger.warn(...args);
    } else if (console?.warn) {
      console.warn(...args);
    }
  };
  var alreadyWarned2 = {};
  var warnOnce = (i18n, code, msg, rest) => {
    if (isString2(msg) && alreadyWarned2[msg]) return;
    if (isString2(msg)) alreadyWarned2[msg] = /* @__PURE__ */ new Date();
    warn(i18n, code, msg, rest);
  };
  var loadedClb = (i18n, cb) => () => {
    if (i18n.isInitialized) {
      cb();
    } else {
      const initialized = () => {
        setTimeout(() => {
          i18n.off("initialized", initialized);
        }, 0);
        cb();
      };
      i18n.on("initialized", initialized);
    }
  };
  var loadNamespaces2 = (i18n, ns, cb) => {
    i18n.loadNamespaces(ns, loadedClb(i18n, cb));
  };
  var loadLanguages2 = (i18n, lng, ns, cb) => {
    if (isString2(ns)) ns = [ns];
    if (i18n.options.preload && i18n.options.preload.indexOf(lng) > -1) return loadNamespaces2(i18n, ns, cb);
    ns.forEach((n) => {
      if (i18n.options.ns.indexOf(n) < 0) i18n.options.ns.push(n);
    });
    i18n.loadLanguages(lng, loadedClb(i18n, cb));
  };
  var hasLoadedNamespace2 = (ns, i18n, options = {}) => {
    if (!i18n.languages || !i18n.languages.length) {
      warnOnce(i18n, "NO_LANGUAGES", "i18n.languages were undefined or empty", {
        languages: i18n.languages
      });
      return true;
    }
    return i18n.hasLoadedNamespace(ns, {
      lng: options.lng,
      precheck: (i18nInstance2, loadNotPending) => {
        if (options.bindI18n && options.bindI18n.indexOf("languageChanging") > -1 && i18nInstance2.services.backendConnector.backend && i18nInstance2.isLanguageChangingTo && !loadNotPending(i18nInstance2.isLanguageChangingTo, ns)) return false;
      }
    });
  };
  var isString2 = (obj) => typeof obj === "string";
  var isObject = (obj) => typeof obj === "object" && obj !== null;

  // node_modules/react-i18next/dist/es/unescape.js
  var matchHtmlEntity = /&(?:amp|#38|lt|#60|gt|#62|apos|#39|quot|#34|nbsp|#160|copy|#169|reg|#174|hellip|#8230|#x2F|#47);/g;
  var htmlEntities = {
    "&amp;": "&",
    "&#38;": "&",
    "&lt;": "<",
    "&#60;": "<",
    "&gt;": ">",
    "&#62;": ">",
    "&apos;": "'",
    "&#39;": "'",
    "&quot;": '"',
    "&#34;": '"',
    "&nbsp;": " ",
    "&#160;": " ",
    "&copy;": "\xA9",
    "&#169;": "\xA9",
    "&reg;": "\xAE",
    "&#174;": "\xAE",
    "&hellip;": "\u2026",
    "&#8230;": "\u2026",
    "&#x2F;": "/",
    "&#47;": "/"
  };
  var unescapeHtmlEntity = (m) => htmlEntities[m];
  var unescape = (text) => text.replace(matchHtmlEntity, unescapeHtmlEntity);

  // node_modules/react-i18next/dist/es/defaults.js
  var defaultOptions = {
    bindI18n: "languageChanged",
    bindI18nStore: "",
    transEmptyNodeValue: "",
    transSupportBasicHtmlNodes: true,
    transWrapTextNodes: "",
    transKeepBasicHtmlNodesFor: ["br", "strong", "i", "p"],
    useSuspense: true,
    unescape,
    transDefaultProps: void 0
  };
  var getDefaults = () => defaultOptions;

  // node_modules/react-i18next/dist/es/i18nInstance.js
  var i18nInstance;
  var getI18n = () => i18nInstance;

  // node_modules/react-i18next/dist/es/context.js
  var import_react2 = __toESM(require_react_shim(), 1);
  var I18nContext = (0, import_react2.createContext)();
  var ReportNamespaces = class {
    constructor() {
      this.usedNamespaces = {};
    }
    addUsedNamespaces(namespaces) {
      namespaces.forEach((ns) => {
        if (!this.usedNamespaces[ns]) this.usedNamespaces[ns] = true;
      });
    }
    getUsedNamespaces() {
      return Object.keys(this.usedNamespaces);
    }
  };

  // node_modules/react-i18next/dist/es/IcuTrans.js
  var import_react6 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/IcuTransWithoutContext.js
  var import_react5 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/IcuTransUtils/TranslationParserError.js
  var TranslationParserError = class _TranslationParserError extends Error {
    constructor(message, position, translationString) {
      super(message);
      this.name = "TranslationParserError";
      this.position = position;
      this.translationString = translationString;
      if (Error.captureStackTrace) {
        Error.captureStackTrace(this, _TranslationParserError);
      }
    }
  };

  // node_modules/react-i18next/dist/es/IcuTransUtils/htmlEntityDecoder.js
  var commonEntities = {
    "&nbsp;": "\xA0",
    "&amp;": "&",
    "&lt;": "<",
    "&gt;": ">",
    "&quot;": '"',
    "&apos;": "'",
    "&copy;": "\xA9",
    "&reg;": "\xAE",
    "&trade;": "\u2122",
    "&hellip;": "\u2026",
    "&ndash;": "\u2013",
    "&mdash;": "\u2014",
    "&lsquo;": "\u2018",
    "&rsquo;": "\u2019",
    "&sbquo;": "\u201A",
    "&ldquo;": "\u201C",
    "&rdquo;": "\u201D",
    "&bdquo;": "\u201E",
    "&dagger;": "\u2020",
    "&Dagger;": "\u2021",
    "&bull;": "\u2022",
    "&prime;": "\u2032",
    "&Prime;": "\u2033",
    "&lsaquo;": "\u2039",
    "&rsaquo;": "\u203A",
    "&sect;": "\xA7",
    "&para;": "\xB6",
    "&middot;": "\xB7",
    "&ensp;": "\u2002",
    "&emsp;": "\u2003",
    "&thinsp;": "\u2009",
    "&euro;": "\u20AC",
    "&pound;": "\xA3",
    "&yen;": "\xA5",
    "&cent;": "\xA2",
    "&curren;": "\xA4",
    "&times;": "\xD7",
    "&divide;": "\xF7",
    "&minus;": "\u2212",
    "&plusmn;": "\xB1",
    "&ne;": "\u2260",
    "&le;": "\u2264",
    "&ge;": "\u2265",
    "&asymp;": "\u2248",
    "&equiv;": "\u2261",
    "&infin;": "\u221E",
    "&int;": "\u222B",
    "&sum;": "\u2211",
    "&prod;": "\u220F",
    "&radic;": "\u221A",
    "&part;": "\u2202",
    "&permil;": "\u2030",
    "&deg;": "\xB0",
    "&micro;": "\xB5",
    "&larr;": "\u2190",
    "&uarr;": "\u2191",
    "&rarr;": "\u2192",
    "&darr;": "\u2193",
    "&harr;": "\u2194",
    "&crarr;": "\u21B5",
    "&lArr;": "\u21D0",
    "&uArr;": "\u21D1",
    "&rArr;": "\u21D2",
    "&dArr;": "\u21D3",
    "&hArr;": "\u21D4",
    "&alpha;": "\u03B1",
    "&beta;": "\u03B2",
    "&gamma;": "\u03B3",
    "&delta;": "\u03B4",
    "&epsilon;": "\u03B5",
    "&zeta;": "\u03B6",
    "&eta;": "\u03B7",
    "&theta;": "\u03B8",
    "&iota;": "\u03B9",
    "&kappa;": "\u03BA",
    "&lambda;": "\u03BB",
    "&mu;": "\u03BC",
    "&nu;": "\u03BD",
    "&xi;": "\u03BE",
    "&omicron;": "\u03BF",
    "&pi;": "\u03C0",
    "&rho;": "\u03C1",
    "&sigma;": "\u03C3",
    "&tau;": "\u03C4",
    "&upsilon;": "\u03C5",
    "&phi;": "\u03C6",
    "&chi;": "\u03C7",
    "&psi;": "\u03C8",
    "&omega;": "\u03C9",
    "&Alpha;": "\u0391",
    "&Beta;": "\u0392",
    "&Gamma;": "\u0393",
    "&Delta;": "\u0394",
    "&Epsilon;": "\u0395",
    "&Zeta;": "\u0396",
    "&Eta;": "\u0397",
    "&Theta;": "\u0398",
    "&Iota;": "\u0399",
    "&Kappa;": "\u039A",
    "&Lambda;": "\u039B",
    "&Mu;": "\u039C",
    "&Nu;": "\u039D",
    "&Xi;": "\u039E",
    "&Omicron;": "\u039F",
    "&Pi;": "\u03A0",
    "&Rho;": "\u03A1",
    "&Sigma;": "\u03A3",
    "&Tau;": "\u03A4",
    "&Upsilon;": "\u03A5",
    "&Phi;": "\u03A6",
    "&Chi;": "\u03A7",
    "&Psi;": "\u03A8",
    "&Omega;": "\u03A9",
    "&Agrave;": "\xC0",
    "&Aacute;": "\xC1",
    "&Acirc;": "\xC2",
    "&Atilde;": "\xC3",
    "&Auml;": "\xC4",
    "&Aring;": "\xC5",
    "&AElig;": "\xC6",
    "&Ccedil;": "\xC7",
    "&Egrave;": "\xC8",
    "&Eacute;": "\xC9",
    "&Ecirc;": "\xCA",
    "&Euml;": "\xCB",
    "&Igrave;": "\xCC",
    "&Iacute;": "\xCD",
    "&Icirc;": "\xCE",
    "&Iuml;": "\xCF",
    "&ETH;": "\xD0",
    "&Ntilde;": "\xD1",
    "&Ograve;": "\xD2",
    "&Oacute;": "\xD3",
    "&Ocirc;": "\xD4",
    "&Otilde;": "\xD5",
    "&Ouml;": "\xD6",
    "&Oslash;": "\xD8",
    "&Ugrave;": "\xD9",
    "&Uacute;": "\xDA",
    "&Ucirc;": "\xDB",
    "&Uuml;": "\xDC",
    "&Yacute;": "\xDD",
    "&THORN;": "\xDE",
    "&szlig;": "\xDF",
    "&agrave;": "\xE0",
    "&aacute;": "\xE1",
    "&acirc;": "\xE2",
    "&atilde;": "\xE3",
    "&auml;": "\xE4",
    "&aring;": "\xE5",
    "&aelig;": "\xE6",
    "&ccedil;": "\xE7",
    "&egrave;": "\xE8",
    "&eacute;": "\xE9",
    "&ecirc;": "\xEA",
    "&euml;": "\xEB",
    "&igrave;": "\xEC",
    "&iacute;": "\xED",
    "&icirc;": "\xEE",
    "&iuml;": "\xEF",
    "&eth;": "\xF0",
    "&ntilde;": "\xF1",
    "&ograve;": "\xF2",
    "&oacute;": "\xF3",
    "&ocirc;": "\xF4",
    "&otilde;": "\xF5",
    "&ouml;": "\xF6",
    "&oslash;": "\xF8",
    "&ugrave;": "\xF9",
    "&uacute;": "\xFA",
    "&ucirc;": "\xFB",
    "&uuml;": "\xFC",
    "&yacute;": "\xFD",
    "&thorn;": "\xFE",
    "&yuml;": "\xFF",
    "&iexcl;": "\xA1",
    "&iquest;": "\xBF",
    "&fnof;": "\u0192",
    "&circ;": "\u02C6",
    "&tilde;": "\u02DC",
    "&OElig;": "\u0152",
    "&oelig;": "\u0153",
    "&Scaron;": "\u0160",
    "&scaron;": "\u0161",
    "&Yuml;": "\u0178",
    "&ordf;": "\xAA",
    "&ordm;": "\xBA",
    "&macr;": "\xAF",
    "&acute;": "\xB4",
    "&cedil;": "\xB8",
    "&sup1;": "\xB9",
    "&sup2;": "\xB2",
    "&sup3;": "\xB3",
    "&frac14;": "\xBC",
    "&frac12;": "\xBD",
    "&frac34;": "\xBE",
    "&spades;": "\u2660",
    "&clubs;": "\u2663",
    "&hearts;": "\u2665",
    "&diams;": "\u2666",
    "&loz;": "\u25CA",
    "&oline;": "\u203E",
    "&frasl;": "\u2044",
    "&weierp;": "\u2118",
    "&image;": "\u2111",
    "&real;": "\u211C",
    "&alefsym;": "\u2135"
  };
  var entityPattern = new RegExp(Object.keys(commonEntities).map((entity) => entity.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")).join("|"), "g");
  var decodeHtmlEntities = (text) => text.replace(entityPattern, (match) => commonEntities[match]).replace(/&#(\d+);/g, (_, num) => String.fromCharCode(parseInt(num, 10))).replace(/&#x([0-9a-fA-F]+);/g, (_, hex) => String.fromCharCode(parseInt(hex, 16)));

  // node_modules/react-i18next/dist/es/IcuTransUtils/tokenizer.js
  var tokenize = (translation) => {
    const tokens = [];
    let position = 0;
    let currentText = "";
    const flushText = () => {
      if (currentText) {
        tokens.push({
          type: "Text",
          value: currentText,
          position: position - currentText.length
        });
        currentText = "";
      }
    };
    while (position < translation.length) {
      const char = translation[position];
      if (char === "<") {
        const tagMatch = translation.slice(position).match(/^<(\d+)>/);
        if (tagMatch) {
          flushText();
          tokens.push({
            type: "TagOpen",
            value: tagMatch[0],
            position,
            tagNumber: parseInt(tagMatch[1], 10)
          });
          position += tagMatch[0].length;
        } else {
          const closeTagMatch = translation.slice(position).match(/^<\/(\d+)>/);
          if (closeTagMatch) {
            flushText();
            tokens.push({
              type: "TagClose",
              value: closeTagMatch[0],
              position,
              tagNumber: parseInt(closeTagMatch[1], 10)
            });
            position += closeTagMatch[0].length;
          } else {
            currentText += char;
            position += 1;
          }
        }
      } else {
        currentText += char;
        position += 1;
      }
    }
    flushText();
    return tokens;
  };

  // node_modules/react-i18next/dist/es/IcuTransUtils/renderTranslation.js
  var import_react4 = __toESM(require_react_shim(), 1);
  var renderDeclarationNode = (declaration, children, childDeclarations) => {
    const {
      type,
      props = {}
    } = declaration;
    if (props.children && Array.isArray(props.children) && childDeclarations) {
      const {
        children: _childrenToRemove,
        ...propsWithoutChildren
      } = props;
      return import_react4.default.createElement(type, propsWithoutChildren, ...children);
    }
    if (children.length === 0) {
      return import_react4.default.createElement(type, props);
    }
    if (children.length === 1) {
      return import_react4.default.createElement(type, props, children[0]);
    }
    return import_react4.default.createElement(type, props, ...children);
  };
  var renderTranslation = (translation, declarations = []) => {
    if (!translation) {
      return [];
    }
    const tokens = tokenize(translation);
    const result = [];
    const stack = [];
    const literalTagNumbers = /* @__PURE__ */ new Set();
    const getCurrentDeclarations = () => {
      if (stack.length === 0) {
        return declarations;
      }
      const parentFrame = stack[stack.length - 1];
      if (parentFrame.declaration.props?.children && Array.isArray(parentFrame.declaration.props.children)) {
        return parentFrame.declaration.props.children;
      }
      return parentFrame.declarations;
    };
    tokens.forEach((token) => {
      switch (token.type) {
        case "Text":
          {
            const decoded = decodeHtmlEntities(token.value);
            const targetArray = stack.length > 0 ? stack[stack.length - 1].children : result;
            targetArray.push(decoded);
          }
          break;
        case "TagOpen":
          {
            const {
              tagNumber
            } = token;
            const currentDeclarations = getCurrentDeclarations();
            const declaration = currentDeclarations[tagNumber];
            if (!declaration) {
              literalTagNumbers.add(tagNumber);
              const literalText = `<${tagNumber}>`;
              const targetArray = stack.length > 0 ? stack[stack.length - 1].children : result;
              targetArray.push(literalText);
              break;
            }
            stack.push({
              tagNumber,
              children: [],
              position: token.position,
              declaration,
              declarations: currentDeclarations
            });
          }
          break;
        case "TagClose":
          {
            const {
              tagNumber
            } = token;
            if (literalTagNumbers.has(tagNumber)) {
              const literalText = `</${tagNumber}>`;
              const literalTargetArray = stack.length > 0 ? stack[stack.length - 1].children : result;
              literalTargetArray.push(literalText);
              literalTagNumbers.delete(tagNumber);
              break;
            }
            if (stack.length === 0) {
              throw new TranslationParserError(`Unexpected closing tag </${tagNumber}> at position ${token.position}`, token.position, translation);
            }
            const frame = stack.pop();
            if (frame.tagNumber !== tagNumber) {
              throw new TranslationParserError(`Mismatched tags: expected </${frame.tagNumber}> but got </${tagNumber}> at position ${token.position}`, token.position, translation);
            }
            const element = renderDeclarationNode(frame.declaration, frame.children, frame.declarations);
            const elementTargetArray = stack.length > 0 ? stack[stack.length - 1].children : result;
            elementTargetArray.push(element);
          }
          break;
      }
    });
    if (stack.length > 0) {
      const unclosed = stack[stack.length - 1];
      throw new TranslationParserError(`Unclosed tag <${unclosed.tagNumber}> at position ${unclosed.position}`, unclosed.position, translation);
    }
    return result;
  };

  // node_modules/react-i18next/dist/es/IcuTransWithoutContext.js
  function IcuTransWithoutContext({
    i18nKey,
    defaultTranslation,
    content,
    ns,
    values = {},
    i18n: i18nFromProps,
    t: tFromProps
  }) {
    const i18n = i18nFromProps || getI18n();
    if (!i18n) {
      warnOnce(i18n, "NO_I18NEXT_INSTANCE", `IcuTrans: You need to pass in an i18next instance using i18nextReactModule`, {
        i18nKey
      });
      return import_react5.default.createElement(import_react5.default.Fragment, {}, defaultTranslation);
    }
    const t2 = tFromProps || i18n.t?.bind(i18n) || ((k) => k);
    let namespaces = ns || t2.ns || i18n.options?.defaultNS;
    namespaces = isString2(namespaces) ? [namespaces] : namespaces || ["translation"];
    let mergedValues = values;
    if (i18n.options?.interpolation?.defaultVariables) {
      mergedValues = values && Object.keys(values).length > 0 ? {
        ...values,
        ...i18n.options.interpolation.defaultVariables
      } : {
        ...i18n.options.interpolation.defaultVariables
      };
    }
    const translation = t2(i18nKey, {
      defaultValue: defaultTranslation,
      ...mergedValues,
      ns: namespaces
    });
    try {
      const rendered = renderTranslation(translation, content);
      return import_react5.default.createElement(import_react5.default.Fragment, {}, ...rendered);
    } catch (error) {
      warn(i18n, "ICU_TRANS_RENDER_ERROR", `IcuTrans component error for key "${i18nKey}": ${error.message}`, {
        i18nKey,
        error
      });
      return import_react5.default.createElement(import_react5.default.Fragment, {}, translation);
    }
  }
  IcuTransWithoutContext.displayName = "IcuTransWithoutContext";

  // node_modules/react-i18next/dist/es/IcuTrans.js
  function IcuTrans({
    i18nKey,
    defaultTranslation,
    content,
    ns,
    values = {},
    i18n: i18nFromProps,
    t: tFromProps
  }) {
    const {
      i18n: i18nFromContext,
      defaultNS: defaultNSFromContext
    } = (0, import_react6.useContext)(I18nContext) || {};
    const i18n = i18nFromProps || i18nFromContext || getI18n();
    const t2 = tFromProps || i18n?.t.bind(i18n);
    return IcuTransWithoutContext({
      i18nKey,
      defaultTranslation,
      content,
      ns: ns || t2?.ns || defaultNSFromContext || i18n?.options?.defaultNS,
      values,
      i18n,
      t: tFromProps
    });
  }
  IcuTrans.displayName = "IcuTrans";

  // node_modules/react-i18next/dist/es/useTranslation.js
  var import_react7 = __toESM(require_react_shim(), 1);
  var import_shim = __toESM(require_shim(), 1);
  var notReadyT = (k, optsOrDefaultValue) => {
    if (isString2(optsOrDefaultValue)) return optsOrDefaultValue;
    if (isObject(optsOrDefaultValue) && isString2(optsOrDefaultValue.defaultValue)) return optsOrDefaultValue.defaultValue;
    if (typeof k === "function") return "";
    if (Array.isArray(k)) {
      const last = k[k.length - 1];
      return typeof last === "function" ? "" : last;
    }
    return k;
  };
  var notReadySnapshot = {
    t: notReadyT,
    ready: false
  };
  var dummySubscribe = () => () => {
  };
  var useTranslation = (ns, props = {}) => {
    const {
      i18n: i18nFromProps
    } = props;
    const {
      i18n: i18nFromContext,
      defaultNS: defaultNSFromContext
    } = (0, import_react7.useContext)(I18nContext) || {};
    const i18n = i18nFromProps || i18nFromContext || getI18n();
    if (i18n && !i18n.reportNamespaces) i18n.reportNamespaces = new ReportNamespaces();
    if (!i18n) {
      warnOnce(i18n, "NO_I18NEXT_INSTANCE", "useTranslation: You will need to pass in an i18next instance by using initReactI18next");
    }
    const i18nOptions = (0, import_react7.useMemo)(() => ({
      ...getDefaults(),
      ...i18n?.options?.react,
      ...props
    }), [i18n, props]);
    const {
      useSuspense,
      keyPrefix
    } = i18nOptions;
    const nsOrContext = ns || defaultNSFromContext || i18n?.options?.defaultNS;
    const unstableNamespaces = isString2(nsOrContext) ? [nsOrContext] : nsOrContext || ["translation"];
    const namespaces = (0, import_react7.useMemo)(() => unstableNamespaces, unstableNamespaces);
    i18n?.reportNamespaces?.addUsedNamespaces?.(namespaces);
    const revisionRef = (0, import_react7.useRef)(0);
    const subscribe = (0, import_react7.useCallback)((callback) => {
      if (!i18n) return dummySubscribe;
      const {
        bindI18n,
        bindI18nStore
      } = i18nOptions;
      const wrappedCallback = () => {
        revisionRef.current += 1;
        callback();
      };
      if (bindI18n) i18n.on(bindI18n, wrappedCallback);
      if (bindI18nStore) i18n.store.on(bindI18nStore, wrappedCallback);
      return () => {
        if (bindI18n) bindI18n.split(" ").forEach((e2) => i18n.off(e2, wrappedCallback));
        if (bindI18nStore) bindI18nStore.split(" ").forEach((e2) => i18n.store.off(e2, wrappedCallback));
      };
    }, [i18n, i18nOptions]);
    const snapshotRef = (0, import_react7.useRef)();
    const getSnapshot = (0, import_react7.useCallback)(() => {
      if (!i18n) {
        return notReadySnapshot;
      }
      const calculatedReady = !!(i18n.isInitialized || i18n.initializedStoreOnce) && namespaces.every((n) => hasLoadedNamespace2(n, i18n, i18nOptions));
      const currentLng = props.lng || i18n.language;
      const currentRevision = revisionRef.current;
      const lastSnapshot = snapshotRef.current;
      if (lastSnapshot && lastSnapshot.ready === calculatedReady && lastSnapshot.lng === currentLng && lastSnapshot.keyPrefix === keyPrefix && lastSnapshot.revision === currentRevision) {
        return lastSnapshot;
      }
      const calculatedT = i18n.getFixedT(currentLng, i18nOptions.nsMode === "fallback" ? namespaces : namespaces[0], keyPrefix, {
        scopeNs: namespaces
      });
      const newSnapshot = {
        t: calculatedT,
        ready: calculatedReady,
        lng: currentLng,
        keyPrefix,
        revision: currentRevision
      };
      snapshotRef.current = newSnapshot;
      return newSnapshot;
    }, [i18n, namespaces, keyPrefix, i18nOptions, props.lng]);
    const [loadCount, setLoadCount] = (0, import_react7.useState)(0);
    const {
      t: t2,
      ready
    } = (0, import_shim.useSyncExternalStore)(subscribe, getSnapshot, getSnapshot);
    (0, import_react7.useEffect)(() => {
      if (i18n && !ready && !useSuspense) {
        const onLoaded = () => setLoadCount((c) => c + 1);
        if (props.lng) {
          loadLanguages2(i18n, props.lng, namespaces, onLoaded);
        } else {
          loadNamespaces2(i18n, namespaces, onLoaded);
        }
      }
    }, [i18n, props.lng, namespaces, ready, useSuspense, loadCount]);
    const finalI18n = i18n || {};
    const wrapperRef = (0, import_react7.useRef)(null);
    const wrapperLangRef = (0, import_react7.useRef)();
    const createI18nWrapper = (original) => {
      const descriptors = Object.getOwnPropertyDescriptors(original);
      if (descriptors.__original) delete descriptors.__original;
      const wrapper = Object.create(Object.getPrototypeOf(original), descriptors);
      if (!Object.prototype.hasOwnProperty.call(wrapper, "__original")) {
        try {
          Object.defineProperty(wrapper, "__original", {
            value: original,
            writable: false,
            enumerable: false,
            configurable: false
          });
        } catch (_) {
        }
      }
      return wrapper;
    };
    const ret = (0, import_react7.useMemo)(() => {
      const original = finalI18n;
      const lang = original?.language;
      let i18nWrapper = original;
      if (original) {
        if (wrapperRef.current && wrapperRef.current.__original === original) {
          if (wrapperLangRef.current !== lang) {
            i18nWrapper = createI18nWrapper(original);
            wrapperRef.current = i18nWrapper;
            wrapperLangRef.current = lang;
          } else {
            i18nWrapper = wrapperRef.current;
          }
        } else {
          i18nWrapper = createI18nWrapper(original);
          wrapperRef.current = i18nWrapper;
          wrapperLangRef.current = lang;
        }
      }
      const effectiveT = !ready && !useSuspense ? (...args) => {
        warnOnce(i18n, "USE_T_BEFORE_READY", "useTranslation: t was called before ready. When using useSuspense: false, make sure to check the ready flag before using t.");
        return t2(...args);
      } : t2;
      const arr = [effectiveT, i18nWrapper, ready];
      arr.t = effectiveT;
      arr.i18n = i18nWrapper;
      arr.ready = ready;
      return arr;
    }, [t2, finalI18n, ready, finalI18n.resolvedLanguage, finalI18n.language, finalI18n.languages]);
    if (i18n && useSuspense && !ready) {
      throw new Promise((resolve) => {
        const onLoaded = () => resolve();
        if (props.lng) {
          loadLanguages2(i18n, props.lng, namespaces, onLoaded);
        } else {
          loadNamespaces2(i18n, namespaces, onLoaded);
        }
      });
    }
    return ret;
  };

  // node_modules/react-i18next/dist/es/withTranslation.js
  var import_react8 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/I18nextProvider.js
  var import_react9 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/withSSR.js
  var import_react11 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/useSSR.js
  var import_react10 = __toESM(require_react_shim(), 1);

  // src/components/icons/SpoolHubLogo.tsx
  var import_jsx_runtime2 = __toESM(require_react_shim(), 1);
  function SpoolHubLogo({ height = 40, className }) {
    const width = Math.round(height * 680 / 180);
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)(
      "svg",
      {
        width,
        height,
        viewBox: "0 0 680 180",
        role: "img",
        "aria-label": "SpoolHub",
        className: `text-[var(--text-primary)]${className ? ` ${className}` : ""}`,
        xmlns: "http://www.w3.org/2000/svg",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("defs", { children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("g", { id: "spoolhub-spokes", children: [0, 45, 90, 135, 180, 225, 270, 315].map((angle) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
            "line",
            {
              x1: "0",
              y1: "-52",
              x2: "0",
              y2: "-34",
              stroke: "#F97316",
              strokeWidth: "2",
              opacity: "0.5",
              transform: angle === 0 ? void 0 : `rotate(${angle})`
            },
            angle
          )) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("circle", { cx: "115", cy: "90", r: "65", fill: "none", stroke: "#0EA5E9", strokeWidth: "3" }),
          /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("g", { transform: "translate(115,90)", children: [
            /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("g", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("animateTransform", { attributeName: "transform", type: "rotate", from: "0", to: "360", dur: "4s", repeatCount: "indefinite" }),
              /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("circle", { cx: "0", cy: "0", r: "53", fill: "none", stroke: "#F97316", strokeWidth: "6", opacity: "0.4" }),
              /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("circle", { cx: "0", cy: "0", r: "43", fill: "none", stroke: "#F97316", strokeWidth: "6", opacity: "0.65" }),
              /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("circle", { cx: "0", cy: "0", r: "33", fill: "none", stroke: "#F97316", strokeWidth: "6", opacity: "0.9" }),
              /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("use", { href: "#spoolhub-spokes" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("circle", { cx: "0", cy: "0", r: "17", fill: "#0EA5E9" }),
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("circle", { cx: "0", cy: "0", r: "9", fill: "none", stroke: "white", strokeWidth: "2", opacity: "0.6" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("path", { d: "M 170.2,34.9 A 78,78 0 0,1 170.2,145.1", fill: "none", stroke: "#0EA5E9", strokeWidth: "3.5", strokeLinecap: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("animate", { attributeName: "opacity", values: "0;1;0", dur: "2s", begin: "0s", repeatCount: "indefinite" }),
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("animate", { attributeName: "stroke-width", values: "1;3.5;1", dur: "2s", begin: "0s", repeatCount: "indefinite" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("path", { d: "M 182.9,22.2 A 96,96 0 0,1 182.9,157.8", fill: "none", stroke: "#0EA5E9", strokeWidth: "2.5", strokeLinecap: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("animate", { attributeName: "opacity", values: "0;0.65;0", dur: "2s", begin: "0.35s", repeatCount: "indefinite" }),
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("animate", { attributeName: "stroke-width", values: "0.5;2.5;0.5", dur: "2s", begin: "0.35s", repeatCount: "indefinite" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("path", { d: "M 195.6,9.4 A 114,114 0 0,1 195.6,170.6", fill: "none", stroke: "#0EA5E9", strokeWidth: "2", strokeLinecap: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("animate", { attributeName: "opacity", values: "0;0.35;0", dur: "2s", begin: "0.7s", repeatCount: "indefinite" }),
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("animate", { attributeName: "stroke-width", values: "0.5;2;0.5", dur: "2s", begin: "0.7s", repeatCount: "indefinite" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)(
            "text",
            {
              x: "242",
              y: "90",
              fill: "currentColor",
              fontFamily: "system-ui, sans-serif",
              fontSize: "80",
              fontWeight: "500",
              dominantBaseline: "middle",
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("animate", { attributeName: "opacity", values: "0;1", dur: "0.8s", fill: "freeze" }),
                "SpoolHub"
              ]
            }
          )
        ]
      }
    );
  }

  // src/components/icons/FilamentIcon.tsx
  var import_jsx_runtime3 = __toESM(require_react_shim(), 1);
  function FilamentIcon({ color, colors, size = 68 }) {
    const multiColor = colors && colors.length >= 2;
    const uid = (multiColor ? colors.join("") : color).replace(/[^a-zA-Z0-9]/g, "");
    const gradientStops = multiColor ? colors.map((c, i, arr) => ({
      color: c,
      offset: `${i / (arr.length - 1) * 100}%`
    })) : null;
    return /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("svg", { width: size, height: size, viewBox: "0 0 120 120", xmlns: "http://www.w3.org/2000/svg", "aria-hidden": "true", children: [
      /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("defs", { children: [
        gradientStops && /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("linearGradient", { id: `g-${uid}`, x1: "0%", y1: "0%", x2: "100%", y2: "100%", children: gradientStops.map((s, i) => /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("stop", { offset: s.offset, stopColor: s.color }, i)) }),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("radialGradient", { id: `l-${uid}`, cx: "36%", cy: "28%", r: "64%", children: [
          /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("stop", { offset: "0%", stopColor: "white", stopOpacity: "0.55" }),
          /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("stop", { offset: "50%", stopColor: "white", stopOpacity: "0.0" }),
          /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("stop", { offset: "100%", stopColor: "white", stopOpacity: "0.0" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("clipPath", { id: `c-${uid}`, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("circle", { cx: "60", cy: "60", r: "50" }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("circle", { cx: "60", cy: "60", r: "50", fill: gradientStops ? `url(#g-${uid})` : color }),
      /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("g", { clipPath: `url(#c-${uid})`, fill: "none", stroke: "rgba(0,0,0,0.07)", strokeWidth: "3", strokeLinecap: "round", children: [
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("path", { d: "M 8 92  C 22 68, 52 55, 58 22" }),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("path", { d: "M 26 108 C 42 82, 72 68, 80 36" }),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("path", { d: "M 48 114 C 62 90, 90 76, 100 48" }),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("path", { d: "M -4 62  C 12 40, 40 28, 48 6" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("circle", { cx: "60", cy: "60", r: "50", fill: `url(#l-${uid})` })
    ] });
  }

  // src/components/icons/SpoolIcon.tsx
  var import_react12 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime4 = __toESM(require_react_shim(), 1);
  function useIsDark() {
    const [isDark, setIsDark] = (0, import_react12.useState)(
      () => document.documentElement.classList.contains("dark")
    );
    (0, import_react12.useEffect)(() => {
      const obs = new MutationObserver(
        () => setIsDark(document.documentElement.classList.contains("dark"))
      );
      obs.observe(document.documentElement, { attributes: true, attributeFilter: ["class"] });
      return () => obs.disconnect();
    }, []);
    return isDark;
  }
  function isNearWhite(hex) {
    if (!hex.startsWith("#") || hex.length < 7) return false;
    const r = parseInt(hex.slice(1, 3), 16);
    const g = parseInt(hex.slice(3, 5), 16);
    const b = parseInt(hex.slice(5, 7), 16);
    return r > 210 && g > 210 && b > 210;
  }
  function isNearBlack(hex) {
    if (!hex.startsWith("#") || hex.length < 7) return false;
    const r = parseInt(hex.slice(1, 3), 16);
    const g = parseInt(hex.slice(3, 5), 16);
    const b = parseInt(hex.slice(5, 7), 16);
    return r < 50 && g < 50 && b < 50;
  }
  var DARK_H = {
    outer: "#2a2a2e",
    inner: "#323237",
    stroke: "#4a4a52",
    rim: "#3c3c44",
    hub: "#252528",
    hubInner: "#2d2d32",
    spoke: "#222226",
    axle: "#1a1a1d",
    axleIn: "#111114"
  };
  var LIGHT_H = {
    outer: "#d4d4d8",
    inner: "#e4e4e7",
    stroke: "#a1a1aa",
    rim: "#9ca3af",
    hub: "#d1d5db",
    hubInner: "#e5e7eb",
    spoke: "#b4b4b8",
    axle: "#71717a",
    axleIn: "#a1a1aa"
  };
  function SpoolIcon({ color, size = 52, showBackground = false, className }) {
    const isDark = useIsDark();
    const uid = color.replace(/[^a-zA-Z0-9]/g, "");
    const useLightHolder = isDark && !isNearWhite(color) || !isDark && isNearBlack(color);
    const h = useLightHolder ? LIGHT_H : DARK_H;
    return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(
      "svg",
      {
        width: className ? "100%" : size,
        height: className ? "100%" : size,
        className,
        viewBox: "155 75 370 370",
        role: "img",
        xmlns: "http://www.w3.org/2000/svg",
        "aria-hidden": "true",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("defs", { children: /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)("pattern", { id: `filament-${uid}`, patternUnits: "userSpaceOnUse", width: "12", height: "8", patternTransform: "rotate(8)", children: [
            /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { width: "12", height: "8", fill: color }),
            /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("line", { x1: "0", y1: "4", x2: "12", y2: "4", stroke: "rgba(0,0,0,0.25)", strokeWidth: "2.5" })
          ] }) }),
          showBackground && /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("rect", { x: "155", y: "75", width: "370", height: "370", rx: "24", fill: "#f0f0f2" }),
          showBackground && /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "440", rx: "155", ry: "22", fill: "#00000018" }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "155", ry: "155", fill: h.outer }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "151", ry: "151", fill: h.inner }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "155", ry: "155", fill: "none", stroke: h.stroke, strokeWidth: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "140", ry: "140", fill: `url(#filament-${uid})` }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "140", ry: "140", fill: "none", stroke: color, strokeWidth: "1", opacity: "0.4" }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "122", ry: "122", fill: "none", stroke: color, strokeWidth: "1", opacity: "0.3" }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "104", ry: "104", fill: "none", stroke: color, strokeWidth: "1", opacity: "0.25" }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "86", ry: "86", fill: "none", stroke: color, strokeWidth: "1", opacity: "0.2" }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "155", ry: "155", fill: "none", stroke: h.rim, strokeWidth: "28" }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "68", ry: "68", fill: h.hub }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "64", ry: "64", fill: h.hubInner }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)("g", { stroke: h.spoke, strokeWidth: "6", opacity: "0.8", children: [
            /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("line", { x1: "340", y1: "196", x2: "340", y2: "324" }),
            /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("line", { x1: "276", y1: "260", x2: "404", y2: "260" }),
            /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("line", { x1: "297", y1: "215", x2: "383", y2: "305" }),
            /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("line", { x1: "297", y1: "305", x2: "383", y2: "215" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "26", ry: "26", fill: h.axle }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("ellipse", { cx: "340", cy: "260", rx: "22", ry: "22", fill: h.axleIn }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M480 235 Q530 210 545 185 Q558 162 548 148", fill: "none", stroke: color, strokeWidth: "3.5", strokeLinecap: "round" }),
          /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("path", { d: "M200 230 Q230 100 340 105 Q450 100 480 230", fill: "none", stroke: h.stroke, strokeWidth: "2", opacity: "0.6" })
        ]
      }
    );
  }

  // src/components/icons/PrinterIcon.tsx
  var import_jsx_runtime5 = __toESM(require_react_shim(), 1);
  function PrinterIcon({ className = "w-6 h-6", stroke = "currentColor" }) {
    return /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("svg", { className, viewBox: "0 0 24 24", fill: "none", stroke, strokeWidth: "1.75", strokeLinecap: "round", strokeLinejoin: "round", children: [
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("line", { x1: "4", y1: "3", x2: "4", y2: "19" }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("line", { x1: "20", y1: "3", x2: "20", y2: "19" }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("path", { d: "M4 3h16" }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("line", { x1: "4", y1: "9", x2: "20", y2: "9" }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("rect", { x: "9.5", y: "6.5", width: "5", height: "4", rx: "0.75" }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("path", { d: "M11.5 10.5 L12 13 L12.5 10.5", strokeWidth: "1.5" }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("rect", { x: "3", y: "19", width: "18", height: "2", rx: "0.75" }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("rect", { x: "8.5", y: "14.5", width: "7", height: "4", rx: "0.5" })
    ] });
  }

  // src/components/icons/SpoolOutlineIcon.tsx
  var import_jsx_runtime6 = __toESM(require_react_shim(), 1);
  function SpoolOutlineIcon({ className = "w-6 h-6", stroke = "currentColor" }) {
    return /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("svg", { className, viewBox: "0 0 24 24", fill: "none", stroke, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("circle", { cx: "12", cy: "12", r: "9" }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("circle", { cx: "12", cy: "12", r: "3" }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("line", { x1: "12", y1: "3", x2: "12", y2: "9" }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("line", { x1: "12", y1: "15", x2: "12", y2: "21" }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("line", { x1: "3", y1: "12", x2: "9", y2: "12" }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("line", { x1: "15", y1: "12", x2: "21", y2: "12" })
    ] });
  }

  // src/components/icons/ActiveSpoolIcon.tsx
  var import_jsx_runtime7 = __toESM(require_react_shim(), 1);
  function ActiveSpoolIcon({ className = "w-6 h-6", stroke = "currentColor" }) {
    return /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("svg", { className, viewBox: "0 0 24 24", fill: "none", stroke, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
      /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("circle", { cx: "12", cy: "12", r: "9" }),
      /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("circle", { cx: "12", cy: "12", r: "3.5" }),
      /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("line", { x1: "12", y1: "3", x2: "12", y2: "8.5" }),
      /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("line", { x1: "12", y1: "15.5", x2: "12", y2: "21" }),
      /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("line", { x1: "3", y1: "12", x2: "8.5", y2: "12" }),
      /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("line", { x1: "15.5", y1: "12", x2: "21", y2: "12" }),
      /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
        "polygon",
        {
          points: "10.5,10.5 10.5,13.5 13.5,12",
          fill: stroke,
          stroke: "none"
        }
      )
    ] });
  }

  // src/components/icons/PenIcon.tsx
  var import_jsx_runtime8 = __toESM(require_react_shim(), 1);
  function PenIcon({ className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)(
      "svg",
      {
        width: "14",
        height: "14",
        className,
        "aria-hidden": true,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("path", { d: "M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7" }),
          /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("path", { d: "M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z" })
        ]
      }
    );
  }

  // src/components/icons/LockIcon.tsx
  var import_jsx_runtime9 = __toESM(require_react_shim(), 1);
  function LockIcon({ className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
      "svg",
      {
        width: "14",
        height: "14",
        className,
        "aria-hidden": true,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("rect", { x: "3", y: "11", width: "18", height: "11", rx: "2", ry: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("path", { d: "M7 11V7a5 5 0 0 1 10 0v4" })
        ]
      }
    );
  }

  // src/components/ActivityCard/ActivityCard.module.css
  var ActivityCard_default = {
    flatRow: "ActivityCard_flatRow",
    flatIcon: "ActivityCard_flatIcon",
    flatContent: "ActivityCard_flatContent",
    flatLine1: "ActivityCard_flatLine1",
    flatLine1Left: "ActivityCard_flatLine1Left",
    flatPrinterName: "ActivityCard_flatPrinterName",
    flatAction: "ActivityCard_flatAction",
    flatDay: "ActivityCard_flatDay",
    flatLine2: "ActivityCard_flatLine2",
    flatSub: "ActivityCard_flatSub",
    flatName: "ActivityCard_flatName",
    flatColorName: "ActivityCard_flatColorName",
    flatDesc: "ActivityCard_flatDesc",
    flatSep: "ActivityCard_flatSep",
    flatMat: "ActivityCard_flatMat",
    flatTime: "ActivityCard_flatTime",
    flatLine3: "ActivityCard_flatLine3",
    flatRowIcon: "ActivityCard_flatRowIcon",
    flatWeightLabel: "ActivityCard_flatWeightLabel",
    flatWeight: "ActivityCard_flatWeight",
    flatSubName: "ActivityCard_flatSubName",
    flatTagUid: "ActivityCard_flatTagUid",
    flatUsedBadge: "ActivityCard_flatUsedBadge",
    flatUsedLabel: "ActivityCard_flatUsedLabel",
    flatPrintingBadge: "ActivityCard_flatPrintingBadge",
    flatPauseSlot: "ActivityCard_flatPauseSlot",
    flatPrintFile: "ActivityCard_flatPrintFile",
    flatEstTime: "ActivityCard_flatEstTime",
    flatFileSkelShimmer: "ActivityCard_flatFileSkelShimmer",
    flatFileSkeleton: "ActivityCard_flatFileSkeleton",
    card: "ActivityCard_card",
    row1: "ActivityCard_row1",
    iconWrap: "ActivityCard_iconWrap",
    action: "ActivityCard_action",
    badge: "ActivityCard_badge",
    weight: "ActivityCard_weight",
    meta: "ActivityCard_meta",
    date: "ActivityCard_date",
    timeMobile: "ActivityCard_timeMobile",
    row2: "ActivityCard_row2",
    dot: "ActivityCard_dot",
    name: "ActivityCard_name",
    colorName: "ActivityCard_colorName",
    mat: "ActivityCard_mat",
    desc: "ActivityCard_desc",
    time: "ActivityCard_time",
    filamentRow: "ActivityCard_filamentRow"
  };

  // src/components/ActivityCard/ActivityCard.tsx
  var import_jsx_runtime10 = __toESM(require_react_shim(), 1);
  function clockTime(iso) {
    return new Date(iso).toLocaleTimeString(void 0, { hour: "2-digit", minute: "2-digit" });
  }
  function dayLabel(iso, today, yesterday) {
    const d = new Date(iso);
    const now = /* @__PURE__ */ new Date();
    const todayStart = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime();
    const yestStart = todayStart - 864e5;
    if (d.getTime() >= todayStart) return today;
    if (d.getTime() >= yestStart) return yesterday;
    return d.toLocaleDateString(void 0, { month: "short", day: "numeric" });
  }
  function formatMins(mins) {
    const h = Math.floor(mins / 60);
    const m = mins % 60;
    return m > 0 ? `${h}h ${m}m` : `${h}h`;
  }
  function relativeTime(iso, t2) {
    const diffMs = Date.now() - new Date(iso).getTime();
    if (diffMs < 864e5) {
      const mins = Math.floor(diffMs / 6e4);
      if (mins < 1) return t2("common.justNow");
      if (mins < 60) return t2("common.minutesAgo", { count: mins });
      return t2("common.hoursAgo", { count: Math.floor(diffMs / 36e5) });
    }
    return clockTime(iso);
  }
  var S = { width: 14, height: 14, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 2.5, strokeLinecap: "round", strokeLinejoin: "round" };
  var ICONS = {
    plus: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("line", { x1: "12", y1: "5", x2: "12", y2: "19" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("line", { x1: "5", y1: "12", x2: "19", y2: "12" })
    ] }),
    edit: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z" })
    ] }),
    trash: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("polyline", { points: "3 6 5 6 21 6" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M10 11v6" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M14 11v6" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2" })
    ] }),
    check: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("svg", { ...S, children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("polyline", { points: "20 6 9 17 4 12" }) }),
    scan: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M3 7V5a2 2 0 0 1 2-2h2" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M17 3h2a2 2 0 0 1 2 2v2" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M21 17v2a2 2 0 0 1-2 2h-2" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M7 21H5a2 2 0 0 1-2-2v-2" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("line", { x1: "7", y1: "12", x2: "17", y2: "12" })
    ] }),
    tag: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("line", { x1: "7", y1: "7", x2: "7.01", y2: "7" })
    ] }),
    minus: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("svg", { ...S, children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("line", { x1: "5", y1: "12", x2: "19", y2: "12" }) }),
    x: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
    ] }),
    printer: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("polyline", { points: "6 9 6 2 18 2 18 9" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("rect", { x: "6", y: "14", width: "12", height: "8" })
    ] }),
    pause: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("rect", { x: "6", y: "4", width: "4", height: "16" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("rect", { x: "14", y: "4", width: "4", height: "16" })
    ] }),
    play: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("svg", { ...S, children: /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("polygon", { points: "5 3 19 12 5 21 5 3" }) }),
    weight: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("circle", { cx: "12", cy: "5", r: "3" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M6.5 8a2 2 0 0 0-1.905 1.4L2.1 17.4A2 2 0 0 0 4 20h16a2 2 0 0 0 1.9-2.6l-2.495-8A2 2 0 0 0 17.5 8z" })
    ] }),
    mapPin: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("path", { d: "M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("circle", { cx: "12", cy: "10", r: "3" })
    ] }),
    clock: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("svg", { ...S, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("circle", { cx: "12", cy: "12", r: "10" }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("polyline", { points: "12 6 12 12 16 14" })
    ] })
  };
  function getIconCfg(eventType) {
    switch (eventType) {
      case "SpoolCreated":
        return { color: "#06b6d4", bg: "rgba(6,182,212,.15)", icon: ICONS.plus };
      case "SpoolActivated":
        return { color: "#22c55e", bg: "rgba(34,197,94,.15)", icon: ICONS.check };
      case "SpoolDeactivated":
        return { color: "#f59e0b", bg: "rgba(245,158,11,.15)", icon: ICONS.minus };
      case "SpoolUpdated":
        return { color: "#3b82f6", bg: "rgba(59,130,246,.15)", icon: ICONS.edit };
      case "SpoolDeleted":
        return { color: "#ef4444", bg: "rgba(239,68,68,.15)", icon: ICONS.trash };
      case "SpoolScanned":
        return { color: "#8b5cf6", bg: "rgba(139,92,246,.15)", icon: ICONS.scan };
      case "SpoolAssigned":
        return { color: "#06b6d4", bg: "rgba(6,182,212,.15)", icon: ICONS.printer };
      case "SpoolUnassigned":
        return { color: "#6b7280", bg: "rgba(107,114,128,.15)", icon: ICONS.printer };
      case "PrinterAdded":
        return { color: "#06b6d4", bg: "rgba(6,182,212,.15)", icon: ICONS.plus };
      case "PrinterUpdated":
        return { color: "#3b82f6", bg: "rgba(59,130,246,.15)", icon: ICONS.edit };
      case "PrinterDeleted":
        return { color: "#ef4444", bg: "rgba(239,68,68,.15)", icon: ICONS.trash };
      case "PrintStarted":
        return { color: "#22c55e", bg: "rgba(34,197,94,.15)", icon: ICONS.printer };
      case "PrintPaused":
        return { color: "#f59e0b", bg: "rgba(245,158,11,.15)", icon: ICONS.pause };
      case "PrintResumed":
        return { color: "#0ea5e9", bg: "rgba(14,165,233,.15)", icon: ICONS.play };
      case "PrintCompleted":
        return { color: "#22c55e", bg: "rgba(34,197,94,.15)", icon: ICONS.check };
      case "PrintFailed":
        return { color: "#ef4444", bg: "rgba(239,68,68,.15)", icon: ICONS.x };
      case "PrintCancelled":
        return { color: "#f97316", bg: "rgba(249,115,22,.15)", icon: ICONS.x };
      case "NfcTagRegistered":
        return { color: "#8b5cf6", bg: "rgba(139,92,246,.15)", icon: ICONS.scan };
      case "NfcTagRemoved":
        return { color: "#ef4444", bg: "rgba(239,68,68,.15)", icon: ICONS.scan };
      case "BrandAdded":
        return { color: "#06b6d4", bg: "rgba(6,182,212,.15)", icon: ICONS.tag };
      case "BrandDeleted":
        return { color: "#ef4444", bg: "rgba(239,68,68,.15)", icon: ICONS.tag };
      default:
        return { color: "#6b7280", bg: "rgba(107,114,128,.15)", icon: ICONS.printer };
    }
  }
  function ActivityCard({ activity, flat }) {
    const { t: t2 } = useTranslation();
    const { color, bg, icon } = getIconCfg(activity.eventType);
    const PRINT_EVENTS2 = /* @__PURE__ */ new Set(["PrintStarted", "PrintCompleted", "PrintPaused", "PrintResumed", "PrintFailed", "PrintCancelled"]);
    const isPrintEvent = PRINT_EVENTS2.has(activity.eventType);
    const snap = activity.snapshot;
    const brand = snap?.brand ?? "";
    const colorName = brand && activity.resourceName.startsWith(brand) ? activity.resourceName.slice(brand.length).trim() : snap?.colorName ?? "";
    const isPrintCompleted = activity.eventType === "PrintCompleted";
    const isPrintPaused = activity.eventType === "PrintPaused";
    const printFileLabel = (() => {
      if (!isPrintEvent) return null;
      if (snap?.printFileName) return snap.printFileName;
      if (!activity.description) return null;
      const d = activity.description;
      if (isPrintCompleted) {
        const m2 = d.match(/^(.+?) — [\d.]+g used$/i);
        return m2 ? m2[1].trim() : d;
      }
      const m = d.match(/^(?:started|paused|resumed|failed:\s*|cancelled:\s*)(.*)/i);
      return m ? m[1].trim() : d;
    })();
    const printUsedGrams = (() => {
      if (!isPrintCompleted) return null;
      if (snap?.gramsUsed != null && snap.gramsUsed > 0) return snap.gramsUsed;
      if (!activity.description) return null;
      const m = activity.description.match(/— ([\d.]+)g used$/i);
      return m ? parseFloat(m[1]) : null;
    })();
    const printEstimatedMins = (activity.eventType === "PrintStarted" || activity.eventType === "PrintPaused") && snap?.estimatedMins ? snap.estimatedMins : null;
    const printStartedFile = (() => {
      if (activity.eventType !== "PrintStarted" || !printFileLabel) return null;
      const m = printFileLabel.match(/^printing\s*-\s*(.*)/i);
      const f = m ? m[1].trim() : "";
      return f || printFileLabel;
    })();
    const today = t2("common.today");
    const yesterday = t2("common.yesterday");
    const actionLabel = (() => {
      switch (activity.eventType) {
        case "SpoolCreated":
          return t2("activityCard.spoolCreated");
        case "SpoolActivated":
          return t2("activityCard.spoolActivated");
        case "SpoolDeactivated":
          return t2("activityCard.spoolDeactivated");
        case "SpoolUpdated":
          return activity.description ? t2("activityCard.spoolUpdatedField", { field: activity.description.split(":")[0].trim() }) : t2("activityCard.spoolUpdated");
        case "SpoolDeleted":
          return t2("activityCard.spoolDeleted");
        case "SpoolScanned":
          return t2("activityCard.spoolScanned");
        case "SpoolAssigned":
          return t2("activityCard.spoolAssigned");
        case "SpoolUnassigned":
          return t2("activityCard.spoolUnassigned");
        case "PrinterAdded":
          return t2("activityCard.printerAdded");
        case "PrinterUpdated":
          return t2("activityCard.printerUpdated");
        case "PrinterDeleted":
          return t2("activityCard.printerDeleted");
        case "PrintStarted":
          return t2("activityCard.printStarted");
        case "PrintPaused":
          return t2("activityCard.printPaused");
        case "PrintResumed":
          return t2("activityCard.printResumed");
        case "PrintCompleted":
          return t2("activityCard.printCompleted");
        case "PrintFailed":
          return t2("activityCard.printFailed");
        case "PrintCancelled":
          return t2("activityCard.printCancelled");
        case "NfcTagRegistered":
          return t2("activityCard.nfcRegistered");
        case "NfcTagRemoved":
          return t2("activityCard.nfcRemoved");
        case "BrandAdded":
          return t2("activityCard.brandAdded");
        case "BrandDeleted":
          return t2("activityCard.brandDeleted");
        default:
          return activity.action;
      }
    })();
    const row1Weight = snap?.weight && snap.weight > 0 && (activity.eventType === "SpoolActivated" || activity.eventType === "SpoolDeactivated" || activity.eventType === "SpoolScanned") ? /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("span", { className: ActivityCard_default.weight, children: [
      snap.weight,
      "g"
    ] }) : null;
    const row2Desc = activity.description ? /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.desc, children: activity.description }) : null;
    const isNfcEvent = activity.eventType === "NfcTagRegistered" || activity.eventType === "NfcTagRemoved";
    const hasSpoolSnapshotForNfc = isNfcEvent && !!(snap?.colorHex || snap?.material || snap?.brand);
    const nfcSpoolName = isNfcEvent && !hasSpoolSnapshotForNfc ? (/\bto (.+)$/i.exec(activity.description ?? "") ?? [])[1]?.trim() ?? "" : "";
    const nfcFallbackParts = (() => {
      if (!nfcSpoolName) return { text: "", mat: "" };
      const words = nfcSpoolName.split(/\s+/);
      const last = words.at(-1) ?? "";
      if (last.length >= 2 && last === last.toUpperCase() && /^[A-Z]/.test(last)) {
        return { text: words.slice(0, -1).join(" "), mat: last };
      }
      return { text: nfcSpoolName, mat: "" };
    })();
    const printerSummary = (() => {
      switch (activity.eventType) {
        case "PrinterAdded":
          return t2("activityCard.resourceAdded", { name: activity.resourceName });
        case "PrinterUpdated":
          return t2("activityCard.resourceUpdated", { name: activity.resourceName });
        case "PrinterDeleted":
          return t2("activityCard.resourceDeleted", { name: activity.resourceName });
        case "BrandAdded":
          return t2("activityCard.resourceAdded", { name: activity.resourceName });
        case "BrandDeleted":
          return t2("activityCard.resourceDeleted", { name: activity.resourceName });
        case "PrintStarted":
        case "PrintPaused":
        case "PrintResumed":
        case "PrintCompleted":
        case "PrintFailed":
        case "PrintCancelled":
          return null;
        default:
          return null;
      }
    })();
    const content = /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.row1, children: [
        !flat && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: ActivityCard_default.iconWrap, style: { background: bg, color }, children: icon }),
        /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.action, children: actionLabel }),
        /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.badge, style: { color, background: bg }, children: activity.resourceType }),
        row1Weight,
        /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.meta, children: [
          /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.date, children: dayLabel(activity.createdAt, today, yesterday) }),
          /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.timeMobile, children: clockTime(activity.createdAt) })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: flat ? ActivityCard_default.row2Flat : ActivityCard_default.row2, children: [
        isPrintEvent ? /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.name, children: activity.resourceName }),
          printFileLabel && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.desc, children: printFileLabel })
        ] }) : printerSummary ? /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.desc, children: printerSummary }) : /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
          snap?.colorHex && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.dot, style: { background: snap.colorHex } }),
          brand ? /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.name, children: brand }),
            colorName && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.colorName, children: colorName })
          ] }) : /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.name, children: activity.resourceName }),
          snap?.material && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.mat, style: { color, background: bg }, children: snap.material }),
          row2Desc
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.time, children: clockTime(activity.createdAt) })
      ] }),
      isPrintEvent && snap && !!(snap.brand || snap.material || snap.colorHex) && /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: flat ? ActivityCard_default.filamentRowFlat : ActivityCard_default.filamentRow, children: [
        snap.colorHex && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.dot, style: { background: snap.colorHex } }),
        snap.brand && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.name, children: snap.brand }),
        colorName && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.colorName, children: colorName }),
        snap.material && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.mat, style: { color, background: bg }, children: snap.material }),
        snap.weight != null && snap.weight > 0 && /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("span", { className: ActivityCard_default.weight, children: [
          snap.weight,
          "g left"
        ] })
      ] })
    ] });
    const isSpoolEvent = !isPrintEvent && !isNfcEvent && !printerSummary && (snap?.colorHex || snap?.material || snap?.brand);
    const hasWeight = isSpoolEvent && snap?.weight != null && snap.weight > 0;
    if (flat) {
      return /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatRow, style: { "--accent": color }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: ActivityCard_default.flatIcon, style: { background: bg, color }, children: icon }),
        /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatContent, children: [
          /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatLine1, children: [
            /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatLine1Left, children: [
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatAction, children: actionLabel }),
              isPrintEvent && /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatSep, children: "\xB7" }),
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatPrinterName, children: activity.resourceName })
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatDay, children: dayLabel(activity.createdAt, today, yesterday) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatLine2, children: [
            /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: ActivityCard_default.flatSub, children: isPrintEvent ? /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(import_jsx_runtime10.Fragment, { children: isPrintPaused ? printEstimatedMins != null && printEstimatedMins > 0 ? /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatPauseSlot, children: [
              ICONS.clock,
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { children: formatMins(printEstimatedMins) }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { children: t2("activityCard.minsRemains") })
            ] }) : null : /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
              snap?.colorHex && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(SpoolIcon, { color: snap.colorHex, size: 18 }),
              snap?.brand && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatName, children: snap.brand }),
              snap?.colorName && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatColorName, children: snap.colorName }),
              snap?.material && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatMat, children: snap.material }),
              isPrintCompleted && printUsedGrams != null && printUsedGrams > 0 && /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatSep, children: "\xB7" }),
                /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("span", { className: ActivityCard_default.flatUsedBadge, children: [
                  printUsedGrams,
                  "g"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatUsedLabel, children: t2("activityCard.used") })
              ] })
            ] }) }) : isNfcEvent ? hasSpoolSnapshotForNfc ? /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
              snap?.colorHex && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(SpoolIcon, { color: snap.colorHex, size: 18 }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatName, children: brand }),
              colorName && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatColorName, children: colorName }),
              snap?.material && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatMat, children: snap.material })
            ] }) : nfcSpoolName ? /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(SpoolIcon, { color: "#9ca3af", size: 18 }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatName, children: nfcFallbackParts.text || nfcSpoolName }),
              nfcFallbackParts.mat && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatMat, children: nfcFallbackParts.mat })
            ] }) : null : printerSummary ? /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatDesc, children: printerSummary }) : /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
              snap?.colorHex && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(SpoolIcon, { color: snap.colorHex, size: 18 }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatName, children: brand || activity.resourceName }),
              brand && colorName && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatColorName, children: colorName }),
              snap?.material && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatMat, children: snap.material })
            ] }) }),
            /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatTime, children: relativeTime(activity.createdAt, t2) })
          ] }),
          (hasWeight || isSpoolEvent && snap?.stockLocation) && /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatLine3, children: [
            hasWeight && /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatRowIcon, children: ICONS.weight }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("span", { className: ActivityCard_default.flatWeight, children: [
                snap.weight,
                "g"
              ] })
            ] }),
            isSpoolEvent && snap?.stockLocation && /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(import_jsx_runtime10.Fragment, { children: [
              hasWeight && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatSep, children: "\xB7" }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatRowIcon, children: ICONS.mapPin }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatSubName, children: snap.stockLocation })
            ] })
          ] }),
          isNfcEvent && /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatLine3, children: [
            /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("span", { className: ActivityCard_default.flatWeightLabel, children: [
              t2("activityCard.tagUidLabel"),
              ":"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatTagUid, children: activity.resourceName })
          ] }),
          activity.eventType === "PrintStarted" && (printEstimatedMins != null && printEstimatedMins > 0 || !!printStartedFile || !!snap?.printJobId) && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: ActivityCard_default.flatLine3, children: /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: ActivityCard_default.flatPauseSlot, children: [
            ICONS.clock,
            printEstimatedMins != null && printEstimatedMins > 0 && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { children: formatMins(printEstimatedMins) }),
            /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { children: t2("activityCard.printing") }),
            printStartedFile ? /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatPrintFile, children: printStartedFile }) : snap?.printJobId ? /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatFileSkeleton }) : null
          ] }) }),
          isPrintEvent && activity.eventType !== "PrintStarted" && (!!printFileLabel || !!snap?.printJobId) && /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: ActivityCard_default.flatLine3, children: printFileLabel ? /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatPrintFile, children: printFileLabel }) : /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: ActivityCard_default.flatFileSkeleton }) })
        ] })
      ] });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: ActivityCard_default.card, style: { "--accent": color }, children: content });
  }

  // src/components/AddBrandModal/AddBrandModal.tsx
  var import_react13 = __toESM(require_react_shim(), 1);

  // node_modules/axios/lib/helpers/bind.js
  function bind(fn, thisArg) {
    return function wrap() {
      return fn.apply(thisArg, arguments);
    };
  }

  // node_modules/axios/lib/utils.js
  var { toString } = Object.prototype;
  var { getPrototypeOf } = Object;
  var { iterator, toStringTag } = Symbol;
  var kindOf = /* @__PURE__ */ ((cache2) => (thing) => {
    const str = toString.call(thing);
    return cache2[str] || (cache2[str] = str.slice(8, -1).toLowerCase());
  })(/* @__PURE__ */ Object.create(null));
  var kindOfTest = (type) => {
    type = type.toLowerCase();
    return (thing) => kindOf(thing) === type;
  };
  var typeOfTest = (type) => (thing) => typeof thing === type;
  var { isArray } = Array;
  var isUndefined = typeOfTest("undefined");
  function isBuffer(val) {
    return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor) && isFunction(val.constructor.isBuffer) && val.constructor.isBuffer(val);
  }
  var isArrayBuffer = kindOfTest("ArrayBuffer");
  function isArrayBufferView(val) {
    let result;
    if (typeof ArrayBuffer !== "undefined" && ArrayBuffer.isView) {
      result = ArrayBuffer.isView(val);
    } else {
      result = val && val.buffer && isArrayBuffer(val.buffer);
    }
    return result;
  }
  var isString3 = typeOfTest("string");
  var isFunction = typeOfTest("function");
  var isNumber = typeOfTest("number");
  var isObject2 = (thing) => thing !== null && typeof thing === "object";
  var isBoolean = (thing) => thing === true || thing === false;
  var isPlainObject = (val) => {
    if (kindOf(val) !== "object") {
      return false;
    }
    const prototype2 = getPrototypeOf(val);
    return (prototype2 === null || prototype2 === Object.prototype || Object.getPrototypeOf(prototype2) === null) && !(toStringTag in val) && !(iterator in val);
  };
  var isEmptyObject = (val) => {
    if (!isObject2(val) || isBuffer(val)) {
      return false;
    }
    try {
      return Object.keys(val).length === 0 && Object.getPrototypeOf(val) === Object.prototype;
    } catch (e2) {
      return false;
    }
  };
  var isDate = kindOfTest("Date");
  var isFile = kindOfTest("File");
  var isReactNativeBlob = (value) => {
    return !!(value && typeof value.uri !== "undefined");
  };
  var isReactNative = (formData) => formData && typeof formData.getParts !== "undefined";
  var isBlob = kindOfTest("Blob");
  var isFileList = kindOfTest("FileList");
  var isStream = (val) => isObject2(val) && isFunction(val.pipe);
  function getGlobal() {
    if (typeof globalThis !== "undefined") return globalThis;
    if (typeof self !== "undefined") return self;
    if (typeof window !== "undefined") return window;
    if (typeof global !== "undefined") return global;
    return {};
  }
  var G = getGlobal();
  var FormDataCtor = typeof G.FormData !== "undefined" ? G.FormData : void 0;
  var isFormData = (thing) => {
    if (!thing) return false;
    if (FormDataCtor && thing instanceof FormDataCtor) return true;
    const proto = getPrototypeOf(thing);
    if (!proto || proto === Object.prototype) return false;
    if (!isFunction(thing.append)) return false;
    const kind = kindOf(thing);
    return kind === "formdata" || // detect form-data instance
    kind === "object" && isFunction(thing.toString) && thing.toString() === "[object FormData]";
  };
  var isURLSearchParams = kindOfTest("URLSearchParams");
  var [isReadableStream, isRequest, isResponse, isHeaders] = [
    "ReadableStream",
    "Request",
    "Response",
    "Headers"
  ].map(kindOfTest);
  var trim = (str) => {
    return str.trim ? str.trim() : str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
  };
  function forEach(obj, fn, { allOwnKeys = false } = {}) {
    if (obj === null || typeof obj === "undefined") {
      return;
    }
    let i;
    let l;
    if (typeof obj !== "object") {
      obj = [obj];
    }
    if (isArray(obj)) {
      for (i = 0, l = obj.length; i < l; i++) {
        fn.call(null, obj[i], i, obj);
      }
    } else {
      if (isBuffer(obj)) {
        return;
      }
      const keys = allOwnKeys ? Object.getOwnPropertyNames(obj) : Object.keys(obj);
      const len = keys.length;
      let key;
      for (i = 0; i < len; i++) {
        key = keys[i];
        fn.call(null, obj[key], key, obj);
      }
    }
  }
  function findKey(obj, key) {
    if (isBuffer(obj)) {
      return null;
    }
    key = key.toLowerCase();
    const keys = Object.keys(obj);
    let i = keys.length;
    let _key;
    while (i-- > 0) {
      _key = keys[i];
      if (key === _key.toLowerCase()) {
        return _key;
      }
    }
    return null;
  }
  var _global = (() => {
    if (typeof globalThis !== "undefined") return globalThis;
    return typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : global;
  })();
  var isContextDefined = (context) => !isUndefined(context) && context !== _global;
  function merge(...objs) {
    const { caseless, skipUndefined } = isContextDefined(this) && this || {};
    const result = {};
    const assignValue = (val, key) => {
      if (key === "__proto__" || key === "constructor" || key === "prototype") {
        return;
      }
      const targetKey = caseless && findKey(result, key) || key;
      const existing = hasOwnProperty(result, targetKey) ? result[targetKey] : void 0;
      if (isPlainObject(existing) && isPlainObject(val)) {
        result[targetKey] = merge(existing, val);
      } else if (isPlainObject(val)) {
        result[targetKey] = merge({}, val);
      } else if (isArray(val)) {
        result[targetKey] = val.slice();
      } else if (!skipUndefined || !isUndefined(val)) {
        result[targetKey] = val;
      }
    };
    for (let i = 0, l = objs.length; i < l; i++) {
      objs[i] && forEach(objs[i], assignValue);
    }
    return result;
  }
  var extend = (a, b, thisArg, { allOwnKeys } = {}) => {
    forEach(
      b,
      (val, key) => {
        if (thisArg && isFunction(val)) {
          Object.defineProperty(a, key, {
            // Null-proto descriptor so a polluted Object.prototype.get cannot
            // hijack defineProperty's accessor-vs-data resolution.
            __proto__: null,
            value: bind(val, thisArg),
            writable: true,
            enumerable: true,
            configurable: true
          });
        } else {
          Object.defineProperty(a, key, {
            __proto__: null,
            value: val,
            writable: true,
            enumerable: true,
            configurable: true
          });
        }
      },
      { allOwnKeys }
    );
    return a;
  };
  var stripBOM = (content) => {
    if (content.charCodeAt(0) === 65279) {
      content = content.slice(1);
    }
    return content;
  };
  var inherits = (constructor, superConstructor, props, descriptors) => {
    constructor.prototype = Object.create(superConstructor.prototype, descriptors);
    Object.defineProperty(constructor.prototype, "constructor", {
      __proto__: null,
      value: constructor,
      writable: true,
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(constructor, "super", {
      __proto__: null,
      value: superConstructor.prototype
    });
    props && Object.assign(constructor.prototype, props);
  };
  var toFlatObject = (sourceObj, destObj, filter2, propFilter) => {
    let props;
    let i;
    let prop;
    const merged = {};
    destObj = destObj || {};
    if (sourceObj == null) return destObj;
    do {
      props = Object.getOwnPropertyNames(sourceObj);
      i = props.length;
      while (i-- > 0) {
        prop = props[i];
        if ((!propFilter || propFilter(prop, sourceObj, destObj)) && !merged[prop]) {
          destObj[prop] = sourceObj[prop];
          merged[prop] = true;
        }
      }
      sourceObj = filter2 !== false && getPrototypeOf(sourceObj);
    } while (sourceObj && (!filter2 || filter2(sourceObj, destObj)) && sourceObj !== Object.prototype);
    return destObj;
  };
  var endsWith = (str, searchString, position) => {
    str = String(str);
    if (position === void 0 || position > str.length) {
      position = str.length;
    }
    position -= searchString.length;
    const lastIndex = str.indexOf(searchString, position);
    return lastIndex !== -1 && lastIndex === position;
  };
  var toArray = (thing) => {
    if (!thing) return null;
    if (isArray(thing)) return thing;
    let i = thing.length;
    if (!isNumber(i)) return null;
    const arr = new Array(i);
    while (i-- > 0) {
      arr[i] = thing[i];
    }
    return arr;
  };
  var isTypedArray = /* @__PURE__ */ ((TypedArray) => {
    return (thing) => {
      return TypedArray && thing instanceof TypedArray;
    };
  })(typeof Uint8Array !== "undefined" && getPrototypeOf(Uint8Array));
  var forEachEntry = (obj, fn) => {
    const generator = obj && obj[iterator];
    const _iterator = generator.call(obj);
    let result;
    while ((result = _iterator.next()) && !result.done) {
      const pair = result.value;
      fn.call(obj, pair[0], pair[1]);
    }
  };
  var matchAll = (regExp, str) => {
    let matches;
    const arr = [];
    while ((matches = regExp.exec(str)) !== null) {
      arr.push(matches);
    }
    return arr;
  };
  var isHTMLForm = kindOfTest("HTMLFormElement");
  var toCamelCase = (str) => {
    return str.toLowerCase().replace(/[-_\s]([a-z\d])(\w*)/g, function replacer(m, p1, p2) {
      return p1.toUpperCase() + p2;
    });
  };
  var hasOwnProperty = (({ hasOwnProperty: hasOwnProperty2 }) => (obj, prop) => hasOwnProperty2.call(obj, prop))(Object.prototype);
  var isRegExp = kindOfTest("RegExp");
  var reduceDescriptors = (obj, reducer) => {
    const descriptors = Object.getOwnPropertyDescriptors(obj);
    const reducedDescriptors = {};
    forEach(descriptors, (descriptor, name) => {
      let ret;
      if ((ret = reducer(descriptor, name, obj)) !== false) {
        reducedDescriptors[name] = ret || descriptor;
      }
    });
    Object.defineProperties(obj, reducedDescriptors);
  };
  var freezeMethods = (obj) => {
    reduceDescriptors(obj, (descriptor, name) => {
      if (isFunction(obj) && ["arguments", "caller", "callee"].includes(name)) {
        return false;
      }
      const value = obj[name];
      if (!isFunction(value)) return;
      descriptor.enumerable = false;
      if ("writable" in descriptor) {
        descriptor.writable = false;
        return;
      }
      if (!descriptor.set) {
        descriptor.set = () => {
          throw Error("Can not rewrite read-only method '" + name + "'");
        };
      }
    });
  };
  var toObjectSet = (arrayOrString, delimiter) => {
    const obj = {};
    const define = (arr) => {
      arr.forEach((value) => {
        obj[value] = true;
      });
    };
    isArray(arrayOrString) ? define(arrayOrString) : define(String(arrayOrString).split(delimiter));
    return obj;
  };
  var noop2 = () => {
  };
  var toFiniteNumber = (value, defaultValue) => {
    return value != null && Number.isFinite(value = +value) ? value : defaultValue;
  };
  function isSpecCompliantForm(thing) {
    return !!(thing && isFunction(thing.append) && thing[toStringTag] === "FormData" && thing[iterator]);
  }
  var toJSONObject = (obj) => {
    const visited = /* @__PURE__ */ new WeakSet();
    const visit = (source) => {
      if (isObject2(source)) {
        if (visited.has(source)) {
          return;
        }
        if (isBuffer(source)) {
          return source;
        }
        if (!("toJSON" in source)) {
          visited.add(source);
          const target = isArray(source) ? [] : {};
          forEach(source, (value, key) => {
            const reducedValue = visit(value);
            !isUndefined(reducedValue) && (target[key] = reducedValue);
          });
          visited.delete(source);
          return target;
        }
      }
      return source;
    };
    return visit(obj);
  };
  var isAsyncFn = kindOfTest("AsyncFunction");
  var isThenable = (thing) => thing && (isObject2(thing) || isFunction(thing)) && isFunction(thing.then) && isFunction(thing.catch);
  var _setImmediate = ((setImmediateSupported, postMessageSupported) => {
    if (setImmediateSupported) {
      return setImmediate;
    }
    return postMessageSupported ? ((token, callbacks) => {
      _global.addEventListener(
        "message",
        ({ source, data: data2 }) => {
          if (source === _global && data2 === token) {
            callbacks.length && callbacks.shift()();
          }
        },
        false
      );
      return (cb) => {
        callbacks.push(cb);
        _global.postMessage(token, "*");
      };
    })(`axios@${Math.random()}`, []) : (cb) => setTimeout(cb);
  })(typeof setImmediate === "function", isFunction(_global.postMessage));
  var asap = typeof queueMicrotask !== "undefined" ? queueMicrotask.bind(_global) : typeof process !== "undefined" && process.nextTick || _setImmediate;
  var isIterable = (thing) => thing != null && isFunction(thing[iterator]);
  var utils_default = {
    isArray,
    isArrayBuffer,
    isBuffer,
    isFormData,
    isArrayBufferView,
    isString: isString3,
    isNumber,
    isBoolean,
    isObject: isObject2,
    isPlainObject,
    isEmptyObject,
    isReadableStream,
    isRequest,
    isResponse,
    isHeaders,
    isUndefined,
    isDate,
    isFile,
    isReactNativeBlob,
    isReactNative,
    isBlob,
    isRegExp,
    isFunction,
    isStream,
    isURLSearchParams,
    isTypedArray,
    isFileList,
    forEach,
    merge,
    extend,
    trim,
    stripBOM,
    inherits,
    toFlatObject,
    kindOf,
    kindOfTest,
    endsWith,
    toArray,
    forEachEntry,
    matchAll,
    isHTMLForm,
    hasOwnProperty,
    hasOwnProp: hasOwnProperty,
    // an alias to avoid ESLint no-prototype-builtins detection
    reduceDescriptors,
    freezeMethods,
    toObjectSet,
    toCamelCase,
    noop: noop2,
    toFiniteNumber,
    findKey,
    global: _global,
    isContextDefined,
    isSpecCompliantForm,
    toJSONObject,
    isAsyncFn,
    isThenable,
    setImmediate: _setImmediate,
    asap,
    isIterable
  };

  // node_modules/axios/lib/helpers/parseHeaders.js
  var ignoreDuplicateOf = utils_default.toObjectSet([
    "age",
    "authorization",
    "content-length",
    "content-type",
    "etag",
    "expires",
    "from",
    "host",
    "if-modified-since",
    "if-unmodified-since",
    "last-modified",
    "location",
    "max-forwards",
    "proxy-authorization",
    "referer",
    "retry-after",
    "user-agent"
  ]);
  var parseHeaders_default = (rawHeaders) => {
    const parsed = {};
    let key;
    let val;
    let i;
    rawHeaders && rawHeaders.split("\n").forEach(function parser(line) {
      i = line.indexOf(":");
      key = line.substring(0, i).trim().toLowerCase();
      val = line.substring(i + 1).trim();
      if (!key || parsed[key] && ignoreDuplicateOf[key]) {
        return;
      }
      if (key === "set-cookie") {
        if (parsed[key]) {
          parsed[key].push(val);
        } else {
          parsed[key] = [val];
        }
      } else {
        parsed[key] = parsed[key] ? parsed[key] + ", " + val : val;
      }
    });
    return parsed;
  };

  // node_modules/axios/lib/helpers/sanitizeHeaderValue.js
  function trimSPorHTAB(str) {
    let start = 0;
    let end = str.length;
    while (start < end) {
      const code = str.charCodeAt(start);
      if (code !== 9 && code !== 32) {
        break;
      }
      start += 1;
    }
    while (end > start) {
      const code = str.charCodeAt(end - 1);
      if (code !== 9 && code !== 32) {
        break;
      }
      end -= 1;
    }
    return start === 0 && end === str.length ? str : str.slice(start, end);
  }
  var INVALID_UNICODE_HEADER_VALUE_CHARS = new RegExp("[\\u0000-\\u0008\\u000a-\\u001f\\u007f]+", "g");
  var INVALID_BYTE_STRING_HEADER_VALUE_CHARS = new RegExp("[^\\u0009\\u0020-\\u007e\\u0080-\\u00ff]+", "g");
  function sanitizeValue(value, invalidChars) {
    if (utils_default.isArray(value)) {
      return value.map((item) => sanitizeValue(item, invalidChars));
    }
    return trimSPorHTAB(String(value).replace(invalidChars, ""));
  }
  var sanitizeHeaderValue = (value) => sanitizeValue(value, INVALID_UNICODE_HEADER_VALUE_CHARS);
  var sanitizeByteStringHeaderValue = (value) => sanitizeValue(value, INVALID_BYTE_STRING_HEADER_VALUE_CHARS);
  function toByteStringHeaderObject(headers) {
    const byteStringHeaders = /* @__PURE__ */ Object.create(null);
    utils_default.forEach(headers.toJSON(), (value, header) => {
      byteStringHeaders[header] = sanitizeByteStringHeaderValue(value);
    });
    return byteStringHeaders;
  }

  // node_modules/axios/lib/core/AxiosHeaders.js
  var $internals = /* @__PURE__ */ Symbol("internals");
  function normalizeHeader(header) {
    return header && String(header).trim().toLowerCase();
  }
  function normalizeValue(value) {
    if (value === false || value == null) {
      return value;
    }
    return utils_default.isArray(value) ? value.map(normalizeValue) : sanitizeHeaderValue(String(value));
  }
  function parseTokens(str) {
    const tokens = /* @__PURE__ */ Object.create(null);
    const tokensRE = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
    let match;
    while (match = tokensRE.exec(str)) {
      tokens[match[1]] = match[2];
    }
    return tokens;
  }
  var isValidHeaderName = (str) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(str.trim());
  function matchHeaderValue(context, value, header, filter2, isHeaderNameFilter) {
    if (utils_default.isFunction(filter2)) {
      return filter2.call(this, value, header);
    }
    if (isHeaderNameFilter) {
      value = header;
    }
    if (!utils_default.isString(value)) return;
    if (utils_default.isString(filter2)) {
      return value.indexOf(filter2) !== -1;
    }
    if (utils_default.isRegExp(filter2)) {
      return filter2.test(value);
    }
  }
  function formatHeader(header) {
    return header.trim().toLowerCase().replace(/([a-z\d])(\w*)/g, (w, char, str) => {
      return char.toUpperCase() + str;
    });
  }
  function buildAccessors(obj, header) {
    const accessorName = utils_default.toCamelCase(" " + header);
    ["get", "set", "has"].forEach((methodName) => {
      Object.defineProperty(obj, methodName + accessorName, {
        // Null-proto descriptor so a polluted Object.prototype.get cannot turn
        // this data descriptor into an accessor descriptor on the way in.
        __proto__: null,
        value: function(arg1, arg2, arg3) {
          return this[methodName].call(this, header, arg1, arg2, arg3);
        },
        configurable: true
      });
    });
  }
  var AxiosHeaders = class {
    constructor(headers) {
      headers && this.set(headers);
    }
    set(header, valueOrRewrite, rewrite) {
      const self2 = this;
      function setHeader(_value, _header, _rewrite) {
        const lHeader = normalizeHeader(_header);
        if (!lHeader) {
          throw new Error("header name must be a non-empty string");
        }
        const key = utils_default.findKey(self2, lHeader);
        if (!key || self2[key] === void 0 || _rewrite === true || _rewrite === void 0 && self2[key] !== false) {
          self2[key || _header] = normalizeValue(_value);
        }
      }
      const setHeaders = (headers, _rewrite) => utils_default.forEach(headers, (_value, _header) => setHeader(_value, _header, _rewrite));
      if (utils_default.isPlainObject(header) || header instanceof this.constructor) {
        setHeaders(header, valueOrRewrite);
      } else if (utils_default.isString(header) && (header = header.trim()) && !isValidHeaderName(header)) {
        setHeaders(parseHeaders_default(header), valueOrRewrite);
      } else if (utils_default.isObject(header) && utils_default.isIterable(header)) {
        let obj = {}, dest, key;
        for (const entry of header) {
          if (!utils_default.isArray(entry)) {
            throw TypeError("Object iterator must return a key-value pair");
          }
          obj[key = entry[0]] = (dest = obj[key]) ? utils_default.isArray(dest) ? [...dest, entry[1]] : [dest, entry[1]] : entry[1];
        }
        setHeaders(obj, valueOrRewrite);
      } else {
        header != null && setHeader(valueOrRewrite, header, rewrite);
      }
      return this;
    }
    get(header, parser) {
      header = normalizeHeader(header);
      if (header) {
        const key = utils_default.findKey(this, header);
        if (key) {
          const value = this[key];
          if (!parser) {
            return value;
          }
          if (parser === true) {
            return parseTokens(value);
          }
          if (utils_default.isFunction(parser)) {
            return parser.call(this, value, key);
          }
          if (utils_default.isRegExp(parser)) {
            return parser.exec(value);
          }
          throw new TypeError("parser must be boolean|regexp|function");
        }
      }
    }
    has(header, matcher) {
      header = normalizeHeader(header);
      if (header) {
        const key = utils_default.findKey(this, header);
        return !!(key && this[key] !== void 0 && (!matcher || matchHeaderValue(this, this[key], key, matcher)));
      }
      return false;
    }
    delete(header, matcher) {
      const self2 = this;
      let deleted = false;
      function deleteHeader(_header) {
        _header = normalizeHeader(_header);
        if (_header) {
          const key = utils_default.findKey(self2, _header);
          if (key && (!matcher || matchHeaderValue(self2, self2[key], key, matcher))) {
            delete self2[key];
            deleted = true;
          }
        }
      }
      if (utils_default.isArray(header)) {
        header.forEach(deleteHeader);
      } else {
        deleteHeader(header);
      }
      return deleted;
    }
    clear(matcher) {
      const keys = Object.keys(this);
      let i = keys.length;
      let deleted = false;
      while (i--) {
        const key = keys[i];
        if (!matcher || matchHeaderValue(this, this[key], key, matcher, true)) {
          delete this[key];
          deleted = true;
        }
      }
      return deleted;
    }
    normalize(format) {
      const self2 = this;
      const headers = {};
      utils_default.forEach(this, (value, header) => {
        const key = utils_default.findKey(headers, header);
        if (key) {
          self2[key] = normalizeValue(value);
          delete self2[header];
          return;
        }
        const normalized = format ? formatHeader(header) : String(header).trim();
        if (normalized !== header) {
          delete self2[header];
        }
        self2[normalized] = normalizeValue(value);
        headers[normalized] = true;
      });
      return this;
    }
    concat(...targets) {
      return this.constructor.concat(this, ...targets);
    }
    toJSON(asStrings) {
      const obj = /* @__PURE__ */ Object.create(null);
      utils_default.forEach(this, (value, header) => {
        value != null && value !== false && (obj[header] = asStrings && utils_default.isArray(value) ? value.join(", ") : value);
      });
      return obj;
    }
    [Symbol.iterator]() {
      return Object.entries(this.toJSON())[Symbol.iterator]();
    }
    toString() {
      return Object.entries(this.toJSON()).map(([header, value]) => header + ": " + value).join("\n");
    }
    getSetCookie() {
      return this.get("set-cookie") || [];
    }
    get [Symbol.toStringTag]() {
      return "AxiosHeaders";
    }
    static from(thing) {
      return thing instanceof this ? thing : new this(thing);
    }
    static concat(first, ...targets) {
      const computed = new this(first);
      targets.forEach((target) => computed.set(target));
      return computed;
    }
    static accessor(header) {
      const internals = this[$internals] = this[$internals] = {
        accessors: {}
      };
      const accessors = internals.accessors;
      const prototype2 = this.prototype;
      function defineAccessor(_header) {
        const lHeader = normalizeHeader(_header);
        if (!accessors[lHeader]) {
          buildAccessors(prototype2, _header);
          accessors[lHeader] = true;
        }
      }
      utils_default.isArray(header) ? header.forEach(defineAccessor) : defineAccessor(header);
      return this;
    }
  };
  AxiosHeaders.accessor([
    "Content-Type",
    "Content-Length",
    "Accept",
    "Accept-Encoding",
    "User-Agent",
    "Authorization"
  ]);
  utils_default.reduceDescriptors(AxiosHeaders.prototype, ({ value }, key) => {
    let mapped = key[0].toUpperCase() + key.slice(1);
    return {
      get: () => value,
      set(headerValue) {
        this[mapped] = headerValue;
      }
    };
  });
  utils_default.freezeMethods(AxiosHeaders);
  var AxiosHeaders_default = AxiosHeaders;

  // node_modules/axios/lib/core/AxiosError.js
  var REDACTED = "[REDACTED ****]";
  function hasOwnOrPrototypeToJSON(source) {
    if (utils_default.hasOwnProp(source, "toJSON")) {
      return true;
    }
    let prototype2 = Object.getPrototypeOf(source);
    while (prototype2 && prototype2 !== Object.prototype) {
      if (utils_default.hasOwnProp(prototype2, "toJSON")) {
        return true;
      }
      prototype2 = Object.getPrototypeOf(prototype2);
    }
    return false;
  }
  function redactConfig(config, redactKeys) {
    const lowerKeys = new Set(redactKeys.map((k) => String(k).toLowerCase()));
    const seen = [];
    const visit = (source) => {
      if (source === null || typeof source !== "object") return source;
      if (utils_default.isBuffer(source)) return source;
      if (seen.indexOf(source) !== -1) return void 0;
      if (source instanceof AxiosHeaders_default) {
        source = source.toJSON();
      }
      seen.push(source);
      let result;
      if (utils_default.isArray(source)) {
        result = [];
        source.forEach((v, i) => {
          const reducedValue = visit(v);
          if (!utils_default.isUndefined(reducedValue)) {
            result[i] = reducedValue;
          }
        });
      } else {
        if (!utils_default.isPlainObject(source) && hasOwnOrPrototypeToJSON(source)) {
          seen.pop();
          return source;
        }
        result = /* @__PURE__ */ Object.create(null);
        for (const [key, value] of Object.entries(source)) {
          const reducedValue = lowerKeys.has(key.toLowerCase()) ? REDACTED : visit(value);
          if (!utils_default.isUndefined(reducedValue)) {
            result[key] = reducedValue;
          }
        }
      }
      seen.pop();
      return result;
    };
    return visit(config);
  }
  var AxiosError = class _AxiosError extends Error {
    static from(error, code, config, request, response, customProps) {
      const axiosError = new _AxiosError(error.message, code || error.code, config, request, response);
      axiosError.cause = error;
      axiosError.name = error.name;
      if (error.status != null && axiosError.status == null) {
        axiosError.status = error.status;
      }
      customProps && Object.assign(axiosError, customProps);
      return axiosError;
    }
    /**
     * Create an Error with the specified message, config, error code, request and response.
     *
     * @param {string} message The error message.
     * @param {string} [code] The error code (for example, 'ECONNABORTED').
     * @param {Object} [config] The config.
     * @param {Object} [request] The request.
     * @param {Object} [response] The response.
     *
     * @returns {Error} The created error.
     */
    constructor(message, code, config, request, response) {
      super(message);
      Object.defineProperty(this, "message", {
        // Null-proto descriptor so a polluted Object.prototype.get cannot turn
        // this data descriptor into an accessor descriptor on the way in.
        __proto__: null,
        value: message,
        enumerable: true,
        writable: true,
        configurable: true
      });
      this.name = "AxiosError";
      this.isAxiosError = true;
      code && (this.code = code);
      config && (this.config = config);
      request && (this.request = request);
      if (response) {
        this.response = response;
        this.status = response.status;
      }
    }
    toJSON() {
      const config = this.config;
      const redactKeys = config && utils_default.hasOwnProp(config, "redact") ? config.redact : void 0;
      const serializedConfig = utils_default.isArray(redactKeys) && redactKeys.length > 0 ? redactConfig(config, redactKeys) : utils_default.toJSONObject(config);
      return {
        // Standard
        message: this.message,
        name: this.name,
        // Microsoft
        description: this.description,
        number: this.number,
        // Mozilla
        fileName: this.fileName,
        lineNumber: this.lineNumber,
        columnNumber: this.columnNumber,
        stack: this.stack,
        // Axios
        config: serializedConfig,
        code: this.code,
        status: this.status
      };
    }
  };
  AxiosError.ERR_BAD_OPTION_VALUE = "ERR_BAD_OPTION_VALUE";
  AxiosError.ERR_BAD_OPTION = "ERR_BAD_OPTION";
  AxiosError.ECONNABORTED = "ECONNABORTED";
  AxiosError.ETIMEDOUT = "ETIMEDOUT";
  AxiosError.ECONNREFUSED = "ECONNREFUSED";
  AxiosError.ERR_NETWORK = "ERR_NETWORK";
  AxiosError.ERR_FR_TOO_MANY_REDIRECTS = "ERR_FR_TOO_MANY_REDIRECTS";
  AxiosError.ERR_DEPRECATED = "ERR_DEPRECATED";
  AxiosError.ERR_BAD_RESPONSE = "ERR_BAD_RESPONSE";
  AxiosError.ERR_BAD_REQUEST = "ERR_BAD_REQUEST";
  AxiosError.ERR_CANCELED = "ERR_CANCELED";
  AxiosError.ERR_NOT_SUPPORT = "ERR_NOT_SUPPORT";
  AxiosError.ERR_INVALID_URL = "ERR_INVALID_URL";
  AxiosError.ERR_FORM_DATA_DEPTH_EXCEEDED = "ERR_FORM_DATA_DEPTH_EXCEEDED";
  var AxiosError_default = AxiosError;

  // node_modules/axios/lib/helpers/null.js
  var null_default = null;

  // node_modules/axios/lib/helpers/toFormData.js
  function isVisitable(thing) {
    return utils_default.isPlainObject(thing) || utils_default.isArray(thing);
  }
  function removeBrackets(key) {
    return utils_default.endsWith(key, "[]") ? key.slice(0, -2) : key;
  }
  function renderKey(path, key, dots) {
    if (!path) return key;
    return path.concat(key).map(function each(token, i) {
      token = removeBrackets(token);
      return !dots && i ? "[" + token + "]" : token;
    }).join(dots ? "." : "");
  }
  function isFlatArray(arr) {
    return utils_default.isArray(arr) && !arr.some(isVisitable);
  }
  var predicates = utils_default.toFlatObject(utils_default, {}, null, function filter(prop) {
    return /^is[A-Z]/.test(prop);
  });
  function toFormData(obj, formData, options) {
    if (!utils_default.isObject(obj)) {
      throw new TypeError("target must be an object");
    }
    formData = formData || new (null_default || FormData)();
    options = utils_default.toFlatObject(
      options,
      {
        metaTokens: true,
        dots: false,
        indexes: false
      },
      false,
      function defined(option, source) {
        return !utils_default.isUndefined(source[option]);
      }
    );
    const metaTokens = options.metaTokens;
    const visitor = options.visitor || defaultVisitor;
    const dots = options.dots;
    const indexes = options.indexes;
    const _Blob = options.Blob || typeof Blob !== "undefined" && Blob;
    const maxDepth = options.maxDepth === void 0 ? 100 : options.maxDepth;
    const useBlob = _Blob && utils_default.isSpecCompliantForm(formData);
    if (!utils_default.isFunction(visitor)) {
      throw new TypeError("visitor must be a function");
    }
    function convertValue(value) {
      if (value === null) return "";
      if (utils_default.isDate(value)) {
        return value.toISOString();
      }
      if (utils_default.isBoolean(value)) {
        return value.toString();
      }
      if (!useBlob && utils_default.isBlob(value)) {
        throw new AxiosError_default("Blob is not supported. Use a Buffer instead.");
      }
      if (utils_default.isArrayBuffer(value) || utils_default.isTypedArray(value)) {
        return useBlob && typeof Blob === "function" ? new Blob([value]) : Buffer.from(value);
      }
      return value;
    }
    function defaultVisitor(value, key, path) {
      let arr = value;
      if (utils_default.isReactNative(formData) && utils_default.isReactNativeBlob(value)) {
        formData.append(renderKey(path, key, dots), convertValue(value));
        return false;
      }
      if (value && !path && typeof value === "object") {
        if (utils_default.endsWith(key, "{}")) {
          key = metaTokens ? key : key.slice(0, -2);
          value = JSON.stringify(value);
        } else if (utils_default.isArray(value) && isFlatArray(value) || (utils_default.isFileList(value) || utils_default.endsWith(key, "[]")) && (arr = utils_default.toArray(value))) {
          key = removeBrackets(key);
          arr.forEach(function each(el, index) {
            !(utils_default.isUndefined(el) || el === null) && formData.append(
              // eslint-disable-next-line no-nested-ternary
              indexes === true ? renderKey([key], index, dots) : indexes === null ? key : key + "[]",
              convertValue(el)
            );
          });
          return false;
        }
      }
      if (isVisitable(value)) {
        return true;
      }
      formData.append(renderKey(path, key, dots), convertValue(value));
      return false;
    }
    const stack = [];
    const exposedHelpers = Object.assign(predicates, {
      defaultVisitor,
      convertValue,
      isVisitable
    });
    function build(value, path, depth = 0) {
      if (utils_default.isUndefined(value)) return;
      if (depth > maxDepth) {
        throw new AxiosError_default(
          "Object is too deeply nested (" + depth + " levels). Max depth: " + maxDepth,
          AxiosError_default.ERR_FORM_DATA_DEPTH_EXCEEDED
        );
      }
      if (stack.indexOf(value) !== -1) {
        throw Error("Circular reference detected in " + path.join("."));
      }
      stack.push(value);
      utils_default.forEach(value, function each(el, key) {
        const result = !(utils_default.isUndefined(el) || el === null) && visitor.call(formData, el, utils_default.isString(key) ? key.trim() : key, path, exposedHelpers);
        if (result === true) {
          build(el, path ? path.concat(key) : [key], depth + 1);
        }
      });
      stack.pop();
    }
    if (!utils_default.isObject(obj)) {
      throw new TypeError("data must be an object");
    }
    build(obj);
    return formData;
  }
  var toFormData_default = toFormData;

  // node_modules/axios/lib/helpers/AxiosURLSearchParams.js
  function encode(str) {
    const charMap = {
      "!": "%21",
      "'": "%27",
      "(": "%28",
      ")": "%29",
      "~": "%7E",
      "%20": "+"
    };
    return encodeURIComponent(str).replace(/[!'()~]|%20/g, function replacer(match) {
      return charMap[match];
    });
  }
  function AxiosURLSearchParams(params, options) {
    this._pairs = [];
    params && toFormData_default(params, this, options);
  }
  var prototype = AxiosURLSearchParams.prototype;
  prototype.append = function append(name, value) {
    this._pairs.push([name, value]);
  };
  prototype.toString = function toString2(encoder) {
    const _encode = encoder ? function(value) {
      return encoder.call(this, value, encode);
    } : encode;
    return this._pairs.map(function each(pair) {
      return _encode(pair[0]) + "=" + _encode(pair[1]);
    }, "").join("&");
  };
  var AxiosURLSearchParams_default = AxiosURLSearchParams;

  // node_modules/axios/lib/helpers/buildURL.js
  function encode2(val) {
    return encodeURIComponent(val).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+");
  }
  function buildURL(url, params, options) {
    if (!params) {
      return url;
    }
    const _encode = options && options.encode || encode2;
    const _options = utils_default.isFunction(options) ? {
      serialize: options
    } : options;
    const serializeFn = _options && _options.serialize;
    let serializedParams;
    if (serializeFn) {
      serializedParams = serializeFn(params, _options);
    } else {
      serializedParams = utils_default.isURLSearchParams(params) ? params.toString() : new AxiosURLSearchParams_default(params, _options).toString(_encode);
    }
    if (serializedParams) {
      const hashmarkIndex = url.indexOf("#");
      if (hashmarkIndex !== -1) {
        url = url.slice(0, hashmarkIndex);
      }
      url += (url.indexOf("?") === -1 ? "?" : "&") + serializedParams;
    }
    return url;
  }

  // node_modules/axios/lib/core/InterceptorManager.js
  var InterceptorManager = class {
    constructor() {
      this.handlers = [];
    }
    /**
     * Add a new interceptor to the stack
     *
     * @param {Function} fulfilled The function to handle `then` for a `Promise`
     * @param {Function} rejected The function to handle `reject` for a `Promise`
     * @param {Object} options The options for the interceptor, synchronous and runWhen
     *
     * @return {Number} An ID used to remove interceptor later
     */
    use(fulfilled, rejected, options) {
      this.handlers.push({
        fulfilled,
        rejected,
        synchronous: options ? options.synchronous : false,
        runWhen: options ? options.runWhen : null
      });
      return this.handlers.length - 1;
    }
    /**
     * Remove an interceptor from the stack
     *
     * @param {Number} id The ID that was returned by `use`
     *
     * @returns {void}
     */
    eject(id) {
      if (this.handlers[id]) {
        this.handlers[id] = null;
      }
    }
    /**
     * Clear all interceptors from the stack
     *
     * @returns {void}
     */
    clear() {
      if (this.handlers) {
        this.handlers = [];
      }
    }
    /**
     * Iterate over all the registered interceptors
     *
     * This method is particularly useful for skipping over any
     * interceptors that may have become `null` calling `eject`.
     *
     * @param {Function} fn The function to call for each interceptor
     *
     * @returns {void}
     */
    forEach(fn) {
      utils_default.forEach(this.handlers, function forEachHandler(h) {
        if (h !== null) {
          fn(h);
        }
      });
    }
  };
  var InterceptorManager_default = InterceptorManager;

  // node_modules/axios/lib/defaults/transitional.js
  var transitional_default = {
    silentJSONParsing: true,
    forcedJSONParsing: true,
    clarifyTimeoutError: false,
    legacyInterceptorReqResOrdering: true
  };

  // node_modules/axios/lib/platform/browser/classes/URLSearchParams.js
  var URLSearchParams_default = typeof URLSearchParams !== "undefined" ? URLSearchParams : AxiosURLSearchParams_default;

  // node_modules/axios/lib/platform/browser/classes/FormData.js
  var FormData_default = typeof FormData !== "undefined" ? FormData : null;

  // node_modules/axios/lib/platform/browser/classes/Blob.js
  var Blob_default = typeof Blob !== "undefined" ? Blob : null;

  // node_modules/axios/lib/platform/browser/index.js
  var browser_default = {
    isBrowser: true,
    classes: {
      URLSearchParams: URLSearchParams_default,
      FormData: FormData_default,
      Blob: Blob_default
    },
    protocols: ["http", "https", "file", "blob", "url", "data"]
  };

  // node_modules/axios/lib/platform/common/utils.js
  var utils_exports = {};
  __export(utils_exports, {
    hasBrowserEnv: () => hasBrowserEnv,
    hasStandardBrowserEnv: () => hasStandardBrowserEnv,
    hasStandardBrowserWebWorkerEnv: () => hasStandardBrowserWebWorkerEnv,
    navigator: () => _navigator,
    origin: () => origin
  });
  var hasBrowserEnv = typeof window !== "undefined" && typeof document !== "undefined";
  var _navigator = typeof navigator === "object" && navigator || void 0;
  var hasStandardBrowserEnv = hasBrowserEnv && (!_navigator || ["ReactNative", "NativeScript", "NS"].indexOf(_navigator.product) < 0);
  var hasStandardBrowserWebWorkerEnv = (() => {
    return typeof WorkerGlobalScope !== "undefined" && // eslint-disable-next-line no-undef
    self instanceof WorkerGlobalScope && typeof self.importScripts === "function";
  })();
  var origin = hasBrowserEnv && window.location.href || "http://localhost";

  // node_modules/axios/lib/platform/index.js
  var platform_default = {
    ...utils_exports,
    ...browser_default
  };

  // node_modules/axios/lib/helpers/toURLEncodedForm.js
  function toURLEncodedForm(data2, options) {
    return toFormData_default(data2, new platform_default.classes.URLSearchParams(), {
      visitor: function(value, key, path, helpers) {
        if (platform_default.isNode && utils_default.isBuffer(value)) {
          this.append(key, value.toString("base64"));
          return false;
        }
        return helpers.defaultVisitor.apply(this, arguments);
      },
      ...options
    });
  }

  // node_modules/axios/lib/helpers/formDataToJSON.js
  function parsePropPath(name) {
    return utils_default.matchAll(/\w+|\[(\w*)]/g, name).map((match) => {
      return match[0] === "[]" ? "" : match[1] || match[0];
    });
  }
  function arrayToObject(arr) {
    const obj = {};
    const keys = Object.keys(arr);
    let i;
    const len = keys.length;
    let key;
    for (i = 0; i < len; i++) {
      key = keys[i];
      obj[key] = arr[key];
    }
    return obj;
  }
  function formDataToJSON(formData) {
    function buildPath(path, value, target, index) {
      let name = path[index++];
      if (name === "__proto__") return true;
      const isNumericKey = Number.isFinite(+name);
      const isLast = index >= path.length;
      name = !name && utils_default.isArray(target) ? target.length : name;
      if (isLast) {
        if (utils_default.hasOwnProp(target, name)) {
          target[name] = utils_default.isArray(target[name]) ? target[name].concat(value) : [target[name], value];
        } else {
          target[name] = value;
        }
        return !isNumericKey;
      }
      if (!utils_default.hasOwnProp(target, name) || !utils_default.isObject(target[name])) {
        target[name] = [];
      }
      const result = buildPath(path, value, target[name], index);
      if (result && utils_default.isArray(target[name])) {
        target[name] = arrayToObject(target[name]);
      }
      return !isNumericKey;
    }
    if (utils_default.isFormData(formData) && utils_default.isFunction(formData.entries)) {
      const obj = {};
      utils_default.forEachEntry(formData, (name, value) => {
        buildPath(parsePropPath(name), value, obj, 0);
      });
      return obj;
    }
    return null;
  }
  var formDataToJSON_default = formDataToJSON;

  // node_modules/axios/lib/defaults/index.js
  var own = (obj, key) => obj != null && utils_default.hasOwnProp(obj, key) ? obj[key] : void 0;
  function stringifySafely(rawValue, parser, encoder) {
    if (utils_default.isString(rawValue)) {
      try {
        (parser || JSON.parse)(rawValue);
        return utils_default.trim(rawValue);
      } catch (e2) {
        if (e2.name !== "SyntaxError") {
          throw e2;
        }
      }
    }
    return (encoder || JSON.stringify)(rawValue);
  }
  var defaults = {
    transitional: transitional_default,
    adapter: ["xhr", "http", "fetch"],
    transformRequest: [
      function transformRequest(data2, headers) {
        const contentType = headers.getContentType() || "";
        const hasJSONContentType = contentType.indexOf("application/json") > -1;
        const isObjectPayload = utils_default.isObject(data2);
        if (isObjectPayload && utils_default.isHTMLForm(data2)) {
          data2 = new FormData(data2);
        }
        const isFormData2 = utils_default.isFormData(data2);
        if (isFormData2) {
          return hasJSONContentType ? JSON.stringify(formDataToJSON_default(data2)) : data2;
        }
        if (utils_default.isArrayBuffer(data2) || utils_default.isBuffer(data2) || utils_default.isStream(data2) || utils_default.isFile(data2) || utils_default.isBlob(data2) || utils_default.isReadableStream(data2)) {
          return data2;
        }
        if (utils_default.isArrayBufferView(data2)) {
          return data2.buffer;
        }
        if (utils_default.isURLSearchParams(data2)) {
          headers.setContentType("application/x-www-form-urlencoded;charset=utf-8", false);
          return data2.toString();
        }
        let isFileList2;
        if (isObjectPayload) {
          const formSerializer = own(this, "formSerializer");
          if (contentType.indexOf("application/x-www-form-urlencoded") > -1) {
            return toURLEncodedForm(data2, formSerializer).toString();
          }
          if ((isFileList2 = utils_default.isFileList(data2)) || contentType.indexOf("multipart/form-data") > -1) {
            const env = own(this, "env");
            const _FormData = env && env.FormData;
            return toFormData_default(
              isFileList2 ? { "files[]": data2 } : data2,
              _FormData && new _FormData(),
              formSerializer
            );
          }
        }
        if (isObjectPayload || hasJSONContentType) {
          headers.setContentType("application/json", false);
          return stringifySafely(data2);
        }
        return data2;
      }
    ],
    transformResponse: [
      function transformResponse(data2) {
        const transitional2 = own(this, "transitional") || defaults.transitional;
        const forcedJSONParsing = transitional2 && transitional2.forcedJSONParsing;
        const responseType = own(this, "responseType");
        const JSONRequested = responseType === "json";
        if (utils_default.isResponse(data2) || utils_default.isReadableStream(data2)) {
          return data2;
        }
        if (data2 && utils_default.isString(data2) && (forcedJSONParsing && !responseType || JSONRequested)) {
          const silentJSONParsing = transitional2 && transitional2.silentJSONParsing;
          const strictJSONParsing = !silentJSONParsing && JSONRequested;
          try {
            return JSON.parse(data2, own(this, "parseReviver"));
          } catch (e2) {
            if (strictJSONParsing) {
              if (e2.name === "SyntaxError") {
                throw AxiosError_default.from(e2, AxiosError_default.ERR_BAD_RESPONSE, this, null, own(this, "response"));
              }
              throw e2;
            }
          }
        }
        return data2;
      }
    ],
    /**
     * A timeout in milliseconds to abort a request. If set to 0 (default) a
     * timeout is not created.
     */
    timeout: 0,
    xsrfCookieName: "XSRF-TOKEN",
    xsrfHeaderName: "X-XSRF-TOKEN",
    maxContentLength: -1,
    maxBodyLength: -1,
    env: {
      FormData: platform_default.classes.FormData,
      Blob: platform_default.classes.Blob
    },
    validateStatus: function validateStatus(status) {
      return status >= 200 && status < 300;
    },
    headers: {
      common: {
        Accept: "application/json, text/plain, */*",
        "Content-Type": void 0
      }
    }
  };
  utils_default.forEach(["delete", "get", "head", "post", "put", "patch", "query"], (method) => {
    defaults.headers[method] = {};
  });
  var defaults_default = defaults;

  // node_modules/axios/lib/core/transformData.js
  function transformData(fns, response) {
    const config = this || defaults_default;
    const context = response || config;
    const headers = AxiosHeaders_default.from(context.headers);
    let data2 = context.data;
    utils_default.forEach(fns, function transform(fn) {
      data2 = fn.call(config, data2, headers.normalize(), response ? response.status : void 0);
    });
    headers.normalize();
    return data2;
  }

  // node_modules/axios/lib/cancel/isCancel.js
  function isCancel(value) {
    return !!(value && value.__CANCEL__);
  }

  // node_modules/axios/lib/cancel/CanceledError.js
  var CanceledError = class extends AxiosError_default {
    /**
     * A `CanceledError` is an object that is thrown when an operation is canceled.
     *
     * @param {string=} message The message.
     * @param {Object=} config The config.
     * @param {Object=} request The request.
     *
     * @returns {CanceledError} The created error.
     */
    constructor(message, config, request) {
      super(message == null ? "canceled" : message, AxiosError_default.ERR_CANCELED, config, request);
      this.name = "CanceledError";
      this.__CANCEL__ = true;
    }
  };
  var CanceledError_default = CanceledError;

  // node_modules/axios/lib/core/settle.js
  function settle(resolve, reject, response) {
    const validateStatus2 = response.config.validateStatus;
    if (!response.status || !validateStatus2 || validateStatus2(response.status)) {
      resolve(response);
    } else {
      reject(new AxiosError_default(
        "Request failed with status code " + response.status,
        response.status >= 400 && response.status < 500 ? AxiosError_default.ERR_BAD_REQUEST : AxiosError_default.ERR_BAD_RESPONSE,
        response.config,
        response.request,
        response
      ));
    }
  }

  // node_modules/axios/lib/helpers/parseProtocol.js
  function parseProtocol(url) {
    const match = /^([-+\w]{1,25}):(?:\/\/)?/.exec(url);
    return match && match[1] || "";
  }

  // node_modules/axios/lib/helpers/speedometer.js
  function speedometer(samplesCount, min) {
    samplesCount = samplesCount || 10;
    const bytes = new Array(samplesCount);
    const timestamps = new Array(samplesCount);
    let head = 0;
    let tail = 0;
    let firstSampleTS;
    min = min !== void 0 ? min : 1e3;
    return function push(chunkLength) {
      const now = Date.now();
      const startedAt = timestamps[tail];
      if (!firstSampleTS) {
        firstSampleTS = now;
      }
      bytes[head] = chunkLength;
      timestamps[head] = now;
      let i = tail;
      let bytesCount = 0;
      while (i !== head) {
        bytesCount += bytes[i++];
        i = i % samplesCount;
      }
      head = (head + 1) % samplesCount;
      if (head === tail) {
        tail = (tail + 1) % samplesCount;
      }
      if (now - firstSampleTS < min) {
        return;
      }
      const passed = startedAt && now - startedAt;
      return passed ? Math.round(bytesCount * 1e3 / passed) : void 0;
    };
  }
  var speedometer_default = speedometer;

  // node_modules/axios/lib/helpers/throttle.js
  function throttle(fn, freq) {
    let timestamp = 0;
    let threshold = 1e3 / freq;
    let lastArgs;
    let timer;
    const invoke = (args, now = Date.now()) => {
      timestamp = now;
      lastArgs = null;
      if (timer) {
        clearTimeout(timer);
        timer = null;
      }
      fn(...args);
    };
    const throttled = (...args) => {
      const now = Date.now();
      const passed = now - timestamp;
      if (passed >= threshold) {
        invoke(args, now);
      } else {
        lastArgs = args;
        if (!timer) {
          timer = setTimeout(() => {
            timer = null;
            invoke(lastArgs);
          }, threshold - passed);
        }
      }
    };
    const flush = () => lastArgs && invoke(lastArgs);
    return [throttled, flush];
  }
  var throttle_default = throttle;

  // node_modules/axios/lib/helpers/progressEventReducer.js
  var progressEventReducer = (listener, isDownloadStream, freq = 3) => {
    let bytesNotified = 0;
    const _speedometer = speedometer_default(50, 250);
    return throttle_default((e2) => {
      if (!e2 || typeof e2.loaded !== "number") {
        return;
      }
      const rawLoaded = e2.loaded;
      const total = e2.lengthComputable ? e2.total : void 0;
      const loaded = total != null ? Math.min(rawLoaded, total) : rawLoaded;
      const progressBytes = Math.max(0, loaded - bytesNotified);
      const rate = _speedometer(progressBytes);
      bytesNotified = Math.max(bytesNotified, loaded);
      const data2 = {
        loaded,
        total,
        progress: total ? loaded / total : void 0,
        bytes: progressBytes,
        rate: rate ? rate : void 0,
        estimated: rate && total ? (total - loaded) / rate : void 0,
        event: e2,
        lengthComputable: total != null,
        [isDownloadStream ? "download" : "upload"]: true
      };
      listener(data2);
    }, freq);
  };
  var progressEventDecorator = (total, throttled) => {
    const lengthComputable = total != null;
    return [
      (loaded) => throttled[0]({
        lengthComputable,
        total,
        loaded
      }),
      throttled[1]
    ];
  };
  var asyncDecorator = (fn) => (...args) => utils_default.asap(() => fn(...args));

  // node_modules/axios/lib/helpers/isURLSameOrigin.js
  var isURLSameOrigin_default = platform_default.hasStandardBrowserEnv ? /* @__PURE__ */ ((origin2, isMSIE) => (url) => {
    url = new URL(url, platform_default.origin);
    return origin2.protocol === url.protocol && origin2.host === url.host && (isMSIE || origin2.port === url.port);
  })(
    new URL(platform_default.origin),
    platform_default.navigator && /(msie|trident)/i.test(platform_default.navigator.userAgent)
  ) : () => true;

  // node_modules/axios/lib/helpers/cookies.js
  var cookies_default = platform_default.hasStandardBrowserEnv ? (
    // Standard browser envs support document.cookie
    {
      write(name, value, expires, path, domain, secure, sameSite) {
        if (typeof document === "undefined") return;
        const cookie = [`${name}=${encodeURIComponent(value)}`];
        if (utils_default.isNumber(expires)) {
          cookie.push(`expires=${new Date(expires).toUTCString()}`);
        }
        if (utils_default.isString(path)) {
          cookie.push(`path=${path}`);
        }
        if (utils_default.isString(domain)) {
          cookie.push(`domain=${domain}`);
        }
        if (secure === true) {
          cookie.push("secure");
        }
        if (utils_default.isString(sameSite)) {
          cookie.push(`SameSite=${sameSite}`);
        }
        document.cookie = cookie.join("; ");
      },
      read(name) {
        if (typeof document === "undefined") return null;
        const cookies = document.cookie.split(";");
        for (let i = 0; i < cookies.length; i++) {
          const cookie = cookies[i].replace(/^\s+/, "");
          const eq = cookie.indexOf("=");
          if (eq !== -1 && cookie.slice(0, eq) === name) {
            return decodeURIComponent(cookie.slice(eq + 1));
          }
        }
        return null;
      },
      remove(name) {
        this.write(name, "", Date.now() - 864e5, "/");
      }
    }
  ) : (
    // Non-standard browser env (web workers, react-native) lack needed support.
    {
      write() {
      },
      read() {
        return null;
      },
      remove() {
      }
    }
  );

  // node_modules/axios/lib/helpers/isAbsoluteURL.js
  function isAbsoluteURL(url) {
    if (typeof url !== "string") {
      return false;
    }
    return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url);
  }

  // node_modules/axios/lib/helpers/combineURLs.js
  function combineURLs(baseURL, relativeURL) {
    return relativeURL ? baseURL.replace(/\/?\/$/, "") + "/" + relativeURL.replace(/^\/+/, "") : baseURL;
  }

  // node_modules/axios/lib/core/buildFullPath.js
  function buildFullPath(baseURL, requestedURL, allowAbsoluteUrls) {
    let isRelativeUrl = !isAbsoluteURL(requestedURL);
    if (baseURL && (isRelativeUrl || allowAbsoluteUrls === false)) {
      return combineURLs(baseURL, requestedURL);
    }
    return requestedURL;
  }

  // node_modules/axios/lib/core/mergeConfig.js
  var headersToObject = (thing) => thing instanceof AxiosHeaders_default ? { ...thing } : thing;
  function mergeConfig(config1, config2) {
    config2 = config2 || {};
    const config = /* @__PURE__ */ Object.create(null);
    Object.defineProperty(config, "hasOwnProperty", {
      // Null-proto descriptor so a polluted Object.prototype.get cannot turn
      // this data descriptor into an accessor descriptor on the way in.
      __proto__: null,
      value: Object.prototype.hasOwnProperty,
      enumerable: false,
      writable: true,
      configurable: true
    });
    function getMergedValue(target, source, prop, caseless) {
      if (utils_default.isPlainObject(target) && utils_default.isPlainObject(source)) {
        return utils_default.merge.call({ caseless }, target, source);
      } else if (utils_default.isPlainObject(source)) {
        return utils_default.merge({}, source);
      } else if (utils_default.isArray(source)) {
        return source.slice();
      }
      return source;
    }
    function mergeDeepProperties(a, b, prop, caseless) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(a, b, prop, caseless);
      } else if (!utils_default.isUndefined(a)) {
        return getMergedValue(void 0, a, prop, caseless);
      }
    }
    function valueFromConfig2(a, b) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(void 0, b);
      }
    }
    function defaultToConfig2(a, b) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(void 0, b);
      } else if (!utils_default.isUndefined(a)) {
        return getMergedValue(void 0, a);
      }
    }
    function mergeDirectKeys(a, b, prop) {
      if (utils_default.hasOwnProp(config2, prop)) {
        return getMergedValue(a, b);
      } else if (utils_default.hasOwnProp(config1, prop)) {
        return getMergedValue(void 0, a);
      }
    }
    const mergeMap = {
      url: valueFromConfig2,
      method: valueFromConfig2,
      data: valueFromConfig2,
      baseURL: defaultToConfig2,
      transformRequest: defaultToConfig2,
      transformResponse: defaultToConfig2,
      paramsSerializer: defaultToConfig2,
      timeout: defaultToConfig2,
      timeoutMessage: defaultToConfig2,
      withCredentials: defaultToConfig2,
      withXSRFToken: defaultToConfig2,
      adapter: defaultToConfig2,
      responseType: defaultToConfig2,
      xsrfCookieName: defaultToConfig2,
      xsrfHeaderName: defaultToConfig2,
      onUploadProgress: defaultToConfig2,
      onDownloadProgress: defaultToConfig2,
      decompress: defaultToConfig2,
      maxContentLength: defaultToConfig2,
      maxBodyLength: defaultToConfig2,
      beforeRedirect: defaultToConfig2,
      transport: defaultToConfig2,
      httpAgent: defaultToConfig2,
      httpsAgent: defaultToConfig2,
      cancelToken: defaultToConfig2,
      socketPath: defaultToConfig2,
      allowedSocketPaths: defaultToConfig2,
      responseEncoding: defaultToConfig2,
      validateStatus: mergeDirectKeys,
      headers: (a, b, prop) => mergeDeepProperties(headersToObject(a), headersToObject(b), prop, true)
    };
    utils_default.forEach(Object.keys({ ...config1, ...config2 }), function computeConfigValue(prop) {
      if (prop === "__proto__" || prop === "constructor" || prop === "prototype") return;
      const merge2 = utils_default.hasOwnProp(mergeMap, prop) ? mergeMap[prop] : mergeDeepProperties;
      const a = utils_default.hasOwnProp(config1, prop) ? config1[prop] : void 0;
      const b = utils_default.hasOwnProp(config2, prop) ? config2[prop] : void 0;
      const configValue = merge2(a, b, prop);
      utils_default.isUndefined(configValue) && merge2 !== mergeDirectKeys || (config[prop] = configValue);
    });
    return config;
  }

  // node_modules/axios/lib/helpers/resolveConfig.js
  var FORM_DATA_CONTENT_HEADERS = ["content-type", "content-length"];
  function setFormDataHeaders(headers, formHeaders, policy) {
    if (policy !== "content-only") {
      headers.set(formHeaders);
      return;
    }
    Object.entries(formHeaders).forEach(([key, val]) => {
      if (FORM_DATA_CONTENT_HEADERS.includes(key.toLowerCase())) {
        headers.set(key, val);
      }
    });
  }
  var encodeUTF8 = (str) => encodeURIComponent(str).replace(
    /%([0-9A-F]{2})/gi,
    (_, hex) => String.fromCharCode(parseInt(hex, 16))
  );
  var resolveConfig_default = (config) => {
    const newConfig = mergeConfig({}, config);
    const own2 = (key) => utils_default.hasOwnProp(newConfig, key) ? newConfig[key] : void 0;
    const data2 = own2("data");
    let withXSRFToken = own2("withXSRFToken");
    const xsrfHeaderName = own2("xsrfHeaderName");
    const xsrfCookieName = own2("xsrfCookieName");
    let headers = own2("headers");
    const auth = own2("auth");
    const baseURL = own2("baseURL");
    const allowAbsoluteUrls = own2("allowAbsoluteUrls");
    const url = own2("url");
    newConfig.headers = headers = AxiosHeaders_default.from(headers);
    newConfig.url = buildURL(
      buildFullPath(baseURL, url, allowAbsoluteUrls),
      config.params,
      config.paramsSerializer
    );
    if (auth) {
      headers.set(
        "Authorization",
        "Basic " + btoa((auth.username || "") + ":" + (auth.password ? encodeUTF8(auth.password) : ""))
      );
    }
    if (utils_default.isFormData(data2)) {
      if (platform_default.hasStandardBrowserEnv || platform_default.hasStandardBrowserWebWorkerEnv) {
        headers.setContentType(void 0);
      } else if (utils_default.isFunction(data2.getHeaders)) {
        setFormDataHeaders(headers, data2.getHeaders(), own2("formDataHeaderPolicy"));
      }
    }
    if (platform_default.hasStandardBrowserEnv) {
      if (utils_default.isFunction(withXSRFToken)) {
        withXSRFToken = withXSRFToken(newConfig);
      }
      const shouldSendXSRF = withXSRFToken === true || withXSRFToken == null && isURLSameOrigin_default(newConfig.url);
      if (shouldSendXSRF) {
        const xsrfValue = xsrfHeaderName && xsrfCookieName && cookies_default.read(xsrfCookieName);
        if (xsrfValue) {
          headers.set(xsrfHeaderName, xsrfValue);
        }
      }
    }
    return newConfig;
  };

  // node_modules/axios/lib/adapters/xhr.js
  var isXHRAdapterSupported = typeof XMLHttpRequest !== "undefined";
  var xhr_default = isXHRAdapterSupported && function(config) {
    return new Promise(function dispatchXhrRequest(resolve, reject) {
      const _config = resolveConfig_default(config);
      let requestData = _config.data;
      const requestHeaders = AxiosHeaders_default.from(_config.headers).normalize();
      let { responseType, onUploadProgress, onDownloadProgress } = _config;
      let onCanceled;
      let uploadThrottled, downloadThrottled;
      let flushUpload, flushDownload;
      function done() {
        flushUpload && flushUpload();
        flushDownload && flushDownload();
        _config.cancelToken && _config.cancelToken.unsubscribe(onCanceled);
        _config.signal && _config.signal.removeEventListener("abort", onCanceled);
      }
      let request = new XMLHttpRequest();
      request.open(_config.method.toUpperCase(), _config.url, true);
      request.timeout = _config.timeout;
      function onloadend() {
        if (!request) {
          return;
        }
        const responseHeaders = AxiosHeaders_default.from(
          "getAllResponseHeaders" in request && request.getAllResponseHeaders()
        );
        const responseData = !responseType || responseType === "text" || responseType === "json" ? request.responseText : request.response;
        const response = {
          data: responseData,
          status: request.status,
          statusText: request.statusText,
          headers: responseHeaders,
          config,
          request
        };
        settle(
          function _resolve(value) {
            resolve(value);
            done();
          },
          function _reject(err) {
            reject(err);
            done();
          },
          response
        );
        request = null;
      }
      if ("onloadend" in request) {
        request.onloadend = onloadend;
      } else {
        request.onreadystatechange = function handleLoad() {
          if (!request || request.readyState !== 4) {
            return;
          }
          if (request.status === 0 && !(request.responseURL && request.responseURL.startsWith("file:"))) {
            return;
          }
          setTimeout(onloadend);
        };
      }
      request.onabort = function handleAbort() {
        if (!request) {
          return;
        }
        reject(new AxiosError_default("Request aborted", AxiosError_default.ECONNABORTED, config, request));
        done();
        request = null;
      };
      request.onerror = function handleError(event) {
        const msg = event && event.message ? event.message : "Network Error";
        const err = new AxiosError_default(msg, AxiosError_default.ERR_NETWORK, config, request);
        err.event = event || null;
        reject(err);
        done();
        request = null;
      };
      request.ontimeout = function handleTimeout() {
        let timeoutErrorMessage = _config.timeout ? "timeout of " + _config.timeout + "ms exceeded" : "timeout exceeded";
        const transitional2 = _config.transitional || transitional_default;
        if (_config.timeoutErrorMessage) {
          timeoutErrorMessage = _config.timeoutErrorMessage;
        }
        reject(
          new AxiosError_default(
            timeoutErrorMessage,
            transitional2.clarifyTimeoutError ? AxiosError_default.ETIMEDOUT : AxiosError_default.ECONNABORTED,
            config,
            request
          )
        );
        done();
        request = null;
      };
      requestData === void 0 && requestHeaders.setContentType(null);
      if ("setRequestHeader" in request) {
        utils_default.forEach(toByteStringHeaderObject(requestHeaders), function setRequestHeader(val, key) {
          request.setRequestHeader(key, val);
        });
      }
      if (!utils_default.isUndefined(_config.withCredentials)) {
        request.withCredentials = !!_config.withCredentials;
      }
      if (responseType && responseType !== "json") {
        request.responseType = _config.responseType;
      }
      if (onDownloadProgress) {
        [downloadThrottled, flushDownload] = progressEventReducer(onDownloadProgress, true);
        request.addEventListener("progress", downloadThrottled);
      }
      if (onUploadProgress && request.upload) {
        [uploadThrottled, flushUpload] = progressEventReducer(onUploadProgress);
        request.upload.addEventListener("progress", uploadThrottled);
        request.upload.addEventListener("loadend", flushUpload);
      }
      if (_config.cancelToken || _config.signal) {
        onCanceled = (cancel) => {
          if (!request) {
            return;
          }
          reject(!cancel || cancel.type ? new CanceledError_default(null, config, request) : cancel);
          request.abort();
          done();
          request = null;
        };
        _config.cancelToken && _config.cancelToken.subscribe(onCanceled);
        if (_config.signal) {
          _config.signal.aborted ? onCanceled() : _config.signal.addEventListener("abort", onCanceled);
        }
      }
      const protocol = parseProtocol(_config.url);
      if (protocol && !platform_default.protocols.includes(protocol)) {
        reject(
          new AxiosError_default(
            "Unsupported protocol " + protocol + ":",
            AxiosError_default.ERR_BAD_REQUEST,
            config
          )
        );
        return;
      }
      request.send(requestData || null);
    });
  };

  // node_modules/axios/lib/helpers/composeSignals.js
  var composeSignals = (signals, timeout) => {
    signals = signals ? signals.filter(Boolean) : [];
    if (!timeout && !signals.length) {
      return;
    }
    const controller = new AbortController();
    let aborted = false;
    const onabort = function(reason) {
      if (!aborted) {
        aborted = true;
        unsubscribe();
        const err = reason instanceof Error ? reason : this.reason;
        controller.abort(
          err instanceof AxiosError_default ? err : new CanceledError_default(err instanceof Error ? err.message : err)
        );
      }
    };
    let timer = timeout && setTimeout(() => {
      timer = null;
      onabort(new AxiosError_default(`timeout of ${timeout}ms exceeded`, AxiosError_default.ETIMEDOUT));
    }, timeout);
    const unsubscribe = () => {
      if (!signals) {
        return;
      }
      timer && clearTimeout(timer);
      timer = null;
      signals.forEach((signal2) => {
        signal2.unsubscribe ? signal2.unsubscribe(onabort) : signal2.removeEventListener("abort", onabort);
      });
      signals = null;
    };
    signals.forEach((signal2) => signal2.addEventListener("abort", onabort));
    const { signal } = controller;
    signal.unsubscribe = () => utils_default.asap(unsubscribe);
    return signal;
  };
  var composeSignals_default = composeSignals;

  // node_modules/axios/lib/helpers/trackStream.js
  var streamChunk = function* (chunk, chunkSize) {
    let len = chunk.byteLength;
    if (!chunkSize || len < chunkSize) {
      yield chunk;
      return;
    }
    let pos = 0;
    let end;
    while (pos < len) {
      end = pos + chunkSize;
      yield chunk.slice(pos, end);
      pos = end;
    }
  };
  var readBytes = async function* (iterable, chunkSize) {
    for await (const chunk of readStream(iterable)) {
      yield* streamChunk(chunk, chunkSize);
    }
  };
  var readStream = async function* (stream) {
    if (stream[Symbol.asyncIterator]) {
      yield* stream;
      return;
    }
    const reader = stream.getReader();
    try {
      for (; ; ) {
        const { done, value } = await reader.read();
        if (done) {
          break;
        }
        yield value;
      }
    } finally {
      await reader.cancel();
    }
  };
  var trackStream = (stream, chunkSize, onProgress, onFinish) => {
    const iterator2 = readBytes(stream, chunkSize);
    let bytes = 0;
    let done;
    let _onFinish = (e2) => {
      if (!done) {
        done = true;
        onFinish && onFinish(e2);
      }
    };
    return new ReadableStream(
      {
        async pull(controller) {
          try {
            const { done: done2, value } = await iterator2.next();
            if (done2) {
              _onFinish();
              controller.close();
              return;
            }
            let len = value.byteLength;
            if (onProgress) {
              let loadedBytes = bytes += len;
              onProgress(loadedBytes);
            }
            controller.enqueue(new Uint8Array(value));
          } catch (err) {
            _onFinish(err);
            throw err;
          }
        },
        cancel(reason) {
          _onFinish(reason);
          return iterator2.return();
        }
      },
      {
        highWaterMark: 2
      }
    );
  };

  // node_modules/axios/lib/helpers/estimateDataURLDecodedBytes.js
  function estimateDataURLDecodedBytes(url) {
    if (!url || typeof url !== "string") return 0;
    if (!url.startsWith("data:")) return 0;
    const comma = url.indexOf(",");
    if (comma < 0) return 0;
    const meta = url.slice(5, comma);
    const body = url.slice(comma + 1);
    const isBase64 = /;base64/i.test(meta);
    if (isBase64) {
      let effectiveLen = body.length;
      const len = body.length;
      for (let i = 0; i < len; i++) {
        if (body.charCodeAt(i) === 37 && i + 2 < len) {
          const a = body.charCodeAt(i + 1);
          const b = body.charCodeAt(i + 2);
          const isHex = (a >= 48 && a <= 57 || a >= 65 && a <= 70 || a >= 97 && a <= 102) && (b >= 48 && b <= 57 || b >= 65 && b <= 70 || b >= 97 && b <= 102);
          if (isHex) {
            effectiveLen -= 2;
            i += 2;
          }
        }
      }
      let pad = 0;
      let idx = len - 1;
      const tailIsPct3D = (j) => j >= 2 && body.charCodeAt(j - 2) === 37 && // '%'
      body.charCodeAt(j - 1) === 51 && // '3'
      (body.charCodeAt(j) === 68 || body.charCodeAt(j) === 100);
      if (idx >= 0) {
        if (body.charCodeAt(idx) === 61) {
          pad++;
          idx--;
        } else if (tailIsPct3D(idx)) {
          pad++;
          idx -= 3;
        }
      }
      if (pad === 1 && idx >= 0) {
        if (body.charCodeAt(idx) === 61) {
          pad++;
        } else if (tailIsPct3D(idx)) {
          pad++;
        }
      }
      const groups = Math.floor(effectiveLen / 4);
      const bytes2 = groups * 3 - (pad || 0);
      return bytes2 > 0 ? bytes2 : 0;
    }
    if (typeof Buffer !== "undefined" && typeof Buffer.byteLength === "function") {
      return Buffer.byteLength(body, "utf8");
    }
    let bytes = 0;
    for (let i = 0, len = body.length; i < len; i++) {
      const c = body.charCodeAt(i);
      if (c < 128) {
        bytes += 1;
      } else if (c < 2048) {
        bytes += 2;
      } else if (c >= 55296 && c <= 56319 && i + 1 < len) {
        const next = body.charCodeAt(i + 1);
        if (next >= 56320 && next <= 57343) {
          bytes += 4;
          i++;
        } else {
          bytes += 3;
        }
      } else {
        bytes += 3;
      }
    }
    return bytes;
  }

  // node_modules/axios/lib/env/data.js
  var VERSION = "1.16.1";

  // node_modules/axios/lib/adapters/fetch.js
  var DEFAULT_CHUNK_SIZE = 64 * 1024;
  var { isFunction: isFunction2 } = utils_default;
  var test = (fn, ...args) => {
    try {
      return !!fn(...args);
    } catch (e2) {
      return false;
    }
  };
  var factory = (env) => {
    const globalObject = utils_default.global !== void 0 && utils_default.global !== null ? utils_default.global : globalThis;
    const { ReadableStream: ReadableStream2, TextEncoder: TextEncoder2 } = globalObject;
    env = utils_default.merge.call(
      {
        skipUndefined: true
      },
      {
        Request: globalObject.Request,
        Response: globalObject.Response
      },
      env
    );
    const { fetch: envFetch, Request: Request2, Response: Response2 } = env;
    const isFetchSupported = envFetch ? isFunction2(envFetch) : typeof fetch === "function";
    const isRequestSupported = isFunction2(Request2);
    const isResponseSupported = isFunction2(Response2);
    if (!isFetchSupported) {
      return false;
    }
    const isReadableStreamSupported = isFetchSupported && isFunction2(ReadableStream2);
    const encodeText = isFetchSupported && (typeof TextEncoder2 === "function" ? /* @__PURE__ */ ((encoder) => (str) => encoder.encode(str))(new TextEncoder2()) : async (str) => new Uint8Array(await new Request2(str).arrayBuffer()));
    const supportsRequestStream = isRequestSupported && isReadableStreamSupported && test(() => {
      let duplexAccessed = false;
      const request = new Request2(platform_default.origin, {
        body: new ReadableStream2(),
        method: "POST",
        get duplex() {
          duplexAccessed = true;
          return "half";
        }
      });
      const hasContentType = request.headers.has("Content-Type");
      if (request.body != null) {
        request.body.cancel();
      }
      return duplexAccessed && !hasContentType;
    });
    const supportsResponseStream = isResponseSupported && isReadableStreamSupported && test(() => utils_default.isReadableStream(new Response2("").body));
    const resolvers = {
      stream: supportsResponseStream && ((res) => res.body)
    };
    isFetchSupported && (() => {
      ["text", "arrayBuffer", "blob", "formData", "stream"].forEach((type) => {
        !resolvers[type] && (resolvers[type] = (res, config) => {
          let method = res && res[type];
          if (method) {
            return method.call(res);
          }
          throw new AxiosError_default(
            `Response type '${type}' is not supported`,
            AxiosError_default.ERR_NOT_SUPPORT,
            config
          );
        });
      });
    })();
    const getBodyLength = async (body) => {
      if (body == null) {
        return 0;
      }
      if (utils_default.isBlob(body)) {
        return body.size;
      }
      if (utils_default.isSpecCompliantForm(body)) {
        const _request = new Request2(platform_default.origin, {
          method: "POST",
          body
        });
        return (await _request.arrayBuffer()).byteLength;
      }
      if (utils_default.isArrayBufferView(body) || utils_default.isArrayBuffer(body)) {
        return body.byteLength;
      }
      if (utils_default.isURLSearchParams(body)) {
        body = body + "";
      }
      if (utils_default.isString(body)) {
        return (await encodeText(body)).byteLength;
      }
    };
    const resolveBodyLength = async (headers, body) => {
      const length = utils_default.toFiniteNumber(headers.getContentLength());
      return length == null ? getBodyLength(body) : length;
    };
    return async (config) => {
      let {
        url,
        method,
        data: data2,
        signal,
        cancelToken,
        timeout,
        onDownloadProgress,
        onUploadProgress,
        responseType,
        headers,
        withCredentials = "same-origin",
        fetchOptions,
        maxContentLength,
        maxBodyLength
      } = resolveConfig_default(config);
      const hasMaxContentLength = utils_default.isNumber(maxContentLength) && maxContentLength > -1;
      const hasMaxBodyLength = utils_default.isNumber(maxBodyLength) && maxBodyLength > -1;
      let _fetch = envFetch || fetch;
      responseType = responseType ? (responseType + "").toLowerCase() : "text";
      let composedSignal = composeSignals_default(
        [signal, cancelToken && cancelToken.toAbortSignal()],
        timeout
      );
      let request = null;
      const unsubscribe = composedSignal && composedSignal.unsubscribe && (() => {
        composedSignal.unsubscribe();
      });
      let requestContentLength;
      try {
        if (hasMaxContentLength && typeof url === "string" && url.startsWith("data:")) {
          const estimated = estimateDataURLDecodedBytes(url);
          if (estimated > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        if (hasMaxBodyLength && method !== "get" && method !== "head") {
          const outboundLength = await resolveBodyLength(headers, data2);
          if (typeof outboundLength === "number" && isFinite(outboundLength) && outboundLength > maxBodyLength) {
            throw new AxiosError_default(
              "Request body larger than maxBodyLength limit",
              AxiosError_default.ERR_BAD_REQUEST,
              config,
              request
            );
          }
        }
        if (onUploadProgress && supportsRequestStream && method !== "get" && method !== "head" && (requestContentLength = await resolveBodyLength(headers, data2)) !== 0) {
          let _request = new Request2(url, {
            method: "POST",
            body: data2,
            duplex: "half"
          });
          let contentTypeHeader;
          if (utils_default.isFormData(data2) && (contentTypeHeader = _request.headers.get("content-type"))) {
            headers.setContentType(contentTypeHeader);
          }
          if (_request.body) {
            const [onProgress, flush] = progressEventDecorator(
              requestContentLength,
              progressEventReducer(asyncDecorator(onUploadProgress))
            );
            data2 = trackStream(_request.body, DEFAULT_CHUNK_SIZE, onProgress, flush);
          }
        }
        if (!utils_default.isString(withCredentials)) {
          withCredentials = withCredentials ? "include" : "omit";
        }
        const isCredentialsSupported = isRequestSupported && "credentials" in Request2.prototype;
        if (utils_default.isFormData(data2)) {
          const contentType = headers.getContentType();
          if (contentType && /^multipart\/form-data/i.test(contentType) && !/boundary=/i.test(contentType)) {
            headers.delete("content-type");
          }
        }
        headers.set("User-Agent", "axios/" + VERSION, false);
        const resolvedOptions = {
          ...fetchOptions,
          signal: composedSignal,
          method: method.toUpperCase(),
          headers: toByteStringHeaderObject(headers.normalize()),
          body: data2,
          duplex: "half",
          credentials: isCredentialsSupported ? withCredentials : void 0
        };
        request = isRequestSupported && new Request2(url, resolvedOptions);
        let response = await (isRequestSupported ? _fetch(request, fetchOptions) : _fetch(url, resolvedOptions));
        if (hasMaxContentLength) {
          const declaredLength = utils_default.toFiniteNumber(response.headers.get("content-length"));
          if (declaredLength != null && declaredLength > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        const isStreamResponse = supportsResponseStream && (responseType === "stream" || responseType === "response");
        if (supportsResponseStream && response.body && (onDownloadProgress || hasMaxContentLength || isStreamResponse && unsubscribe)) {
          const options = {};
          ["status", "statusText", "headers"].forEach((prop) => {
            options[prop] = response[prop];
          });
          const responseContentLength = utils_default.toFiniteNumber(response.headers.get("content-length"));
          const [onProgress, flush] = onDownloadProgress && progressEventDecorator(
            responseContentLength,
            progressEventReducer(asyncDecorator(onDownloadProgress), true)
          ) || [];
          let bytesRead = 0;
          const onChunkProgress = (loadedBytes) => {
            if (hasMaxContentLength) {
              bytesRead = loadedBytes;
              if (bytesRead > maxContentLength) {
                throw new AxiosError_default(
                  "maxContentLength size of " + maxContentLength + " exceeded",
                  AxiosError_default.ERR_BAD_RESPONSE,
                  config,
                  request
                );
              }
            }
            onProgress && onProgress(loadedBytes);
          };
          response = new Response2(
            trackStream(response.body, DEFAULT_CHUNK_SIZE, onChunkProgress, () => {
              flush && flush();
              unsubscribe && unsubscribe();
            }),
            options
          );
        }
        responseType = responseType || "text";
        let responseData = await resolvers[utils_default.findKey(resolvers, responseType) || "text"](
          response,
          config
        );
        if (hasMaxContentLength && !supportsResponseStream && !isStreamResponse) {
          let materializedSize;
          if (responseData != null) {
            if (typeof responseData.byteLength === "number") {
              materializedSize = responseData.byteLength;
            } else if (typeof responseData.size === "number") {
              materializedSize = responseData.size;
            } else if (typeof responseData === "string") {
              materializedSize = typeof TextEncoder2 === "function" ? new TextEncoder2().encode(responseData).byteLength : responseData.length;
            }
          }
          if (typeof materializedSize === "number" && materializedSize > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        !isStreamResponse && unsubscribe && unsubscribe();
        return await new Promise((resolve, reject) => {
          settle(resolve, reject, {
            data: responseData,
            headers: AxiosHeaders_default.from(response.headers),
            status: response.status,
            statusText: response.statusText,
            config,
            request
          });
        });
      } catch (err) {
        unsubscribe && unsubscribe();
        if (composedSignal && composedSignal.aborted && composedSignal.reason instanceof AxiosError_default) {
          const canceledError = composedSignal.reason;
          canceledError.config = config;
          request && (canceledError.request = request);
          err !== canceledError && (canceledError.cause = err);
          throw canceledError;
        }
        if (err && err.name === "TypeError" && /Load failed|fetch/i.test(err.message)) {
          throw Object.assign(
            new AxiosError_default(
              "Network Error",
              AxiosError_default.ERR_NETWORK,
              config,
              request,
              err && err.response
            ),
            {
              cause: err.cause || err
            }
          );
        }
        throw AxiosError_default.from(err, err && err.code, config, request, err && err.response);
      }
    };
  };
  var seedCache = /* @__PURE__ */ new Map();
  var getFetch = (config) => {
    let env = config && config.env || {};
    const { fetch: fetch2, Request: Request2, Response: Response2 } = env;
    const seeds = [Request2, Response2, fetch2];
    let len = seeds.length, i = len, seed, target, map = seedCache;
    while (i--) {
      seed = seeds[i];
      target = map.get(seed);
      target === void 0 && map.set(seed, target = i ? /* @__PURE__ */ new Map() : factory(env));
      map = target;
    }
    return target;
  };
  var adapter = getFetch();

  // node_modules/axios/lib/adapters/adapters.js
  var knownAdapters = {
    http: null_default,
    xhr: xhr_default,
    fetch: {
      get: getFetch
    }
  };
  utils_default.forEach(knownAdapters, (fn, value) => {
    if (fn) {
      try {
        Object.defineProperty(fn, "name", { __proto__: null, value });
      } catch (e2) {
      }
      Object.defineProperty(fn, "adapterName", { __proto__: null, value });
    }
  });
  var renderReason = (reason) => `- ${reason}`;
  var isResolvedHandle = (adapter2) => utils_default.isFunction(adapter2) || adapter2 === null || adapter2 === false;
  function getAdapter(adapters, config) {
    adapters = utils_default.isArray(adapters) ? adapters : [adapters];
    const { length } = adapters;
    let nameOrAdapter;
    let adapter2;
    const rejectedReasons = {};
    for (let i = 0; i < length; i++) {
      nameOrAdapter = adapters[i];
      let id;
      adapter2 = nameOrAdapter;
      if (!isResolvedHandle(nameOrAdapter)) {
        adapter2 = knownAdapters[(id = String(nameOrAdapter)).toLowerCase()];
        if (adapter2 === void 0) {
          throw new AxiosError_default(`Unknown adapter '${id}'`);
        }
      }
      if (adapter2 && (utils_default.isFunction(adapter2) || (adapter2 = adapter2.get(config)))) {
        break;
      }
      rejectedReasons[id || "#" + i] = adapter2;
    }
    if (!adapter2) {
      const reasons = Object.entries(rejectedReasons).map(
        ([id, state]) => `adapter ${id} ` + (state === false ? "is not supported by the environment" : "is not available in the build")
      );
      let s = length ? reasons.length > 1 ? "since :\n" + reasons.map(renderReason).join("\n") : " " + renderReason(reasons[0]) : "as no adapter specified";
      throw new AxiosError_default(
        `There is no suitable adapter to dispatch the request ` + s,
        "ERR_NOT_SUPPORT"
      );
    }
    return adapter2;
  }
  var adapters_default = {
    /**
     * Resolve an adapter from a list of adapter names or functions.
     * @type {Function}
     */
    getAdapter,
    /**
     * Exposes all known adapters
     * @type {Object<string, Function|Object>}
     */
    adapters: knownAdapters
  };

  // node_modules/axios/lib/core/dispatchRequest.js
  function throwIfCancellationRequested(config) {
    if (config.cancelToken) {
      config.cancelToken.throwIfRequested();
    }
    if (config.signal && config.signal.aborted) {
      throw new CanceledError_default(null, config);
    }
  }
  function dispatchRequest(config) {
    throwIfCancellationRequested(config);
    config.headers = AxiosHeaders_default.from(config.headers);
    config.data = transformData.call(config, config.transformRequest);
    if (["post", "put", "patch"].indexOf(config.method) !== -1) {
      config.headers.setContentType("application/x-www-form-urlencoded", false);
    }
    const adapter2 = adapters_default.getAdapter(config.adapter || defaults_default.adapter, config);
    return adapter2(config).then(
      function onAdapterResolution(response) {
        throwIfCancellationRequested(config);
        config.response = response;
        try {
          response.data = transformData.call(config, config.transformResponse, response);
        } finally {
          delete config.response;
        }
        response.headers = AxiosHeaders_default.from(response.headers);
        return response;
      },
      function onAdapterRejection(reason) {
        if (!isCancel(reason)) {
          throwIfCancellationRequested(config);
          if (reason && reason.response) {
            config.response = reason.response;
            try {
              reason.response.data = transformData.call(
                config,
                config.transformResponse,
                reason.response
              );
            } finally {
              delete config.response;
            }
            reason.response.headers = AxiosHeaders_default.from(reason.response.headers);
          }
        }
        return Promise.reject(reason);
      }
    );
  }

  // node_modules/axios/lib/helpers/validator.js
  var validators = {};
  ["object", "boolean", "number", "function", "string", "symbol"].forEach((type, i) => {
    validators[type] = function validator(thing) {
      return typeof thing === type || "a" + (i < 1 ? "n " : " ") + type;
    };
  });
  var deprecatedWarnings = {};
  validators.transitional = function transitional(validator, version, message) {
    function formatMessage(opt, desc) {
      return "[Axios v" + VERSION + "] Transitional option '" + opt + "'" + desc + (message ? ". " + message : "");
    }
    return (value, opt, opts) => {
      if (validator === false) {
        throw new AxiosError_default(
          formatMessage(opt, " has been removed" + (version ? " in " + version : "")),
          AxiosError_default.ERR_DEPRECATED
        );
      }
      if (version && !deprecatedWarnings[opt]) {
        deprecatedWarnings[opt] = true;
        console.warn(
          formatMessage(
            opt,
            " has been deprecated since v" + version + " and will be removed in the near future"
          )
        );
      }
      return validator ? validator(value, opt, opts) : true;
    };
  };
  validators.spelling = function spelling(correctSpelling) {
    return (value, opt) => {
      console.warn(`${opt} is likely a misspelling of ${correctSpelling}`);
      return true;
    };
  };
  function assertOptions(options, schema, allowUnknown) {
    if (typeof options !== "object") {
      throw new AxiosError_default("options must be an object", AxiosError_default.ERR_BAD_OPTION_VALUE);
    }
    const keys = Object.keys(options);
    let i = keys.length;
    while (i-- > 0) {
      const opt = keys[i];
      const validator = Object.prototype.hasOwnProperty.call(schema, opt) ? schema[opt] : void 0;
      if (validator) {
        const value = options[opt];
        const result = value === void 0 || validator(value, opt, options);
        if (result !== true) {
          throw new AxiosError_default(
            "option " + opt + " must be " + result,
            AxiosError_default.ERR_BAD_OPTION_VALUE
          );
        }
        continue;
      }
      if (allowUnknown !== true) {
        throw new AxiosError_default("Unknown option " + opt, AxiosError_default.ERR_BAD_OPTION);
      }
    }
  }
  var validator_default = {
    assertOptions,
    validators
  };

  // node_modules/axios/lib/core/Axios.js
  var validators2 = validator_default.validators;
  var Axios = class {
    constructor(instanceConfig) {
      this.defaults = instanceConfig || {};
      this.interceptors = {
        request: new InterceptorManager_default(),
        response: new InterceptorManager_default()
      };
    }
    /**
     * Dispatch a request
     *
     * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
     * @param {?Object} config
     *
     * @returns {Promise} The Promise to be fulfilled
     */
    async request(configOrUrl, config) {
      try {
        return await this._request(configOrUrl, config);
      } catch (err) {
        if (err instanceof Error) {
          let dummy = {};
          Error.captureStackTrace ? Error.captureStackTrace(dummy) : dummy = new Error();
          const stack = (() => {
            if (!dummy.stack) {
              return "";
            }
            const firstNewlineIndex = dummy.stack.indexOf("\n");
            return firstNewlineIndex === -1 ? "" : dummy.stack.slice(firstNewlineIndex + 1);
          })();
          try {
            if (!err.stack) {
              err.stack = stack;
            } else if (stack) {
              const firstNewlineIndex = stack.indexOf("\n");
              const secondNewlineIndex = firstNewlineIndex === -1 ? -1 : stack.indexOf("\n", firstNewlineIndex + 1);
              const stackWithoutTwoTopLines = secondNewlineIndex === -1 ? "" : stack.slice(secondNewlineIndex + 1);
              if (!String(err.stack).endsWith(stackWithoutTwoTopLines)) {
                err.stack += "\n" + stack;
              }
            }
          } catch (e2) {
          }
        }
        throw err;
      }
    }
    _request(configOrUrl, config) {
      if (typeof configOrUrl === "string") {
        config = config || {};
        config.url = configOrUrl;
      } else {
        config = configOrUrl || {};
      }
      config = mergeConfig(this.defaults, config);
      const { transitional: transitional2, paramsSerializer, headers } = config;
      if (transitional2 !== void 0) {
        validator_default.assertOptions(
          transitional2,
          {
            silentJSONParsing: validators2.transitional(validators2.boolean),
            forcedJSONParsing: validators2.transitional(validators2.boolean),
            clarifyTimeoutError: validators2.transitional(validators2.boolean),
            legacyInterceptorReqResOrdering: validators2.transitional(validators2.boolean)
          },
          false
        );
      }
      if (paramsSerializer != null) {
        if (utils_default.isFunction(paramsSerializer)) {
          config.paramsSerializer = {
            serialize: paramsSerializer
          };
        } else {
          validator_default.assertOptions(
            paramsSerializer,
            {
              encode: validators2.function,
              serialize: validators2.function
            },
            true
          );
        }
      }
      if (config.allowAbsoluteUrls !== void 0) {
      } else if (this.defaults.allowAbsoluteUrls !== void 0) {
        config.allowAbsoluteUrls = this.defaults.allowAbsoluteUrls;
      } else {
        config.allowAbsoluteUrls = true;
      }
      validator_default.assertOptions(
        config,
        {
          baseUrl: validators2.spelling("baseURL"),
          withXsrfToken: validators2.spelling("withXSRFToken")
        },
        true
      );
      config.method = (config.method || this.defaults.method || "get").toLowerCase();
      let contextHeaders = headers && utils_default.merge(headers.common, headers[config.method]);
      headers && utils_default.forEach(["delete", "get", "head", "post", "put", "patch", "query", "common"], (method) => {
        delete headers[method];
      });
      config.headers = AxiosHeaders_default.concat(contextHeaders, headers);
      const requestInterceptorChain = [];
      let synchronousRequestInterceptors = true;
      this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
        if (typeof interceptor.runWhen === "function" && interceptor.runWhen(config) === false) {
          return;
        }
        synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;
        const transitional3 = config.transitional || transitional_default;
        const legacyInterceptorReqResOrdering = transitional3 && transitional3.legacyInterceptorReqResOrdering;
        if (legacyInterceptorReqResOrdering) {
          requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
        } else {
          requestInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
        }
      });
      const responseInterceptorChain = [];
      this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
        responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
      });
      let promise;
      let i = 0;
      let len;
      if (!synchronousRequestInterceptors) {
        const chain = [dispatchRequest.bind(this), void 0];
        chain.unshift(...requestInterceptorChain);
        chain.push(...responseInterceptorChain);
        len = chain.length;
        promise = Promise.resolve(config);
        while (i < len) {
          promise = promise.then(chain[i++], chain[i++]);
        }
        return promise;
      }
      len = requestInterceptorChain.length;
      let newConfig = config;
      while (i < len) {
        const onFulfilled = requestInterceptorChain[i++];
        const onRejected = requestInterceptorChain[i++];
        try {
          newConfig = onFulfilled(newConfig);
        } catch (error) {
          onRejected.call(this, error);
          break;
        }
      }
      try {
        promise = dispatchRequest.call(this, newConfig);
      } catch (error) {
        return Promise.reject(error);
      }
      i = 0;
      len = responseInterceptorChain.length;
      while (i < len) {
        promise = promise.then(responseInterceptorChain[i++], responseInterceptorChain[i++]);
      }
      return promise;
    }
    getUri(config) {
      config = mergeConfig(this.defaults, config);
      const fullPath = buildFullPath(config.baseURL, config.url, config.allowAbsoluteUrls);
      return buildURL(fullPath, config.params, config.paramsSerializer);
    }
  };
  utils_default.forEach(["delete", "get", "head", "options"], function forEachMethodNoData(method) {
    Axios.prototype[method] = function(url, config) {
      return this.request(
        mergeConfig(config || {}, {
          method,
          url,
          data: (config || {}).data
        })
      );
    };
  });
  utils_default.forEach(["post", "put", "patch", "query"], function forEachMethodWithData(method) {
    function generateHTTPMethod(isForm) {
      return function httpMethod(url, data2, config) {
        return this.request(
          mergeConfig(config || {}, {
            method,
            headers: isForm ? {
              "Content-Type": "multipart/form-data"
            } : {},
            url,
            data: data2
          })
        );
      };
    }
    Axios.prototype[method] = generateHTTPMethod();
    if (method !== "query") {
      Axios.prototype[method + "Form"] = generateHTTPMethod(true);
    }
  });
  var Axios_default = Axios;

  // node_modules/axios/lib/cancel/CancelToken.js
  var CancelToken = class _CancelToken {
    constructor(executor) {
      if (typeof executor !== "function") {
        throw new TypeError("executor must be a function.");
      }
      let resolvePromise;
      this.promise = new Promise(function promiseExecutor(resolve) {
        resolvePromise = resolve;
      });
      const token = this;
      this.promise.then((cancel) => {
        if (!token._listeners) return;
        let i = token._listeners.length;
        while (i-- > 0) {
          token._listeners[i](cancel);
        }
        token._listeners = null;
      });
      this.promise.then = (onfulfilled) => {
        let _resolve;
        const promise = new Promise((resolve) => {
          token.subscribe(resolve);
          _resolve = resolve;
        }).then(onfulfilled);
        promise.cancel = function reject() {
          token.unsubscribe(_resolve);
        };
        return promise;
      };
      executor(function cancel(message, config, request) {
        if (token.reason) {
          return;
        }
        token.reason = new CanceledError_default(message, config, request);
        resolvePromise(token.reason);
      });
    }
    /**
     * Throws a `CanceledError` if cancellation has been requested.
     */
    throwIfRequested() {
      if (this.reason) {
        throw this.reason;
      }
    }
    /**
     * Subscribe to the cancel signal
     */
    subscribe(listener) {
      if (this.reason) {
        listener(this.reason);
        return;
      }
      if (this._listeners) {
        this._listeners.push(listener);
      } else {
        this._listeners = [listener];
      }
    }
    /**
     * Unsubscribe from the cancel signal
     */
    unsubscribe(listener) {
      if (!this._listeners) {
        return;
      }
      const index = this._listeners.indexOf(listener);
      if (index !== -1) {
        this._listeners.splice(index, 1);
      }
    }
    toAbortSignal() {
      const controller = new AbortController();
      const abort = (err) => {
        controller.abort(err);
      };
      this.subscribe(abort);
      controller.signal.unsubscribe = () => this.unsubscribe(abort);
      return controller.signal;
    }
    /**
     * Returns an object that contains a new `CancelToken` and a function that, when called,
     * cancels the `CancelToken`.
     */
    static source() {
      let cancel;
      const token = new _CancelToken(function executor(c) {
        cancel = c;
      });
      return {
        token,
        cancel
      };
    }
  };
  var CancelToken_default = CancelToken;

  // node_modules/axios/lib/helpers/spread.js
  function spread(callback) {
    return function wrap(arr) {
      return callback.apply(null, arr);
    };
  }

  // node_modules/axios/lib/helpers/isAxiosError.js
  function isAxiosError(payload) {
    return utils_default.isObject(payload) && payload.isAxiosError === true;
  }

  // node_modules/axios/lib/helpers/HttpStatusCode.js
  var HttpStatusCode = {
    Continue: 100,
    SwitchingProtocols: 101,
    Processing: 102,
    EarlyHints: 103,
    Ok: 200,
    Created: 201,
    Accepted: 202,
    NonAuthoritativeInformation: 203,
    NoContent: 204,
    ResetContent: 205,
    PartialContent: 206,
    MultiStatus: 207,
    AlreadyReported: 208,
    ImUsed: 226,
    MultipleChoices: 300,
    MovedPermanently: 301,
    Found: 302,
    SeeOther: 303,
    NotModified: 304,
    UseProxy: 305,
    Unused: 306,
    TemporaryRedirect: 307,
    PermanentRedirect: 308,
    BadRequest: 400,
    Unauthorized: 401,
    PaymentRequired: 402,
    Forbidden: 403,
    NotFound: 404,
    MethodNotAllowed: 405,
    NotAcceptable: 406,
    ProxyAuthenticationRequired: 407,
    RequestTimeout: 408,
    Conflict: 409,
    Gone: 410,
    LengthRequired: 411,
    PreconditionFailed: 412,
    PayloadTooLarge: 413,
    UriTooLong: 414,
    UnsupportedMediaType: 415,
    RangeNotSatisfiable: 416,
    ExpectationFailed: 417,
    ImATeapot: 418,
    MisdirectedRequest: 421,
    UnprocessableEntity: 422,
    Locked: 423,
    FailedDependency: 424,
    TooEarly: 425,
    UpgradeRequired: 426,
    PreconditionRequired: 428,
    TooManyRequests: 429,
    RequestHeaderFieldsTooLarge: 431,
    UnavailableForLegalReasons: 451,
    InternalServerError: 500,
    NotImplemented: 501,
    BadGateway: 502,
    ServiceUnavailable: 503,
    GatewayTimeout: 504,
    HttpVersionNotSupported: 505,
    VariantAlsoNegotiates: 506,
    InsufficientStorage: 507,
    LoopDetected: 508,
    NotExtended: 510,
    NetworkAuthenticationRequired: 511,
    WebServerIsDown: 521,
    ConnectionTimedOut: 522,
    OriginIsUnreachable: 523,
    TimeoutOccurred: 524,
    SslHandshakeFailed: 525,
    InvalidSslCertificate: 526
  };
  Object.entries(HttpStatusCode).forEach(([key, value]) => {
    HttpStatusCode[value] = key;
  });
  var HttpStatusCode_default = HttpStatusCode;

  // node_modules/axios/lib/axios.js
  function createInstance2(defaultConfig) {
    const context = new Axios_default(defaultConfig);
    const instance2 = bind(Axios_default.prototype.request, context);
    utils_default.extend(instance2, Axios_default.prototype, context, { allOwnKeys: true });
    utils_default.extend(instance2, context, null, { allOwnKeys: true });
    instance2.create = function create2(instanceConfig) {
      return createInstance2(mergeConfig(defaultConfig, instanceConfig));
    };
    return instance2;
  }
  var axios = createInstance2(defaults_default);
  axios.Axios = Axios_default;
  axios.CanceledError = CanceledError_default;
  axios.CancelToken = CancelToken_default;
  axios.isCancel = isCancel;
  axios.VERSION = VERSION;
  axios.toFormData = toFormData_default;
  axios.AxiosError = AxiosError_default;
  axios.Cancel = axios.CanceledError;
  axios.all = function all(promises) {
    return Promise.all(promises);
  };
  axios.spread = spread;
  axios.isAxiosError = isAxiosError;
  axios.mergeConfig = mergeConfig;
  axios.AxiosHeaders = AxiosHeaders_default;
  axios.formToJSON = (thing) => formDataToJSON_default(utils_default.isHTMLForm(thing) ? new FormData(thing) : thing);
  axios.getAdapter = adapters_default.getAdapter;
  axios.HttpStatusCode = HttpStatusCode_default;
  axios.default = axios;
  var axios_default = axios;

  // node_modules/axios/index.js
  var {
    Axios: Axios2,
    AxiosError: AxiosError2,
    CanceledError: CanceledError2,
    isCancel: isCancel2,
    CancelToken: CancelToken2,
    VERSION: VERSION2,
    all: all2,
    Cancel,
    isAxiosError: isAxiosError2,
    spread: spread2,
    toFormData: toFormData2,
    AxiosHeaders: AxiosHeaders2,
    HttpStatusCode: HttpStatusCode2,
    formToJSON,
    getAdapter: getAdapter2,
    mergeConfig: mergeConfig2,
    create
  } = axios_default;

  // src/api/client.ts
  var apiClient = axios_default.create({
    baseURL: __import_meta_env__.VITE_API_URL,
    headers: {
      "Content-Type": "application/json"
    }
  });
  apiClient.interceptors.response.use(
    (response) => response,
    (error) => {
      const isNetworkError = !error.response;
      const is5xx = (error.response?.status ?? 0) >= 500;
      if (isNetworkError || is5xx) {
        window.dispatchEvent(new CustomEvent("app-offline"));
      }
      return Promise.reject(error);
    }
  );

  // src/api/brands.ts
  var brandsApi = {
    getAll: () => apiClient.get("/api/brands").then((r) => r.data),
    add: (body) => apiClient.post("/api/brands", body).then((r) => r.data),
    delete: (id) => apiClient.delete(`/api/brands/${id}`),
    searchOfd: (q, signal) => apiClient.get(`/api/brands/ofd-search?q=${encodeURIComponent(q)}`, { signal }).then((r) => r.data)
  };

  // src/components/BrandCard/brandDomains.ts
  var BRAND_DOMAINS = {
    "Bambu Lab": "bambulab.com",
    "eSUN 3D": "esun3d.com",
    "Prusament": "prusament.com",
    "Polymaker": "polymaker.com",
    "Hatchbox": "hatchbox3d.com",
    "SUNLU": "sunlu.com",
    "Creality": "creality.com",
    "ELEGOO": "elegoo.com",
    "Overture": "overture3d.com",
    "PolyLite": "polymaker.com",
    "colorFabb": "colorfabb.com",
    "Fillamentum": "fillamentum.com",
    "Fiberlogy": "fiberlogy.com",
    "extrudr": "extrudr.com",
    "Das Filament": "dasfilament.de",
    "Protopasta": "proto-pasta.com",
    "MatterHackers": "matterhackers.com",
    "NinjaTek": "ninjatek.com",
    "Atomic Filament": "atomicfilament.com",
    "Inland": "microcenter.com",
    "JAYO": "jayofilament.com",
    "TINMORRY": "tinmorry.com",
    "AzureFilm": "azurefilm.hr",
    "Spectrum": "spectrumfilaments.com",
    "Devil Design": "devildesign.pl",
    "Anycubic": "anycubic.com"
  };
  var brandDomains_default = BRAND_DOMAINS;

  // src/components/AddBrandModal/AddBrandModal.module.css
  var AddBrandModal_default = {
    spin: "AddBrandModal_spin",
    overlay: "AddBrandModal_overlay",
    modal: "AddBrandModal_modal",
    header: "AddBrandModal_header",
    title: "AddBrandModal_title",
    closeBtn: "AddBrandModal_closeBtn",
    form: "AddBrandModal_form",
    fieldWrap: "AddBrandModal_fieldWrap",
    label: "AddBrandModal_label",
    input: "AddBrandModal_input",
    spinner: "AddBrandModal_spinner",
    dropdown: "AddBrandModal_dropdown",
    option: "AddBrandModal_option",
    optionDisabled: "AddBrandModal_optionDisabled",
    optionName: "AddBrandModal_optionName",
    optionMeta: "AddBrandModal_optionMeta",
    warning: "AddBrandModal_warning",
    error: "AddBrandModal_error",
    footer: "AddBrandModal_footer",
    btnCancel: "AddBrandModal_btnCancel",
    btnSubmit: "AddBrandModal_btnSubmit"
  };

  // src/components/AddBrandModal/AddBrandModal.tsx
  var import_jsx_runtime11 = __toESM(require_react_shim(), 1);
  function guessDomain(name) {
    return brandDomains_default[name] ?? "";
  }
  function AddBrandModal({ existingSlugs, onClose, onAdded }) {
    const { t: t2 } = useTranslation();
    const [query, setQuery] = (0, import_react13.useState)("");
    const [results, setResults] = (0, import_react13.useState)([]);
    const [selected, setSelected] = (0, import_react13.useState)(null);
    const [domain, setDomain] = (0, import_react13.useState)("");
    const [searching, setSearching] = (0, import_react13.useState)(false);
    const [submitting, setSubmitting] = (0, import_react13.useState)(false);
    const [error, setError] = (0, import_react13.useState)(null);
    const abortRef = (0, import_react13.useRef)(null);
    const suppressSearchRef = (0, import_react13.useRef)(false);
    const isDuplicate = selected !== null && existingSlugs.has(selected.slug);
    (0, import_react13.useEffect)(() => {
      if (suppressSearchRef.current) {
        suppressSearchRef.current = false;
        return;
      }
      const timeout = setTimeout(async () => {
        if (!query.trim()) {
          setResults([]);
          return;
        }
        abortRef.current?.abort();
        const ctrl = new AbortController();
        abortRef.current = ctrl;
        setSearching(true);
        try {
          const res = await brandsApi.searchOfd(query, ctrl.signal);
          setResults(res);
        } catch {
        } finally {
          setSearching(false);
        }
      }, 300);
      return () => clearTimeout(timeout);
    }, [query]);
    (0, import_react13.useEffect)(() => {
      function onKey(e2) {
        if (e2.key === "Escape") onClose();
      }
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
    }, [onClose]);
    function handleSelect(r) {
      if (existingSlugs.has(r.slug)) return;
      suppressSearchRef.current = true;
      setSelected(r);
      setResults([]);
      setQuery(r.name);
      setDomain(guessDomain(r.name));
      setError(null);
    }
    async function handleSubmit(e2) {
      e2.preventDefault();
      if (!selected) {
        setError(t2("addBrand.selectError"));
        return;
      }
      if (isDuplicate) return;
      setSubmitting(true);
      setError(null);
      try {
        await brandsApi.add({ name: selected.name, domain: domain.trim(), ofdSlug: selected.slug });
        onAdded(selected.name);
        onClose();
      } catch (err) {
        const status = err?.response?.status;
        setError(
          status === 409 ? t2("addBrand.duplicateError") : t2("addBrand.addError")
        );
      } finally {
        setSubmitting(false);
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("div", { className: AddBrandModal_default.overlay, onClick: (e2) => {
      if (e2.target === e2.currentTarget) onClose();
    }, children: /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("div", { className: AddBrandModal_default.modal, children: [
      /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("div", { className: AddBrandModal_default.header, children: [
        /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("h2", { className: AddBrandModal_default.title, children: t2("filaments.addBrand") }),
        /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("button", { onClick: onClose, className: AddBrandModal_default.closeBtn, children: /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
          /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
          /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
        ] }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("form", { onSubmit: handleSubmit, className: AddBrandModal_default.form, children: [
        /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("div", { className: AddBrandModal_default.fieldWrap, children: [
          /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("label", { className: AddBrandModal_default.label, children: t2("addBrand.searchLabel") }),
          /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
            "input",
            {
              type: "text",
              value: query,
              onChange: (e2) => {
                setQuery(e2.target.value);
                setSelected(null);
              },
              placeholder: "e.g. Bambu Lab",
              className: AddBrandModal_default.input,
              autoFocus: true
            }
          ),
          searching && /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("div", { className: AddBrandModal_default.spinner }),
          results.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("ul", { className: AddBrandModal_default.dropdown, role: "listbox", children: results.map((r) => {
            const already = existingSlugs.has(r.slug);
            return /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)(
              "button",
              {
                type: "button",
                role: "option",
                "aria-selected": selected?.slug === r.slug,
                "aria-disabled": already,
                onClick: () => handleSelect(r),
                className: `${AddBrandModal_default.option}${already ? ` ${AddBrandModal_default.optionDisabled}` : ""}`,
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("span", { className: AddBrandModal_default.optionName, children: r.name }),
                  /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("span", { className: AddBrandModal_default.optionMeta, children: already ? t2("addBrand.alreadyAdded") : t2("addBrand.materialCount", { count: r.materialCount }) })
                ]
              }
            ) }, r.slug);
          }) })
        ] }),
        isDuplicate && /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("p", { className: AddBrandModal_default.warning, role: "alert", children: t2("addBrand.duplicateWarning") }),
        error && /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("p", { className: AddBrandModal_default.error, role: "alert", children: error }),
        /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("div", { className: AddBrandModal_default.footer, children: [
          /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("button", { type: "button", onClick: onClose, className: AddBrandModal_default.btnCancel, children: t2("common.cancel") }),
          /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
            "button",
            {
              type: "submit",
              disabled: submitting || !selected || isDuplicate,
              className: AddBrandModal_default.btnSubmit,
              children: submitting ? t2("addBrand.adding") : t2("filaments.addBrand")
            }
          )
        ] })
      ] })
    ] }) });
  }

  // src/components/AddPrinterModal/AddPrinterModal.tsx
  var import_react14 = __toESM(require_react_shim(), 1);

  // src/api/printers.ts
  var printersApi = {
    getAll: () => apiClient.get("/api/printers").then((r) => r.data),
    getById: (id) => apiClient.get(`/api/printers/${id}`).then((r) => r.data),
    getStatus: (id) => apiClient.get(`/api/printers/${id}/status`).then((r) => r.data),
    discoverLan: () => apiClient.get("/api/printers/discover/lan").then((r) => r.data),
    registerLan: (req) => apiClient.post("/api/printers/register/lan", req).then((r) => r.data),
    registerCloud: (req) => apiClient.post("/api/printers/register/cloud", req).then((r) => r.data),
    verifyCloud: (req) => apiClient.post("/api/printers/cloud/verify", req).then((r) => r.data),
    selectCloud: (serials) => apiClient.post("/api/printers/cloud/select", { serials }).then((r) => r.data),
    update: (id, req) => apiClient.put(`/api/printers/${id}`, req).then((r) => r.data),
    remove: (id) => apiClient.delete(`/api/printers/${id}`),
    mockStatus: (id, req) => apiClient.post(`/api/printers/${id}/status/mock`, req),
    assignTraySpool: (id, slot, spoolId) => apiClient.put(`/api/printers/${id}/trays/${slot}`, { spoolId }).then((r) => r.data),
    assignExtraSpool: (id, spoolId) => apiClient.put(`/api/printers/${id}/extra-spool`, { spoolId }).then((r) => r.data)
  };

  // src/components/AddPrinterModal/AddPrinterModal.module.css
  var AddPrinterModal_default = {
    overlay: "AddPrinterModal_overlay",
    panel: "AddPrinterModal_panel",
    header: "AddPrinterModal_header",
    headerTitle: "AddPrinterModal_headerTitle",
    closeBtn: "AddPrinterModal_closeBtn",
    form: "AddPrinterModal_form",
    label: "AddPrinterModal_label",
    input: "AddPrinterModal_input",
    inputMono: "AddPrinterModal_inputMono",
    row2: "AddPrinterModal_row2",
    row3: "AddPrinterModal_row3",
    checkRow: "AddPrinterModal_checkRow",
    checkLabel: "AddPrinterModal_checkLabel",
    checkDesc: "AddPrinterModal_checkDesc",
    error: "AddPrinterModal_error",
    actions: "AddPrinterModal_actions",
    btnSecondary: "AddPrinterModal_btnSecondary",
    btnPrimary: "AddPrinterModal_btnPrimary"
  };

  // src/components/AddPrinterModal/AddPrinterModal.tsx
  var import_jsx_runtime12 = __toESM(require_react_shim(), 1);
  function AddPrinterModal({ onClose, onAdded }) {
    const { t: t2 } = useTranslation();
    const [name, setName] = (0, import_react14.useState)("");
    const [brand, setBrand] = (0, import_react14.useState)("Bambu Lab");
    const [model, setModel] = (0, import_react14.useState)("");
    const [ip, setIp] = (0, import_react14.useState)("");
    const [port, setPort] = (0, import_react14.useState)("");
    const [hasAms, setHasAms] = (0, import_react14.useState)(false);
    const [submitting, setSubmitting] = (0, import_react14.useState)(false);
    const [error, setError] = (0, import_react14.useState)(null);
    (0, import_react14.useEffect)(() => {
      function onKey(e2) {
        if (e2.key === "Escape") onClose();
      }
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
    }, [onClose]);
    async function handleSubmit(e2) {
      e2.preventDefault();
      setSubmitting(true);
      setError(null);
      try {
        const printer = await printersApi.registerLan({
          name: name.trim(),
          brand: brand.trim(),
          model: model.trim(),
          ipAddress: ip.trim(),
          port: port ? parseInt(port, 10) : null,
          hasAms
        });
        onAdded(printer);
      } catch {
        setError(t2("addPrinter.registerError"));
      } finally {
        setSubmitting(false);
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("div", { className: AddPrinterModal_default.overlay, onClick: onClose, children: /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: AddPrinterModal_default.panel, onClick: (e2) => e2.stopPropagation(), children: [
      /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: AddPrinterModal_default.header, children: [
        /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("h2", { className: AddPrinterModal_default.headerTitle, children: t2("addPrinter.registerTitle") }),
        /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("button", { onClick: onClose, "aria-label": "Close", className: AddPrinterModal_default.closeBtn, children: /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
          /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
          /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
        ] }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("form", { onSubmit: handleSubmit, className: AddPrinterModal_default.form, children: [
        /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("label", { className: AddPrinterModal_default.label, children: t2("addPrinter.labelName") }),
          /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
            "input",
            {
              type: "text",
              value: name,
              onChange: (e2) => setName(e2.target.value),
              placeholder: "e.g. Garage X1C",
              required: true,
              className: AddPrinterModal_default.input
            }
          )
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: AddPrinterModal_default.row2, children: [
          /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("label", { className: AddPrinterModal_default.label, children: [
              t2("filters.brand"),
              " *"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
              "input",
              {
                list: "printer-brands",
                type: "text",
                value: brand,
                onChange: (e2) => setBrand(e2.target.value),
                placeholder: "Bambu Lab",
                required: true,
                className: AddPrinterModal_default.input
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("datalist", { id: "printer-brands", children: [
              /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("option", { value: "Bambu Lab" }),
              /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("option", { value: "Prusa" }),
              /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("option", { value: "Creality" }),
              /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("option", { value: "AnkerMake" }),
              /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("option", { value: "Elegoo" }),
              /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("option", { value: "Flashforge" })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("label", { className: AddPrinterModal_default.label, children: t2("addPrinter.labelModel") }),
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
              "input",
              {
                type: "text",
                value: model,
                onChange: (e2) => setModel(e2.target.value),
                placeholder: "X1 Carbon",
                required: true,
                className: AddPrinterModal_default.input
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: AddPrinterModal_default.row3, children: [
          /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("label", { className: AddPrinterModal_default.label, children: t2("addPrinter.labelIpAddress") }),
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
              "input",
              {
                type: "text",
                value: ip,
                onChange: (e2) => setIp(e2.target.value),
                placeholder: "192.168.1.100",
                required: true,
                className: `${AddPrinterModal_default.input} ${AddPrinterModal_default.inputMono}`
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("label", { className: AddPrinterModal_default.label, children: t2("addPrinter.labelPort") }),
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
              "input",
              {
                type: "number",
                value: port,
                onChange: (e2) => setPort(e2.target.value),
                placeholder: "1883",
                min: 1,
                max: 65535,
                className: `${AddPrinterModal_default.input} ${AddPrinterModal_default.inputMono}`
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("label", { className: AddPrinterModal_default.checkRow, children: [
          /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
            "input",
            {
              type: "checkbox",
              checked: hasAms,
              onChange: (e2) => setHasAms(e2.target.checked),
              className: "w-4 h-4 rounded accent-amber-400"
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("span", { className: AddPrinterModal_default.checkLabel, children: t2("addPrinter.hasAms") }),
            /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("p", { className: AddPrinterModal_default.checkDesc, children: t2("addPrinter.hasAmsDescShort") })
          ] })
        ] }),
        error && /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("p", { className: AddPrinterModal_default.error, children: error }),
        /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: AddPrinterModal_default.actions, children: [
          /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("button", { type: "button", onClick: onClose, className: AddPrinterModal_default.btnSecondary, children: t2("common.cancel") }),
          /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("button", { type: "submit", disabled: submitting, className: AddPrinterModal_default.btnPrimary, children: submitting ? t2("addPrinter.registering") : t2("addPrinter.register") })
        ] })
      ] })
    ] }) });
  }

  // src/components/AddSpoolForm/AddSpoolForm.tsx
  var import_react17 = __toESM(require_react_shim(), 1);

  // src/api/spools.ts
  var cache = null;
  var CACHE_TTL = 3e4;
  function invalidate() {
    cache = null;
  }
  var spoolsApi = {
    getAll: () => {
      if (cache && Date.now() - cache.ts < CACHE_TTL)
        return Promise.resolve(cache.data);
      return apiClient.get("/api/spools").then((r) => {
        cache = { data: r.data, ts: Date.now() };
        return r.data;
      });
    },
    getById: (id) => {
      const cached = cache && Date.now() - cache.ts < CACHE_TTL ? cache.data.find((s) => s.id === id) : null;
      if (cached) return Promise.resolve(cached);
      return apiClient.get(`/api/spools/${id}`).then((r) => r.data);
    },
    add: (body) => apiClient.post("/api/spools", body).then((r) => {
      invalidate();
      return r.data;
    }),
    update: (id, body) => apiClient.put(`/api/spools/${id}`, body).then((r) => {
      invalidate();
      return r.data;
    }),
    activate: (id) => apiClient.patch(`/api/spools/activate/${id}`).then((r) => {
      invalidate();
      return r.data;
    }),
    deactivate: (id) => apiClient.patch(`/api/spools/deactivate/${id}`).then((r) => {
      invalidate();
      return r.data;
    }),
    delete: (id) => apiClient.delete(`/api/spools/${id}`).then((r) => {
      invalidate();
      return r;
    }),
    assignPrinter: (id, body) => apiClient.patch(`/api/spools/${id}/assign-printer`, body).then((r) => {
      invalidate();
      return r.data;
    }),
    getMonthlyStats: () => apiClient.get("/api/spools/monthly-stats").then((r) => r.data)
  };

  // src/api/settings.ts
  var settingsApi = {
    getAlerts: () => apiClient.get("/api/settings/alerts").then((r) => r.data),
    updateAlerts: (data2) => apiClient.put("/api/settings/alerts", data2).then((r) => r.data),
    testAlert: () => apiClient.post("/api/settings/alerts/test").then((r) => r.data),
    getFilaments: () => apiClient.get("/api/settings/filaments").then((r) => r.data),
    updateFilaments: (data2) => apiClient.put("/api/settings/filaments", data2).then((r) => r.data),
    syncFilaments: () => apiClient.post("/api/settings/filaments/sync").then((r) => r.data),
    getApp: () => apiClient.get("/api/settings/app").then((r) => r.data),
    updateApp: (data2) => apiClient.put("/api/settings/app", data2).then((r) => r.data)
  };

  // src/api/locations.ts
  var locationsApi = {
    getAll: () => apiClient.get("/api/locations").then((r) => r.data),
    add: (body) => apiClient.post("/api/locations", body).then((r) => r.data),
    delete: (id) => apiClient.delete(`/api/locations/${id}`)
  };

  // src/api/nfc.ts
  function scanTag(tagUid) {
    return apiClient.post("/api/nfc-tags/scan", { tagUid }).then((r) => r.data);
  }
  function registerTag(tagUid, spoolId) {
    return apiClient.post("/api/nfc-tags", { tagUid, spoolId, type: "NFC" }).then(() => {
    });
  }

  // src/api/spoolProfiles.ts
  var spoolProfilesApi = {
    getAll: () => apiClient.get("/api/spool-profiles").then((r) => r.data),
    getById: (id) => apiClient.get(`/api/spool-profiles/${id}`).then((r) => r.data),
    add: (body) => apiClient.post("/api/spool-profiles", body).then((r) => r.data),
    update: (id, body) => apiClient.put(`/api/spool-profiles/${id}`, body).then((r) => r.data),
    delete: (id) => apiClient.delete(`/api/spool-profiles/${id}`)
  };

  // src/api/filaments.ts
  var filamentsApi = {
    getAll: () => apiClient.get("/api/filaments").then((r) => r.data),
    refresh: () => apiClient.post("/api/filaments/refresh")
  };

  // src/utils/materialColors.ts
  var MATERIAL_COLORS = {
    PLA: "#22c55e",
    PETG: "#3b82f6",
    ABS: "#ef4444",
    TPU: "#f97316",
    ASA: "#a855f7",
    Nylon: "#eab308",
    PA: "#eab308",
    PC: "#06b6d4",
    HIPS: "#6b7280",
    PVA: "#ec4899"
  };
  function getMaterialColor(material) {
    const key = Object.keys(MATERIAL_COLORS).find((k) => material.toUpperCase().includes(k.toUpperCase()));
    return key ? MATERIAL_COLORS[key] : "#6b7280";
  }

  // src/components/MaterialTag/MaterialTag.module.css
  var MaterialTag_default = {
    tag: "MaterialTag_tag"
  };

  // src/components/MaterialTag/MaterialTag.tsx
  var import_jsx_runtime13 = __toESM(require_react_shim(), 1);
  function MaterialTag({ material }) {
    const color = getMaterialColor(material);
    return /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "span",
      {
        className: MaterialTag_default.tag,
        style: { color, borderColor: `${color}50`, backgroundColor: `${color}15` },
        children: material
      }
    );
  }

  // src/components/BrandCard/BrandCard.tsx
  var import_react15 = __toESM(require_react_shim(), 1);

  // src/components/BrandCard/BrandCard.module.css
  var BrandCard_default = {
    pulse: "BrandCard_pulse",
    logo: "BrandCard_logo",
    logoFallback: "BrandCard_logoFallback",
    confirmCard: "BrandCard_confirmCard",
    confirmText: "BrandCard_confirmText",
    confirmBrand: "BrandCard_confirmBrand",
    confirmBtns: "BrandCard_confirmBtns",
    confirmDelete: "BrandCard_confirmDelete",
    confirmCancel: "BrandCard_confirmCancel",
    card: "BrandCard_card",
    gradient: "BrandCard_gradient",
    topRow: "BrandCard_topRow",
    logoBtn: "BrandCard_logoBtn",
    logoWrap: "BrandCard_logoWrap",
    logoSkeleton: "BrandCard_logoSkeleton",
    nameWrap: "BrandCard_nameWrap",
    brandName: "BrandCard_brandName",
    brandCount: "BrandCard_brandCount",
    countSkeleton: "BrandCard_countSkeleton",
    rightCol: "BrandCard_rightCol",
    deleteBtn: "BrandCard_deleteBtn",
    deleteBtnDisabled: "BrandCard_deleteBtnDisabled",
    deleteBtnEnabled: "BrandCard_deleteBtnEnabled",
    spoolCount: "BrandCard_spoolCount",
    spoolCountLabel: "BrandCard_spoolCountLabel",
    divider: "BrandCard_divider",
    hidden: "BrandCard_hidden",
    materialsSkeleton: "BrandCard_materialsSkeleton",
    materialSkeletonPill: "BrandCard_materialSkeletonPill",
    skeletonPillLg: "BrandCard_skeletonPillLg",
    skeletonPillSm: "BrandCard_skeletonPillSm",
    skeletonPillMd: "BrandCard_skeletonPillMd",
    materials: "BrandCard_materials",
    pill: "BrandCard_pill",
    pillActive: "BrandCard_pillActive",
    pillInactive: "BrandCard_pillInactive"
  };

  // src/components/BrandCard/BrandCard.tsx
  var import_jsx_runtime14 = __toESM(require_react_shim(), 1);
  function BrandLogo({ brand, domain: domainProp, size = 36 }) {
    const [error, setError] = (0, import_react15.useState)(false);
    const domain = brandDomains_default[brand] ?? domainProp;
    if (domain && !error) {
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
        "img",
        {
          src: `https://www.google.com/s2/favicons?sz=64&domain=${domain}`,
          alt: brand,
          style: { width: size, height: size },
          className: BrandCard_default.logo,
          onError: () => setError(true)
        }
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
      "div",
      {
        style: { width: size, height: size },
        className: BrandCard_default.logoFallback,
        children: brand[0]
      }
    );
  }
  function BrandCard({ brand, isLoading = false, onSelect, onSelectMaterial, onDelete }) {
    const { t: t2 } = useTranslation();
    const [confirming, setConfirming] = (0, import_react15.useState)(false);
    if (confirming) {
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { className: BrandCard_default.confirmCard, children: [
        /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("p", { className: BrandCard_default.confirmText, children: t2("brandCard.deleteConfirm", { name: brand.name }) }),
        /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { className: BrandCard_default.confirmBtns, children: [
          /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
            "button",
            {
              onClick: () => {
                onDelete(brand.id);
                setConfirming(false);
              },
              className: BrandCard_default.confirmDelete,
              children: t2("common.delete")
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
            "button",
            {
              onClick: () => setConfirming(false),
              className: BrandCard_default.confirmCancel,
              children: t2("common.cancel")
            }
          )
        ] })
      ] });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { className: BrandCard_default.card, children: [
      /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: BrandCard_default.gradient }),
      /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { className: BrandCard_default.topRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("button", { onClick: () => onSelect(brand.name), className: BrandCard_default.logoBtn, children: [
          /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: BrandCard_default.logoWrap, children: isLoading ? /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: BrandCard_default.logoSkeleton }) : /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(BrandLogo, { brand: brand.name, domain: brand.domain, size: 44 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { className: BrandCard_default.nameWrap, children: [
            /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("p", { className: BrandCard_default.brandName, children: brand.name }),
            isLoading ? /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: BrandCard_default.countSkeleton }) : /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("p", { className: BrandCard_default.brandCount, children: [
              brand.filamentCount,
              " ",
              t2("brandCard.filaments")
            ] })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { className: BrandCard_default.rightCol, children: [
          /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
            "button",
            {
              onClick: () => {
                if (brand.spoolCount === 0) setConfirming(true);
              },
              "aria-label": brand.spoolCount > 0 ? "Remove all spools before deleting" : `Delete ${brand.name}`,
              title: brand.spoolCount > 0 ? "Remove all spools before deleting this brand" : void 0,
              disabled: brand.spoolCount > 0,
              className: `${BrandCard_default.deleteBtn}${brand.spoolCount > 0 ? ` ${BrandCard_default.deleteBtnDisabled}` : ` ${BrandCard_default.deleteBtnEnabled}`}`,
              children: /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("polyline", { points: "3 6 5 6 21 6" }),
                /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("path", { d: "M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6" }),
                /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("path", { d: "M10 11v6M14 11v6" }),
                /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("path", { d: "M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2" })
              ] })
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)(
            "div",
            {
              "aria-hidden": brand.spoolCount === 0,
              className: brand.spoolCount === 0 ? BrandCard_default.hidden : void 0,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("p", { className: BrandCard_default.spoolCount, children: brand.spoolCount }),
                /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("p", { className: BrandCard_default.spoolCountLabel, children: t2("brandCard.inStock") })
              ]
            }
          )
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: BrandCard_default.divider }),
      isLoading ? /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: BrandCard_default.materialsSkeleton, children: [BrandCard_default.skeletonPillLg, BrandCard_default.skeletonPillSm, BrandCard_default.skeletonPillMd].map((widthClass, i) => /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: `${BrandCard_default.materialSkeletonPill} ${widthClass}` }, i)) }) : /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: BrandCard_default.materials, children: brand.materials.map((m) => {
        const inStock = brand.inStockMaterials.includes(m);
        return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
          "button",
          {
            type: "button",
            onClick: () => onSelectMaterial(brand.name, m),
            className: `${BrandCard_default.pill}${inStock ? ` ${BrandCard_default.pillActive}` : ` ${BrandCard_default.pillInactive}`}`,
            children: m
          },
          m
        );
      }) })
    ] });
  }

  // src/utils/colorUtils.ts
  function colorNameToHex(name) {
    if (typeof document === "undefined") return null;
    const canvas = document.createElement("canvas");
    canvas.width = canvas.height = 1;
    const ctx = canvas.getContext("2d");
    if (!ctx) return null;
    const sentinel = "#7f3f1f";
    const candidates = [.../* @__PURE__ */ new Set([name.trim(), name.trim().split(/\s+/).pop() ?? ""])];
    for (const candidate of candidates) {
      if (!candidate) continue;
      ctx.fillStyle = sentinel;
      ctx.fillStyle = candidate;
      const resolved = ctx.fillStyle;
      if (resolved !== sentinel) return resolved;
      if (candidate.toLowerCase().replace(/\s/g, "") === sentinel) return resolved;
    }
    return null;
  }
  function parseDualColors(colorName) {
    const parenMatch = colorName.match(/\(([^)]+)\)/);
    if (parenMatch) {
      const parts = parenMatch[1].split("-").map((s) => s.trim()).filter(Boolean);
      if (parts.length >= 2) {
        const hexes = parts.map((c) => colorNameToHex(c)).filter(Boolean);
        if (hexes.length >= 2) return hexes;
      }
    }
    if (colorName.includes("+")) {
      const parts = colorName.split("+").map((s) => s.trim()).filter(Boolean);
      if (parts.length >= 2) {
        const hexes = parts.map((c) => colorNameToHex(c)).filter(Boolean);
        if (hexes.length >= 2) return hexes;
      }
    }
    const dualPrefix = colorName.match(/^dual\s+colou?r\s+(.+)$/i);
    if (dualPrefix) {
      const parts = dualPrefix[1].trim().split(/\s+/);
      const hexes = parts.map((c) => colorNameToHex(c)).filter(Boolean);
      if (hexes.length >= 2) return hexes;
    }
    const spaceWords = colorName.trim().split(/\s+/);
    if (spaceWords.length >= 2) {
      const hexes = spaceWords.map((c) => colorNameToHex(c)).filter(Boolean);
      if (hexes.length === spaceWords.length) return hexes;
    }
    return null;
  }

  // src/components/FilamentCard/FilamentCard.module.css
  var FilamentCard_default = {
    card: "FilamentCard_card",
    cardBtn: "FilamentCard_cardBtn",
    cardBtnSelected: "FilamentCard_cardBtnSelected",
    inner: "FilamentCard_inner",
    iconWrap: "FilamentCard_iconWrap",
    info: "FilamentCard_info",
    topRow: "FilamentCard_topRow",
    brandRow: "FilamentCard_brandRow",
    brandName: "FilamentCard_brandName",
    filamentName: "FilamentCard_filamentName",
    checkWrap: "FilamentCard_checkWrap"
  };

  // src/components/FilamentCard/FilamentCard.tsx
  var import_jsx_runtime15 = __toESM(require_react_shim(), 1);
  function FilamentCard({ filament, onClick, selected = false }) {
    const navigate = useNavigate();
    const dualColors = filament.colorName ? parseDualColors(filament.colorName) : null;
    const resolvedColor = dualColors ? null : filament.colorHex ?? (filament.colorName ? colorNameToHex(filament.colorName) : null);
    const iconColors = dualColors ?? (resolvedColor === null && (filament.variantColors?.length ?? 0) >= 2 ? filament.variantColors : void 0);
    const iconColor = resolvedColor ?? getMaterialColor(filament.material);
    const brandSlug = encodeURIComponent(filament.brand);
    const colorSlug = encodeURIComponent(filament.colorName ?? filament.filamentName);
    const materialTag = /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
      "button",
      {
        type: "button",
        "aria-label": `View ${filament.brand} filaments`,
        onClick: (e2) => {
          e2.preventDefault();
          e2.stopPropagation();
          navigate(`/filaments/${brandSlug}`);
        },
        children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(MaterialTag, { material: filament.material })
      }
    );
    const innerContent = /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: FilamentCard_default.inner, children: [
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: FilamentCard_default.iconWrap, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(FilamentIcon, { color: iconColor, colors: iconColors, size: 56 }) }),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: FilamentCard_default.info, children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: FilamentCard_default.topRow, children: [
          /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: FilamentCard_default.brandRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(BrandLogo, { brand: filament.brand, size: 16 }),
            /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("p", { className: FilamentCard_default.brandName, children: filament.brand })
          ] }),
          materialTag
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("p", { className: FilamentCard_default.filamentName, children: filament.colorName ?? filament.filamentName })
      ] }),
      selected && /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: FilamentCard_default.checkWrap, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "3", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("polyline", { points: "20 6 9 17 4 12" }) }) })
    ] });
    if (onClick) {
      return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
        "button",
        {
          type: "button",
          onClick,
          className: `${FilamentCard_default.cardBtn}${selected ? ` ${FilamentCard_default.cardBtnSelected}` : ""}`,
          children: innerContent
        }
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
      Link,
      {
        to: `/filaments/${brandSlug}/${colorSlug}`,
        state: { filament },
        className: FilamentCard_default.card,
        children: innerContent
      }
    );
  }

  // src/components/SpoolEditor/SpoolEditor.tsx
  var import_react16 = __toESM(require_react_shim(), 1);

  // src/components/icons/NfcIcon.tsx
  var import_jsx_runtime16 = __toESM(require_react_shim(), 1);
  function NfcIcon({ className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
      "svg",
      {
        className,
        "aria-hidden": true,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        strokeWidth: "1.5",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("line", { x1: "5", y1: "7", x2: "5", y2: "17" }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("path", { d: "M8 9.5a4 4 0 0 1 0 5" }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("path", { d: "M11 8a7 7 0 0 1 0 8" }),
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("path", { d: "M14 6.5a9.5 9.5 0 0 1 0 11" })
        ]
      }
    );
  }

  // src/utils/printerImages.ts
  var imageMap = {
    "bambu lab x1 carbon": "/printers/bambu/X1C.png",
    "bambu lab x1": "/printers/bambu/X1C.png",
    "bambu lab x1e": "/printers/bambu/X1E.png",
    "bambu lab p1s": "/printers/bambu/P1S.png",
    "bambu lab p1p": "/printers/bambu/P1S.png",
    "bambu lab p2s": "/printers/bambu/P2S.png",
    "bambu lab a1": "/printers/bambu/A1.png",
    "bambu lab a1 mini": "/printers/bambu/A1mini.png",
    "bambu lab a2l": "/printers/bambu/a2l.png",
    "bambu lab h2d": "/printers/bambu/H2D.png",
    "bambu lab h2d pro": "/printers/bambu/H2D.png",
    "bambu lab h2c": "/printers/bambu/h2c.png",
    "bambu lab h2s": "/printers/bambu/H2S.png",
    "bambu lab x2d": "/printers/bambu/X2D.png"
  };
  function getPrinterImage(brand, model) {
    const key = `${brand.toLowerCase().trim()} ${model.toLowerCase().trim()}`;
    return imageMap[key] ?? "/printers/bambu/A1.png";
  }

  // src/utils/currency.ts
  var SYMBOLS = {
    USD: "$",
    EUR: "\u20AC",
    GBP: "\xA3",
    CAD: "CA$",
    AUD: "A$",
    CHF: "Fr",
    JPY: "\xA5",
    CNY: "CN\xA5",
    SEK: "SEK",
    TRY: "\u20BA"
  };
  function getCurrencySymbol(code) {
    return SYMBOLS[code] ?? code;
  }

  // src/components/SpoolEditor/SpoolEditor.module.css
  var SpoolEditor_default = {
    shake: "SpoolEditor_shake",
    form: "SpoolEditor_form",
    card: "SpoolEditor_card",
    section: "SpoolEditor_section",
    sectionHeader: "SpoolEditor_sectionHeader",
    header: "SpoolEditor_header",
    headerColorOverlay: "SpoolEditor_headerColorOverlay",
    headerRow: "SpoolEditor_headerRow",
    headerInfo: "SpoolEditor_headerInfo",
    headerMeta: "SpoolEditor_headerMeta",
    headerBrand: "SpoolEditor_headerBrand",
    headerBadges: "SpoolEditor_headerBadges",
    headerTitle: "SpoolEditor_headerTitle",
    headerHex: "SpoolEditor_headerHex",
    brandLogo: "SpoolEditor_brandLogo",
    brandFallback: "SpoolEditor_brandFallback",
    mainGrid: "SpoolEditor_mainGrid",
    statsArea: "SpoolEditor_statsArea",
    rightColWrap: "SpoolEditor_rightColWrap",
    box: "SpoolEditor_box",
    boxEditing: "SpoolEditor_boxEditing",
    boxLabel: "SpoolEditor_boxLabel",
    boxInput: "SpoolEditor_boxInput",
    boxInputError: "SpoolEditor_boxInputError",
    boxInputValid: "SpoolEditor_boxInputValid",
    boxError: "SpoolEditor_boxError",
    boxLabelRow: "SpoolEditor_boxLabelRow",
    editLabel: "SpoolEditor_editLabel",
    boxEditBtn: "SpoolEditor_boxEditBtn",
    boxReadonly: "SpoolEditor_boxReadonly",
    activeBox: "SpoolEditor_activeBox",
    activeRow: "SpoolEditor_activeRow",
    activeBoxOn: "SpoolEditor_activeBoxOn",
    toggleLabel: "SpoolEditor_toggleLabel",
    toggleSub: "SpoolEditor_toggleSub",
    toggle: "SpoolEditor_toggle",
    toggleOn: "SpoolEditor_toggleOn",
    toggleThumb: "SpoolEditor_toggleThumb",
    toggleThumbOn: "SpoolEditor_toggleThumbOn",
    priceBox: "SpoolEditor_priceBox",
    boxInputWrap: "SpoolEditor_boxInputWrap",
    boxUnit: "SpoolEditor_boxUnit",
    newLocRow: "SpoolEditor_newLocRow",
    newLocInput: "SpoolEditor_newLocInput",
    newLocSave: "SpoolEditor_newLocSave",
    newLocCancel: "SpoolEditor_newLocCancel",
    printerBox: "SpoolEditor_printerBox",
    printerBoxError: "SpoolEditor_printerBoxError",
    printerLabel: "SpoolEditor_printerLabel",
    printerLabelError: "SpoolEditor_printerLabelError",
    printerSelect: "SpoolEditor_printerSelect",
    printerSelectError: "SpoolEditor_printerSelectError",
    printerDetail: "SpoolEditor_printerDetail",
    printerImg: "SpoolEditor_printerImg",
    printerImgSrc: "SpoolEditor_printerImgSrc",
    printerMeta: "SpoolEditor_printerMeta",
    printerName: "SpoolEditor_printerName",
    printerBrandLabel: "SpoolEditor_printerBrandLabel",
    amsLabel: "SpoolEditor_amsLabel",
    amsLabelError: "SpoolEditor_amsLabelError",
    amsTray: "SpoolEditor_amsTray",
    amsSlotError: "SpoolEditor_amsSlotError",
    amsBtn: "SpoolEditor_amsBtn",
    amsBtnSelected: "SpoolEditor_amsBtnSelected",
    trayText: "SpoolEditor_trayText",
    directSpool: "SpoolEditor_directSpool",
    nfcBadge: "SpoolEditor_nfcBadge",
    nfcBadgeText: "SpoolEditor_nfcBadgeText",
    nfcIcon: "SpoolEditor_nfcIcon",
    nfcBox: "SpoolEditor_nfcBox",
    nfcReadonly: "SpoolEditor_nfcReadonly",
    nfcNone: "SpoolEditor_nfcNone",
    nfcEditRow: "SpoolEditor_nfcEditRow",
    nfcInput: "SpoolEditor_nfcInput",
    nfcSaveBtn: "SpoolEditor_nfcSaveBtn",
    nfcCancelBtn: "SpoolEditor_nfcCancelBtn",
    specBox: "SpoolEditor_specBox",
    specBoxEditing: "SpoolEditor_specBoxEditing",
    specLabelRow: "SpoolEditor_specLabelRow",
    specLabel: "SpoolEditor_specLabel",
    specEditBtn: "SpoolEditor_specEditBtn",
    specValue: "SpoolEditor_specValue",
    specInput: "SpoolEditor_specInput",
    rangeInputRow: "SpoolEditor_rangeInputRow",
    rangeInput: "SpoolEditor_rangeInput",
    rangeSep: "SpoolEditor_rangeSep",
    rangeUnit: "SpoolEditor_rangeUnit",
    bottomGrid: "SpoolEditor_bottomGrid",
    bottomDivider: "SpoolEditor_bottomDivider",
    bottomPanel: "SpoolEditor_bottomPanel",
    specGrid2: "SpoolEditor_specGrid2",
    specStack: "SpoolEditor_specStack",
    textarea: "SpoolEditor_textarea",
    submitError: "SpoolEditor_submitError",
    footerRow: "SpoolEditor_footerRow",
    btnCancel: "SpoolEditor_btnCancel",
    btnSave: "SpoolEditor_btnSave"
  };

  // src/components/SpoolEditor/SpoolEditor.tsx
  var import_jsx_runtime17 = __toESM(require_react_shim(), 1);
  var BRAND_DOMAINS2 = {
    "Bambu Lab": "bambulab.com",
    "eSUN 3D": "esun3d.com",
    "Prusament": "prusament.com",
    "Polymaker": "polymaker.com",
    "Hatchbox": "hatchbox3d.com",
    "SUNLU": "sunlu.com",
    "Creality": "creality.com",
    "ELEGOO": "elegoo.com",
    "Overture": "overture3d.com",
    "PolyLite": "polymaker.com",
    "colorFabb": "colorfabb.com",
    "Fillamentum": "fillamentum.com",
    "Fiberlogy": "fiberlogy.com",
    "extrudr": "extrudr.com",
    "Das Filament": "dasfilament.de",
    "Protopasta": "proto-pasta.com",
    "MatterHackers": "matterhackers.com",
    "NinjaTek": "ninjatek.com",
    "Atomic Filament": "atomicfilament.com",
    "Inland": "microcenter.com",
    "JAYO": "jayofilament.com",
    "TINMORRY": "tinmorry.com",
    "AzureFilm": "azurefilm.hr",
    "Spectrum": "spectrumfilaments.com",
    "Devil Design": "devildesign.pl",
    "Anycubic": "anycubic.com"
  };
  function BrandFavicon({ brand }) {
    const [error, setError] = (0, import_react16.useState)(false);
    const domain = BRAND_DOMAINS2[brand];
    if (domain && !error) {
      return /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
        "img",
        {
          src: `https://www.google.com/s2/favicons?sz=64&domain=${domain}`,
          alt: brand,
          className: SpoolEditor_default.brandLogo,
          onError: () => setError(true)
        }
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: SpoolEditor_default.brandFallback, children: brand[0] });
  }
  function SpecBox({ label, value }) {
    if (!value) return null;
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.specBox, children: [
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.specLabel, children: label }),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.specValue, children: value })
    ] });
  }
  function EditableSpecBox({ label, value, unit, onChange }) {
    const [editing, setEditing] = (0, import_react16.useState)(false);
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: `${SpoolEditor_default.specBox}${editing ? ` ${SpoolEditor_default.specBoxEditing}` : ""}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.specLabelRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.specLabel, children: label }),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("button", { type: "button", className: SpoolEditor_default.specEditBtn, onClick: () => setEditing((e2) => !e2), title: editing ? "Lock" : "Edit", children: editing ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(LockIcon, {}) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(PenIcon, {}) })
      ] }),
      editing ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("input", { type: "number", step: "1", value, onChange: (e2) => onChange(e2.target.value), className: SpoolEditor_default.specInput, autoFocus: true }) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.specValue, children: value !== "" ? `${value}${unit ? ` ${unit}` : ""}` : "\u2014" })
    ] });
  }
  function RangeEditableBox({ label, minVal, maxVal, unit, onMinChange, onMaxChange }) {
    const [editing, setEditing] = (0, import_react16.useState)(false);
    const isEditable = !!onMinChange;
    const hasMin = minVal !== "";
    const hasMax = maxVal !== "";
    const rangeText = hasMin && hasMax ? `${minVal}\u2013${maxVal}${unit ? ` ${unit}` : ""}` : hasMin ? `${minVal}${unit ? ` ${unit}` : ""}` : hasMax ? `${maxVal}${unit ? ` ${unit}` : ""}` : "\u2014";
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: `${SpoolEditor_default.specBox}${editing ? ` ${SpoolEditor_default.specBoxEditing}` : ""}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.specLabelRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.specLabel, children: label }),
        isEditable && /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("button", { type: "button", className: SpoolEditor_default.specEditBtn, onClick: () => setEditing((e2) => !e2), title: editing ? "Lock" : "Edit", children: editing ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(LockIcon, {}) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(PenIcon, {}) })
      ] }),
      isEditable && editing ? /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.rangeInputRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("input", { type: "number", step: "1", value: minVal, onChange: (e2) => onMinChange(e2.target.value), className: SpoolEditor_default.rangeInput, placeholder: "min", autoFocus: true }),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: SpoolEditor_default.rangeSep, children: "\u2013" }),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("input", { type: "number", step: "1", value: maxVal, onChange: (e2) => onMaxChange(e2.target.value), className: SpoolEditor_default.rangeInput, placeholder: "max" }),
        unit && /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: SpoolEditor_default.rangeUnit, children: unit })
      ] }) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.specValue, children: rangeText })
    ] });
  }
  function EditableWeightBox({ label, value, onChange, editing, onToggleEdit, errorText }) {
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: `${SpoolEditor_default.box}${editing ? ` ${SpoolEditor_default.boxEditing}` : ""}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.boxLabelRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: SpoolEditor_default.editLabel, children: label }),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("button", { type: "button", className: SpoolEditor_default.boxEditBtn, onClick: onToggleEdit, title: editing ? "Lock" : "Edit", children: editing ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(LockIcon, {}) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(PenIcon, {}) })
      ] }),
      editing ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("input", { type: "number", value, onChange: (e2) => onChange(e2.target.value), className: SpoolEditor_default.boxInput, autoFocus: true }) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.boxReadonly, children: value !== "" ? `${value} g` : "\u2014" }),
      errorText && /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.boxError, children: errorText })
    ] });
  }
  function PrinterAssignSection({
    printers,
    printerId,
    onPrinterChange,
    amsSlot,
    onAmsSlotChange,
    occupiedSlots,
    currentSpoolColor,
    disabled,
    printerError,
    printerShakeKey,
    onPrinterShakeEnd,
    amsSlotError,
    onBusyTrayClick
  }) {
    const { t: t2 } = useTranslation();
    const selected = printers.find((p) => p.id === printerId);
    const showAms = selected?.hasAms && onAmsSlotChange !== null;
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)(
      "div",
      {
        className: `${SpoolEditor_default.printerBox}${printerError ? ` ${SpoolEditor_default.printerBoxError} ${SpoolEditor_default.shake}` : ""}`,
        onAnimationEnd: onPrinterShakeEnd,
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("label", { className: `${SpoolEditor_default.printerLabel}${printerError ? ` ${SpoolEditor_default.printerLabelError}` : ""}`, children: printerError ? t2("spoolForm.assignPrinterWarning") : t2("spoolForm.assignedPrinter") }),
          /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)(
            "select",
            {
              value: printerId ?? "",
              onChange: (e2) => onPrinterChange(e2.target.value || null),
              disabled,
              className: `${SpoolEditor_default.printerSelect}${printerError ? ` ${SpoolEditor_default.printerSelectError}` : ""}`,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("option", { value: "", children: t2("spoolForm.noPrinter") }),
                printers.map((p) => /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("option", { value: p.id, children: [
                  p.name,
                  " \xB7 ",
                  p.brand,
                  " ",
                  p.model
                ] }, p.id))
              ]
            }
          ),
          selected && /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.printerDetail, children: [
            /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("div", { className: SpoolEditor_default.printerImg, children: /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
              "img",
              {
                src: getPrinterImage(selected.brand, selected.model),
                alt: `${selected.brand} ${selected.model}`,
                className: SpoolEditor_default.printerImgSrc,
                onError: (e2) => {
                  e2.currentTarget.src = "/printers/generic.svg";
                }
              }
            ) }),
            /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.printerMeta, children: [
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.printerName, children: selected.name }),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.printerBrandLabel, children: selected.brand }),
              showAms ? /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)(import_jsx_runtime17.Fragment, { children: [
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: `${SpoolEditor_default.amsLabel}${amsSlotError ? ` ${SpoolEditor_default.amsLabelError}` : ""}`, children: amsSlotError ? t2("spoolForm.selectTrayWarning") : t2("spoolForm.amsTray") }),
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("div", { className: `${SpoolEditor_default.amsTray}${amsSlotError ? ` ${SpoolEditor_default.amsSlotError}` : ""}`, children: [1, 2, 3, 4].map((slotNum) => {
                  const occupant = occupiedSlots[slotNum];
                  const isSel = amsSlot === slotNum;
                  const color = isSel ? currentSpoolColor : occupant?.colorHex;
                  return /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                    "button",
                    {
                      type: "button",
                      title: isSel ? "This spool" : occupant ? `${occupant.colorName} \u2014 ${occupant.brand} ${occupant.material}` : `Tray ${slotNum} \u2014 empty`,
                      onClick: () => {
                        if (isSel) {
                          onAmsSlotChange(null);
                          return;
                        }
                        if (occupiedSlots[slotNum] && onBusyTrayClick) {
                          onBusyTrayClick(slotNum);
                          return;
                        }
                        onAmsSlotChange(slotNum);
                      },
                      disabled,
                      style: color ? { backgroundColor: color } : void 0,
                      className: `${SpoolEditor_default.amsBtn}${isSel ? ` ${SpoolEditor_default.amsBtnSelected}` : ""}`,
                      children: /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: color ? SpoolEditor_default.trayText : void 0, children: slotNum })
                    },
                    slotNum
                  );
                }) })
              ] }) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: SpoolEditor_default.directSpool, children: t2("spoolForm.directSpool") })
            ] })
          ] })
        ]
      },
      printerShakeKey
    );
  }
  function SpoolEditor({
    brand,
    material,
    colorName,
    colorHex,
    tagUid,
    extruderMin,
    extruderMax,
    bedMin,
    bedMax,
    density,
    diameterTolerance,
    currentWeightG,
    setCurrentWeightG,
    currentWeightShake,
    currentWeightStatus,
    initialWeightG,
    setInitialWeightG,
    spoolWeightG,
    setSpoolWeightG,
    lowStockThresholdG,
    setLowStockThresholdG,
    weightError,
    setWeightError,
    notes,
    setNotes,
    stockLocation,
    setStockLocation,
    stockLocationOptions = [],
    price,
    setPrice,
    currency = "USD",
    isActive,
    setIsActive,
    printers,
    printerId,
    setPrinterId,
    amsSlot,
    onAmsSlotChange,
    occupiedSlots,
    currentSpoolColor,
    printerError,
    printerShakeKey,
    onPrinterShakeEnd,
    amsSlotError,
    onBusyTrayClick,
    stockLocationError,
    stockLocationShakeKey,
    onStockLocationShakeEnd,
    submitting,
    submitError,
    onSubmit,
    onCancel,
    onExtruderMinChange,
    onExtruderMaxChange,
    onBedMinChange,
    onBedMaxChange,
    onDensityChange,
    onDiameterToleranceChange
  }) {
    const { t: t2 } = useTranslation();
    const [editInitial, setEditInitial] = (0, import_react16.useState)(false);
    const [editSpool, setEditSpool] = (0, import_react16.useState)(false);
    const [editLowStock, setEditLowStock] = (0, import_react16.useState)(false);
    const [addingLoc, setAddingLoc] = (0, import_react16.useState)(false);
    const [newLocName, setNewLocName] = (0, import_react16.useState)("");
    const [savingLoc, setSavingLoc] = (0, import_react16.useState)(false);
    const [extraLocations, setExtraLocations] = (0, import_react16.useState)([]);
    const newLocInputRef = (0, import_react16.useRef)(null);
    const allLocOptions = [.../* @__PURE__ */ new Set([...stockLocationOptions, ...extraLocations])];
    async function handleSaveLoc() {
      const name = newLocName.trim();
      if (!name || savingLoc) return;
      setSavingLoc(true);
      try {
        await locationsApi.add({ name });
        setExtraLocations((prev) => [...prev, name]);
        setStockLocation(name);
        setAddingLoc(false);
        setNewLocName("");
      } finally {
        setSavingLoc(false);
      }
    }
    const isEditMode = onExtruderMinChange !== void 0;
    const showPrintPanel = isEditMode || extruderMin != null || extruderMax != null || bedMin != null || bedMax != null;
    const showMaterialPanel = isEditMode || density != null || diameterTolerance != null;
    const showBottomSection = showPrintPanel || showMaterialPanel;
    const cwInputClass = `${SpoolEditor_default.boxInput}${currentWeightStatus === "error" ? ` ${SpoolEditor_default.boxInputError}` : currentWeightStatus === "valid" ? ` ${SpoolEditor_default.boxInputValid}` : ""}`;
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("form", { onSubmit, noValidate: true, className: SpoolEditor_default.form, children: [
      /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.card, children: [
        /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.header, children: [
          /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("div", { className: SpoolEditor_default.headerColorOverlay, style: { backgroundColor: colorHex ?? void 0 } }),
          /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.headerRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(SpoolIcon, { color: colorHex ?? "#888", size: 80 }),
            /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.headerInfo, children: [
              /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.headerMeta, children: [
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(BrandFavicon, { brand }),
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: SpoolEditor_default.headerBrand, children: brand })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.headerTitle, children: colorName }),
              colorHex && /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.headerHex, children: colorHex })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.headerBadges, children: [
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(MaterialTag, { material }),
              tagUid && /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.nfcBadge, children: [
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(NfcIcon, { className: SpoolEditor_default.nfcIcon }),
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: SpoolEditor_default.nfcBadgeText, children: t2("addSpool.nfcTag") })
              ] })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.section, children: [
          /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.sectionHeader, children: t2("spoolForm.sectionSpoolStats") }),
          /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.mainGrid, children: [
            /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.statsArea, children: [
              /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: `${SpoolEditor_default.box} ${SpoolEditor_default.boxEditing}${currentWeightShake ? ` ${SpoolEditor_default.shake}` : ""}`, children: [
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("label", { className: SpoolEditor_default.boxLabel, children: t2("spoolDetail.currentWeightG") }),
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                  "input",
                  {
                    type: "number",
                    value: currentWeightG,
                    onChange: (e2) => setCurrentWeightG(e2.target.value),
                    className: cwInputClass
                  }
                )
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.priceBox, children: [
                /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("label", { className: SpoolEditor_default.boxLabel, children: [
                  t2("spoolForm.price"),
                  " (",
                  getCurrencySymbol(currency),
                  ")"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.boxInputWrap, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                    "input",
                    {
                      type: "number",
                      min: 0,
                      step: 0.01,
                      value: price,
                      onChange: (e2) => setPrice(e2.target.value),
                      placeholder: "0.00",
                      className: SpoolEditor_default.boxInput
                    }
                  ),
                  /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: SpoolEditor_default.boxUnit, children: getCurrencySymbol(currency) })
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: `${SpoolEditor_default.activeBox}${isActive ? ` ${SpoolEditor_default.activeBoxOn}` : ""}`, children: [
                /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.toggleLabel, children: t2("spoolForm.active") }),
                /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.activeRow, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.toggleSub, children: t2("spoolForm.loadedSpool") }),
                  /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                    "button",
                    {
                      type: "button",
                      onClick: () => setIsActive(!isActive),
                      className: `${SpoolEditor_default.toggle}${isActive ? ` ${SpoolEditor_default.toggleOn}` : ""}`,
                      children: /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("span", { className: `${SpoolEditor_default.toggleThumb}${isActive ? ` ${SpoolEditor_default.toggleThumbOn}` : ""}` })
                    }
                  )
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                EditableWeightBox,
                {
                  label: t2("spoolDetail.initialWeightG"),
                  value: initialWeightG,
                  onChange: (v) => {
                    setInitialWeightG(v);
                    setWeightError("");
                  },
                  editing: editInitial,
                  onToggleEdit: () => setEditInitial((e2) => !e2),
                  errorText: weightError || void 0
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                EditableWeightBox,
                {
                  label: t2("spoolDetail.spoolWeightG"),
                  value: spoolWeightG,
                  onChange: setSpoolWeightG,
                  editing: editSpool,
                  onToggleEdit: () => setEditSpool((e2) => !e2)
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                EditableWeightBox,
                {
                  label: t2("spoolDetail.lowStockAtG"),
                  value: lowStockThresholdG,
                  onChange: setLowStockThresholdG,
                  editing: editLowStock,
                  onToggleEdit: () => setEditLowStock((e2) => !e2)
                }
              )
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("div", { className: SpoolEditor_default.rightColWrap, children: setPrinterId && isActive ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
              PrinterAssignSection,
              {
                printers: printers ?? [],
                printerId: printerId ?? null,
                onPrinterChange: (v) => {
                  setPrinterId(v);
                  if (!v) onAmsSlotChange?.(null);
                },
                amsSlot: amsSlot ?? null,
                onAmsSlotChange: onAmsSlotChange ?? null,
                occupiedSlots: occupiedSlots ?? {},
                currentSpoolColor,
                disabled: submitting,
                printerError,
                printerShakeKey,
                onPrinterShakeEnd,
                amsSlotError,
                onBusyTrayClick
              }
            ) : !isActive ? /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)(
              "div",
              {
                className: `${SpoolEditor_default.printerBox}${stockLocationError ? ` ${SpoolEditor_default.printerBoxError} ${SpoolEditor_default.shake}` : ""}`,
                onAnimationEnd: onStockLocationShakeEnd,
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("label", { className: `${SpoolEditor_default.printerLabel}${stockLocationError ? ` ${SpoolEditor_default.printerLabelError}` : ""}`, children: stockLocationError ? t2("spoolForm.stockLocationRequired") : t2("spoolForm.stockLocation") }),
                  addingLoc ? /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.newLocRow, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                      "input",
                      {
                        ref: newLocInputRef,
                        type: "text",
                        value: newLocName,
                        onChange: (e2) => setNewLocName(e2.target.value),
                        onKeyDown: (e2) => {
                          if (e2.key === "Enter") {
                            e2.preventDefault();
                            handleSaveLoc();
                          }
                          if (e2.key === "Escape") {
                            setAddingLoc(false);
                            setNewLocName("");
                          }
                        },
                        placeholder: t2("locations.namePlaceholder"),
                        className: SpoolEditor_default.newLocInput,
                        autoFocus: true
                      }
                    ),
                    /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("button", { type: "button", className: SpoolEditor_default.newLocSave, onClick: handleSaveLoc, disabled: savingLoc || !newLocName.trim(), children: /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("polyline", { points: "20 6 9 17 4 12" }) }) }),
                    /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("button", { type: "button", className: SpoolEditor_default.newLocCancel, onClick: () => {
                      setAddingLoc(false);
                      setNewLocName("");
                    }, children: /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: [
                      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
                      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
                    ] }) })
                  ] }) : /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)(
                    "select",
                    {
                      value: stockLocation,
                      onChange: (e2) => {
                        if (e2.target.value === "__new__") {
                          setAddingLoc(true);
                          setNewLocName("");
                        } else setStockLocation(e2.target.value);
                      },
                      className: `${SpoolEditor_default.printerSelect}${stockLocationError ? ` ${SpoolEditor_default.printerSelectError}` : ""}`,
                      children: [
                        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("option", { value: "", children: t2("spoolForm.selectStorage") }),
                        allLocOptions.map((loc) => /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("option", { value: loc, children: loc }, loc)),
                        /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("option", { value: "__new__", children: [
                          "\uFF0B ",
                          t2("locations.addLocation")
                        ] })
                      ]
                    }
                  )
                ]
              },
              stockLocationShakeKey
            ) : null })
          ] })
        ] }),
        showBottomSection && /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("div", { className: SpoolEditor_default.section, children: /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.bottomGrid, children: [
          showPrintPanel && /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.bottomPanel, children: [
            /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.sectionHeader, children: t2("spoolForm.sectionPrintSettings") }),
            /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.specGrid2, children: [
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                RangeEditableBox,
                {
                  label: t2("spoolForm.extruderRange"),
                  minVal: extruderMin != null ? String(extruderMin) : "",
                  maxVal: extruderMax != null ? String(extruderMax) : "",
                  unit: "\xB0C",
                  onMinChange: onExtruderMinChange,
                  onMaxChange: onExtruderMaxChange
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
                RangeEditableBox,
                {
                  label: t2("spoolForm.bedRange"),
                  minVal: bedMin != null ? String(bedMin) : "",
                  maxVal: bedMax != null ? String(bedMax) : "",
                  unit: "\xB0C",
                  onMinChange: onBedMinChange,
                  onMaxChange: onBedMaxChange
                }
              )
            ] })
          ] }),
          showPrintPanel && showMaterialPanel && /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("div", { className: SpoolEditor_default.bottomDivider }),
          showMaterialPanel && /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.bottomPanel, children: [
            /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.sectionHeader, children: t2("spoolForm.sectionMaterialProperties") }),
            /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.specGrid2, children: [
              onDensityChange ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(EditableSpecBox, { label: t2("spoolForm.density"), value: density != null ? String(density) : "", unit: "g/cm\xB3", onChange: onDensityChange }) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(SpecBox, { label: t2("spoolForm.density"), value: density != null ? `${density} g/cm\xB3` : null }),
              onDiameterToleranceChange ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(EditableSpecBox, { label: t2("spoolForm.diameterTolerance"), value: diameterTolerance != null ? String(diameterTolerance) : "", unit: "mm", onChange: onDiameterToleranceChange }) : /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(SpecBox, { label: t2("spoolForm.diameterTolerance"), value: diameterTolerance != null ? `\xB1${diameterTolerance} mm` : null })
            ] })
          ] })
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.section, children: [
          /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.sectionHeader, children: t2("spoolForm.sectionNotes") }),
          /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
            "textarea",
            {
              rows: 3,
              value: notes,
              onChange: (e2) => setNotes(e2.target.value),
              placeholder: t2("common.notesPlaceholder"),
              className: SpoolEditor_default.textarea
            }
          )
        ] })
      ] }),
      submitError && /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: SpoolEditor_default.submitError, children: submitError }),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: SpoolEditor_default.footerRow, children: [
        onCancel && /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("button", { type: "button", onClick: onCancel, className: SpoolEditor_default.btnCancel, children: t2("common.cancel") }),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("button", { type: "submit", disabled: submitting, className: SpoolEditor_default.btnSave, children: submitting ? t2("common.saving") : t2("common.save") })
      ] })
    ] });
  }

  // src/components/AmsConflictModal/AmsConflictModal.tsx
  var import_react_dom = __toESM(require_react_dom_shim(), 1);

  // src/components/AmsConflictModal/AmsConflictModal.module.css
  var AmsConflictModal_default = {
    overlay: "AmsConflictModal_overlay",
    modal: "AmsConflictModal_modal",
    imgArea: "AmsConflictModal_imgArea",
    img: "AmsConflictModal_img",
    body: "AmsConflictModal_body",
    printerMeta: "AmsConflictModal_printerMeta",
    title: "AmsConflictModal_title",
    subtitle: "AmsConflictModal_subtitle",
    spoolPreview: "AmsConflictModal_spoolPreview",
    spoolName: "AmsConflictModal_spoolName",
    spoolMeta: "AmsConflictModal_spoolMeta",
    spoolInfo: "AmsConflictModal_spoolInfo",
    btnRow: "AmsConflictModal_btnRow",
    btnCancel: "AmsConflictModal_btnCancel",
    btnConfirm: "AmsConflictModal_btnConfirm"
  };

  // src/components/AmsConflictModal/AmsConflictModal.tsx
  var import_jsx_runtime18 = __toESM(require_react_shim(), 1);
  function AmsConflictModal({
    printerImgSrc,
    printerBrand,
    printerModel,
    traySlot,
    occupantSpool,
    onCancel,
    onConfirm
  }) {
    const { t: t2 } = useTranslation();
    return (0, import_react_dom.createPortal)(
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("div", { className: AmsConflictModal_default.overlay, onClick: onCancel, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("div", { className: AmsConflictModal_default.modal, onClick: (e2) => e2.stopPropagation(), children: [
        /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("div", { className: AmsConflictModal_default.imgArea, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
          "img",
          {
            src: printerImgSrc,
            alt: "",
            className: AmsConflictModal_default.img,
            onError: (e2) => {
              e2.currentTarget.src = "/printers/generic.svg";
            }
          }
        ) }),
        /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("div", { className: AmsConflictModal_default.body, children: [
          (printerBrand || printerModel) && /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("p", { className: AmsConflictModal_default.printerMeta, children: [printerBrand, printerModel].filter(Boolean).join(" \xB7 ") }),
          /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("p", { className: AmsConflictModal_default.title, children: traySlot != null ? t2("amsConflict.trayInUse", { slot: traySlot }) : t2("amsConflict.printerInUse") }),
          /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("p", { className: AmsConflictModal_default.subtitle, children: traySlot != null ? t2("amsConflict.trayOccupied") : t2("amsConflict.printerOccupied") }),
          /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("div", { className: AmsConflictModal_default.spoolPreview, children: [
            /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(SpoolIcon, { color: occupantSpool.colorHex, size: 44 }),
            /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("div", { className: AmsConflictModal_default.spoolInfo, children: [
              /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("p", { className: AmsConflictModal_default.spoolName, children: occupantSpool.colorName }),
              /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("p", { className: AmsConflictModal_default.spoolMeta, children: [
                occupantSpool.brand,
                " \xB7 ",
                occupantSpool.material
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("div", { className: AmsConflictModal_default.btnRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("button", { onClick: onCancel, className: AmsConflictModal_default.btnCancel, children: t2("common.cancel") }),
            /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("button", { onClick: onConfirm, className: AmsConflictModal_default.btnConfirm, children: t2("amsConflict.reassignTray") })
          ] })
        ] })
      ] }) }),
      document.body
    );
  }

  // src/utils/materialDefaults.ts
  var MATERIAL_TEMP_DEFAULTS = {
    PETG: { extruderMin: 220, extruderMax: 250, bedMin: 70, bedMax: 85 },
    HIPS: { extruderMin: 220, extruderMax: 240, bedMin: 85, bedMax: 110 },
    PLA: { extruderMin: 190, extruderMax: 230, bedMin: 35, bedMax: 60 },
    ABS: { extruderMin: 230, extruderMax: 260, bedMin: 85, bedMax: 110 },
    ASA: { extruderMin: 240, extruderMax: 260, bedMin: 85, bedMax: 110 },
    TPU: { extruderMin: 200, extruderMax: 240, bedMin: 30, bedMax: 60 },
    PVA: { extruderMin: 185, extruderMax: 210, bedMin: 45, bedMax: 60 },
    PA: { extruderMin: 260, extruderMax: 280, bedMin: 70, bedMax: 90 },
    PC: { extruderMin: 260, extruderMax: 280, bedMin: 100, bedMax: 120 }
  };
  function getMaterialDefaults(material) {
    const m = material.toUpperCase();
    if (MATERIAL_TEMP_DEFAULTS[m]) return MATERIAL_TEMP_DEFAULTS[m];
    const sortedKeys = Object.keys(MATERIAL_TEMP_DEFAULTS).sort((a, b) => b.length - a.length);
    for (const key of sortedKeys) {
      if (m.startsWith(key)) return MATERIAL_TEMP_DEFAULTS[key];
    }
    return null;
  }

  // src/components/AddSpoolForm/AddSpoolForm.module.css
  var AddSpoolForm_default = {
    wrap: "AddSpoolForm_wrap",
    card: "AddSpoolForm_card",
    cardHeader: "AddSpoolForm_cardHeader",
    title: "AddSpoolForm_title",
    subtitle: "AddSpoolForm_subtitle",
    nfcBadge: "AddSpoolForm_nfcBadge",
    nfcBadgeText: "AddSpoolForm_nfcBadgeText",
    profileSelect: "AddSpoolForm_profileSelect",
    filterGrid: "AddSpoolForm_filterGrid",
    filterCount: "AddSpoolForm_filterCount",
    label: "AddSpoolForm_label",
    select: "AddSpoolForm_select",
    brandFlash: "AddSpoolForm_brandFlash",
    selectHighlight: "AddSpoolForm_selectHighlight",
    addBrandOption: "AddSpoolForm_addBrandOption",
    dotBlink: "AddSpoolForm_dotBlink",
    addBrandPolling: "AddSpoolForm_addBrandPolling",
    dots: "AddSpoolForm_dots",
    inlineBrandSearch: "AddSpoolForm_inlineBrandSearch",
    inlineBrandInput: "AddSpoolForm_inlineBrandInput",
    inlineBrandSpinner: "AddSpoolForm_inlineBrandSpinner",
    spin: "AddSpoolForm_spin",
    inlineBrandDropdown: "AddSpoolForm_inlineBrandDropdown",
    inlineBrandResult: "AddSpoolForm_inlineBrandResult",
    inlineBrandMeta: "AddSpoolForm_inlineBrandMeta",
    inlineBrandError: "AddSpoolForm_inlineBrandError",
    inlineBrandClose: "AddSpoolForm_inlineBrandClose",
    selectChevron: "AddSpoolForm_selectChevron",
    colorTrigger: "AddSpoolForm_colorTrigger",
    colorTriggerDisabled: "AddSpoolForm_colorTriggerDisabled",
    colorDot: "AddSpoolForm_colorDot",
    colorDotEmpty: "AddSpoolForm_colorDotEmpty",
    colorTriggerLabel: "AddSpoolForm_colorTriggerLabel",
    colorTriggerPlaceholder: "AddSpoolForm_colorTriggerPlaceholder",
    colorSelectWrap: "AddSpoolForm_colorSelectWrap",
    colorTriggerChevron: "AddSpoolForm_colorTriggerChevron",
    colorDropdown: "AddSpoolForm_colorDropdown",
    colorOption: "AddSpoolForm_colorOption",
    colorOptionLabel: "AddSpoolForm_colorOptionLabel",
    successIcon: "AddSpoolForm_successIcon",
    successBtns: "AddSpoolForm_successBtns",
    btnAddAnother: "AddSpoolForm_btnAddAnother",
    btnDone: "AddSpoolForm_btnDone",
    filterEnabled: "AddSpoolForm_filterEnabled",
    filterDisabled: "AddSpoolForm_filterDisabled",
    filamentScroll: "AddSpoolForm_filamentScroll",
    filamentGrid: "AddSpoolForm_filamentGrid"
  };

  // src/components/AddSpoolForm/AddSpoolForm.tsx
  var import_jsx_runtime19 = __toESM(require_react_shim(), 1);
  function ColorSelect({ colors, value, onChange, disabled = false }) {
    const { t: t2 } = useTranslation();
    const [open, setOpen] = (0, import_react17.useState)(false);
    const ref = (0, import_react17.useRef)(null);
    const selected = colors.find((f) => f.colorName === value);
    (0, import_react17.useEffect)(() => {
      function onOutside(e2) {
        if (ref.current && !ref.current.contains(e2.target)) setOpen(false);
      }
      document.addEventListener("mousedown", onOutside);
      return () => document.removeEventListener("mousedown", onOutside);
    }, []);
    return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { ref, className: AddSpoolForm_default.colorSelectWrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
        "button",
        {
          type: "button",
          onClick: () => !disabled && setOpen((o) => !o),
          className: `${AddSpoolForm_default.colorTrigger}${disabled ? ` ${AddSpoolForm_default.colorTriggerDisabled}` : ""}`,
          children: [
            selected?.colorHex ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: AddSpoolForm_default.colorDot, style: { backgroundColor: selected.colorHex } }) : /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: AddSpoolForm_default.colorDotEmpty }),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: `${AddSpoolForm_default.colorTriggerLabel}${!selected ? ` ${AddSpoolForm_default.colorTriggerPlaceholder}` : ""}`, children: selected ? selected.colorName : t2("addSpool.selectColor") }),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("svg", { className: AddSpoolForm_default.colorTriggerChevron, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("polyline", { points: "6 9 12 15 18 9" }) })
          ]
        }
      ),
      open && /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: AddSpoolForm_default.colorDropdown, children: colors.map((f) => /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
        "button",
        {
          type: "button",
          onClick: () => {
            onChange(f.colorName ?? "");
            setOpen(false);
          },
          className: AddSpoolForm_default.colorOption,
          children: [
            f.colorHex ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: AddSpoolForm_default.colorDot, style: { backgroundColor: f.colorHex } }) : /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: AddSpoolForm_default.colorDotEmpty }),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: AddSpoolForm_default.colorOptionLabel, children: f.colorName })
          ]
        },
        f.colorName
      )) })
    ] });
  }
  function AddSpoolForm({ tagUid, onFilamentSectionVisible, onSpoolEditorVisible }) {
    const navigate = useNavigate();
    const { t: t2 } = useTranslation();
    const [filaments, setFilaments] = (0, import_react17.useState)([]);
    const [printers, setPrinters] = (0, import_react17.useState)([]);
    const [allSpools, setAllSpools] = (0, import_react17.useState)([]);
    const [locationNames, setLocationNames] = (0, import_react17.useState)([]);
    const [profiles, setProfiles] = (0, import_react17.useState)([]);
    const [selectedProfileId, setSelectedProfileId] = (0, import_react17.useState)("");
    const profilesRef = (0, import_react17.useRef)([]);
    (0, import_react17.useEffect)(() => {
      spoolProfilesApi.getAll().then((data2) => {
        setProfiles(data2);
        profilesRef.current = data2;
      }).catch(() => {
      });
    }, []);
    const [ofdBrand, setOfdBrand] = (0, import_react17.useState)("");
    const [ofdMaterial, setOfdMaterial] = (0, import_react17.useState)("");
    const [ofdColor, setOfdColor] = (0, import_react17.useState)("");
    const [selectedFilament, setSelectedFilament] = (0, import_react17.useState)(null);
    (0, import_react17.useEffect)(() => {
      if (onFilamentSectionVisible) {
        onFilamentSectionVisible(!!ofdBrand && !!ofdMaterial);
      }
    }, [ofdBrand, ofdMaterial, onFilamentSectionVisible]);
    const [currentWeightG, setCurrentWeightG] = (0, import_react17.useState)("");
    const [initialWeightG, setInitialWeightG] = (0, import_react17.useState)("");
    const [spoolWeightG, setSpoolWeightG] = (0, import_react17.useState)("");
    const [lowStockThresholdG, setLowStockThresholdG] = (0, import_react17.useState)("");
    const [notes, setNotes] = (0, import_react17.useState)("");
    const [stockLocation, setStockLocation] = (0, import_react17.useState)("");
    const [isActive, setIsActive] = (0, import_react17.useState)(false);
    const [printerId, setPrinterId] = (0, import_react17.useState)(null);
    const [amsSlot, setAmsSlot] = (0, import_react17.useState)(null);
    const [weightError, setWeightError] = (0, import_react17.useState)("");
    const [currentWeightShake, setCurrentWeightShake] = (0, import_react17.useState)(false);
    const [currentWeightStatus, setCurrentWeightStatus] = (0, import_react17.useState)("neutral");
    const [printerError, setPrinterError] = (0, import_react17.useState)(false);
    const [printerShakeKey, setPrinterShakeKey] = (0, import_react17.useState)(0);
    const [amsSlotError, setAmsSlotError] = (0, import_react17.useState)(false);
    const [stockLocationError, setStockLocationError] = (0, import_react17.useState)(false);
    const [stockLocationShakeKey, setStockLocationShakeKey] = (0, import_react17.useState)(0);
    const [conflictSpool, setConflictSpool] = (0, import_react17.useState)(null);
    const [spoolToEvict, setSpoolToEvict] = (0, import_react17.useState)(null);
    const [price, setPrice] = (0, import_react17.useState)("");
    const [currency, setCurrency] = (0, import_react17.useState)("USD");
    const [diameterTolerance, setDiameterTolerance] = (0, import_react17.useState)("");
    const [density, setDensity] = (0, import_react17.useState)("");
    const [extruderMin, setExtruderMin] = (0, import_react17.useState)("");
    const [extruderMax, setExtruderMax] = (0, import_react17.useState)("");
    const [bedMin, setBedMin] = (0, import_react17.useState)("");
    const [bedMax, setBedMax] = (0, import_react17.useState)("");
    const [submitting, setSubmitting] = (0, import_react17.useState)(false);
    const [submitError, setSubmitError] = (0, import_react17.useState)(null);
    const [addedSpool, setAddedSpool] = (0, import_react17.useState)(null);
    const [showAddBrandModal, setShowAddBrandModal] = (0, import_react17.useState)(false);
    const [brandPolling, setBrandPolling] = (0, import_react17.useState)(null);
    const [dbBrands, setDbBrands] = (0, import_react17.useState)([]);
    const [inlineBrandQuery, setInlineBrandQuery] = (0, import_react17.useState)("");
    const [inlineBrandResults, setInlineBrandResults] = (0, import_react17.useState)([]);
    const [inlineBrandSearching, setInlineBrandSearching] = (0, import_react17.useState)(false);
    const [inlineBrandError, setInlineBrandError] = (0, import_react17.useState)(null);
    const [highlightBrand, setHighlightBrand] = (0, import_react17.useState)(false);
    const highlightTimerRef = (0, import_react17.useRef)(null);
    const inlineAbortRef = (0, import_react17.useRef)(null);
    const inlineSearchTimerRef = (0, import_react17.useRef)(null);
    const handleProfileSelect = (0, import_react17.useCallback)((profileId) => {
      setSelectedProfileId(profileId);
      if (!profileId) return;
      const list = profilesRef.current;
      const profile = list.find((p) => p.id === profileId);
      if (!profile) return;
      setOfdBrand(profile.brand);
      setOfdMaterial(profile.material);
      setOfdColor(profile.colorName);
      setCurrentWeightG(String(profile.initialWeightG));
      setInitialWeightG(String(profile.initialWeightG));
      setSpoolWeightG(String(profile.spoolWeightG));
      setLowStockThresholdG(String(profile.lowStockThresholdG));
      setNotes("");
      setStockLocation("");
      setPrice(profile.price != null ? String(profile.price) : "");
      setDensity(profile.density != null ? String(profile.density) : "");
      setDiameterTolerance(profile.diameterTolerance != null ? String(profile.diameterTolerance) : "");
      setExtruderMin(profile.extruderMin != null ? String(profile.extruderMin) : "");
      setExtruderMax(profile.extruderMax != null ? String(profile.extruderMax) : "");
      setBedMin(profile.bedMin != null ? String(profile.bedMin) : "");
      setBedMax(profile.bedMax != null ? String(profile.bedMax) : "");
      setIsActive(false);
      setWeightError("");
      setCurrentWeightStatus("neutral");
      setPrinterId(null);
      setAmsSlot(null);
      setPrinterError(false);
      setAmsSlotError(false);
      setConflictSpool(null);
      setSpoolToEvict(null);
      setSubmitError(null);
    }, []);
    (0, import_react17.useEffect)(() => {
      if (inlineSearchTimerRef.current) clearTimeout(inlineSearchTimerRef.current);
      if (!inlineBrandQuery.trim()) return;
      inlineSearchTimerRef.current = setTimeout(async () => {
        inlineAbortRef.current?.abort();
        const ctrl = new AbortController();
        inlineAbortRef.current = ctrl;
        setInlineBrandSearching(true);
        setInlineBrandError(null);
        try {
          const res = await brandsApi.searchOfd(inlineBrandQuery.trim(), ctrl.signal);
          setInlineBrandResults(res);
        } catch {
        } finally {
          setInlineBrandSearching(false);
        }
      }, 300);
      return () => {
        if (inlineSearchTimerRef.current) clearTimeout(inlineSearchTimerRef.current);
      };
    }, [inlineBrandQuery]);
    (0, import_react17.useEffect)(() => {
      return () => {
        if (highlightTimerRef.current) clearTimeout(highlightTimerRef.current);
      };
    }, []);
    const allSpoolsRef = (0, import_react17.useRef)([]);
    const printersRef = (0, import_react17.useRef)([]);
    (0, import_react17.useEffect)(() => {
      filamentsApi.getAll().then(setFilaments).catch(() => {
      });
      printersApi.getAll().then((data2) => {
        setPrinters(data2);
        printersRef.current = data2;
      }).catch(() => {
      });
      spoolsApi.getAll().then((data2) => {
        setAllSpools(data2);
        allSpoolsRef.current = data2;
      }).catch(() => {
      });
      settingsApi.getApp().then((s) => {
        if (s.currency) setCurrency(s.currency);
      }).catch(() => {
      });
      locationsApi.getAll().then((locs) => setLocationNames(locs.map((l) => l.name))).catch(() => {
      });
      brandsApi.getAll().then(setDbBrands).catch(() => {
      });
    }, []);
    const handleBrandAdded = (0, import_react17.useCallback)((brandName) => {
      setShowAddBrandModal(false);
      setBrandPolling(brandName);
      setInlineBrandQuery("");
      setInlineBrandResults([]);
      setHighlightBrand(true);
      if (highlightTimerRef.current) clearTimeout(highlightTimerRef.current);
      highlightTimerRef.current = setTimeout(() => setHighlightBrand(false), 1500);
      filamentsApi.refresh().catch(() => {
      });
      const poll = async () => {
        const updated = await filamentsApi.getAll();
        setFilaments(updated);
        const brandFilaments = updated.filter((f) => f.brand === brandName);
        if (brandFilaments.length > 0) {
          setOfdBrand(brandName);
          setOfdMaterial("");
          setOfdColor("");
          setBrandPolling(null);
          return;
        }
        if (brandPolling) setTimeout(poll, 2e3);
      };
      setTimeout(poll, 2e3);
    }, [brandPolling]);
    const selectInlineBrand = (0, import_react17.useCallback)(async (r) => {
      if (dbBrands.some((b) => b.ofdSlug === r.slug)) {
        setInlineBrandError(t2("addBrand.duplicateError"));
        return;
      }
      setInlineBrandError(null);
      setInlineBrandSearching(true);
      try {
        await brandsApi.add({ name: r.name, domain: "", ofdSlug: r.slug });
        const updatedBrands = await brandsApi.getAll();
        setDbBrands(updatedBrands);
        handleBrandAdded(r.name);
      } catch {
        setInlineBrandError(t2("addBrand.addError"));
      } finally {
        setInlineBrandSearching(false);
      }
    }, [dbBrands, handleBrandAdded, t2]);
    const checkNonAmsConflict = (0, import_react17.useCallback)((pid) => {
      const printer = printersRef.current.find((p) => p.id === pid);
      if (!printer || printer.hasAms) return;
      const occupant = allSpoolsRef.current.find((s) => s.printerId === pid);
      if (occupant) setConflictSpool(occupant);
    }, []);
    const ofdBrands = (0, import_react17.useMemo)(() => [...new Set(filaments.map((f) => f.brand))].sort(), [filaments]);
    const ofdMaterials = (0, import_react17.useMemo)(() => [...new Set(filaments.filter((f) => f.brand === ofdBrand).map((f) => f.material))].sort(), [filaments, ofdBrand]);
    const ofdColors = (0, import_react17.useMemo)(() => filaments.filter((f) => f.brand === ofdBrand && f.material === ofdMaterial && f.colorName), [filaments, ofdBrand, ofdMaterial]);
    const occupiedSlots = (0, import_react17.useMemo)(() => {
      if (!printerId) return {};
      const result = {};
      for (const s of allSpools) {
        if (s.printerId === printerId && s.amsSlot != null)
          result[s.amsSlot] = { colorHex: s.colorHex, colorName: s.colorName, brand: s.brand, material: s.material };
      }
      return result;
    }, [allSpools, printerId]);
    const filteredFilaments = (0, import_react17.useMemo)(() => {
      if (!ofdBrand || !ofdMaterial) return [];
      return filaments.filter(
        (f) => f.brand === ofdBrand && f.material === ofdMaterial && (!ofdColor || f.colorName === ofdColor)
      );
    }, [filaments, ofdBrand, ofdMaterial, ofdColor]);
    function pickFilament(f) {
      setSelectedFilament(f);
      onSpoolEditorVisible?.(true);
      setCurrentWeightG("1000");
      setInitialWeightG("1000");
      setSpoolWeightG("250");
      setLowStockThresholdG("100");
      setNotes("");
      setStockLocation("");
      setPrice("");
      setDiameterTolerance(f.diameterTolerance != null ? String(f.diameterTolerance) : "");
      setDensity(f.density != null ? String(f.density) : "");
      const matDefaults = getMaterialDefaults(f.material);
      setExtruderMin(f.extruderMin != null ? String(f.extruderMin) : matDefaults ? String(matDefaults.extruderMin) : "");
      setExtruderMax(f.extruderMax != null ? String(f.extruderMax) : matDefaults ? String(matDefaults.extruderMax) : "");
      setBedMin(f.bedMin != null ? String(f.bedMin) : matDefaults ? String(matDefaults.bedMin) : "");
      setBedMax(f.bedMax != null ? String(f.bedMax) : matDefaults ? String(matDefaults.bedMax) : "");
      setIsActive(false);
      setWeightError("");
      setCurrentWeightStatus("neutral");
      setPrinterId(null);
      setAmsSlot(null);
      setPrinterError(false);
      setAmsSlotError(false);
      setConflictSpool(null);
      setSpoolToEvict(null);
      setSubmitError(null);
    }
    function handleCurrentWeightChange(v) {
      setCurrentWeightG(v);
      if (currentWeightStatus === "neutral") return;
      const cw = parseFloat(v), iw = parseFloat(initialWeightG);
      setCurrentWeightStatus(!isNaN(cw) && !isNaN(iw) && cw <= iw ? "valid" : "error");
    }
    function handleBusyTrayClick(slot) {
      const occupant = allSpools.find((s) => s.printerId === printerId && s.amsSlot === slot);
      if (occupant) setConflictSpool(occupant);
    }
    async function handleSubmit(e2) {
      e2.preventDefault();
      if (!selectedFilament) return;
      const w = parseFloat(initialWeightG);
      if (!initialWeightG || isNaN(w) || w <= 0) {
        setWeightError(t2("addSpool.weightError"));
        return;
      }
      const cw = currentWeightG ? parseFloat(currentWeightG) : w;
      if (cw > w) {
        setCurrentWeightStatus("error");
        setCurrentWeightShake(true);
        setTimeout(() => setCurrentWeightShake(false), 500);
        return;
      }
      setWeightError("");
      if (!isActive && !stockLocation.trim()) {
        setStockLocationError(true);
        setStockLocationShakeKey((k) => k + 1);
        return;
      }
      if (isActive && !printerId) {
        setPrinterError(true);
        setPrinterShakeKey((k) => k + 1);
        return;
      }
      const selectedPrinter = printers.find((p) => p.id === printerId);
      if (isActive && selectedPrinter?.hasAms && amsSlot == null) {
        setAmsSlotError(true);
        return;
      }
      setPrinterError(false);
      setAmsSlotError(false);
      setSubmitting(true);
      setSubmitError(null);
      try {
        if (spoolToEvict) await spoolsApi.deactivate(spoolToEvict.id);
        const cwFinal = currentWeightG ? parseFloat(currentWeightG) : w;
        let created = await spoolsApi.add({
          brand: selectedFilament.brand,
          material: selectedFilament.material,
          colorName: selectedFilament.colorName ?? "",
          colorHex: selectedFilament.colorHex ?? "#ffffff",
          initialWeightG: w,
          currentWeightG: cwFinal,
          spoolWeightG: spoolWeightG ? parseFloat(spoolWeightG) : void 0,
          lowStockThresholdG: lowStockThresholdG ? parseFloat(lowStockThresholdG) : void 0,
          isActive,
          notes: notes.trim() || void 0,
          density: density ? parseFloat(density) : void 0,
          diameterTolerance: diameterTolerance ? parseFloat(diameterTolerance) : void 0,
          extruderMin: extruderMin ? parseInt(extruderMin) : void 0,
          extruderMax: extruderMax ? parseInt(extruderMax) : void 0,
          bedMin: bedMin ? parseInt(bedMin) : void 0,
          bedMax: bedMax ? parseInt(bedMax) : void 0,
          tagUid: tagUid?.trim() || void 0,
          price: price ? parseFloat(price) : void 0,
          stockLocation: stockLocation.trim() || void 0
        });
        if (printerId) {
          created = await spoolsApi.assignPrinter(created.id, { printerId, amsSlot });
        }
        if (tagUid?.trim()) await registerTag(tagUid.trim(), created.id);
        window.dispatchEvent(new CustomEvent("spools-updated"));
        if (tagUid?.trim()) {
          navigate("/spools");
        } else {
          onSpoolEditorVisible?.(false);
          setAddedSpool({ brand: selectedFilament.brand, colorName: selectedFilament.colorName ?? "" });
        }
      } catch {
        setSubmitError(t2("addSpool.saveError"));
      } finally {
        setSubmitting(false);
      }
    }
    if (addedSpool) {
      return /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: AddSpoolForm_default.wrap, children: /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.card, children: [
        /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.cardHeader, children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("h1", { className: AddSpoolForm_default.title, children: t2("addSpool.successTitle") }),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("p", { className: AddSpoolForm_default.subtitle, children: [
              addedSpool.brand,
              " \xB7 ",
              addedSpool.colorName
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: AddSpoolForm_default.successIcon, children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("polyline", { points: "20 6 9 17 4 12" }) }) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.successBtns, children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
            "button",
            {
              type: "button",
              className: AddSpoolForm_default.btnAddAnother,
              onClick: () => {
                setAddedSpool(null);
                setSelectedFilament(null);
              },
              children: t2("addSpool.addAnother")
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
            "button",
            {
              type: "button",
              className: AddSpoolForm_default.btnDone,
              onClick: () => navigate("/spools"),
              children: t2("addSpool.viewSpools")
            }
          )
        ] })
      ] }) });
    }
    if (selectedFilament) {
      const conflictPrinter = printers.find((p) => p.id === printerId);
      return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(import_jsx_runtime19.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
          SpoolEditor,
          {
            brand: selectedFilament.brand,
            material: selectedFilament.material,
            colorName: selectedFilament.colorName ?? selectedFilament.filamentName,
            colorHex: selectedFilament.colorHex,
            tagUid,
            extruderMin: extruderMin ? parseInt(extruderMin) : selectedFilament.extruderMin,
            extruderMax: extruderMax ? parseInt(extruderMax) : selectedFilament.extruderMax,
            bedMin: bedMin ? parseInt(bedMin) : selectedFilament.bedMin,
            bedMax: bedMax ? parseInt(bedMax) : selectedFilament.bedMax,
            density: density ? parseFloat(density) : selectedFilament.density,
            diameterTolerance: diameterTolerance ? parseFloat(diameterTolerance) : selectedFilament.diameterTolerance,
            onExtruderMinChange: setExtruderMin,
            onExtruderMaxChange: setExtruderMax,
            onBedMinChange: setBedMin,
            onBedMaxChange: setBedMax,
            onDensityChange: setDensity,
            onDiameterToleranceChange: setDiameterTolerance,
            currentWeightG,
            setCurrentWeightG: handleCurrentWeightChange,
            currentWeightShake,
            currentWeightStatus,
            initialWeightG,
            setInitialWeightG,
            spoolWeightG,
            setSpoolWeightG,
            lowStockThresholdG,
            setLowStockThresholdG,
            notes,
            setNotes,
            stockLocation,
            setStockLocation: (v) => {
              setStockLocation(v);
              setStockLocationError(false);
            },
            stockLocationError,
            stockLocationShakeKey,
            onStockLocationShakeEnd: () => setStockLocationError(false),
            stockLocationOptions: [.../* @__PURE__ */ new Set([...locationNames, ...allSpools.map((s) => s.stockLocation).filter(Boolean)])],
            price,
            setPrice,
            currency,
            isActive,
            setIsActive: (v) => {
              setIsActive(v);
              if (!v) {
                setPrinterError(false);
                setAmsSlotError(false);
              }
            },
            printers,
            printerId,
            setPrinterId: (v) => {
              setPrinterId(v);
              setPrinterError(false);
              if (!v) {
                setAmsSlot(null);
                setAmsSlotError(false);
              } else {
                checkNonAmsConflict(v);
              }
            },
            amsSlot,
            onAmsSlotChange: (v) => {
              setAmsSlot(v);
              if (v != null) setAmsSlotError(false);
            },
            occupiedSlots,
            currentSpoolColor: selectedFilament.colorHex ?? "#888",
            printerError,
            printerShakeKey,
            onPrinterShakeEnd: () => setPrinterError(false),
            amsSlotError,
            onBusyTrayClick: handleBusyTrayClick,
            weightError,
            setWeightError,
            submitting,
            submitError,
            onSubmit: handleSubmit,
            onCancel: () => {
              setSelectedFilament(null);
              onSpoolEditorVisible?.(false);
            }
          }
        ),
        conflictSpool && /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
          AmsConflictModal,
          {
            printerImgSrc: conflictPrinter ? getPrinterImage(conflictPrinter.brand, conflictPrinter.model) : "/printers/generic.svg",
            printerBrand: conflictPrinter?.brand,
            printerModel: conflictPrinter?.model,
            traySlot: conflictSpool.amsSlot ?? void 0,
            occupantSpool: conflictSpool,
            onCancel: () => {
              if (conflictSpool.amsSlot == null) setPrinterId(null);
              setConflictSpool(null);
            },
            onConfirm: () => {
              setSpoolToEvict(conflictSpool);
              if (conflictSpool.amsSlot != null) {
                setAmsSlot(conflictSpool.amsSlot);
                setAmsSlotError(false);
              }
              setConflictSpool(null);
            }
          }
        )
      ] });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.wrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.card, children: [
        /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.cardHeader, children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("h1", { className: AddSpoolForm_default.title, children: t2("addSpool.title") }),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("p", { className: AddSpoolForm_default.subtitle, children: t2("addSpool.subtitle") })
          ] }),
          tagUid && /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.nfcBadge, children: [
            /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("line", { x1: "5", y1: "7", x2: "5", y2: "17" }),
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("path", { d: "M8 9.5a4 4 0 0 1 0 5" }),
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("path", { d: "M11 8a7 7 0 0 1 0 8" }),
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("path", { d: "M14 6.5a9.5 9.5 0 0 1 0 11" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: AddSpoolForm_default.nfcBadgeText, children: t2("addSpool.nfcTag") })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.profileSelect, children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("label", { className: AddSpoolForm_default.label, children: t2("spoolProfile.selectProfile") }),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
            "select",
            {
              value: selectedProfileId,
              onChange: (e2) => handleProfileSelect(e2.target.value),
              className: AddSpoolForm_default.select,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("option", { value: "", children: t2("spoolProfile.custom") }),
                profiles.map((p) => /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("option", { value: p.id, children: [
                  p.name,
                  " \u2014 ",
                  p.brand,
                  " ",
                  p.material,
                  " ",
                  p.colorName
                ] }, p.id))
              ]
            }
          )
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.filterGrid, children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("label", { className: AddSpoolForm_default.label, children: t2("filters.brand") }),
            !showAddBrandModal ? /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
              "select",
              {
                value: ofdBrand,
                onChange: (e2) => {
                  if (e2.target.value === "__add__") {
                    setShowAddBrandModal(true);
                    setOfdBrand("");
                    return;
                  }
                  setOfdBrand(e2.target.value);
                  setOfdMaterial("");
                  setOfdColor("");
                },
                className: `${AddSpoolForm_default.select}${highlightBrand ? ` ${AddSpoolForm_default.selectHighlight}` : ""}`,
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("option", { value: "", children: t2("addSpool.selectBrand") }),
                  ofdBrands.map((b) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("option", { value: b, children: b }, b)),
                  /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("option", { value: "__add__", className: AddSpoolForm_default.addBrandOption, children: [
                    "+ ",
                    t2("filaments.addBrand")
                  ] })
                ]
              }
            ) : /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: AddSpoolForm_default.inlineBrandSearch, children: [
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
                "input",
                {
                  type: "text",
                  className: AddSpoolForm_default.inlineBrandInput,
                  placeholder: t2("addBrand.searchLabel"),
                  value: inlineBrandQuery,
                  onChange: (e2) => {
                    setInlineBrandQuery(e2.target.value);
                    if (!e2.target.value) setInlineBrandResults([]);
                  },
                  autoFocus: true
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
                "button",
                {
                  type: "button",
                  className: AddSpoolForm_default.inlineBrandClose,
                  onClick: () => {
                    setShowAddBrandModal(false);
                    setInlineBrandQuery("");
                    setInlineBrandResults([]);
                  },
                  children: /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
                    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
                  ] })
                }
              ),
              inlineBrandSearching && /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: AddSpoolForm_default.inlineBrandSpinner }),
              inlineBrandResults.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("ul", { className: AddSpoolForm_default.inlineBrandDropdown, children: inlineBrandResults.map((r) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
                "button",
                {
                  type: "button",
                  onClick: () => selectInlineBrand(r),
                  className: AddSpoolForm_default.inlineBrandResult,
                  children: [
                    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { children: r.name }),
                    /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: AddSpoolForm_default.inlineBrandMeta, children: t2("addBrand.materialCount", { count: r.materialCount }) })
                  ]
                }
              ) }, r.slug)) }),
              inlineBrandError && /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("p", { className: AddSpoolForm_default.inlineBrandError, children: inlineBrandError })
            ] }),
            brandPolling && /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("p", { className: AddSpoolForm_default.addBrandPolling, children: [
              t2("addBrand.adding"),
              " ",
              brandPolling,
              /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("span", { className: AddSpoolForm_default.dots, children: [
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { children: "." }),
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { children: "." }),
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { children: "." })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: ofdBrand ? AddSpoolForm_default.filterEnabled : AddSpoolForm_default.filterDisabled, children: [
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("label", { className: AddSpoolForm_default.label, children: t2("spoolForm.material") }),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
              "select",
              {
                value: ofdMaterial,
                onChange: (e2) => {
                  setOfdMaterial(e2.target.value);
                  setOfdColor("");
                },
                disabled: !ofdBrand,
                className: AddSpoolForm_default.select,
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("option", { value: "", children: t2("addSpool.selectMaterial") }),
                  ofdMaterials.map((m) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("option", { value: m, children: m }, m))
                ]
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: ofdMaterial ? AddSpoolForm_default.filterEnabled : AddSpoolForm_default.filterDisabled, children: [
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("label", { className: AddSpoolForm_default.label, children: t2("filters.color") }),
            /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(ColorSelect, { colors: ofdColors, value: ofdColor, onChange: setOfdColor, disabled: !ofdMaterial })
          ] }),
          ofdBrand && ofdMaterial && /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("p", { className: AddSpoolForm_default.filterCount, children: t2("addSpool.filamentsCount", { count: filteredFilaments.length, material: ofdMaterial }) })
        ] })
      ] }),
      ofdBrand && ofdMaterial && /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: AddSpoolForm_default.filamentScroll, children: /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { className: AddSpoolForm_default.filamentGrid, children: filteredFilaments.map((f, i) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(FilamentCard, { filament: f, onClick: () => pickFilament(f) }, i)) }) })
    ] });
  }

  // src/components/AddSpoolProfileModal/AddSpoolProfileModal.tsx
  var import_react18 = __toESM(require_react_shim(), 1);

  // src/components/AddSpoolProfileModal/AddSpoolProfileModal.module.css
  var AddSpoolProfileModal_default = {
    overlay: "AddSpoolProfileModal_overlay",
    modal: "AddSpoolProfileModal_modal",
    header: "AddSpoolProfileModal_header",
    title: "AddSpoolProfileModal_title",
    closeBtn: "AddSpoolProfileModal_closeBtn",
    form: "AddSpoolProfileModal_form",
    grid: "AddSpoolProfileModal_grid",
    field: "AddSpoolProfileModal_field",
    label: "AddSpoolProfileModal_label",
    input: "AddSpoolProfileModal_input",
    colorRow: "AddSpoolProfileModal_colorRow",
    colorPicker: "AddSpoolProfileModal_colorPicker",
    rangeRow: "AddSpoolProfileModal_rangeRow",
    error: "AddSpoolProfileModal_error",
    footer: "AddSpoolProfileModal_footer",
    btnCancel: "AddSpoolProfileModal_btnCancel",
    btnSubmit: "AddSpoolProfileModal_btnSubmit"
  };

  // src/components/AddSpoolProfileModal/AddSpoolProfileModal.tsx
  var import_jsx_runtime20 = __toESM(require_react_shim(), 1);
  function AddSpoolProfileModal({ profile, onClose, onSaved }) {
    const { t: t2 } = useTranslation();
    const [name, setName] = (0, import_react18.useState)("");
    const [brand, setBrand] = (0, import_react18.useState)("");
    const [material, setMaterial] = (0, import_react18.useState)("");
    const [colorName, setColorName] = (0, import_react18.useState)("");
    const [colorHex, setColorHex] = (0, import_react18.useState)("#ffffff");
    const [initialWeightG, setInitialWeightG] = (0, import_react18.useState)("1000");
    const [spoolWeightG, setSpoolWeightG] = (0, import_react18.useState)("200");
    const [lowStockThresholdG, setLowStockThresholdG] = (0, import_react18.useState)("100");
    const [density, setDensity] = (0, import_react18.useState)("");
    const [diameterTolerance, setDiameterTolerance] = (0, import_react18.useState)("");
    const [extruderMin, setExtruderMin] = (0, import_react18.useState)("");
    const [extruderMax, setExtruderMax] = (0, import_react18.useState)("");
    const [bedMin, setBedMin] = (0, import_react18.useState)("");
    const [bedMax, setBedMax] = (0, import_react18.useState)("");
    const [price, setPrice] = (0, import_react18.useState)("");
    const [saving, setSaving] = (0, import_react18.useState)(false);
    const [error, setError] = (0, import_react18.useState)(null);
    const nameRef = (0, import_react18.useRef)(null);
    (0, import_react18.useEffect)(() => {
      if (profile) {
        setName(profile.name);
        setBrand(profile.brand);
        setMaterial(profile.material);
        setColorName(profile.colorName);
        setColorHex(profile.colorHex);
        setInitialWeightG(String(profile.initialWeightG));
        setSpoolWeightG(String(profile.spoolWeightG));
        setLowStockThresholdG(String(profile.lowStockThresholdG));
        setDensity(profile.density != null ? String(profile.density) : "");
        setDiameterTolerance(profile.diameterTolerance != null ? String(profile.diameterTolerance) : "");
        setExtruderMin(profile.extruderMin != null ? String(profile.extruderMin) : "");
        setExtruderMax(profile.extruderMax != null ? String(profile.extruderMax) : "");
        setBedMin(profile.bedMin != null ? String(profile.bedMin) : "");
        setBedMax(profile.bedMax != null ? String(profile.bedMax) : "");
        setPrice(profile.price != null ? String(profile.price) : "");
      }
      nameRef.current?.focus();
    }, [profile]);
    (0, import_react18.useEffect)(() => {
      function onKey(e2) {
        if (e2.key === "Escape") onClose();
      }
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
    }, [onClose]);
    async function handleSubmit(e2) {
      e2.preventDefault();
      if (!name.trim() || !brand.trim() || !material.trim()) {
        setError(t2("spoolProfile.errorRequired"));
        return;
      }
      setSaving(true);
      setError(null);
      const body = {
        name: name.trim(),
        brand: brand.trim(),
        material: material.trim(),
        colorName: colorName.trim() || brand.trim(),
        colorHex,
        initialWeightG: parseFloat(initialWeightG) || 1e3,
        spoolWeightG: parseFloat(spoolWeightG) || 200,
        lowStockThresholdG: parseFloat(lowStockThresholdG) || 100,
        density: density ? parseFloat(density) : null,
        diameterTolerance: diameterTolerance ? parseFloat(diameterTolerance) : null,
        extruderMin: extruderMin ? parseInt(extruderMin) : null,
        extruderMax: extruderMax ? parseInt(extruderMax) : null,
        bedMin: bedMin ? parseInt(bedMin) : null,
        bedMax: bedMax ? parseInt(bedMax) : null,
        price: price ? parseFloat(price) : null
      };
      try {
        if (profile) {
          await spoolProfilesApi.update(profile.id, body);
        } else {
          await spoolProfilesApi.add(body);
        }
        onSaved();
      } catch {
        setError(t2("spoolProfile.errorSave"));
      } finally {
        setSaving(false);
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("div", { className: AddSpoolProfileModal_default.overlay, onClick: (e2) => {
      if (e2.target === e2.currentTarget) onClose();
    }, children: /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.modal, children: [
      /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.header, children: [
        /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("h2", { className: AddSpoolProfileModal_default.title, children: profile ? t2("spoolProfile.editProfile") : t2("spoolProfile.addProfile") }),
        /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("button", { onClick: onClose, className: AddSpoolProfileModal_default.closeBtn, children: /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
          /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
        ] }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("form", { onSubmit: handleSubmit, className: AddSpoolProfileModal_default.form, children: [
        /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.grid, children: [
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolProfile.nameLabel") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                ref: nameRef,
                type: "text",
                value: name,
                onChange: (e2) => setName(e2.target.value),
                placeholder: t2("spoolProfile.namePlaceholder"),
                className: AddSpoolProfileModal_default.input
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("filters.brand") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                type: "text",
                value: brand,
                onChange: (e2) => setBrand(e2.target.value),
                placeholder: "e.g. Bambu Lab",
                className: AddSpoolProfileModal_default.input
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.material") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                type: "text",
                value: material,
                onChange: (e2) => setMaterial(e2.target.value),
                placeholder: "e.g. PLA",
                className: AddSpoolProfileModal_default.input
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.color") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.colorRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
                "input",
                {
                  type: "color",
                  value: colorHex,
                  onChange: (e2) => setColorHex(e2.target.value),
                  className: AddSpoolProfileModal_default.colorPicker
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
                "input",
                {
                  type: "text",
                  value: colorName,
                  onChange: (e2) => setColorName(e2.target.value),
                  placeholder: "e.g. Jade White",
                  className: AddSpoolProfileModal_default.input
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.initialWeight") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                type: "number",
                value: initialWeightG,
                onChange: (e2) => setInitialWeightG(e2.target.value),
                className: AddSpoolProfileModal_default.input
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.spoolWeight") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                type: "number",
                value: spoolWeightG,
                onChange: (e2) => setSpoolWeightG(e2.target.value),
                className: AddSpoolProfileModal_default.input
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.lowStockThreshold") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                type: "number",
                value: lowStockThresholdG,
                onChange: (e2) => setLowStockThresholdG(e2.target.value),
                className: AddSpoolProfileModal_default.input
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.density") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                type: "number",
                step: "0.01",
                value: density,
                onChange: (e2) => setDensity(e2.target.value),
                className: AddSpoolProfileModal_default.input
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.diameterTolerance") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                type: "number",
                step: "0.01",
                value: diameterTolerance,
                onChange: (e2) => setDiameterTolerance(e2.target.value),
                className: AddSpoolProfileModal_default.input
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.extruderTemp") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.rangeRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
                "input",
                {
                  type: "number",
                  value: extruderMin,
                  onChange: (e2) => setExtruderMin(e2.target.value),
                  placeholder: t2("spoolForm.min"),
                  className: AddSpoolProfileModal_default.input
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("span", { children: t2("spoolForm.to") }),
              /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
                "input",
                {
                  type: "number",
                  value: extruderMax,
                  onChange: (e2) => setExtruderMax(e2.target.value),
                  placeholder: t2("spoolForm.max"),
                  className: AddSpoolProfileModal_default.input
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.bedTemp") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.rangeRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
                "input",
                {
                  type: "number",
                  value: bedMin,
                  onChange: (e2) => setBedMin(e2.target.value),
                  placeholder: t2("spoolForm.min"),
                  className: AddSpoolProfileModal_default.input
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("span", { children: t2("spoolForm.to") }),
              /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
                "input",
                {
                  type: "number",
                  value: bedMax,
                  onChange: (e2) => setBedMax(e2.target.value),
                  placeholder: t2("spoolForm.max"),
                  className: AddSpoolProfileModal_default.input
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.field, children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("label", { className: AddSpoolProfileModal_default.label, children: t2("spoolForm.price") }),
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              "input",
              {
                type: "number",
                step: "0.01",
                value: price,
                onChange: (e2) => setPrice(e2.target.value),
                className: AddSpoolProfileModal_default.input
              }
            )
          ] })
        ] }),
        error && /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("p", { className: AddSpoolProfileModal_default.error, role: "alert", children: error }),
        /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: AddSpoolProfileModal_default.footer, children: [
          /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("button", { type: "button", onClick: onClose, className: AddSpoolProfileModal_default.btnCancel, children: t2("common.cancel") }),
          /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("button", { type: "submit", disabled: saving, className: AddSpoolProfileModal_default.btnSubmit, children: saving ? t2("spoolProfile.saving") : profile ? t2("spoolProfile.update") : t2("spoolProfile.save") })
        ] })
      ] })
    ] }) });
  }

  // src/components/BrandFilterBar/BrandFilterBar.tsx
  var import_react19 = __toESM(require_react_shim(), 1);

  // src/components/BrandFilterBar/BrandFilterBar.module.css
  var BrandFilterBar_default = {
    wrap: "BrandFilterBar_wrap",
    btn: "BrandFilterBar_btn",
    btnActive: "BrandFilterBar_btnActive",
    logo: "BrandFilterBar_logo",
    fallback: "BrandFilterBar_fallback",
    fallbackActive: "BrandFilterBar_fallbackActive"
  };

  // src/components/BrandFilterBar/BrandFilterBar.tsx
  var import_jsx_runtime21 = __toESM(require_react_shim(), 1);
  var BRAND_DOMAINS3 = {
    "Bambu Lab": "bambulab.com",
    "eSUN 3D": "esun3d.com",
    "Prusament": "prusament.com",
    "Polymaker": "polymaker.com",
    "Hatchbox": "hatchbox3d.com",
    "SUNLU": "sunlu.com",
    "Creality": "creality.com",
    "ELEGOO": "elegoo.com",
    "Overture": "overture3d.com",
    "PolyLite": "polymaker.com",
    "colorFabb": "colorfabb.com",
    "Fillamentum": "fillamentum.com",
    "Fiberlogy": "fiberlogy.com",
    "extrudr": "extrudr.com",
    "Das Filament": "dasfilament.de",
    "Protopasta": "proto-pasta.com",
    "MatterHackers": "matterhackers.com",
    "NinjaTek": "ninjatek.com",
    "Atomic Filament": "atomicfilament.com",
    "Inland": "microcenter.com",
    "JAYO": "jayofilament.com",
    "TINMORRY": "tinmorry.com",
    "AzureFilm": "azurefilm.hr",
    "Spectrum": "spectrumfilaments.com",
    "Devil Design": "devildesign.pl",
    "Anycubic": "anycubic.com"
  };
  function logoUrl(brand) {
    const domain = BRAND_DOMAINS3[brand];
    if (!domain) return null;
    return `https://www.google.com/s2/favicons?sz=64&domain=${domain}`;
  }
  function BrandFilterBar({ brands, selected, onChange }) {
    const { t: t2 } = useTranslation();
    const [imgErrors, setImgErrors] = (0, import_react19.useState)({});
    if (brands.length === 0) return null;
    return /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("div", { className: BrandFilterBar_default.wrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        "button",
        {
          onClick: () => onChange(""),
          className: `${BrandFilterBar_default.btn}${selected === "" ? ` ${BrandFilterBar_default.btnActive}` : ""}`,
          children: t2("brandFilterBar.allBrands")
        }
      ),
      brands.map((brand) => {
        const url = logoUrl(brand);
        const hasError = imgErrors[brand];
        const isSelected = selected === brand;
        return /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)(
          "button",
          {
            onClick: () => onChange(brand === selected ? "" : brand),
            className: `${BrandFilterBar_default.btn}${isSelected ? ` ${BrandFilterBar_default.btnActive}` : ""}`,
            children: [
              url && !hasError ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
                "img",
                {
                  src: url,
                  alt: brand,
                  className: BrandFilterBar_default.logo,
                  onError: () => setImgErrors((e2) => ({ ...e2, [brand]: true }))
                }
              ) : /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("span", { className: `${BrandFilterBar_default.fallback}${isSelected ? ` ${BrandFilterBar_default.fallbackActive}` : ""}`, children: brand[0] }),
              brand
            ]
          },
          brand
        );
      })
    ] });
  }

  // src/context/ConnectionContext.tsx
  var import_react21 = __toESM(require_react_shim(), 1);

  // src/hooks/useConnectionMonitor.ts
  var import_react20 = __toESM(require_react_shim(), 1);
  var HEALTH_URL = `${__import_meta_env__.VITE_API_URL.replace(/\/$/, "")}/health`;

  // src/context/ConnectionContext.tsx
  var import_jsx_runtime22 = __toESM(require_react_shim(), 1);
  var ConnectionContext = (0, import_react21.createContext)({ isOffline: false, refreshKey: 0 });
  function useConnection() {
    return (0, import_react21.useContext)(ConnectionContext);
  }

  // src/components/ConnectionBanner/ConnectionBanner.module.css
  var ConnectionBanner_default = {
    spin: "ConnectionBanner_spin",
    banner: "ConnectionBanner_banner",
    spinner: "ConnectionBanner_spinner"
  };

  // src/components/ConnectionBanner/ConnectionBanner.tsx
  var import_jsx_runtime23 = __toESM(require_react_shim(), 1);
  function ConnectionBanner() {
    const { t: t2 } = useTranslation();
    const { isOffline } = useConnection();
    if (!isOffline) return null;
    return /* @__PURE__ */ (0, import_jsx_runtime23.jsxs)("div", { className: ConnectionBanner_default.banner, children: [
      /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
        "svg",
        {
          className: ConnectionBanner_default.spinner,
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          strokeWidth: "2.5",
          strokeLinecap: "round",
          strokeLinejoin: "round",
          children: /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("path", { d: "M21 12a9 9 0 11-6.219-8.56" })
        }
      ),
      t2("connection.reconnecting")
    ] });
  }

  // src/components/DarkModeToggle/DarkModeToggle.module.css
  var DarkModeToggle_default = {
    toggle: "DarkModeToggle_toggle",
    toggleLight: "DarkModeToggle_toggleLight",
    toggleDark: "DarkModeToggle_toggleDark",
    stars: "DarkModeToggle_stars",
    stars1: "DarkModeToggle_stars1",
    stars2: "DarkModeToggle_stars2",
    starsVisible: "DarkModeToggle_starsVisible",
    starsHidden: "DarkModeToggle_starsHidden",
    starsDelay: "DarkModeToggle_starsDelay",
    sunRays: "DarkModeToggle_sunRays",
    sunRaysHidden: "DarkModeToggle_sunRaysHidden",
    thumb: "DarkModeToggle_thumb",
    thumbLight: "DarkModeToggle_thumbLight",
    thumbDark: "DarkModeToggle_thumbDark"
  };

  // src/components/DarkModeToggle/DarkModeToggle.tsx
  var import_jsx_runtime24 = __toESM(require_react_shim(), 1);
  function DarkModeToggle({ isDark, onToggle }) {
    return /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)(
      "button",
      {
        onClick: onToggle,
        "aria-label": "Toggle dark mode",
        className: `${DarkModeToggle_default.toggle} ${isDark ? DarkModeToggle_default.toggleDark : DarkModeToggle_default.toggleLight}`,
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("span", { className: `${DarkModeToggle_default.stars} ${DarkModeToggle_default.stars1} ${isDark ? DarkModeToggle_default.starsVisible : DarkModeToggle_default.starsHidden}`, children: /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("svg", { width: "4", height: "4", viewBox: "0 0 4 4", children: /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("circle", { cx: "2", cy: "2", r: "2", fill: "white" }) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("span", { className: `${DarkModeToggle_default.stars} ${DarkModeToggle_default.stars2} ${isDark ? `${DarkModeToggle_default.starsVisible} ${DarkModeToggle_default.starsDelay}` : DarkModeToggle_default.starsHidden}`, children: /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("svg", { width: "3", height: "3", viewBox: "0 0 3 3", children: /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("circle", { cx: "1.5", cy: "1.5", r: "1.5", fill: "white" }) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("span", { className: `${DarkModeToggle_default.sunRays} ${isDark ? DarkModeToggle_default.sunRaysHidden : ""}`, children: /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)("svg", { width: "10", height: "10", viewBox: "0 0 10 10", fill: "none", children: [
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "5", y1: "0", x2: "5", y2: "2", stroke: "white", strokeWidth: "1.2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "5", y1: "8", x2: "5", y2: "10", stroke: "white", strokeWidth: "1.2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "0", y1: "5", x2: "2", y2: "5", stroke: "white", strokeWidth: "1.2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "8", y1: "5", x2: "10", y2: "5", stroke: "white", strokeWidth: "1.2", strokeLinecap: "round" })
          ] }) }),
          /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("span", { className: `${DarkModeToggle_default.thumb} ${isDark ? DarkModeToggle_default.thumbDark : DarkModeToggle_default.thumbLight}`, children: isDark ? /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("svg", { width: "13", height: "13", viewBox: "0 0 24 24", fill: "none", children: /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("path", { d: "M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z", fill: "#4338ca", stroke: "#4338ca", strokeWidth: "1" }) }) : /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)("svg", { width: "13", height: "13", viewBox: "0 0 24 24", fill: "none", children: [
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("circle", { cx: "12", cy: "12", r: "5", fill: "#f59e0b" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "12", y1: "2", x2: "12", y2: "5", stroke: "#f59e0b", strokeWidth: "2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "12", y1: "19", x2: "12", y2: "22", stroke: "#f59e0b", strokeWidth: "2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "2", y1: "12", x2: "5", y2: "12", stroke: "#f59e0b", strokeWidth: "2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "19", y1: "12", x2: "22", y2: "12", stroke: "#f59e0b", strokeWidth: "2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "4.93", y1: "4.93", x2: "7.05", y2: "7.05", stroke: "#f59e0b", strokeWidth: "2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "16.95", y1: "16.95", x2: "19.07", y2: "19.07", stroke: "#f59e0b", strokeWidth: "2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "4.93", y1: "19.07", x2: "7.05", y2: "16.95", stroke: "#f59e0b", strokeWidth: "2", strokeLinecap: "round" }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("line", { x1: "16.95", y1: "7.05", x2: "19.07", y2: "4.93", stroke: "#f59e0b", strokeWidth: "2", strokeLinecap: "round" })
          ] }) })
        ]
      }
    );
  }

  // src/components/FilamentFilterDropdown/FilamentFilterDropdown.tsx
  var import_react22 = __toESM(require_react_shim(), 1);

  // src/types/filamentFilters.ts
  var DEFAULT_FILAMENT_FILTERS = {
    materials: [],
    colors: [],
    hideDiscontinued: false
  };

  // src/components/FilamentFilterDropdown/FilamentFilterDropdown.module.css
  var FilamentFilterDropdown_default = {
    wrap: "FilamentFilterDropdown_wrap",
    trigger: "FilamentFilterDropdown_trigger",
    triggerActive: "FilamentFilterDropdown_triggerActive",
    triggerIcon: "FilamentFilterDropdown_triggerIcon",
    count: "FilamentFilterDropdown_count",
    dropdown: "FilamentFilterDropdown_dropdown",
    dropdownHeader: "FilamentFilterDropdown_dropdownHeader",
    dropdownHeaderLabel: "FilamentFilterDropdown_dropdownHeaderLabel",
    clearBtn: "FilamentFilterDropdown_clearBtn",
    scroll: "FilamentFilterDropdown_scroll",
    section: "FilamentFilterDropdown_section",
    sectionLabel: "FilamentFilterDropdown_sectionLabel",
    checkLabel: "FilamentFilterDropdown_checkLabel",
    checkbox: "FilamentFilterDropdown_checkbox",
    pillGroup: "FilamentFilterDropdown_pillGroup",
    pill: "FilamentFilterDropdown_pill",
    pillActive: "FilamentFilterDropdown_pillActive",
    colorGrid: "FilamentFilterDropdown_colorGrid",
    colorSwatch: "FilamentFilterDropdown_colorSwatch",
    colorSwatchActive: "FilamentFilterDropdown_colorSwatchActive"
  };

  // src/components/FilamentFilterDropdown/FilamentFilterDropdown.tsx
  var import_jsx_runtime25 = __toESM(require_react_shim(), 1);
  function FilamentFilterDropdown({
    allMaterials,
    allColors,
    filters,
    onChange
  }) {
    const { t: t2 } = useTranslation();
    const [open, setOpen] = (0, import_react22.useState)(false);
    const ref = (0, import_react22.useRef)(null);
    const activeCount = filters.materials.length + filters.colors.length + (filters.hideDiscontinued ? 1 : 0);
    (0, import_react22.useEffect)(() => {
      function handleClickOutside(e2) {
        if (ref.current && !ref.current.contains(e2.target)) setOpen(false);
      }
      document.addEventListener("mousedown", handleClickOutside);
      return () => document.removeEventListener("mousedown", handleClickOutside);
    }, []);
    function toggleItem(arr, item) {
      return arr.includes(item) ? arr.filter((x) => x !== item) : [...arr, item];
    }
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { ref, className: FilamentFilterDropdown_default.wrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)(
        "button",
        {
          onClick: () => setOpen((o) => !o),
          className: `${FilamentFilterDropdown_default.trigger}${activeCount > 0 ? ` ${FilamentFilterDropdown_default.triggerActive}` : ""}`,
          "aria-label": "Filter filaments",
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("svg", { className: FilamentFilterDropdown_default.triggerIcon, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("polygon", { points: "22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3" }) }),
            activeCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { className: FilamentFilterDropdown_default.count, children: activeCount })
          ]
        }
      ),
      open && /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: FilamentFilterDropdown_default.dropdown, children: [
        /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: FilamentFilterDropdown_default.dropdownHeader, children: [
          /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { className: FilamentFilterDropdown_default.dropdownHeaderLabel, children: t2("filters.header") }),
          activeCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("button", { onClick: () => onChange({ ...DEFAULT_FILAMENT_FILTERS }), className: FilamentFilterDropdown_default.clearBtn, children: t2("filters.clearAll") })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: FilamentFilterDropdown_default.scroll, children: [
          /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: FilamentFilterDropdown_default.section, children: [
            /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("p", { className: FilamentFilterDropdown_default.sectionLabel, children: t2("filters.status") }),
            /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("label", { className: FilamentFilterDropdown_default.checkLabel, children: [
              /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
                "input",
                {
                  type: "checkbox",
                  checked: filters.hideDiscontinued,
                  onChange: () => onChange({ ...filters, hideDiscontinued: !filters.hideDiscontinued }),
                  className: FilamentFilterDropdown_default.checkbox
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { children: t2("filamentFilter.hideDiscontinued") })
            ] })
          ] }),
          allMaterials.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: FilamentFilterDropdown_default.section, children: [
            /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("p", { className: FilamentFilterDropdown_default.sectionLabel, children: t2("filters.material") }),
            /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("div", { className: FilamentFilterDropdown_default.pillGroup, children: allMaterials.map((m) => /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
              "button",
              {
                onClick: () => onChange({ ...filters, materials: toggleItem(filters.materials, m) }),
                className: `${FilamentFilterDropdown_default.pill}${filters.materials.includes(m) ? ` ${FilamentFilterDropdown_default.pillActive}` : ""}`,
                children: m
              },
              m
            )) })
          ] }),
          allColors.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: FilamentFilterDropdown_default.section, children: [
            /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("p", { className: FilamentFilterDropdown_default.sectionLabel, children: t2("filters.color") }),
            /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("div", { className: FilamentFilterDropdown_default.colorGrid, children: allColors.map(({ hex, name }) => /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
              "button",
              {
                onClick: () => onChange({ ...filters, colors: toggleItem(filters.colors, hex) }),
                title: name,
                "aria-label": name,
                className: `${FilamentFilterDropdown_default.colorSwatch}${filters.colors.includes(hex) ? ` ${FilamentFilterDropdown_default.colorSwatchActive}` : ""}`,
                style: { backgroundColor: hex }
              },
              hex
            )) })
          ] })
        ] })
      ] })
    ] });
  }

  // src/components/LanguageSelector/LanguageSelector.tsx
  var import_react23 = __toESM(require_react_shim(), 1);

  // src/components/LanguageSelector/LanguageSelector.module.css
  var LanguageSelector_default = {
    wrap: "LanguageSelector_wrap",
    trigger: "LanguageSelector_trigger",
    label: "LanguageSelector_label",
    chevron: "LanguageSelector_chevron",
    chevronOpen: "LanguageSelector_chevronOpen",
    dropdown: "LanguageSelector_dropdown",
    option: "LanguageSelector_option",
    optionActive: "LanguageSelector_optionActive",
    flagIcon: "LanguageSelector_flagIcon"
  };

  // src/components/LanguageSelector/LanguageSelector.tsx
  var import_jsx_runtime26 = __toESM(require_react_shim(), 1);
  var LANGUAGES = [
    { code: "en", flag: "gb", label: "English" },
    { code: "es", flag: "es", label: "Spanish" },
    { code: "sv", flag: "se", label: "Swedish" }
  ];
  function LanguageSelector() {
    const { i18n } = useTranslation();
    const [open, setOpen] = (0, import_react23.useState)(false);
    const ref = (0, import_react23.useRef)(null);
    const selected = LANGUAGES.find((l) => l.code === i18n.language) ?? LANGUAGES[0];
    function handleSelect(lang) {
      i18n.changeLanguage(lang.code);
      localStorage.setItem("language", lang.code);
      setOpen(false);
    }
    (0, import_react23.useEffect)(() => {
      document.documentElement.lang = i18n.language;
    }, [i18n.language]);
    (0, import_react23.useEffect)(() => {
      function handleClickOutside(e2) {
        if (ref.current && !ref.current.contains(e2.target)) setOpen(false);
      }
      document.addEventListener("mousedown", handleClickOutside);
      return () => document.removeEventListener("mousedown", handleClickOutside);
    }, []);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)("div", { ref, className: LanguageSelector_default.wrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)("button", { onClick: () => setOpen((o) => !o), className: LanguageSelector_default.trigger, "aria-label": "Select language", children: [
        /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: `fi fi-${selected.flag} rounded-sm ${LanguageSelector_default.flagIcon}` }),
        /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: LanguageSelector_default.label, children: selected.label }),
        /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("svg", { className: `${LanguageSelector_default.chevron}${open ? ` ${LanguageSelector_default.chevronOpen}` : ""}`, viewBox: "0 0 12 12", fill: "none", children: /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("path", { d: "M2 4l4 4 4-4", stroke: "currentColor", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }) })
      ] }),
      open && /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("div", { className: LanguageSelector_default.dropdown, children: LANGUAGES.map((lang) => /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(
        "button",
        {
          onClick: () => handleSelect(lang),
          className: `${LanguageSelector_default.option}${selected.code === lang.code ? ` ${LanguageSelector_default.optionActive}` : ""}`,
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: `fi fi-${lang.flag} rounded-sm flex-shrink-0 ${LanguageSelector_default.flagIcon}` }),
            /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { children: lang.label })
          ]
        },
        lang.code
      )) })
    ] });
  }

  // src/components/Header/Header.module.css
  var Header_default = {
    header: "Header_header",
    inner: "Header_inner",
    hamburger: "Header_hamburger",
    logoLink: "Header_logoLink",
    actions: "Header_actions"
  };

  // src/components/Header/Header.tsx
  var import_jsx_runtime27 = __toESM(require_react_shim(), 1);
  function Header({ isDark, onToggleDark, onOpenSidebar }) {
    const { t: t2 } = useTranslation();
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("header", { className: Header_default.header, children: /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { className: Header_default.inner, children: [
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
        "button",
        {
          onClick: onOpenSidebar,
          className: Header_default.hamburger,
          "aria-label": t2("header.openSidebar"),
          children: /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("line", { x1: "3", y1: "6", x2: "21", y2: "6" }),
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("line", { x1: "3", y1: "12", x2: "21", y2: "12" }),
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("line", { x1: "3", y1: "18", x2: "21", y2: "18" })
          ] })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(Link, { to: "/", className: Header_default.logoLink, children: /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(SpoolHubLogo, { height: 52 }) }),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { className: Header_default.actions, children: [
        /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(LanguageSelector, {}),
        /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(DarkModeToggle, { isDark, onToggle: onToggleDark })
      ] })
    ] }) });
  }

  // src/components/LowStockSpools/LowStockSpools.module.css
  var LowStockSpools_default = {
    list: "LowStockSpools_list",
    row: "LowStockSpools_row",
    spoolIconWrap: "LowStockSpools_spoolIconWrap",
    content: "LowStockSpools_content",
    topLine: "LowStockSpools_topLine",
    name: "LowStockSpools_name",
    mat: "LowStockSpools_mat",
    color: "LowStockSpools_color",
    bottomLine: "LowStockSpools_bottomLine",
    barTrack: "LowStockSpools_barTrack",
    barFill: "LowStockSpools_barFill",
    weight: "LowStockSpools_weight",
    weightOf: "LowStockSpools_weightOf",
    pctBadge: "LowStockSpools_pctBadge",
    skeletonRow: "LowStockSpools_skeletonRow",
    skeletonIcon: "LowStockSpools_skeletonIcon",
    shimmer: "LowStockSpools_shimmer",
    skeletonContent: "LowStockSpools_skeletonContent",
    skeletonLine: "LowStockSpools_skeletonLine",
    skeletonLineShort: "LowStockSpools_skeletonLineShort",
    skeletonLineBar: "LowStockSpools_skeletonLineBar",
    skeletonBadge: "LowStockSpools_skeletonBadge",
    empty: "LowStockSpools_empty",
    emptyIcon: "LowStockSpools_emptyIcon",
    emptyText: "LowStockSpools_emptyText"
  };

  // src/components/LowStockSpools/LowStockSpools.tsx
  var import_jsx_runtime28 = __toESM(require_react_shim(), 1);
  function LowStockSpools({ spools, loading }) {
    const { t: t2 } = useTranslation();
    if (loading) {
      return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: LowStockSpools_default.list, children: [0, 1, 2].map((i) => /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: LowStockSpools_default.skeletonRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: LowStockSpools_default.skeletonIcon }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: LowStockSpools_default.skeletonContent, children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: `${LowStockSpools_default.skeletonLine} ${LowStockSpools_default.skeletonLineShort}` }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: `${LowStockSpools_default.skeletonLine} ${LowStockSpools_default.skeletonLineBar}` })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: LowStockSpools_default.skeletonBadge })
      ] }, i)) });
    }
    if (spools.length === 0) {
      return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: LowStockSpools_default.empty, children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round", className: LowStockSpools_default.emptyIcon, children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("polyline", { points: "21 8 21 21 3 21 3 8" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("rect", { x: "1", y: "3", width: "22", height: "5" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("line", { x1: "10", y1: "12", x2: "14", y2: "12" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { className: LowStockSpools_default.emptyText, children: t2("lowStock.noLow") })
      ] });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: LowStockSpools_default.list, children: spools.map((s) => {
      const pct = s.initialWeightG > 0 ? Math.round(s.currentWeightG / s.initialWeightG * 100) : 0;
      const fillColor = pct <= 5 ? "#ef4444" : pct <= 15 ? "#f59e0b" : "#22c55e";
      const bgColor = pct <= 5 ? "rgba(239,68,68,0.12)" : pct <= 15 ? "rgba(245,158,11,0.12)" : "rgba(34,197,94,0.12)";
      return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(Link, { to: `/spools/${s.id}`, className: LowStockSpools_default.row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: LowStockSpools_default.spoolIconWrap, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SpoolIcon, { color: s.colorHex ?? "#6b7280", size: 36 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: LowStockSpools_default.content, children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: LowStockSpools_default.topLine, children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { className: LowStockSpools_default.name, children: s.brand }),
            s.material && /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { className: LowStockSpools_default.mat, children: s.material }),
            s.colorName && /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { className: LowStockSpools_default.color, children: s.colorName })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: LowStockSpools_default.bottomLine, children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: LowStockSpools_default.barTrack, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: LowStockSpools_default.barFill, style: { width: `${pct}%`, background: fillColor } }) }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("span", { className: LowStockSpools_default.weight, children: [
              s.currentWeightG,
              "g ",
              /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("span", { className: LowStockSpools_default.weightOf, children: [
                "/ ",
                s.initialWeightG,
                "g"
              ] })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("span", { className: LowStockSpools_default.pctBadge, style: { color: fillColor, background: bgColor }, children: [
          pct,
          "%"
        ] })
      ] }, s.id);
    }) });
  }

  // src/components/MetricCard/MetricCard.tsx
  var import_react24 = __toESM(require_react_shim(), 1);

  // src/components/MetricCard/MetricCard.module.css
  var MetricCard_default = {
    card: "MetricCard_card",
    expanded: "MetricCard_expanded",
    header: "MetricCard_header",
    label: "MetricCard_label",
    iconWrap: "MetricCard_iconWrap",
    value: "MetricCard_value",
    sub: "MetricCard_sub",
    trend: "MetricCard_trend",
    positive: "MetricCard_positive",
    neutral: "MetricCard_neutral",
    warning: "MetricCard_warning",
    error: "MetricCard_error",
    pulse: "MetricCard_pulse",
    skeleton: "MetricCard_skeleton",
    link: "MetricCard_link",
    details: "MetricCard_details",
    detailRow: "MetricCard_detailRow",
    detailLabel: "MetricCard_detailLabel",
    detailValue: "MetricCard_detailValue",
    detailDot: "MetricCard_detailDot",
    dotPrinting: "MetricCard_dotPrinting",
    dotIdle: "MetricCard_dotIdle"
  };

  // src/components/MetricCard/MetricCard.tsx
  var import_jsx_runtime29 = __toESM(require_react_shim(), 1);
  function useAnimatedNumber(target, duration = 600) {
    const [display, setDisplay] = (0, import_react24.useState)(target);
    const prevRef = (0, import_react24.useRef)(target);
    const seenRealValue = (0, import_react24.useRef)(false);
    (0, import_react24.useEffect)(() => {
      const from = prevRef.current;
      if (from === target) return;
      prevRef.current = target;
      if (!seenRealValue.current) {
        seenRealValue.current = true;
        setDisplay(target);
        return;
      }
      const startTime = performance.now();
      let raf;
      function step(now) {
        const progress = Math.min((now - startTime) / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 3);
        setDisplay(Math.round(from + (target - from) * eased));
        if (progress < 1) raf = requestAnimationFrame(step);
      }
      raf = requestAnimationFrame(step);
      return () => cancelAnimationFrame(raf);
    }, [target, duration]);
    return display;
  }
  function formatNumber(n) {
    return n.toLocaleString();
  }
  function MetricCard({
    label,
    value,
    sub,
    trend,
    to,
    loading,
    icon,
    details
  }) {
    const numericValue = typeof value === "number" ? value : 0;
    const displayed = useAnimatedNumber(loading ? 0 : numericValue);
    const displayedValue = typeof value === "string" ? value : formatNumber(displayed);
    const inner = /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)("div", { className: `${MetricCard_default.card}${details ? ` ${MetricCard_default.expanded}` : ""}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)("div", { className: MetricCard_default.header, children: [
        /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("span", { className: MetricCard_default.label, children: label }),
        /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("div", { className: MetricCard_default.iconWrap, children: icon })
      ] }),
      loading ? /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("div", { className: MetricCard_default.skeleton }) : /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("span", { className: MetricCard_default.value, children: displayedValue }),
      sub && !loading && /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("span", { className: MetricCard_default.sub, children: sub }),
      trend && !loading && /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("span", { className: `${MetricCard_default.trend} ${MetricCard_default[trend.variant]}`, children: trend.text }),
      details && !loading && /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("div", { className: MetricCard_default.details, children: details })
    ] });
    if (to) {
      return /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(Link, { to, className: MetricCard_default.link, children: inner });
    }
    return inner;
  }

  // src/components/NfcScanModal/NfcScanModal.tsx
  var import_react25 = __toESM(require_react_shim(), 1);
  var import_react_dom2 = __toESM(require_react_dom_shim(), 1);

  // src/api/printJobs.ts
  var printJobsApi = {
    getByPrinter: (printerId) => apiClient.get(`/api/printers/${printerId}/jobs`).then((r) => r.data),
    getBySpool: (spoolId) => apiClient.get(`/api/spools/${spoolId}/jobs`).then((r) => r.data),
    getRecent: (limit = 5) => apiClient.get(`/api/print-jobs/recent?limit=${limit}`).then((r) => r.data),
    getAll: (filters = {}) => {
      const params = new URLSearchParams();
      if (filters.page) params.set("page", String(filters.page));
      if (filters.limit) params.set("limit", String(filters.limit));
      if (filters.status) params.set("status", filters.status);
      if (filters.printerId) params.set("printerId", filters.printerId);
      if (filters.spoolId) params.set("spoolId", filters.spoolId);
      if (filters.search) params.set("search", filters.search);
      if (filters.sortBy) params.set("sortBy", filters.sortBy);
      return apiClient.get(`/api/print-jobs?${params}`).then((r) => r.data);
    },
    getById: (id) => apiClient.get(`/api/print-jobs/${id}`).then((r) => r.data)
  };

  // src/components/PrinterPicker/PrinterPicker.module.css
  var PrinterPicker_default = {
    shake: "PrinterPicker_shake",
    wrap: "PrinterPicker_wrap",
    wrapError: "PrinterPicker_wrapError",
    label: "PrinterPicker_label",
    labelError: "PrinterPicker_labelError",
    selectWrap: "PrinterPicker_selectWrap",
    select: "PrinterPicker_select",
    selectError: "PrinterPicker_selectError",
    chevron: "PrinterPicker_chevron",
    chevronError: "PrinterPicker_chevronError",
    amsSection: "PrinterPicker_amsSection",
    amsLabel: "PrinterPicker_amsLabel",
    amsLabelError: "PrinterPicker_amsLabelError",
    amsTray: "PrinterPicker_amsTray",
    amsTrayError: "PrinterPicker_amsTrayError",
    amsBtn: "PrinterPicker_amsBtn",
    amsBtnSelected: "PrinterPicker_amsBtnSelected",
    amsBtnOccupied: "PrinterPicker_amsBtnOccupied",
    amsBtnEmpty: "PrinterPicker_amsBtnEmpty",
    amsBtnLabelColored: "PrinterPicker_amsBtnLabelColored"
  };

  // src/components/PrinterPicker/PrinterPicker.tsx
  var import_jsx_runtime30 = __toESM(require_react_shim(), 1);
  function PrinterPicker({
    printers,
    value,
    onChange,
    amsSlot,
    onAmsSlotChange,
    occupiedSlots = {},
    currentSpoolColor,
    onBusyTrayClick,
    disabled = false,
    error = false,
    amsSlotError = false,
    shakeKey,
    onShakeEnd
  }) {
    const { t: t2 } = useTranslation();
    const selectedPrinter = printers.find((p) => p.id === value);
    const showAms = selectedPrinter?.hasAms && onAmsSlotChange !== void 0;
    return /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)(
      "div",
      {
        className: `${PrinterPicker_default.wrap}${error ? ` ${PrinterPicker_default.wrapError} ${PrinterPicker_default.shake}` : ""}`,
        onAnimationEnd: onShakeEnd,
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("label", { className: `${PrinterPicker_default.label}${error ? ` ${PrinterPicker_default.labelError}` : ""}`, children: error ? t2("spoolForm.assignPrinterWarning") : t2("spoolForm.assignedPrinter") }),
          /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)(
            "select",
            {
              value: value ?? "",
              onChange: (e2) => {
                const v = e2.target.value || null;
                onChange(v);
                if (!v) onAmsSlotChange?.(null);
              },
              disabled,
              className: `${PrinterPicker_default.select}${error ? ` ${PrinterPicker_default.selectError}` : ""}`,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("option", { value: "", children: t2("spoolForm.noPrinter") }),
                printers.map((p) => /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("option", { value: p.id, children: [
                  p.name,
                  " \xB7 ",
                  p.brand,
                  " ",
                  p.model
                ] }, p.id))
              ]
            }
          ),
          showAms && /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { className: PrinterPicker_default.amsSection, children: [
            /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("p", { className: `${PrinterPicker_default.amsLabel}${amsSlotError ? ` ${PrinterPicker_default.amsLabelError}` : ""}`, children: amsSlotError ? t2("spoolForm.selectTrayWarning") : t2("spoolForm.amsTray") }),
            /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("div", { className: `${PrinterPicker_default.amsTray}${amsSlotError ? ` ${PrinterPicker_default.amsTrayError}` : ""}`, children: [1, 2, 3, 4].map((slot) => {
              const occupant = occupiedSlots[slot];
              const isSelected = amsSlot === slot;
              const bgColor = isSelected ? currentSpoolColor : occupant?.colorHex;
              const hasColor = !!bgColor;
              return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
                "button",
                {
                  type: "button",
                  title: isSelected ? "This spool" : occupant ? `${occupant.colorName} \u2014 ${occupant.brand} ${occupant.material}` : `Tray ${slot} \u2014 empty`,
                  onClick: () => {
                    if (isSelected) {
                      onAmsSlotChange(null);
                      return;
                    }
                    if (occupant && onBusyTrayClick) {
                      onBusyTrayClick(slot);
                      return;
                    }
                    onAmsSlotChange(slot);
                  },
                  disabled,
                  style: hasColor ? { backgroundColor: bgColor } : void 0,
                  className: `${PrinterPicker_default.amsBtn}${isSelected ? ` ${PrinterPicker_default.amsBtnSelected}` : hasColor ? ` ${PrinterPicker_default.amsBtnOccupied}` : ` ${PrinterPicker_default.amsBtnEmpty}`}`,
                  children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { className: hasColor ? PrinterPicker_default.amsBtnLabelColored : void 0, children: slot })
                },
                slot
              );
            }) })
          ] })
        ]
      },
      shakeKey
    );
  }

  // src/components/NfcScanModal/NfcScanModal.module.css
  var NfcScanModal_default = {
    pulse: "NfcScanModal_pulse",
    overlay: "NfcScanModal_overlay",
    modal: "NfcScanModal_modal",
    headerCard: "NfcScanModal_headerCard",
    spoolHead: "NfcScanModal_spoolHead",
    spoolIconWrap: "NfcScanModal_spoolIconWrap",
    spoolInfo: "NfcScanModal_spoolInfo",
    spoolBrandRow: "NfcScanModal_spoolBrandRow",
    spoolBrand: "NfcScanModal_spoolBrand",
    spoolName: "NfcScanModal_spoolName",
    spoolTagRow: "NfcScanModal_spoolTagRow",
    spoolHex: "NfcScanModal_spoolHex",
    barWrap: "NfcScanModal_barWrap",
    barHeader: "NfcScanModal_barHeader",
    barPct: "NfcScanModal_barPct",
    barTrack: "NfcScanModal_barTrack",
    barFill: "NfcScanModal_barFill",
    recentSection: "NfcScanModal_recentSection",
    recentLabel: "NfcScanModal_recentLabel",
    skeletonList: "NfcScanModal_skeletonList",
    skeletonRow: "NfcScanModal_skeletonRow",
    jobList: "NfcScanModal_jobList",
    jobRow: "NfcScanModal_jobRow",
    jobLeft: "NfcScanModal_jobLeft",
    jobName: "NfcScanModal_jobName",
    jobPrinter: "NfcScanModal_jobPrinter",
    jobRight: "NfcScanModal_jobRight",
    jobGrams: "NfcScanModal_jobGrams",
    jobTime: "NfcScanModal_jobTime",
    footer: "NfcScanModal_footer",
    btnRow: "NfcScanModal_btnRow",
    assignWrap: "NfcScanModal_assignWrap",
    btnSecondary: "NfcScanModal_btnSecondary",
    btnPrimary: "NfcScanModal_btnPrimary"
  };

  // src/components/NfcScanModal/NfcScanModal.tsx
  var import_jsx_runtime31 = __toESM(require_react_shim(), 1);
  function timeAgo(iso) {
    const diff = Date.now() - new Date(iso).getTime();
    const mins = Math.floor(diff / 6e4);
    const hours = Math.floor(diff / 36e5);
    const days = Math.floor(diff / 864e5);
    if (mins < 1) return "Just now";
    if (mins < 60) return `${mins}m ago`;
    if (hours < 24) return `${hours}h ago`;
    if (days === 1) return "Yesterday";
    if (days < 7) return `${days}d ago`;
    return new Date(iso).toLocaleDateString(void 0, { month: "short", day: "numeric" });
  }
  function NfcScanModal({ spool, onClose }) {
    const navigate = useNavigate();
    const { t: t2 } = useTranslation();
    const [step, setStep] = (0, import_react25.useState)("info");
    const [printers, setPrinters] = (0, import_react25.useState)([]);
    const [allSpools, setAllSpools] = (0, import_react25.useState)([]);
    const [printerId, setPrinterId] = (0, import_react25.useState)(spool.printerId);
    const [amsSlot, setAmsSlot] = (0, import_react25.useState)(spool.amsSlot);
    const [saving, setSaving] = (0, import_react25.useState)(false);
    const [recentJobs, setRecentJobs] = (0, import_react25.useState)([]);
    const [jobsLoading, setJobsLoading] = (0, import_react25.useState)(true);
    (0, import_react25.useEffect)(() => {
      printersApi.getAll().then(setPrinters).catch(() => {
      });
      spoolsApi.getAll().then(setAllSpools).catch(() => {
      });
      printJobsApi.getBySpool(spool.id).then((jobs) => setRecentJobs(jobs.slice(0, 3))).catch(() => {
      }).finally(() => setJobsLoading(false));
    }, [spool.id]);
    const occupiedSlots = (0, import_react25.useMemo)(() => {
      if (!printerId) return {};
      const result = {};
      for (const s of allSpools) {
        if (s.printerId === printerId && s.amsSlot != null && s.id !== spool.id) {
          result[s.amsSlot] = { colorHex: s.colorHex, colorName: s.colorName, brand: s.brand, material: s.material };
        }
      }
      return result;
    }, [allSpools, printerId, spool.id]);
    function goToDetails() {
      navigate(`/spools/${spool.id}`);
      onClose();
    }
    async function handleActivate() {
      setSaving(true);
      try {
        await spoolsApi.activate(spool.id);
        if (printerId) await spoolsApi.assignPrinter(spool.id, { printerId, amsSlot });
        window.dispatchEvent(new CustomEvent("spools-updated"));
        onClose();
      } finally {
        setSaving(false);
      }
    }
    const pct = Math.min(100, Math.round(spool.currentWeightG / spool.initialWeightG * 100));
    return (0, import_react_dom2.createPortal)(
      /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: NfcScanModal_default.overlay, onClick: onClose, children: /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.modal, onClick: (e2) => e2.stopPropagation(), children: [
        /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: NfcScanModal_default.headerCard, children: /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.spoolHead, children: [
          /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: NfcScanModal_default.spoolIconWrap, children: /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(SpoolIcon, { color: spool.colorHex, size: 64 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.spoolInfo, children: [
            /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.spoolBrandRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(BrandLogo, { brand: spool.brand, size: 13 }),
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { className: NfcScanModal_default.spoolBrand, children: spool.brand })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("p", { className: NfcScanModal_default.spoolName, children: spool.colorName }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.spoolTagRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { className: NfcScanModal_default.spoolHex, children: spool.colorHex }),
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(MaterialTag, { material: spool.material })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.barWrap, children: [
              /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.barHeader, children: [
                /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("span", { children: [
                  Math.round(spool.currentWeightG),
                  "g / ",
                  Math.round(spool.initialWeightG),
                  "g"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("span", { className: NfcScanModal_default.barPct, children: [
                  pct,
                  "%"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: NfcScanModal_default.barTrack, children: /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: NfcScanModal_default.barFill, style: { width: `${pct}%` } }) })
            ] })
          ] })
        ] }) }),
        step === "info" && (jobsLoading || recentJobs.length > 0) && /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.recentSection, children: [
          /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("p", { className: NfcScanModal_default.recentLabel, children: "Recent prints" }),
          jobsLoading ? /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: NfcScanModal_default.skeletonList, children: [0, 1].map((i) => /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: NfcScanModal_default.skeletonRow }, i)) }) : /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: NfcScanModal_default.jobList, children: recentJobs.map((job) => /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.jobRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.jobLeft, children: [
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { className: NfcScanModal_default.jobName, children: job.printFileName ?? "Unnamed print" }),
              job.printerName && /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { className: NfcScanModal_default.jobPrinter, children: job.printerName })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.jobRight, children: [
              job.gramsUsed > 0 && /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("span", { className: NfcScanModal_default.jobGrams, children: [
                "\u2212",
                job.gramsUsed.toFixed(1),
                "g"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { className: NfcScanModal_default.jobTime, children: timeAgo(job.startedAt) })
            ] })
          ] }, job.id)) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.footer, children: [
          step === "info" && /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.btnRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("button", { onClick: goToDetails, className: NfcScanModal_default.btnSecondary, children: t2("scan.details") }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("button", { onClick: () => setStep("assign"), className: NfcScanModal_default.btnPrimary, children: t2("scan.activateSpool") })
          ] }),
          step === "assign" && /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.assignWrap, children: [
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(
              PrinterPicker,
              {
                printers,
                value: printerId,
                onChange: (v) => {
                  setPrinterId(v);
                  if (!v) setAmsSlot(null);
                },
                amsSlot,
                onAmsSlotChange: setAmsSlot,
                occupiedSlots,
                currentSpoolColor: spool.colorHex
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: NfcScanModal_default.btnRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("button", { onClick: () => setStep("info"), className: NfcScanModal_default.btnSecondary, children: t2("scan.back") }),
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("button", { onClick: handleActivate, disabled: saving, className: NfcScanModal_default.btnPrimary, children: saving ? "\u2026" : t2("scan.activate") })
            ] })
          ] })
        ] })
      ] }) }),
      document.body
    );
  }

  // src/components/Pagination/Pagination.tsx
  var import_react26 = __toESM(require_react_shim(), 1);

  // src/components/Pagination/Pagination.module.css
  var Pagination_default = {
    wrap: "Pagination_wrap",
    info: "Pagination_info",
    controls: "Pagination_controls",
    perPageWrap: "Pagination_perPageWrap",
    perPageLabel: "Pagination_perPageLabel",
    perPageSelect: "Pagination_perPageSelect",
    btn: "Pagination_btn",
    btnActive: "Pagination_btnActive",
    ellipsis: "Pagination_ellipsis",
    pagesDesktop: "Pagination_pagesDesktop",
    pagesMobile: "Pagination_pagesMobile"
  };

  // src/components/Pagination/Pagination.tsx
  var import_jsx_runtime32 = __toESM(require_react_shim(), 1);
  var DEFAULT_PER_PAGE_OPTIONS = [12, 20, 32, 48, 96];
  function Pagination({ total, page, perPage, onPageChange, onPerPageChange, perPageOptions = DEFAULT_PER_PAGE_OPTIONS, itemLabel = "spools", className }) {
    const { t: t2 } = useTranslation();
    const totalPages = Math.ceil(total / perPage);
    (0, import_react26.useEffect)(() => {
      if (total > 0 && page > totalPages) onPageChange(totalPages);
    }, [total, page, totalPages, onPageChange]);
    const from = Math.min((page - 1) * perPage + 1, total);
    const to = Math.min(page * perPage, total);
    function getPages() {
      if (totalPages <= 7) return Array.from({ length: totalPages }, (_, i) => i + 1);
      if (page <= 4) return [1, 2, 3, 4, 5, "\u2026", totalPages];
      if (page >= totalPages - 3) return [1, "\u2026", totalPages - 4, totalPages - 3, totalPages - 2, totalPages - 1, totalPages];
      return [1, "\u2026", page - 1, page, page + 1, "\u2026", totalPages];
    }
    function getMobilePages() {
      if (totalPages <= 5) return Array.from({ length: totalPages }, (_, i) => i + 1);
      if (page <= 2) return [1, 2, 3, "\u2026", totalPages];
      if (page >= totalPages - 1) return [1, "\u2026", totalPages - 2, totalPages - 1, totalPages];
      return [1, "\u2026", page, "\u2026", totalPages];
    }
    function renderPages(pages) {
      return pages.map(
        (p, i) => p === "\u2026" ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { className: Pagination_default.ellipsis, children: "\u2026" }, `e-${i}`) : /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
          "button",
          {
            onClick: () => onPageChange(p),
            className: `${Pagination_default.btn}${p === page ? ` ${Pagination_default.btnActive}` : ""}`,
            children: p
          },
          `${p}-${i}`
        )
      );
    }
    if (total === 0 || totalPages <= 1) return null;
    const prevBtn = /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("button", { onClick: () => onPageChange(page - 1), disabled: page === 1, className: Pagination_default.btn, "aria-label": t2("pagination.previousPage"), children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("svg", { className: "w-3.5 h-3.5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("polyline", { points: "15 18 9 12 15 6" }) }) });
    const nextBtn = /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("button", { onClick: () => onPageChange(page + 1), disabled: page === totalPages, className: Pagination_default.btn, "aria-label": t2("pagination.nextPage"), children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("svg", { className: "w-3.5 h-3.5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("polyline", { points: "9 18 15 12 9 6" }) }) });
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: `${Pagination_default.wrap}${className ? ` ${className}` : ""}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("p", { className: Pagination_default.info, children: t2("pagination.showing", { from, to, total, itemLabel: t2(`pagination.${itemLabel}`, itemLabel) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: Pagination_default.controls, children: [
        /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: Pagination_default.perPageWrap, children: [
          /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { className: Pagination_default.perPageLabel, children: t2("pagination.perPage") }),
          /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("select", { value: perPage, onChange: (e2) => onPerPageChange(Number(e2.target.value)), className: Pagination_default.perPageSelect, children: perPageOptions.map((n) => /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("option", { value: n, children: n }, n)) })
        ] }),
        prevBtn,
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("div", { className: Pagination_default.pagesMobile, children: renderPages(getMobilePages()) }),
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("div", { className: Pagination_default.pagesDesktop, children: renderPages(getPages()) }),
        nextBtn
      ] })
    ] });
  }

  // src/components/PrinterCard/PrinterCard.tsx
  var import_react27 = __toESM(require_react_shim(), 1);

  // src/components/StatusRing/StatusRing.module.css
  var StatusRing_default = {
    wrap: "StatusRing_wrap",
    center: "StatusRing_center",
    label: "StatusRing_label",
    sub: "StatusRing_sub",
    ringCanvas: "StatusRing_ringCanvas",
    ringArc: "StatusRing_ringArc"
  };

  // src/components/StatusRing/StatusRing.tsx
  var import_jsx_runtime33 = __toESM(require_react_shim(), 1);
  function StatusRing({ status, size = 76 }) {
    const { t: t2 } = useTranslation();
    const STROKE = size * 0.059;
    const radius = (size - STROKE) / 2;
    const circ = 2 * Math.PI * radius;
    const state = status?.gcodeState?.toUpperCase() ?? "IDLE";
    const pct = status?.progressPercent ?? 0;
    const isRunning = state === "RUNNING";
    const isPause = state === "PAUSE";
    const isFailed = state === "FAILED";
    const isFinish = state === "FINISH";
    const isIdle = !isRunning && !isPause && !isFailed && !isFinish;
    const arcColor = isRunning ? "#34d399" : isPause ? "#fbbf24" : isFailed ? "#f87171" : isFinish ? "#38bdf8" : "#52525b";
    const dashOffset = isFailed || isFinish ? 0 : isIdle ? circ : circ - pct / 100 * circ;
    const label = isRunning ? `${pct}%` : isPause ? t2("statusRing.pause") : isFailed ? t2("statusRing.error") : isFinish ? t2("statusRing.done") : t2("statusRing.idle");
    const rem = status?.remainingMinutes ?? 0;
    const remLabel = rem >= 60 ? `${Math.floor(rem / 60)}h ${rem % 60}m` : rem > 0 ? `${rem}m` : null;
    const fontSize = Math.round(size * 0.158);
    const subFontSize = Math.round(size * 0.118);
    return /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("div", { className: StatusRing_default.wrap, style: { width: size, height: size }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("svg", { width: size, height: size, className: StatusRing_default.ringCanvas, children: [
        /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("circle", { cx: size / 2, cy: size / 2, r: radius, fill: "none", stroke: "#27272a", strokeWidth: STROKE }),
        /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
          "circle",
          {
            cx: size / 2,
            cy: size / 2,
            r: radius,
            fill: "none",
            stroke: arcColor,
            strokeWidth: STROKE,
            strokeDasharray: circ,
            strokeDashoffset: dashOffset,
            strokeLinecap: "round",
            className: StatusRing_default.ringArc
          }
        )
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("div", { className: StatusRing_default.center, children: [
        /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("span", { style: { fontSize, color: isIdle ? "var(--text-secondary)" : arcColor }, className: StatusRing_default.label, children: label }),
        isRunning && remLabel && /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("span", { style: { fontSize: subFontSize }, className: StatusRing_default.sub, children: remLabel })
      ] })
    ] });
  }

  // src/components/PrinterCard/PrinterCard.module.css
  var PrinterCard_default = {
    card: "PrinterCard_card",
    topGradient: "PrinterCard_topGradient",
    headerLink: "PrinterCard_headerLink",
    photoBlock: "PrinterCard_photoBlock",
    photo: "PrinterCard_photo",
    photoEdgeFade: "PrinterCard_photoEdgeFade",
    infoCol: "PrinterCard_infoCol",
    brandRow: "PrinterCard_brandRow",
    brandName: "PrinterCard_brandName",
    printerName: "PrinterCard_printerName",
    printerModel: "PrinterCard_printerModel",
    errorRow: "PrinterCard_errorRow",
    errorIcon: "PrinterCard_errorIcon",
    errorText: "PrinterCard_errorText",
    statusRingWrap: "PrinterCard_statusRingWrap",
    statusRingSmall: "PrinterCard_statusRingSmall",
    statusRingLarge: "PrinterCard_statusRingLarge",
    section: "PrinterCard_section",
    accordion: "PrinterCard_accordion",
    accordionLeft: "PrinterCard_accordionLeft",
    accordionLabelAms: "PrinterCard_accordionLabelAms",
    accordionLabelSpool: "PrinterCard_accordionLabelSpool",
    accordionSub: "PrinterCard_accordionSub",
    accordionRight: "PrinterCard_accordionRight",
    slotIcons: "PrinterCard_slotIcons",
    slotIcon: "PrinterCard_slotIcon",
    emptySlotIcon: "PrinterCard_emptySlotIcon",
    chevron: "PrinterCard_chevron",
    chevronOpen: "PrinterCard_chevronOpen",
    accordionBody: "PrinterCard_accordionBody",
    slotRow: "PrinterCard_slotRow",
    slotNum: "PrinterCard_slotNum",
    slotContent: "PrinterCard_slotContent",
    spoolRow: "PrinterCard_spoolRow",
    spoolRowInner: "PrinterCard_spoolRowInner",
    spoolIconWrap: "PrinterCard_spoolIconWrap",
    spoolName: "PrinterCard_spoolName",
    spoolMaterial: "PrinterCard_spoolMaterial",
    spoolPct: "PrinterCard_spoolPct",
    spoolBar: "PrinterCard_spoolBar",
    spoolBarFill: "PrinterCard_spoolBarFill",
    emptySlotRow: "PrinterCard_emptySlotRow",
    emptySlotLabel: "PrinterCard_emptySlotLabel",
    emptySlotDot: "PrinterCard_emptySlotDot"
  };

  // src/components/PrinterCard/PrinterCard.tsx
  var import_jsx_runtime34 = __toESM(require_react_shim(), 1);
  function slotBarColor(pct) {
    if (pct <= 10) return "#ef4444";
    if (pct <= 30) return "#f59e0b";
    return "#22c55e";
  }
  function weightPct(s) {
    return s.initialWeightG > 0 ? Math.min(100, Math.round(s.currentWeightG / s.initialWeightG * 100)) : 0;
  }
  function SpoolRow({ spool }) {
    const pct = weightPct(spool);
    const barColor = slotBarColor(pct);
    return /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(Link, { to: `/spools/${spool.id}`, className: PrinterCard_default.spoolRow, children: [
      /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.spoolRowInner, children: [
        /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.spoolIconWrap, children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(SpoolIcon, { color: spool.colorHex, className: PrinterCard_default.spoolIconWrap }) }),
        /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.spoolName, children: spool.colorName }),
        /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.spoolMaterial, children: spool.material }),
        /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("span", { className: PrinterCard_default.spoolPct, style: { color: barColor }, children: [
          pct,
          "%"
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.spoolBar, children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.spoolBarFill, style: { width: `${Math.max(3, pct)}%`, backgroundColor: barColor } }) })
    ] });
  }
  var PlusIcon = () => /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("svg", { width: "10", height: "10", viewBox: "0 0 10 10", fill: "none", stroke: "currentColor", strokeWidth: "1.5", strokeLinecap: "round", children: [
    /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("line", { x1: "5", y1: "1", x2: "5", y2: "9" }),
    /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("line", { x1: "1", y1: "5", x2: "9", y2: "5" })
  ] });
  function EmptySlot({ label, printerId, amsSlot }) {
    const to = amsSlot != null ? `/spools/select?printerId=${printerId}&amsSlot=${amsSlot}` : `/spools/select?printerId=${printerId}`;
    return /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(Link, { to, className: PrinterCard_default.emptySlotRow, children: [
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.emptySlotDot, children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.emptySlotIcon, children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(PlusIcon, {}) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.emptySlotLabel, children: label })
    ] });
  }
  function PrinterCard({ printer, spools, status }) {
    const { t: t2 } = useTranslation();
    const imgSrc = getPrinterImage(printer.brand, printer.model);
    const traySpoolIds = [printer.tray1Spool?.id, printer.tray2Spool?.id, printer.tray3Spool?.id, printer.tray4Spool?.id];
    const amsSlots = printer.hasAms ? traySpoolIds.map((sid) => sid ? spools.find((s) => s.id === sid) ?? null : null) : [];
    const singleSpool = !printer.hasAms ? printer.extraSpool ? spools.find((s) => s.id === printer.extraSpool.id) ?? null : null : null;
    const loadedCount = amsSlots.filter(Boolean).length;
    const [amsOpen, setAmsOpen] = (0, import_react27.useState)(false);
    const [spoolOpen, setSpoolOpen] = (0, import_react27.useState)(false);
    return /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.card, children: [
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.topGradient }),
      /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(Link, { to: `/printers/${printer.id}`, className: PrinterCard_default.headerLink, children: [
        /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.photoBlock, children: [
          /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
            "img",
            {
              src: imgSrc,
              alt: `${printer.brand} ${printer.model}`,
              className: PrinterCard_default.photo,
              onError: (e2) => {
                e2.currentTarget.src = "/printers/generic.svg";
              }
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.photoEdgeFade })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.infoCol, children: [
          /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.brandRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(BrandLogo, { brand: printer.brand, size: 20 }),
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("p", { className: PrinterCard_default.brandName, children: printer.brand })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("p", { className: PrinterCard_default.printerName, children: printer.name }),
          /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("p", { className: PrinterCard_default.printerModel, children: printer.model }),
          status?.connectionError && /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.errorRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("svg", { className: PrinterCard_default.errorIcon, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: [
              /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("circle", { cx: "12", cy: "12", r: "10" }),
              /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("line", { x1: "12", y1: "8", x2: "12", y2: "12" }),
              /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("line", { x1: "12", y1: "16", x2: "12.01", y2: "16" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.errorText, children: "Wrong access code" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.statusRingWrap, children: [
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.statusRingSmall, children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(StatusRing, { status, size: 56 }) }),
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.statusRingLarge, children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(StatusRing, { status, size: 96 }) })
          ] })
        ] })
      ] }),
      printer.hasAms && /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.section, children: [
        /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("button", { onClick: () => setAmsOpen((o) => !o), className: PrinterCard_default.accordion, children: [
          /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.accordionLeft, children: [
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.accordionLabelAms, children: "AMS" }),
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.accordionSub, children: t2("printerCard.amsLoaded", { count: loadedCount }) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.accordionRight, children: [
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.slotIcons, children: amsSlots.map((slot, i) => /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.slotIcon, title: slot ? `${slot.colorName} \xB7 ${slot.material}` : t2("printerCard.noSpoolLoaded"), children: slot ? /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(SpoolIcon, { color: slot.colorHex, className: PrinterCard_default.slotIcon }) : /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.emptySlotIcon, children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(PlusIcon, {}) }) }, i)) }),
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
              "svg",
              {
                className: `${PrinterCard_default.chevron}${amsOpen ? ` ${PrinterCard_default.chevronOpen}` : ""}`,
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                viewBox: "0 0 24 24",
                children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("polyline", { points: "6 9 12 15 18 9" })
              }
            )
          ] })
        ] }),
        amsOpen && /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.accordionBody, children: amsSlots.map((slot, i) => /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.slotRow, children: [
          /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.slotNum, children: i + 1 }),
          /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.slotContent, children: slot ? /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(SpoolRow, { spool: slot }) : /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(EmptySlot, { label: t2("printerCard.noSpoolLoaded"), printerId: printer.id, amsSlot: i + 1 }) })
        ] }, i)) })
      ] }),
      !printer.hasAms && /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.section, children: [
        /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("button", { onClick: () => setSpoolOpen((o) => !o), className: PrinterCard_default.accordion, children: [
          /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.accordionLeft, children: [
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.accordionLabelSpool, children: "Spool" }),
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("span", { className: PrinterCard_default.accordionSub, children: singleSpool ? t2("printerCard.oneAssigned") : t2("printerCard.noneAssigned") })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: PrinterCard_default.accordionRight, children: [
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.slotIcon, children: singleSpool ? /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(SpoolIcon, { color: singleSpool.colorHex, className: PrinterCard_default.slotIcon }) : /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.emptySlotIcon, children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(PlusIcon, {}) }) }),
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
              "svg",
              {
                className: `${PrinterCard_default.chevron}${spoolOpen ? ` ${PrinterCard_default.chevronOpen}` : ""}`,
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                viewBox: "0 0 24 24",
                children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("polyline", { points: "6 9 12 15 18 9" })
              }
            )
          ] })
        ] }),
        spoolOpen && /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: PrinterCard_default.accordionBody, children: singleSpool ? /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(SpoolRow, { spool: singleSpool }) : /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(EmptySlot, { label: t2("printerCard.noSpoolLoaded"), printerId: printer.id }) })
      ] })
    ] });
  }

  // src/components/PrintHistory/PrintHistory.module.css
  var PrintHistory_default = {
    historyList: "PrintHistory_historyList",
    historyEmpty: "PrintHistory_historyEmpty",
    historyItem: "PrintHistory_historyItem",
    historyHeader: "PrintHistory_historyHeader",
    historyName: "PrintHistory_historyName",
    histFileSkelShimmer: "PrintHistory_histFileSkelShimmer",
    fileNameSkeleton: "PrintHistory_fileNameSkeleton",
    statusBadge: "PrintHistory_statusBadge",
    statusFinished: "PrintHistory_statusFinished",
    statusFailed: "PrintHistory_statusFailed",
    statusCancelled: "PrintHistory_statusCancelled",
    statusRunning: "PrintHistory_statusRunning",
    historyRow2: "PrintHistory_historyRow2",
    filamentPills: "PrintHistory_filamentPills",
    pill: "PrintHistory_pill",
    pillLabel: "PrintHistory_pillLabel",
    pillGrams: "PrintHistory_pillGrams",
    usedGrams: "PrintHistory_usedGrams",
    printerName: "PrintHistory_printerName",
    historyDate: "PrintHistory_historyDate",
    historyMeta: "PrintHistory_historyMeta",
    historyTime: "PrintHistory_historyTime"
  };

  // src/components/PrintHistory/PrintHistoryList.tsx
  var import_jsx_runtime35 = __toESM(require_react_shim(), 1);
  function dayLabel2(iso, t2, locale) {
    const d = new Date(iso);
    const now = /* @__PURE__ */ new Date();
    const todayStart = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime();
    const dayStart = new Date(d.getFullYear(), d.getMonth(), d.getDate()).getTime();
    const diffDays = Math.round((todayStart - dayStart) / 864e5);
    if (diffDays <= 0) return t2("common.today");
    if (diffDays === 1) return t2("common.yesterday");
    if (diffDays <= 6) {
      const w = d.toLocaleDateString(locale, { weekday: "long" });
      return w.charAt(0).toUpperCase() + w.slice(1);
    }
    return d.toLocaleDateString(locale, { month: "short", day: "numeric", year: "numeric" });
  }
  function relativeTime2(iso, t2, locale) {
    const diffMs = Date.now() - new Date(iso).getTime();
    if (diffMs < 864e5) {
      const mins = Math.floor(diffMs / 6e4);
      if (mins < 1) return t2("common.justNow");
      if (mins < 60) return t2("common.minutesAgo", { count: mins });
      return t2("common.hoursAgo", { count: Math.floor(diffMs / 36e5) });
    }
    return clock(iso, locale);
  }
  var clock = (iso, locale) => new Date(iso).toLocaleTimeString(locale, { hour: "2-digit", minute: "2-digit", hour12: false });
  function PrintHistoryList({ jobs, showSpool = false }) {
    const { t: t2, i18n } = useTranslation();
    if (jobs.length === 0) {
      return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("p", { className: PrintHistory_default.historyEmpty, children: t2("spoolDetail.noJobs") });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("div", { className: PrintHistory_default.historyList, children: jobs.map((job) => {
      const statusClass = job.status === "finished" ? PrintHistory_default.statusFinished : job.status === "failed" ? PrintHistory_default.statusFailed : job.status === "cancelled" ? PrintHistory_default.statusCancelled : PrintHistory_default.statusRunning;
      const statusLabel = job.status === "finished" ? t2("printerDetail.statusFinished") : job.status === "failed" ? t2("printerDetail.statusFailed") : job.status === "cancelled" ? t2("printerDetail.statusCancelled") : t2("printerDetail.statusRunning");
      const durationMs = job.finishedAt ? new Date(job.finishedAt).getTime() - new Date(job.startedAt).getTime() : null;
      const durationLabel = durationMs != null ? durationMs < 36e5 ? `${Math.round(durationMs / 6e4)}m` : `${Math.floor(durationMs / 36e5)}h ${Math.round(durationMs % 36e5 / 6e4)}m` : t2("printerDetail.ongoing");
      const anchorIso = job.finishedAt ?? job.startedAt;
      const isMultiColor = (job.filaments?.length ?? 0) > 1;
      const pills = isMultiColor ? (job.filaments ?? []).map((f) => ({
        spoolId: f.spoolId,
        colorHex: f.colorHex,
        label: f.colorName ?? "\u2014",
        grams: f.gramsUsed
      })) : [{
        spoolId: job.spoolId,
        colorHex: job.spoolColorHex,
        label: [job.spoolMaterial, job.spoolColorName].filter(Boolean).join(" \xB7 ") || "\u2014",
        grams: job.gramsUsed
      }];
      return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: PrintHistory_default.historyItem, children: [
        /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: PrintHistory_default.historyHeader, children: [
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("p", { className: PrintHistory_default.historyName, children: job.printFileName ?? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: PrintHistory_default.fileNameSkeleton }) }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: `${PrintHistory_default.statusBadge} ${statusClass}`, children: statusLabel })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: PrintHistory_default.historyRow2, children: [
          showSpool ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("div", { className: PrintHistory_default.filamentPills, children: pills.map((pill, i) => {
            const content = /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)(import_jsx_runtime35.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(SpoolIcon, { color: pill.colorHex ?? "#888", className: "w-4 h-4 flex-shrink-0" }),
              /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: PrintHistory_default.pillLabel, children: pill.label }),
              /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("span", { className: PrintHistory_default.pillGrams, children: [
                pill.grams.toFixed(1),
                "g"
              ] })
            ] });
            return pill.spoolId ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(Link, { to: `/spools/${pill.spoolId}`, className: PrintHistory_default.pill, children: content }, i) : /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("div", { className: PrintHistory_default.pill, children: content }, i);
          }) }) : /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: PrintHistory_default.filamentPills, children: [
            /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("span", { className: PrintHistory_default.usedGrams, children: [
              job.gramsUsed.toFixed(1),
              "g"
            ] }),
            job.printerName && /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("span", { className: PrintHistory_default.printerName, children: [
              /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("svg", { width: "12", height: "12", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("polyline", { points: "6 9 6 2 18 2 18 9" }),
                /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("path", { d: "M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2" }),
                /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("rect", { x: "6", y: "14", width: "12", height: "8" })
              ] }),
              job.printerName
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: PrintHistory_default.historyDate, children: dayLabel2(anchorIso, t2, i18n.language) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: PrintHistory_default.historyMeta, children: [
          /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("span", { children: [
            clock(job.startedAt, i18n.language),
            job.finishedAt ? ` \u2192 ${clock(job.finishedAt, i18n.language)}` : ""
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { children: "\xB7" }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { children: durationLabel }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: PrintHistory_default.historyTime, children: relativeTime2(anchorIso, t2, i18n.language) })
        ] })
      ] }, job.id);
    }) });
  }

  // src/components/RecentActivity/RecentActivity.tsx
  var import_react28 = __toESM(require_react_shim(), 1);

  // src/api/activities.ts
  function parseSnapshots(data2) {
    data2.activities = data2.activities.map((a) => ({
      ...a,
      snapshot: typeof a.snapshot === "string" ? (() => {
        try {
          return JSON.parse(a.snapshot);
        } catch {
          return null;
        }
      })() : a.snapshot ?? null
    }));
    return data2;
  }
  var activitiesApi = {
    getRecent: (limit = 20) => apiClient.get(`/api/activities?limit=${limit}`).then((r) => parseSnapshots(r.data)),
    get: (params = {}) => {
      const { limit = 20, page = 1, eventType, action, timePeriod, sortBy } = params;
      const qs = new URLSearchParams({ limit: String(limit), page: String(page) });
      if (eventType) qs.set("eventType", eventType);
      if (action) qs.set("action", action);
      if (timePeriod) qs.set("timePeriod", timePeriod);
      if (sortBy) qs.set("sortBy", sortBy);
      return apiClient.get(`/api/activities?${qs}`).then((r) => parseSnapshots(r.data));
    },
    clearAll: () => apiClient.delete("/api/activities")
  };

  // src/components/RecentActivity/RecentActivity.module.css
  var RecentActivity_default = {
    list: "RecentActivity_list",
    emptyState: "RecentActivity_emptyState",
    emptyText: "RecentActivity_emptyText",
    skeleton: "RecentActivity_skeleton",
    skeletonItem: "RecentActivity_skeletonItem",
    pulse: "RecentActivity_pulse"
  };

  // src/components/RecentActivity/RecentActivity.tsx
  var import_jsx_runtime36 = __toESM(require_react_shim(), 1);
  var NFC_EVENTS = /* @__PURE__ */ new Set(["NfcTagRegistered", "NfcTagRemoved"]);
  var SPOOL_EVENTS = /* @__PURE__ */ new Set(["SpoolCreated", "SpoolActivated", "SpoolDeactivated", "SpoolUpdated", "SpoolDeleted", "SpoolScanned", "SpoolAssigned", "SpoolUnassigned"]);
  var PRINT_EVENTS = /* @__PURE__ */ new Set(["PrintStarted", "PrintPaused", "PrintResumed", "PrintCompleted", "PrintFailed", "PrintCancelled"]);
  function needsEnrich(a) {
    if (!a.resourceId) return false;
    if (NFC_EVENTS.has(a.eventType) && !a.snapshot) return true;
    if (SPOOL_EVENTS.has(a.eventType) && a.snapshot && !a.snapshot.stockLocation) return true;
    return false;
  }
  function applyJobToSnapshot(a, job) {
    const remainingMins = job.estimatedFinishTime ?? void 0;
    return {
      ...a,
      snapshot: {
        ...a.snapshot ?? {},
        brand: job.spoolBrand ?? a.snapshot?.brand,
        colorName: job.spoolColorName ?? a.snapshot?.colorName,
        colorHex: job.spoolColorHex ?? a.snapshot?.colorHex,
        material: job.spoolMaterial ?? a.snapshot?.material,
        estimatedMins: remainingMins ?? a.snapshot?.estimatedMins,
        printFileName: job.printFileName ?? void 0,
        gramsUsed: job.gramsUsed
      }
    };
  }
  async function enrichActivities(activities) {
    const toEnrich = activities.filter(needsEnrich);
    const printEvts = activities.filter((a) => PRINT_EVENTS.has(a.eventType) && a.snapshot?.printJobId);
    if (toEnrich.length === 0 && printEvts.length === 0) return activities;
    const spoolIds = [...new Set(toEnrich.map((a) => a.resourceId))];
    const jobIds = [...new Set(printEvts.map((a) => a.snapshot.printJobId))];
    const spoolById = /* @__PURE__ */ new Map();
    const jobById = /* @__PURE__ */ new Map();
    await Promise.all([
      ...spoolIds.map(async (id) => {
        try {
          const s = await spoolsApi.getById(id);
          spoolById.set(id, {
            brand: s.brand,
            colorName: s.colorName,
            colorHex: s.colorHex,
            material: s.material,
            weight: Math.round(s.currentWeightG),
            stockLocation: s.stockLocation ?? void 0
          });
        } catch {
        }
      }),
      ...jobIds.map(async (id) => {
        try {
          jobById.set(id, await printJobsApi.getById(id));
        } catch {
        }
      })
    ]);
    return activities.map((a) => {
      if (PRINT_EVENTS.has(a.eventType) && a.snapshot?.printJobId) {
        const job = jobById.get(a.snapshot.printJobId);
        if (job) return applyJobToSnapshot(a, job);
      }
      if (!a.resourceId) return a;
      const s = spoolById.get(a.resourceId);
      if (!s) return a;
      if (NFC_EVENTS.has(a.eventType) && !a.snapshot) return { ...a, snapshot: s };
      if (SPOOL_EVENTS.has(a.eventType) && a.snapshot && !a.snapshot.stockLocation && s.stockLocation)
        return { ...a, snapshot: { ...a.snapshot, stockLocation: s.stockLocation } };
      return a;
    });
  }
  function RecentActivity({ limit = 3 }) {
    const { t: t2 } = useTranslation();
    const [activities, setActivities] = (0, import_react28.useState)([]);
    const [loading, setLoading] = (0, import_react28.useState)(true);
    const { refreshKey } = useConnection();
    (0, import_react28.useEffect)(() => {
      setLoading(true);
      activitiesApi.getRecent(20).then((r) => enrichActivities(r.activities)).then((enriched) => {
        setActivities(enriched);
        setLoading(false);
      }).catch(() => {
      });
    }, [refreshKey]);
    (0, import_react28.useEffect)(() => {
      activitiesApi.getRecent(20).then((r) => enrichActivities(r.activities)).then((enriched) => setActivities(enriched)).catch(() => {
      });
    }, [limit]);
    (0, import_react28.useEffect)(() => {
      const timer = setInterval(() => {
        activitiesApi.getRecent(20).then((r) => enrichActivities(r.activities)).then((enriched) => setActivities(enriched)).catch(() => {
        });
      }, 1e4);
      return () => clearInterval(timer);
    }, []);
    if (loading) {
      return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: RecentActivity_default.skeleton, children: [0, 1, 2].map((i) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: RecentActivity_default.skeletonItem }, i)) });
    }
    if (activities.length === 0) {
      return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: RecentActivity_default.emptyState, children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("p", { className: RecentActivity_default.emptyText, children: t2("recentActivity.noActivity") }) });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { children: activities.map((a) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ActivityCard, { activity: a, flat: true }, a.id)) });
  }

  // src/components/ScanView/ScanView.tsx
  var import_react34 = __toESM(require_react_shim(), 1);

  // src/hooks/useNfcHub.ts
  var import_react29 = __toESM(require_react_shim(), 1);

  // node_modules/@microsoft/signalr/dist/esm/Errors.js
  var HttpError = class extends Error {
    /** Constructs a new instance of {@link @microsoft/signalr.HttpError}.
     *
     * @param {string} errorMessage A descriptive error message.
     * @param {number} statusCode The HTTP status code represented by this error.
     */
    constructor(errorMessage, statusCode) {
      const trueProto = new.target.prototype;
      super(`${errorMessage}: Status code '${statusCode}'`);
      this.statusCode = statusCode;
      this.__proto__ = trueProto;
    }
  };
  var TimeoutError = class extends Error {
    /** Constructs a new instance of {@link @microsoft/signalr.TimeoutError}.
     *
     * @param {string} errorMessage A descriptive error message.
     */
    constructor(errorMessage = "A timeout occurred.") {
      const trueProto = new.target.prototype;
      super(errorMessage);
      this.__proto__ = trueProto;
    }
  };
  var AbortError = class extends Error {
    /** Constructs a new instance of {@link AbortError}.
     *
     * @param {string} errorMessage A descriptive error message.
     */
    constructor(errorMessage = "An abort occurred.") {
      const trueProto = new.target.prototype;
      super(errorMessage);
      this.__proto__ = trueProto;
    }
  };
  var UnsupportedTransportError = class extends Error {
    /** Constructs a new instance of {@link @microsoft/signalr.UnsupportedTransportError}.
     *
     * @param {string} message A descriptive error message.
     * @param {HttpTransportType} transport The {@link @microsoft/signalr.HttpTransportType} this error occurred on.
     */
    constructor(message, transport) {
      const trueProto = new.target.prototype;
      super(message);
      this.transport = transport;
      this.errorType = "UnsupportedTransportError";
      this.__proto__ = trueProto;
    }
  };
  var DisabledTransportError = class extends Error {
    /** Constructs a new instance of {@link @microsoft/signalr.DisabledTransportError}.
     *
     * @param {string} message A descriptive error message.
     * @param {HttpTransportType} transport The {@link @microsoft/signalr.HttpTransportType} this error occurred on.
     */
    constructor(message, transport) {
      const trueProto = new.target.prototype;
      super(message);
      this.transport = transport;
      this.errorType = "DisabledTransportError";
      this.__proto__ = trueProto;
    }
  };
  var FailedToStartTransportError = class extends Error {
    /** Constructs a new instance of {@link @microsoft/signalr.FailedToStartTransportError}.
     *
     * @param {string} message A descriptive error message.
     * @param {HttpTransportType} transport The {@link @microsoft/signalr.HttpTransportType} this error occurred on.
     */
    constructor(message, transport) {
      const trueProto = new.target.prototype;
      super(message);
      this.transport = transport;
      this.errorType = "FailedToStartTransportError";
      this.__proto__ = trueProto;
    }
  };
  var FailedToNegotiateWithServerError = class extends Error {
    /** Constructs a new instance of {@link @microsoft/signalr.FailedToNegotiateWithServerError}.
     *
     * @param {string} message A descriptive error message.
     */
    constructor(message) {
      const trueProto = new.target.prototype;
      super(message);
      this.errorType = "FailedToNegotiateWithServerError";
      this.__proto__ = trueProto;
    }
  };
  var AggregateErrors = class extends Error {
    /** Constructs a new instance of {@link @microsoft/signalr.AggregateErrors}.
     *
     * @param {string} message A descriptive error message.
     * @param {Error[]} innerErrors The collection of errors this error is aggregating.
     */
    constructor(message, innerErrors) {
      const trueProto = new.target.prototype;
      super(message);
      this.innerErrors = innerErrors;
      this.__proto__ = trueProto;
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/HttpClient.js
  var HttpResponse = class {
    constructor(statusCode, statusText, content) {
      this.statusCode = statusCode;
      this.statusText = statusText;
      this.content = content;
    }
  };
  var HttpClient = class {
    get(url, options) {
      return this.send({
        ...options,
        method: "GET",
        url
      });
    }
    post(url, options) {
      return this.send({
        ...options,
        method: "POST",
        url
      });
    }
    delete(url, options) {
      return this.send({
        ...options,
        method: "DELETE",
        url
      });
    }
    /** Gets all cookies that apply to the specified URL.
     *
     * @param url The URL that the cookies are valid for.
     * @returns {string} A string containing all the key-value cookie pairs for the specified URL.
     */
    // @ts-ignore
    getCookieString(url) {
      return "";
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/ILogger.js
  var LogLevel;
  (function(LogLevel2) {
    LogLevel2[LogLevel2["Trace"] = 0] = "Trace";
    LogLevel2[LogLevel2["Debug"] = 1] = "Debug";
    LogLevel2[LogLevel2["Information"] = 2] = "Information";
    LogLevel2[LogLevel2["Warning"] = 3] = "Warning";
    LogLevel2[LogLevel2["Error"] = 4] = "Error";
    LogLevel2[LogLevel2["Critical"] = 5] = "Critical";
    LogLevel2[LogLevel2["None"] = 6] = "None";
  })(LogLevel || (LogLevel = {}));

  // node_modules/@microsoft/signalr/dist/esm/Loggers.js
  var NullLogger = class {
    constructor() {
    }
    /** @inheritDoc */
    // eslint-disable-next-line
    log(_logLevel, _message) {
    }
  };
  NullLogger.instance = new NullLogger();

  // node_modules/@microsoft/signalr/dist/esm/pkg-version.js
  var VERSION3 = "10.0.0";

  // node_modules/@microsoft/signalr/dist/esm/Utils.js
  var Arg = class {
    static isRequired(val, name) {
      if (val === null || val === void 0) {
        throw new Error(`The '${name}' argument is required.`);
      }
    }
    static isNotEmpty(val, name) {
      if (!val || val.match(/^\s*$/)) {
        throw new Error(`The '${name}' argument should not be empty.`);
      }
    }
    static isIn(val, values, name) {
      if (!(val in values)) {
        throw new Error(`Unknown ${name} value: ${val}.`);
      }
    }
  };
  var Platform = class _Platform {
    // react-native has a window but no document so we should check both
    static get isBrowser() {
      return !_Platform.isNode && typeof window === "object" && typeof window.document === "object";
    }
    // WebWorkers don't have a window object so the isBrowser check would fail
    static get isWebWorker() {
      return !_Platform.isNode && typeof self === "object" && "importScripts" in self;
    }
    // react-native has a window but no document
    static get isReactNative() {
      return !_Platform.isNode && typeof window === "object" && typeof window.document === "undefined";
    }
    // Node apps shouldn't have a window object, but WebWorkers don't either
    // so we need to check for both WebWorker and window
    static get isNode() {
      return typeof process !== "undefined" && process.release && process.release.name === "node";
    }
  };
  function getDataDetail(data2, includeContent) {
    let detail = "";
    if (isArrayBuffer2(data2)) {
      detail = `Binary data of length ${data2.byteLength}`;
      if (includeContent) {
        detail += `. Content: '${formatArrayBuffer(data2)}'`;
      }
    } else if (typeof data2 === "string") {
      detail = `String data of length ${data2.length}`;
      if (includeContent) {
        detail += `. Content: '${data2}'`;
      }
    }
    return detail;
  }
  function formatArrayBuffer(data2) {
    const view = new Uint8Array(data2);
    let str = "";
    view.forEach((num) => {
      const pad = num < 16 ? "0" : "";
      str += `0x${pad}${num.toString(16)} `;
    });
    return str.substring(0, str.length - 1);
  }
  function isArrayBuffer2(val) {
    return val && typeof ArrayBuffer !== "undefined" && (val instanceof ArrayBuffer || // Sometimes we get an ArrayBuffer that doesn't satisfy instanceof
    val.constructor && val.constructor.name === "ArrayBuffer");
  }
  async function sendMessage(logger, transportName, httpClient, url, content, options) {
    const headers = {};
    const [name, value] = getUserAgentHeader();
    headers[name] = value;
    logger.log(LogLevel.Trace, `(${transportName} transport) sending data. ${getDataDetail(content, options.logMessageContent)}.`);
    const responseType = isArrayBuffer2(content) ? "arraybuffer" : "text";
    const response = await httpClient.post(url, {
      content,
      headers: { ...headers, ...options.headers },
      responseType,
      timeout: options.timeout,
      withCredentials: options.withCredentials
    });
    logger.log(LogLevel.Trace, `(${transportName} transport) request complete. Response status: ${response.statusCode}.`);
  }
  function createLogger(logger) {
    if (logger === void 0) {
      return new ConsoleLogger(LogLevel.Information);
    }
    if (logger === null) {
      return NullLogger.instance;
    }
    if (logger.log !== void 0) {
      return logger;
    }
    return new ConsoleLogger(logger);
  }
  var SubjectSubscription = class {
    constructor(subject, observer) {
      this._subject = subject;
      this._observer = observer;
    }
    dispose() {
      const index = this._subject.observers.indexOf(this._observer);
      if (index > -1) {
        this._subject.observers.splice(index, 1);
      }
      if (this._subject.observers.length === 0 && this._subject.cancelCallback) {
        this._subject.cancelCallback().catch((_) => {
        });
      }
    }
  };
  var ConsoleLogger = class {
    constructor(minimumLogLevel) {
      this._minLevel = minimumLogLevel;
      this.out = console;
    }
    log(logLevel, message) {
      if (logLevel >= this._minLevel) {
        const msg = `[${(/* @__PURE__ */ new Date()).toISOString()}] ${LogLevel[logLevel]}: ${message}`;
        switch (logLevel) {
          case LogLevel.Critical:
          case LogLevel.Error:
            this.out.error(msg);
            break;
          case LogLevel.Warning:
            this.out.warn(msg);
            break;
          case LogLevel.Information:
            this.out.info(msg);
            break;
          default:
            this.out.log(msg);
            break;
        }
      }
    }
  };
  function getUserAgentHeader() {
    let userAgentHeaderName = "X-SignalR-User-Agent";
    if (Platform.isNode) {
      userAgentHeaderName = "User-Agent";
    }
    return [userAgentHeaderName, constructUserAgent(VERSION3, getOsName(), getRuntime(), getRuntimeVersion())];
  }
  function constructUserAgent(version, os, runtime, runtimeVersion) {
    let userAgent = "Microsoft SignalR/";
    const majorAndMinor = version.split(".");
    userAgent += `${majorAndMinor[0]}.${majorAndMinor[1]}`;
    userAgent += ` (${version}; `;
    if (os && os !== "") {
      userAgent += `${os}; `;
    } else {
      userAgent += "Unknown OS; ";
    }
    userAgent += `${runtime}`;
    if (runtimeVersion) {
      userAgent += `; ${runtimeVersion}`;
    } else {
      userAgent += "; Unknown Runtime Version";
    }
    userAgent += ")";
    return userAgent;
  }
  function getOsName() {
    if (Platform.isNode) {
      switch (process.platform) {
        case "win32":
          return "Windows NT";
        case "darwin":
          return "macOS";
        case "linux":
          return "Linux";
        default:
          return process.platform;
      }
    } else {
      return "";
    }
  }
  function getRuntimeVersion() {
    if (Platform.isNode) {
      return process.versions.node;
    }
    return void 0;
  }
  function getRuntime() {
    if (Platform.isNode) {
      return "NodeJS";
    } else {
      return "Browser";
    }
  }
  function getErrorString(e2) {
    if (e2.stack) {
      return e2.stack;
    } else if (e2.message) {
      return e2.message;
    }
    return `${e2}`;
  }
  function getGlobalThis() {
    if (typeof globalThis !== "undefined") {
      return globalThis;
    }
    if (typeof self !== "undefined") {
      return self;
    }
    if (typeof window !== "undefined") {
      return window;
    }
    if (typeof global !== "undefined") {
      return global;
    }
    throw new Error("could not find global");
  }

  // node_modules/@microsoft/signalr/dist/esm/FetchHttpClient.js
  var FetchHttpClient = class extends HttpClient {
    constructor(logger) {
      super();
      this._logger = logger;
      if (typeof fetch === "undefined" || Platform.isNode) {
        const requireFunc = typeof __webpack_require__ === "function" ? __non_webpack_require__ : __require;
        this._jar = new (requireFunc("tough-cookie")).CookieJar();
        if (typeof fetch === "undefined") {
          this._fetchType = requireFunc("node-fetch");
        } else {
          this._fetchType = fetch;
        }
        this._fetchType = requireFunc("fetch-cookie")(this._fetchType, this._jar);
      } else {
        this._fetchType = fetch.bind(getGlobalThis());
      }
      if (typeof AbortController === "undefined") {
        const requireFunc = typeof __webpack_require__ === "function" ? __non_webpack_require__ : __require;
        this._abortControllerType = requireFunc("abort-controller");
      } else {
        this._abortControllerType = AbortController;
      }
    }
    /** @inheritDoc */
    async send(request) {
      if (request.abortSignal && request.abortSignal.aborted) {
        throw new AbortError();
      }
      if (!request.method) {
        throw new Error("No method defined.");
      }
      if (!request.url) {
        throw new Error("No url defined.");
      }
      const abortController = new this._abortControllerType();
      let error;
      if (request.abortSignal) {
        request.abortSignal.onabort = () => {
          abortController.abort();
          error = new AbortError();
        };
      }
      let timeoutId = null;
      if (request.timeout) {
        const msTimeout = request.timeout;
        timeoutId = setTimeout(() => {
          abortController.abort();
          this._logger.log(LogLevel.Warning, `Timeout from HTTP request.`);
          error = new TimeoutError();
        }, msTimeout);
      }
      if (request.content === "") {
        request.content = void 0;
      }
      if (request.content) {
        request.headers = request.headers || {};
        if (isArrayBuffer2(request.content)) {
          request.headers["Content-Type"] = "application/octet-stream";
        } else {
          request.headers["Content-Type"] = "text/plain;charset=UTF-8";
        }
      }
      let response;
      try {
        response = await this._fetchType(request.url, {
          body: request.content,
          cache: "no-cache",
          credentials: request.withCredentials === true ? "include" : "same-origin",
          headers: {
            "X-Requested-With": "XMLHttpRequest",
            ...request.headers
          },
          method: request.method,
          mode: "cors",
          redirect: "follow",
          signal: abortController.signal
        });
      } catch (e2) {
        if (error) {
          throw error;
        }
        this._logger.log(LogLevel.Warning, `Error from HTTP request. ${e2}.`);
        throw e2;
      } finally {
        if (timeoutId) {
          clearTimeout(timeoutId);
        }
        if (request.abortSignal) {
          request.abortSignal.onabort = null;
        }
      }
      if (!response.ok) {
        const errorMessage = await deserializeContent(response, "text");
        throw new HttpError(errorMessage || response.statusText, response.status);
      }
      const content = deserializeContent(response, request.responseType);
      const payload = await content;
      return new HttpResponse(response.status, response.statusText, payload);
    }
    getCookieString(url) {
      let cookies = "";
      if (Platform.isNode && this._jar) {
        this._jar.getCookies(url, (e2, c) => cookies = c.join("; "));
      }
      return cookies;
    }
  };
  function deserializeContent(response, responseType) {
    let content;
    switch (responseType) {
      case "arraybuffer":
        content = response.arrayBuffer();
        break;
      case "text":
        content = response.text();
        break;
      case "blob":
      case "document":
      case "json":
        throw new Error(`${responseType} is not supported.`);
      default:
        content = response.text();
        break;
    }
    return content;
  }

  // node_modules/@microsoft/signalr/dist/esm/XhrHttpClient.js
  var XhrHttpClient = class extends HttpClient {
    constructor(logger) {
      super();
      this._logger = logger;
    }
    /** @inheritDoc */
    send(request) {
      if (request.abortSignal && request.abortSignal.aborted) {
        return Promise.reject(new AbortError());
      }
      if (!request.method) {
        return Promise.reject(new Error("No method defined."));
      }
      if (!request.url) {
        return Promise.reject(new Error("No url defined."));
      }
      return new Promise((resolve, reject) => {
        const xhr = new XMLHttpRequest();
        xhr.open(request.method, request.url, true);
        xhr.withCredentials = request.withCredentials === void 0 ? true : request.withCredentials;
        xhr.setRequestHeader("X-Requested-With", "XMLHttpRequest");
        if (request.content === "") {
          request.content = void 0;
        }
        if (request.content) {
          if (isArrayBuffer2(request.content)) {
            xhr.setRequestHeader("Content-Type", "application/octet-stream");
          } else {
            xhr.setRequestHeader("Content-Type", "text/plain;charset=UTF-8");
          }
        }
        const headers = request.headers;
        if (headers) {
          Object.keys(headers).forEach((header) => {
            xhr.setRequestHeader(header, headers[header]);
          });
        }
        if (request.responseType) {
          xhr.responseType = request.responseType;
        }
        if (request.abortSignal) {
          request.abortSignal.onabort = () => {
            xhr.abort();
            reject(new AbortError());
          };
        }
        if (request.timeout) {
          xhr.timeout = request.timeout;
        }
        xhr.onload = () => {
          if (request.abortSignal) {
            request.abortSignal.onabort = null;
          }
          if (xhr.status >= 200 && xhr.status < 300) {
            resolve(new HttpResponse(xhr.status, xhr.statusText, xhr.response || xhr.responseText));
          } else {
            reject(new HttpError(xhr.response || xhr.responseText || xhr.statusText, xhr.status));
          }
        };
        xhr.onerror = () => {
          this._logger.log(LogLevel.Warning, `Error from HTTP request. ${xhr.status}: ${xhr.statusText}.`);
          reject(new HttpError(xhr.statusText, xhr.status));
        };
        xhr.ontimeout = () => {
          this._logger.log(LogLevel.Warning, `Timeout from HTTP request.`);
          reject(new TimeoutError());
        };
        xhr.send(request.content);
      });
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/DefaultHttpClient.js
  var DefaultHttpClient = class extends HttpClient {
    /** Creates a new instance of the {@link @microsoft/signalr.DefaultHttpClient}, using the provided {@link @microsoft/signalr.ILogger} to log messages. */
    constructor(logger) {
      super();
      if (typeof fetch !== "undefined" || Platform.isNode) {
        this._httpClient = new FetchHttpClient(logger);
      } else if (typeof XMLHttpRequest !== "undefined") {
        this._httpClient = new XhrHttpClient(logger);
      } else {
        throw new Error("No usable HttpClient found.");
      }
    }
    /** @inheritDoc */
    send(request) {
      if (request.abortSignal && request.abortSignal.aborted) {
        return Promise.reject(new AbortError());
      }
      if (!request.method) {
        return Promise.reject(new Error("No method defined."));
      }
      if (!request.url) {
        return Promise.reject(new Error("No url defined."));
      }
      return this._httpClient.send(request);
    }
    getCookieString(url) {
      return this._httpClient.getCookieString(url);
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/TextMessageFormat.js
  var TextMessageFormat = class _TextMessageFormat {
    static write(output) {
      return `${output}${_TextMessageFormat.RecordSeparator}`;
    }
    static parse(input) {
      if (input[input.length - 1] !== _TextMessageFormat.RecordSeparator) {
        throw new Error("Message is incomplete.");
      }
      const messages = input.split(_TextMessageFormat.RecordSeparator);
      messages.pop();
      return messages;
    }
  };
  TextMessageFormat.RecordSeparatorCode = 30;
  TextMessageFormat.RecordSeparator = String.fromCharCode(TextMessageFormat.RecordSeparatorCode);

  // node_modules/@microsoft/signalr/dist/esm/HandshakeProtocol.js
  var HandshakeProtocol = class {
    // Handshake request is always JSON
    writeHandshakeRequest(handshakeRequest) {
      return TextMessageFormat.write(JSON.stringify(handshakeRequest));
    }
    parseHandshakeResponse(data2) {
      let messageData;
      let remainingData;
      if (isArrayBuffer2(data2)) {
        const binaryData = new Uint8Array(data2);
        const separatorIndex = binaryData.indexOf(TextMessageFormat.RecordSeparatorCode);
        if (separatorIndex === -1) {
          throw new Error("Message is incomplete.");
        }
        const responseLength = separatorIndex + 1;
        messageData = String.fromCharCode.apply(null, Array.prototype.slice.call(binaryData.slice(0, responseLength)));
        remainingData = binaryData.byteLength > responseLength ? binaryData.slice(responseLength).buffer : null;
      } else {
        const textData = data2;
        const separatorIndex = textData.indexOf(TextMessageFormat.RecordSeparator);
        if (separatorIndex === -1) {
          throw new Error("Message is incomplete.");
        }
        const responseLength = separatorIndex + 1;
        messageData = textData.substring(0, responseLength);
        remainingData = textData.length > responseLength ? textData.substring(responseLength) : null;
      }
      const messages = TextMessageFormat.parse(messageData);
      const response = JSON.parse(messages[0]);
      if (response.type) {
        throw new Error("Expected a handshake response from the server.");
      }
      const responseMessage = response;
      return [remainingData, responseMessage];
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/IHubProtocol.js
  var MessageType;
  (function(MessageType2) {
    MessageType2[MessageType2["Invocation"] = 1] = "Invocation";
    MessageType2[MessageType2["StreamItem"] = 2] = "StreamItem";
    MessageType2[MessageType2["Completion"] = 3] = "Completion";
    MessageType2[MessageType2["StreamInvocation"] = 4] = "StreamInvocation";
    MessageType2[MessageType2["CancelInvocation"] = 5] = "CancelInvocation";
    MessageType2[MessageType2["Ping"] = 6] = "Ping";
    MessageType2[MessageType2["Close"] = 7] = "Close";
    MessageType2[MessageType2["Ack"] = 8] = "Ack";
    MessageType2[MessageType2["Sequence"] = 9] = "Sequence";
  })(MessageType || (MessageType = {}));

  // node_modules/@microsoft/signalr/dist/esm/Subject.js
  var Subject = class {
    constructor() {
      this.observers = [];
    }
    next(item) {
      for (const observer of this.observers) {
        observer.next(item);
      }
    }
    error(err) {
      for (const observer of this.observers) {
        if (observer.error) {
          observer.error(err);
        }
      }
    }
    complete() {
      for (const observer of this.observers) {
        if (observer.complete) {
          observer.complete();
        }
      }
    }
    subscribe(observer) {
      this.observers.push(observer);
      return new SubjectSubscription(this, observer);
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/MessageBuffer.js
  var MessageBuffer = class {
    constructor(protocol, connection, bufferSize) {
      this._bufferSize = 1e5;
      this._messages = [];
      this._totalMessageCount = 0;
      this._waitForSequenceMessage = false;
      this._nextReceivingSequenceId = 1;
      this._latestReceivedSequenceId = 0;
      this._bufferedByteCount = 0;
      this._reconnectInProgress = false;
      this._protocol = protocol;
      this._connection = connection;
      this._bufferSize = bufferSize;
    }
    async _send(message) {
      const serializedMessage = this._protocol.writeMessage(message);
      let backpressurePromise = Promise.resolve();
      if (this._isInvocationMessage(message)) {
        this._totalMessageCount++;
        let backpressurePromiseResolver = () => {
        };
        let backpressurePromiseRejector = () => {
        };
        if (isArrayBuffer2(serializedMessage)) {
          this._bufferedByteCount += serializedMessage.byteLength;
        } else {
          this._bufferedByteCount += serializedMessage.length;
        }
        if (this._bufferedByteCount >= this._bufferSize) {
          backpressurePromise = new Promise((resolve, reject) => {
            backpressurePromiseResolver = resolve;
            backpressurePromiseRejector = reject;
          });
        }
        this._messages.push(new BufferedItem(serializedMessage, this._totalMessageCount, backpressurePromiseResolver, backpressurePromiseRejector));
      }
      try {
        if (!this._reconnectInProgress) {
          await this._connection.send(serializedMessage);
        }
      } catch {
        this._disconnected();
      }
      await backpressurePromise;
    }
    _ack(ackMessage) {
      let newestAckedMessage = -1;
      for (let index = 0; index < this._messages.length; index++) {
        const element = this._messages[index];
        if (element._id <= ackMessage.sequenceId) {
          newestAckedMessage = index;
          if (isArrayBuffer2(element._message)) {
            this._bufferedByteCount -= element._message.byteLength;
          } else {
            this._bufferedByteCount -= element._message.length;
          }
          element._resolver();
        } else if (this._bufferedByteCount < this._bufferSize) {
          element._resolver();
        } else {
          break;
        }
      }
      if (newestAckedMessage !== -1) {
        this._messages = this._messages.slice(newestAckedMessage + 1);
      }
    }
    _shouldProcessMessage(message) {
      if (this._waitForSequenceMessage) {
        if (message.type !== MessageType.Sequence) {
          return false;
        } else {
          this._waitForSequenceMessage = false;
          return true;
        }
      }
      if (!this._isInvocationMessage(message)) {
        return true;
      }
      const currentId = this._nextReceivingSequenceId;
      this._nextReceivingSequenceId++;
      if (currentId <= this._latestReceivedSequenceId) {
        if (currentId === this._latestReceivedSequenceId) {
          this._ackTimer();
        }
        return false;
      }
      this._latestReceivedSequenceId = currentId;
      this._ackTimer();
      return true;
    }
    _resetSequence(message) {
      if (message.sequenceId > this._nextReceivingSequenceId) {
        this._connection.stop(new Error("Sequence ID greater than amount of messages we've received."));
        return;
      }
      this._nextReceivingSequenceId = message.sequenceId;
    }
    _disconnected() {
      this._reconnectInProgress = true;
      this._waitForSequenceMessage = true;
    }
    async _resend() {
      const sequenceId = this._messages.length !== 0 ? this._messages[0]._id : this._totalMessageCount + 1;
      await this._connection.send(this._protocol.writeMessage({ type: MessageType.Sequence, sequenceId }));
      const messages = this._messages;
      for (const element of messages) {
        await this._connection.send(element._message);
      }
      this._reconnectInProgress = false;
    }
    _dispose(error) {
      error !== null && error !== void 0 ? error : error = new Error("Unable to reconnect to server.");
      for (const element of this._messages) {
        element._rejector(error);
      }
    }
    _isInvocationMessage(message) {
      switch (message.type) {
        case MessageType.Invocation:
        case MessageType.StreamItem:
        case MessageType.Completion:
        case MessageType.StreamInvocation:
        case MessageType.CancelInvocation:
          return true;
        case MessageType.Close:
        case MessageType.Sequence:
        case MessageType.Ping:
        case MessageType.Ack:
          return false;
      }
    }
    _ackTimer() {
      if (this._ackTimerHandle === void 0) {
        this._ackTimerHandle = setTimeout(async () => {
          try {
            if (!this._reconnectInProgress) {
              await this._connection.send(this._protocol.writeMessage({ type: MessageType.Ack, sequenceId: this._latestReceivedSequenceId }));
            }
          } catch {
          }
          clearTimeout(this._ackTimerHandle);
          this._ackTimerHandle = void 0;
        }, 1e3);
      }
    }
  };
  var BufferedItem = class {
    constructor(message, id, resolver, rejector) {
      this._message = message;
      this._id = id;
      this._resolver = resolver;
      this._rejector = rejector;
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/HubConnection.js
  var DEFAULT_TIMEOUT_IN_MS = 30 * 1e3;
  var DEFAULT_PING_INTERVAL_IN_MS = 15 * 1e3;
  var DEFAULT_STATEFUL_RECONNECT_BUFFER_SIZE = 1e5;
  var HubConnectionState;
  (function(HubConnectionState2) {
    HubConnectionState2["Disconnected"] = "Disconnected";
    HubConnectionState2["Connecting"] = "Connecting";
    HubConnectionState2["Connected"] = "Connected";
    HubConnectionState2["Disconnecting"] = "Disconnecting";
    HubConnectionState2["Reconnecting"] = "Reconnecting";
  })(HubConnectionState || (HubConnectionState = {}));
  var HubConnection = class _HubConnection {
    /** @internal */
    // Using a public static factory method means we can have a private constructor and an _internal_
    // create method that can be used by HubConnectionBuilder. An "internal" constructor would just
    // be stripped away and the '.d.ts' file would have no constructor, which is interpreted as a
    // public parameter-less constructor.
    static create(connection, logger, protocol, reconnectPolicy, serverTimeoutInMilliseconds, keepAliveIntervalInMilliseconds, statefulReconnectBufferSize) {
      return new _HubConnection(connection, logger, protocol, reconnectPolicy, serverTimeoutInMilliseconds, keepAliveIntervalInMilliseconds, statefulReconnectBufferSize);
    }
    constructor(connection, logger, protocol, reconnectPolicy, serverTimeoutInMilliseconds, keepAliveIntervalInMilliseconds, statefulReconnectBufferSize) {
      this._nextKeepAlive = 0;
      this._freezeEventListener = () => {
        this._logger.log(LogLevel.Warning, "The page is being frozen, this will likely lead to the connection being closed and messages being lost. For more information see the docs at https://learn.microsoft.com/aspnet/core/signalr/javascript-client#bsleep");
      };
      Arg.isRequired(connection, "connection");
      Arg.isRequired(logger, "logger");
      Arg.isRequired(protocol, "protocol");
      this.serverTimeoutInMilliseconds = serverTimeoutInMilliseconds !== null && serverTimeoutInMilliseconds !== void 0 ? serverTimeoutInMilliseconds : DEFAULT_TIMEOUT_IN_MS;
      this.keepAliveIntervalInMilliseconds = keepAliveIntervalInMilliseconds !== null && keepAliveIntervalInMilliseconds !== void 0 ? keepAliveIntervalInMilliseconds : DEFAULT_PING_INTERVAL_IN_MS;
      this._statefulReconnectBufferSize = statefulReconnectBufferSize !== null && statefulReconnectBufferSize !== void 0 ? statefulReconnectBufferSize : DEFAULT_STATEFUL_RECONNECT_BUFFER_SIZE;
      this._logger = logger;
      this._protocol = protocol;
      this.connection = connection;
      this._reconnectPolicy = reconnectPolicy;
      this._handshakeProtocol = new HandshakeProtocol();
      this.connection.onreceive = (data2) => this._processIncomingData(data2);
      this.connection.onclose = (error) => this._connectionClosed(error);
      this._callbacks = {};
      this._methods = {};
      this._closedCallbacks = [];
      this._reconnectingCallbacks = [];
      this._reconnectedCallbacks = [];
      this._invocationId = 0;
      this._receivedHandshakeResponse = false;
      this._connectionState = HubConnectionState.Disconnected;
      this._connectionStarted = false;
      this._cachedPingMessage = this._protocol.writeMessage({ type: MessageType.Ping });
    }
    /** Indicates the state of the {@link HubConnection} to the server. */
    get state() {
      return this._connectionState;
    }
    /** Represents the connection id of the {@link HubConnection} on the server. The connection id will be null when the connection is either
     *  in the disconnected state or if the negotiation step was skipped.
     */
    get connectionId() {
      return this.connection ? this.connection.connectionId || null : null;
    }
    /** Indicates the url of the {@link HubConnection} to the server. */
    get baseUrl() {
      return this.connection.baseUrl || "";
    }
    /**
     * Sets a new url for the HubConnection. Note that the url can only be changed when the connection is in either the Disconnected or
     * Reconnecting states.
     * @param {string} url The url to connect to.
     */
    set baseUrl(url) {
      if (this._connectionState !== HubConnectionState.Disconnected && this._connectionState !== HubConnectionState.Reconnecting) {
        throw new Error("The HubConnection must be in the Disconnected or Reconnecting state to change the url.");
      }
      if (!url) {
        throw new Error("The HubConnection url must be a valid url.");
      }
      this.connection.baseUrl = url;
    }
    /** Starts the connection.
     *
     * @returns {Promise<void>} A Promise that resolves when the connection has been successfully established, or rejects with an error.
     */
    start() {
      this._startPromise = this._startWithStateTransitions();
      return this._startPromise;
    }
    async _startWithStateTransitions() {
      if (this._connectionState !== HubConnectionState.Disconnected) {
        return Promise.reject(new Error("Cannot start a HubConnection that is not in the 'Disconnected' state."));
      }
      this._connectionState = HubConnectionState.Connecting;
      this._logger.log(LogLevel.Debug, "Starting HubConnection.");
      try {
        await this._startInternal();
        if (Platform.isBrowser) {
          window.document.addEventListener("freeze", this._freezeEventListener);
        }
        this._connectionState = HubConnectionState.Connected;
        this._connectionStarted = true;
        this._logger.log(LogLevel.Debug, "HubConnection connected successfully.");
      } catch (e2) {
        this._connectionState = HubConnectionState.Disconnected;
        this._logger.log(LogLevel.Debug, `HubConnection failed to start successfully because of error '${e2}'.`);
        return Promise.reject(e2);
      }
    }
    async _startInternal() {
      this._stopDuringStartError = void 0;
      this._receivedHandshakeResponse = false;
      const handshakePromise = new Promise((resolve, reject) => {
        this._handshakeResolver = resolve;
        this._handshakeRejecter = reject;
      });
      await this.connection.start(this._protocol.transferFormat);
      try {
        let version = this._protocol.version;
        if (!this.connection.features.reconnect) {
          version = 1;
        }
        const handshakeRequest = {
          protocol: this._protocol.name,
          version
        };
        this._logger.log(LogLevel.Debug, "Sending handshake request.");
        await this._sendMessage(this._handshakeProtocol.writeHandshakeRequest(handshakeRequest));
        this._logger.log(LogLevel.Information, `Using HubProtocol '${this._protocol.name}'.`);
        this._cleanupTimeout();
        this._resetTimeoutPeriod();
        this._resetKeepAliveInterval();
        await handshakePromise;
        if (this._stopDuringStartError) {
          throw this._stopDuringStartError;
        }
        const useStatefulReconnect = this.connection.features.reconnect || false;
        if (useStatefulReconnect) {
          this._messageBuffer = new MessageBuffer(this._protocol, this.connection, this._statefulReconnectBufferSize);
          this.connection.features.disconnected = this._messageBuffer._disconnected.bind(this._messageBuffer);
          this.connection.features.resend = () => {
            if (this._messageBuffer) {
              return this._messageBuffer._resend();
            }
          };
        }
        if (!this.connection.features.inherentKeepAlive) {
          await this._sendMessage(this._cachedPingMessage);
        }
      } catch (e2) {
        this._logger.log(LogLevel.Debug, `Hub handshake failed with error '${e2}' during start(). Stopping HubConnection.`);
        this._cleanupTimeout();
        this._cleanupPingTimer();
        await this.connection.stop(e2);
        throw e2;
      }
    }
    /** Stops the connection.
     *
     * @returns {Promise<void>} A Promise that resolves when the connection has been successfully terminated, or rejects with an error.
     */
    async stop() {
      const startPromise = this._startPromise;
      this.connection.features.reconnect = false;
      this._stopPromise = this._stopInternal();
      await this._stopPromise;
      try {
        await startPromise;
      } catch (e2) {
      }
    }
    _stopInternal(error) {
      if (this._connectionState === HubConnectionState.Disconnected) {
        this._logger.log(LogLevel.Debug, `Call to HubConnection.stop(${error}) ignored because it is already in the disconnected state.`);
        return Promise.resolve();
      }
      if (this._connectionState === HubConnectionState.Disconnecting) {
        this._logger.log(LogLevel.Debug, `Call to HttpConnection.stop(${error}) ignored because the connection is already in the disconnecting state.`);
        return this._stopPromise;
      }
      const state = this._connectionState;
      this._connectionState = HubConnectionState.Disconnecting;
      this._logger.log(LogLevel.Debug, "Stopping HubConnection.");
      if (this._reconnectDelayHandle) {
        this._logger.log(LogLevel.Debug, "Connection stopped during reconnect delay. Done reconnecting.");
        clearTimeout(this._reconnectDelayHandle);
        this._reconnectDelayHandle = void 0;
        this._completeClose();
        return Promise.resolve();
      }
      if (state === HubConnectionState.Connected) {
        this._sendCloseMessage();
      }
      this._cleanupTimeout();
      this._cleanupPingTimer();
      this._stopDuringStartError = error || new AbortError("The connection was stopped before the hub handshake could complete.");
      return this.connection.stop(error);
    }
    async _sendCloseMessage() {
      try {
        await this._sendWithProtocol(this._createCloseMessage());
      } catch {
      }
    }
    /** Invokes a streaming hub method on the server using the specified name and arguments.
     *
     * @typeparam T The type of the items returned by the server.
     * @param {string} methodName The name of the server method to invoke.
     * @param {any[]} args The arguments used to invoke the server method.
     * @returns {IStreamResult<T>} An object that yields results from the server as they are received.
     */
    stream(methodName, ...args) {
      const [streams, streamIds] = this._replaceStreamingParams(args);
      const invocationDescriptor = this._createStreamInvocation(methodName, args, streamIds);
      let promiseQueue;
      const subject = new Subject();
      subject.cancelCallback = () => {
        const cancelInvocation = this._createCancelInvocation(invocationDescriptor.invocationId);
        delete this._callbacks[invocationDescriptor.invocationId];
        return promiseQueue.then(() => {
          return this._sendWithProtocol(cancelInvocation);
        });
      };
      this._callbacks[invocationDescriptor.invocationId] = (invocationEvent, error) => {
        if (error) {
          subject.error(error);
          return;
        } else if (invocationEvent) {
          if (invocationEvent.type === MessageType.Completion) {
            if (invocationEvent.error) {
              subject.error(new Error(invocationEvent.error));
            } else {
              subject.complete();
            }
          } else {
            subject.next(invocationEvent.item);
          }
        }
      };
      promiseQueue = this._sendWithProtocol(invocationDescriptor).catch((e2) => {
        subject.error(e2);
        delete this._callbacks[invocationDescriptor.invocationId];
      });
      this._launchStreams(streams, promiseQueue);
      return subject;
    }
    _sendMessage(message) {
      this._resetKeepAliveInterval();
      return this.connection.send(message);
    }
    /**
     * Sends a js object to the server.
     * @param message The js object to serialize and send.
     */
    _sendWithProtocol(message) {
      if (this._messageBuffer) {
        return this._messageBuffer._send(message);
      } else {
        return this._sendMessage(this._protocol.writeMessage(message));
      }
    }
    /** Invokes a hub method on the server using the specified name and arguments. Does not wait for a response from the receiver.
     *
     * The Promise returned by this method resolves when the client has sent the invocation to the server. The server may still
     * be processing the invocation.
     *
     * @param {string} methodName The name of the server method to invoke.
     * @param {any[]} args The arguments used to invoke the server method.
     * @returns {Promise<void>} A Promise that resolves when the invocation has been successfully sent, or rejects with an error.
     */
    send(methodName, ...args) {
      const [streams, streamIds] = this._replaceStreamingParams(args);
      const sendPromise = this._sendWithProtocol(this._createInvocation(methodName, args, true, streamIds));
      this._launchStreams(streams, sendPromise);
      return sendPromise;
    }
    /** Invokes a hub method on the server using the specified name and arguments.
     *
     * The Promise returned by this method resolves when the server indicates it has finished invoking the method. When the promise
     * resolves, the server has finished invoking the method. If the server method returns a result, it is produced as the result of
     * resolving the Promise.
     *
     * @typeparam T The expected return type.
     * @param {string} methodName The name of the server method to invoke.
     * @param {any[]} args The arguments used to invoke the server method.
     * @returns {Promise<T>} A Promise that resolves with the result of the server method (if any), or rejects with an error.
     */
    invoke(methodName, ...args) {
      const [streams, streamIds] = this._replaceStreamingParams(args);
      const invocationDescriptor = this._createInvocation(methodName, args, false, streamIds);
      const p = new Promise((resolve, reject) => {
        this._callbacks[invocationDescriptor.invocationId] = (invocationEvent, error) => {
          if (error) {
            reject(error);
            return;
          } else if (invocationEvent) {
            if (invocationEvent.type === MessageType.Completion) {
              if (invocationEvent.error) {
                reject(new Error(invocationEvent.error));
              } else {
                resolve(invocationEvent.result);
              }
            } else {
              reject(new Error(`Unexpected message type: ${invocationEvent.type}`));
            }
          }
        };
        const promiseQueue = this._sendWithProtocol(invocationDescriptor).catch((e2) => {
          reject(e2);
          delete this._callbacks[invocationDescriptor.invocationId];
        });
        this._launchStreams(streams, promiseQueue);
      });
      return p;
    }
    on(methodName, newMethod) {
      if (!methodName || !newMethod) {
        return;
      }
      methodName = methodName.toLowerCase();
      if (!this._methods[methodName]) {
        this._methods[methodName] = [];
      }
      if (this._methods[methodName].indexOf(newMethod) !== -1) {
        return;
      }
      this._methods[methodName].push(newMethod);
    }
    off(methodName, method) {
      if (!methodName) {
        return;
      }
      methodName = methodName.toLowerCase();
      const handlers = this._methods[methodName];
      if (!handlers) {
        return;
      }
      if (method) {
        const removeIdx = handlers.indexOf(method);
        if (removeIdx !== -1) {
          handlers.splice(removeIdx, 1);
          if (handlers.length === 0) {
            delete this._methods[methodName];
          }
        }
      } else {
        delete this._methods[methodName];
      }
    }
    /** Registers a handler that will be invoked when the connection is closed.
     *
     * @param {Function} callback The handler that will be invoked when the connection is closed. Optionally receives a single argument containing the error that caused the connection to close (if any).
     */
    onclose(callback) {
      if (callback) {
        this._closedCallbacks.push(callback);
      }
    }
    /** Registers a handler that will be invoked when the connection starts reconnecting.
     *
     * @param {Function} callback The handler that will be invoked when the connection starts reconnecting. Optionally receives a single argument containing the error that caused the connection to start reconnecting (if any).
     */
    onreconnecting(callback) {
      if (callback) {
        this._reconnectingCallbacks.push(callback);
      }
    }
    /** Registers a handler that will be invoked when the connection successfully reconnects.
     *
     * @param {Function} callback The handler that will be invoked when the connection successfully reconnects.
     */
    onreconnected(callback) {
      if (callback) {
        this._reconnectedCallbacks.push(callback);
      }
    }
    _processIncomingData(data2) {
      this._cleanupTimeout();
      if (!this._receivedHandshakeResponse) {
        data2 = this._processHandshakeResponse(data2);
        this._receivedHandshakeResponse = true;
      }
      if (data2) {
        const messages = this._protocol.parseMessages(data2, this._logger);
        for (const message of messages) {
          if (this._messageBuffer && !this._messageBuffer._shouldProcessMessage(message)) {
            continue;
          }
          switch (message.type) {
            case MessageType.Invocation:
              this._invokeClientMethod(message).catch((e2) => {
                this._logger.log(LogLevel.Error, `Invoke client method threw error: ${getErrorString(e2)}`);
              });
              break;
            case MessageType.StreamItem:
            case MessageType.Completion: {
              const callback = this._callbacks[message.invocationId];
              if (callback) {
                if (message.type === MessageType.Completion) {
                  delete this._callbacks[message.invocationId];
                }
                try {
                  callback(message);
                } catch (e2) {
                  this._logger.log(LogLevel.Error, `Stream callback threw error: ${getErrorString(e2)}`);
                }
              }
              break;
            }
            case MessageType.Ping:
              break;
            case MessageType.Close: {
              this._logger.log(LogLevel.Information, "Close message received from server.");
              const error = message.error ? new Error("Server returned an error on close: " + message.error) : void 0;
              if (message.allowReconnect === true) {
                this.connection.stop(error);
              } else {
                this._stopPromise = this._stopInternal(error);
              }
              break;
            }
            case MessageType.Ack:
              if (this._messageBuffer) {
                this._messageBuffer._ack(message);
              }
              break;
            case MessageType.Sequence:
              if (this._messageBuffer) {
                this._messageBuffer._resetSequence(message);
              }
              break;
            default:
              this._logger.log(LogLevel.Warning, `Invalid message type: ${message.type}.`);
              break;
          }
        }
      }
      this._resetTimeoutPeriod();
    }
    _processHandshakeResponse(data2) {
      let responseMessage;
      let remainingData;
      try {
        [remainingData, responseMessage] = this._handshakeProtocol.parseHandshakeResponse(data2);
      } catch (e2) {
        const message = "Error parsing handshake response: " + e2;
        this._logger.log(LogLevel.Error, message);
        const error = new Error(message);
        this._handshakeRejecter(error);
        throw error;
      }
      if (responseMessage.error) {
        const message = "Server returned handshake error: " + responseMessage.error;
        this._logger.log(LogLevel.Error, message);
        const error = new Error(message);
        this._handshakeRejecter(error);
        throw error;
      } else {
        this._logger.log(LogLevel.Debug, "Server handshake complete.");
      }
      this._handshakeResolver();
      return remainingData;
    }
    _resetKeepAliveInterval() {
      if (this.connection.features.inherentKeepAlive) {
        return;
      }
      this._nextKeepAlive = (/* @__PURE__ */ new Date()).getTime() + this.keepAliveIntervalInMilliseconds;
      this._cleanupPingTimer();
    }
    _resetTimeoutPeriod() {
      if (!this.connection.features || !this.connection.features.inherentKeepAlive) {
        this._timeoutHandle = setTimeout(() => this.serverTimeout(), this.serverTimeoutInMilliseconds);
        let nextPing = this._nextKeepAlive - (/* @__PURE__ */ new Date()).getTime();
        if (nextPing < 0) {
          if (this._connectionState === HubConnectionState.Connected) {
            this._trySendPingMessage();
          }
          return;
        }
        if (this._pingServerHandle === void 0) {
          if (nextPing < 0) {
            nextPing = 0;
          }
          this._pingServerHandle = setTimeout(async () => {
            if (this._connectionState === HubConnectionState.Connected) {
              await this._trySendPingMessage();
            }
          }, nextPing);
        }
      }
    }
    // eslint-disable-next-line @typescript-eslint/naming-convention
    serverTimeout() {
      this.connection.stop(new Error("Server timeout elapsed without receiving a message from the server."));
    }
    async _invokeClientMethod(invocationMessage) {
      const methodName = invocationMessage.target.toLowerCase();
      const methods = this._methods[methodName];
      if (!methods) {
        this._logger.log(LogLevel.Warning, `No client method with the name '${methodName}' found.`);
        if (invocationMessage.invocationId) {
          this._logger.log(LogLevel.Warning, `No result given for '${methodName}' method and invocation ID '${invocationMessage.invocationId}'.`);
          await this._sendWithProtocol(this._createCompletionMessage(invocationMessage.invocationId, "Client didn't provide a result.", null));
        }
        return;
      }
      const methodsCopy = methods.slice();
      const expectsResponse = invocationMessage.invocationId ? true : false;
      let res;
      let exception;
      let completionMessage;
      for (const m of methodsCopy) {
        try {
          const prevRes = res;
          res = await m.apply(this, invocationMessage.arguments);
          if (expectsResponse && res && prevRes) {
            this._logger.log(LogLevel.Error, `Multiple results provided for '${methodName}'. Sending error to server.`);
            completionMessage = this._createCompletionMessage(invocationMessage.invocationId, `Client provided multiple results.`, null);
          }
          exception = void 0;
        } catch (e2) {
          exception = e2;
          this._logger.log(LogLevel.Error, `A callback for the method '${methodName}' threw error '${e2}'.`);
        }
      }
      if (completionMessage) {
        await this._sendWithProtocol(completionMessage);
      } else if (expectsResponse) {
        if (exception) {
          completionMessage = this._createCompletionMessage(invocationMessage.invocationId, `${exception}`, null);
        } else if (res !== void 0) {
          completionMessage = this._createCompletionMessage(invocationMessage.invocationId, null, res);
        } else {
          this._logger.log(LogLevel.Warning, `No result given for '${methodName}' method and invocation ID '${invocationMessage.invocationId}'.`);
          completionMessage = this._createCompletionMessage(invocationMessage.invocationId, "Client didn't provide a result.", null);
        }
        await this._sendWithProtocol(completionMessage);
      } else {
        if (res) {
          this._logger.log(LogLevel.Error, `Result given for '${methodName}' method but server is not expecting a result.`);
        }
      }
    }
    _connectionClosed(error) {
      this._logger.log(LogLevel.Debug, `HubConnection.connectionClosed(${error}) called while in state ${this._connectionState}.`);
      this._stopDuringStartError = this._stopDuringStartError || error || new AbortError("The underlying connection was closed before the hub handshake could complete.");
      if (this._handshakeResolver) {
        this._handshakeResolver();
      }
      this._cancelCallbacksWithError(error || new Error("Invocation canceled due to the underlying connection being closed."));
      this._cleanupTimeout();
      this._cleanupPingTimer();
      if (this._connectionState === HubConnectionState.Disconnecting) {
        this._completeClose(error);
      } else if (this._connectionState === HubConnectionState.Connected && this._reconnectPolicy) {
        this._reconnect(error);
      } else if (this._connectionState === HubConnectionState.Connected) {
        this._completeClose(error);
      }
    }
    _completeClose(error) {
      if (this._connectionStarted) {
        this._connectionState = HubConnectionState.Disconnected;
        this._connectionStarted = false;
        if (this._messageBuffer) {
          this._messageBuffer._dispose(error !== null && error !== void 0 ? error : new Error("Connection closed."));
          this._messageBuffer = void 0;
        }
        if (Platform.isBrowser) {
          window.document.removeEventListener("freeze", this._freezeEventListener);
        }
        try {
          this._closedCallbacks.forEach((c) => c.apply(this, [error]));
        } catch (e2) {
          this._logger.log(LogLevel.Error, `An onclose callback called with error '${error}' threw error '${e2}'.`);
        }
      }
    }
    async _reconnect(error) {
      const reconnectStartTime = Date.now();
      let previousReconnectAttempts = 0;
      let retryError = error !== void 0 ? error : new Error("Attempting to reconnect due to a unknown error.");
      let nextRetryDelay = this._getNextRetryDelay(previousReconnectAttempts, 0, retryError);
      if (nextRetryDelay === null) {
        this._logger.log(LogLevel.Debug, "Connection not reconnecting because the IRetryPolicy returned null on the first reconnect attempt.");
        this._completeClose(error);
        return;
      }
      this._connectionState = HubConnectionState.Reconnecting;
      if (error) {
        this._logger.log(LogLevel.Information, `Connection reconnecting because of error '${error}'.`);
      } else {
        this._logger.log(LogLevel.Information, "Connection reconnecting.");
      }
      if (this._reconnectingCallbacks.length !== 0) {
        try {
          this._reconnectingCallbacks.forEach((c) => c.apply(this, [error]));
        } catch (e2) {
          this._logger.log(LogLevel.Error, `An onreconnecting callback called with error '${error}' threw error '${e2}'.`);
        }
        if (this._connectionState !== HubConnectionState.Reconnecting) {
          this._logger.log(LogLevel.Debug, "Connection left the reconnecting state in onreconnecting callback. Done reconnecting.");
          return;
        }
      }
      while (nextRetryDelay !== null) {
        this._logger.log(LogLevel.Information, `Reconnect attempt number ${previousReconnectAttempts + 1} will start in ${nextRetryDelay} ms.`);
        await new Promise((resolve) => {
          this._reconnectDelayHandle = setTimeout(resolve, nextRetryDelay);
        });
        this._reconnectDelayHandle = void 0;
        if (this._connectionState !== HubConnectionState.Reconnecting) {
          this._logger.log(LogLevel.Debug, "Connection left the reconnecting state during reconnect delay. Done reconnecting.");
          return;
        }
        try {
          await this._startInternal();
          this._connectionState = HubConnectionState.Connected;
          this._logger.log(LogLevel.Information, "HubConnection reconnected successfully.");
          if (this._reconnectedCallbacks.length !== 0) {
            try {
              this._reconnectedCallbacks.forEach((c) => c.apply(this, [this.connection.connectionId]));
            } catch (e2) {
              this._logger.log(LogLevel.Error, `An onreconnected callback called with connectionId '${this.connection.connectionId}; threw error '${e2}'.`);
            }
          }
          return;
        } catch (e2) {
          this._logger.log(LogLevel.Information, `Reconnect attempt failed because of error '${e2}'.`);
          if (this._connectionState !== HubConnectionState.Reconnecting) {
            this._logger.log(LogLevel.Debug, `Connection moved to the '${this._connectionState}' from the reconnecting state during reconnect attempt. Done reconnecting.`);
            if (this._connectionState === HubConnectionState.Disconnecting) {
              this._completeClose();
            }
            return;
          }
          previousReconnectAttempts++;
          retryError = e2 instanceof Error ? e2 : new Error(e2.toString());
          nextRetryDelay = this._getNextRetryDelay(previousReconnectAttempts, Date.now() - reconnectStartTime, retryError);
        }
      }
      this._logger.log(LogLevel.Information, `Reconnect retries have been exhausted after ${Date.now() - reconnectStartTime} ms and ${previousReconnectAttempts} failed attempts. Connection disconnecting.`);
      this._completeClose();
    }
    _getNextRetryDelay(previousRetryCount, elapsedMilliseconds, retryReason) {
      try {
        return this._reconnectPolicy.nextRetryDelayInMilliseconds({
          elapsedMilliseconds,
          previousRetryCount,
          retryReason
        });
      } catch (e2) {
        this._logger.log(LogLevel.Error, `IRetryPolicy.nextRetryDelayInMilliseconds(${previousRetryCount}, ${elapsedMilliseconds}) threw error '${e2}'.`);
        return null;
      }
    }
    _cancelCallbacksWithError(error) {
      const callbacks = this._callbacks;
      this._callbacks = {};
      Object.keys(callbacks).forEach((key) => {
        const callback = callbacks[key];
        try {
          callback(null, error);
        } catch (e2) {
          this._logger.log(LogLevel.Error, `Stream 'error' callback called with '${error}' threw error: ${getErrorString(e2)}`);
        }
      });
    }
    _cleanupPingTimer() {
      if (this._pingServerHandle) {
        clearTimeout(this._pingServerHandle);
        this._pingServerHandle = void 0;
      }
    }
    _cleanupTimeout() {
      if (this._timeoutHandle) {
        clearTimeout(this._timeoutHandle);
      }
    }
    _createInvocation(methodName, args, nonblocking, streamIds) {
      if (nonblocking) {
        if (streamIds.length !== 0) {
          return {
            target: methodName,
            arguments: args,
            streamIds,
            type: MessageType.Invocation
          };
        } else {
          return {
            target: methodName,
            arguments: args,
            type: MessageType.Invocation
          };
        }
      } else {
        const invocationId = this._invocationId;
        this._invocationId++;
        if (streamIds.length !== 0) {
          return {
            target: methodName,
            arguments: args,
            invocationId: invocationId.toString(),
            streamIds,
            type: MessageType.Invocation
          };
        } else {
          return {
            target: methodName,
            arguments: args,
            invocationId: invocationId.toString(),
            type: MessageType.Invocation
          };
        }
      }
    }
    _launchStreams(streams, promiseQueue) {
      if (streams.length === 0) {
        return;
      }
      if (!promiseQueue) {
        promiseQueue = Promise.resolve();
      }
      for (const streamId in streams) {
        streams[streamId].subscribe({
          complete: () => {
            promiseQueue = promiseQueue.then(() => this._sendWithProtocol(this._createCompletionMessage(streamId)));
          },
          error: (err) => {
            let message;
            if (err instanceof Error) {
              message = err.message;
            } else if (err && err.toString) {
              message = err.toString();
            } else {
              message = "Unknown error";
            }
            promiseQueue = promiseQueue.then(() => this._sendWithProtocol(this._createCompletionMessage(streamId, message)));
          },
          next: (item) => {
            promiseQueue = promiseQueue.then(() => this._sendWithProtocol(this._createStreamItemMessage(streamId, item)));
          }
        });
      }
    }
    _replaceStreamingParams(args) {
      const streams = [];
      const streamIds = [];
      for (let i = 0; i < args.length; i++) {
        const argument = args[i];
        if (this._isObservable(argument)) {
          const streamId = this._invocationId;
          this._invocationId++;
          streams[streamId] = argument;
          streamIds.push(streamId.toString());
          args.splice(i, 1);
        }
      }
      return [streams, streamIds];
    }
    _isObservable(arg) {
      return arg && arg.subscribe && typeof arg.subscribe === "function";
    }
    _createStreamInvocation(methodName, args, streamIds) {
      const invocationId = this._invocationId;
      this._invocationId++;
      if (streamIds.length !== 0) {
        return {
          target: methodName,
          arguments: args,
          invocationId: invocationId.toString(),
          streamIds,
          type: MessageType.StreamInvocation
        };
      } else {
        return {
          target: methodName,
          arguments: args,
          invocationId: invocationId.toString(),
          type: MessageType.StreamInvocation
        };
      }
    }
    _createCancelInvocation(id) {
      return {
        invocationId: id,
        type: MessageType.CancelInvocation
      };
    }
    _createStreamItemMessage(id, item) {
      return {
        invocationId: id,
        item,
        type: MessageType.StreamItem
      };
    }
    _createCompletionMessage(id, error, result) {
      if (error) {
        return {
          error,
          invocationId: id,
          type: MessageType.Completion
        };
      }
      return {
        invocationId: id,
        result,
        type: MessageType.Completion
      };
    }
    _createCloseMessage() {
      return { type: MessageType.Close };
    }
    async _trySendPingMessage() {
      try {
        await this._sendMessage(this._cachedPingMessage);
      } catch {
        this._cleanupPingTimer();
      }
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/DefaultReconnectPolicy.js
  var DEFAULT_RETRY_DELAYS_IN_MILLISECONDS = [0, 2e3, 1e4, 3e4, null];
  var DefaultReconnectPolicy = class {
    constructor(retryDelays) {
      this._retryDelays = retryDelays !== void 0 ? [...retryDelays, null] : DEFAULT_RETRY_DELAYS_IN_MILLISECONDS;
    }
    nextRetryDelayInMilliseconds(retryContext) {
      return this._retryDelays[retryContext.previousRetryCount];
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/HeaderNames.js
  var HeaderNames = class {
  };
  HeaderNames.Authorization = "Authorization";
  HeaderNames.Cookie = "Cookie";

  // node_modules/@microsoft/signalr/dist/esm/AccessTokenHttpClient.js
  var AccessTokenHttpClient = class extends HttpClient {
    constructor(innerClient, accessTokenFactory) {
      super();
      this._innerClient = innerClient;
      this._accessTokenFactory = accessTokenFactory;
    }
    async send(request) {
      let allowRetry = true;
      if (this._accessTokenFactory && (!this._accessToken || request.url && request.url.indexOf("/negotiate?") > 0)) {
        allowRetry = false;
        this._accessToken = await this._accessTokenFactory();
      }
      this._setAuthorizationHeader(request);
      const response = await this._innerClient.send(request);
      if (allowRetry && response.statusCode === 401 && this._accessTokenFactory) {
        this._accessToken = await this._accessTokenFactory();
        this._setAuthorizationHeader(request);
        return await this._innerClient.send(request);
      }
      return response;
    }
    _setAuthorizationHeader(request) {
      if (!request.headers) {
        request.headers = {};
      }
      if (this._accessToken) {
        request.headers[HeaderNames.Authorization] = `Bearer ${this._accessToken}`;
      } else if (this._accessTokenFactory) {
        if (request.headers[HeaderNames.Authorization]) {
          delete request.headers[HeaderNames.Authorization];
        }
      }
    }
    getCookieString(url) {
      return this._innerClient.getCookieString(url);
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/ITransport.js
  var HttpTransportType;
  (function(HttpTransportType2) {
    HttpTransportType2[HttpTransportType2["None"] = 0] = "None";
    HttpTransportType2[HttpTransportType2["WebSockets"] = 1] = "WebSockets";
    HttpTransportType2[HttpTransportType2["ServerSentEvents"] = 2] = "ServerSentEvents";
    HttpTransportType2[HttpTransportType2["LongPolling"] = 4] = "LongPolling";
  })(HttpTransportType || (HttpTransportType = {}));
  var TransferFormat;
  (function(TransferFormat2) {
    TransferFormat2[TransferFormat2["Text"] = 1] = "Text";
    TransferFormat2[TransferFormat2["Binary"] = 2] = "Binary";
  })(TransferFormat || (TransferFormat = {}));

  // node_modules/@microsoft/signalr/dist/esm/AbortController.js
  var AbortController2 = class {
    constructor() {
      this._isAborted = false;
      this.onabort = null;
    }
    abort() {
      if (!this._isAborted) {
        this._isAborted = true;
        if (this.onabort) {
          this.onabort();
        }
      }
    }
    get signal() {
      return this;
    }
    get aborted() {
      return this._isAborted;
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/LongPollingTransport.js
  var LongPollingTransport = class {
    // This is an internal type, not exported from 'index' so this is really just internal.
    get pollAborted() {
      return this._pollAbort.aborted;
    }
    constructor(httpClient, logger, options) {
      this._httpClient = httpClient;
      this._logger = logger;
      this._pollAbort = new AbortController2();
      this._options = options;
      this._running = false;
      this.onreceive = null;
      this.onclose = null;
    }
    async connect(url, transferFormat) {
      Arg.isRequired(url, "url");
      Arg.isRequired(transferFormat, "transferFormat");
      Arg.isIn(transferFormat, TransferFormat, "transferFormat");
      this._url = url;
      this._logger.log(LogLevel.Trace, "(LongPolling transport) Connecting.");
      if (transferFormat === TransferFormat.Binary && (typeof XMLHttpRequest !== "undefined" && typeof new XMLHttpRequest().responseType !== "string")) {
        throw new Error("Binary protocols over XmlHttpRequest not implementing advanced features are not supported.");
      }
      const [name, value] = getUserAgentHeader();
      const headers = { [name]: value, ...this._options.headers };
      const pollOptions = {
        abortSignal: this._pollAbort.signal,
        headers,
        timeout: 1e5,
        withCredentials: this._options.withCredentials
      };
      if (transferFormat === TransferFormat.Binary) {
        pollOptions.responseType = "arraybuffer";
      }
      const pollUrl = `${url}&_=${Date.now()}`;
      this._logger.log(LogLevel.Trace, `(LongPolling transport) polling: ${pollUrl}.`);
      const response = await this._httpClient.get(pollUrl, pollOptions);
      if (response.statusCode !== 200) {
        this._logger.log(LogLevel.Error, `(LongPolling transport) Unexpected response code: ${response.statusCode}.`);
        this._closeError = new HttpError(response.statusText || "", response.statusCode);
        this._running = false;
      } else {
        this._running = true;
      }
      this._receiving = this._poll(this._url, pollOptions);
    }
    async _poll(url, pollOptions) {
      try {
        while (this._running) {
          try {
            const pollUrl = `${url}&_=${Date.now()}`;
            this._logger.log(LogLevel.Trace, `(LongPolling transport) polling: ${pollUrl}.`);
            const response = await this._httpClient.get(pollUrl, pollOptions);
            if (response.statusCode === 204) {
              this._logger.log(LogLevel.Information, "(LongPolling transport) Poll terminated by server.");
              this._running = false;
            } else if (response.statusCode !== 200) {
              this._logger.log(LogLevel.Error, `(LongPolling transport) Unexpected response code: ${response.statusCode}.`);
              this._closeError = new HttpError(response.statusText || "", response.statusCode);
              this._running = false;
            } else {
              if (response.content) {
                this._logger.log(LogLevel.Trace, `(LongPolling transport) data received. ${getDataDetail(response.content, this._options.logMessageContent)}.`);
                if (this.onreceive) {
                  this.onreceive(response.content);
                }
              } else {
                this._logger.log(LogLevel.Trace, "(LongPolling transport) Poll timed out, reissuing.");
              }
            }
          } catch (e2) {
            if (!this._running) {
              this._logger.log(LogLevel.Trace, `(LongPolling transport) Poll errored after shutdown: ${e2.message}`);
            } else {
              if (e2 instanceof TimeoutError) {
                this._logger.log(LogLevel.Trace, "(LongPolling transport) Poll timed out, reissuing.");
              } else {
                this._closeError = e2;
                this._running = false;
              }
            }
          }
        }
      } finally {
        this._logger.log(LogLevel.Trace, "(LongPolling transport) Polling complete.");
        if (!this.pollAborted) {
          this._raiseOnClose();
        }
      }
    }
    async send(data2) {
      if (!this._running) {
        return Promise.reject(new Error("Cannot send until the transport is connected"));
      }
      return sendMessage(this._logger, "LongPolling", this._httpClient, this._url, data2, this._options);
    }
    async stop() {
      this._logger.log(LogLevel.Trace, "(LongPolling transport) Stopping polling.");
      this._running = false;
      this._pollAbort.abort();
      try {
        await this._receiving;
        this._logger.log(LogLevel.Trace, `(LongPolling transport) sending DELETE request to ${this._url}.`);
        const headers = {};
        const [name, value] = getUserAgentHeader();
        headers[name] = value;
        const deleteOptions = {
          headers: { ...headers, ...this._options.headers },
          timeout: this._options.timeout,
          withCredentials: this._options.withCredentials
        };
        let error;
        try {
          await this._httpClient.delete(this._url, deleteOptions);
        } catch (err) {
          error = err;
        }
        if (error) {
          if (error instanceof HttpError) {
            if (error.statusCode === 404) {
              this._logger.log(LogLevel.Trace, "(LongPolling transport) A 404 response was returned from sending a DELETE request.");
            } else {
              this._logger.log(LogLevel.Trace, `(LongPolling transport) Error sending a DELETE request: ${error}`);
            }
          }
        } else {
          this._logger.log(LogLevel.Trace, "(LongPolling transport) DELETE request accepted.");
        }
      } finally {
        this._logger.log(LogLevel.Trace, "(LongPolling transport) Stop finished.");
        this._raiseOnClose();
      }
    }
    _raiseOnClose() {
      if (this.onclose) {
        let logMessage = "(LongPolling transport) Firing onclose event.";
        if (this._closeError) {
          logMessage += " Error: " + this._closeError;
        }
        this._logger.log(LogLevel.Trace, logMessage);
        this.onclose(this._closeError);
      }
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/ServerSentEventsTransport.js
  var ServerSentEventsTransport = class {
    constructor(httpClient, accessToken, logger, options) {
      this._httpClient = httpClient;
      this._accessToken = accessToken;
      this._logger = logger;
      this._options = options;
      this.onreceive = null;
      this.onclose = null;
    }
    async connect(url, transferFormat) {
      Arg.isRequired(url, "url");
      Arg.isRequired(transferFormat, "transferFormat");
      Arg.isIn(transferFormat, TransferFormat, "transferFormat");
      this._logger.log(LogLevel.Trace, "(SSE transport) Connecting.");
      this._url = url;
      if (this._accessToken) {
        url += (url.indexOf("?") < 0 ? "?" : "&") + `access_token=${encodeURIComponent(this._accessToken)}`;
      }
      return new Promise((resolve, reject) => {
        let opened = false;
        if (transferFormat !== TransferFormat.Text) {
          reject(new Error("The Server-Sent Events transport only supports the 'Text' transfer format"));
          return;
        }
        let eventSource;
        if (Platform.isBrowser || Platform.isWebWorker) {
          eventSource = new this._options.EventSource(url, { withCredentials: this._options.withCredentials });
        } else {
          const cookies = this._httpClient.getCookieString(url);
          const headers = {};
          headers.Cookie = cookies;
          const [name, value] = getUserAgentHeader();
          headers[name] = value;
          eventSource = new this._options.EventSource(url, { withCredentials: this._options.withCredentials, headers: { ...headers, ...this._options.headers } });
        }
        try {
          eventSource.onmessage = (e2) => {
            if (this.onreceive) {
              try {
                this._logger.log(LogLevel.Trace, `(SSE transport) data received. ${getDataDetail(e2.data, this._options.logMessageContent)}.`);
                this.onreceive(e2.data);
              } catch (error) {
                this._close(error);
                return;
              }
            }
          };
          eventSource.onerror = (e2) => {
            if (opened) {
              this._close();
            } else {
              reject(new Error("EventSource failed to connect. The connection could not be found on the server, either the connection ID is not present on the server, or a proxy is refusing/buffering the connection. If you have multiple servers check that sticky sessions are enabled."));
            }
          };
          eventSource.onopen = () => {
            this._logger.log(LogLevel.Information, `SSE connected to ${this._url}`);
            this._eventSource = eventSource;
            opened = true;
            resolve();
          };
        } catch (e2) {
          reject(e2);
          return;
        }
      });
    }
    async send(data2) {
      if (!this._eventSource) {
        return Promise.reject(new Error("Cannot send until the transport is connected"));
      }
      return sendMessage(this._logger, "SSE", this._httpClient, this._url, data2, this._options);
    }
    stop() {
      this._close();
      return Promise.resolve();
    }
    _close(e2) {
      if (this._eventSource) {
        this._eventSource.close();
        this._eventSource = void 0;
        if (this.onclose) {
          this.onclose(e2);
        }
      }
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/WebSocketTransport.js
  var WebSocketTransport = class {
    constructor(httpClient, accessTokenFactory, logger, logMessageContent, webSocketConstructor, headers) {
      this._logger = logger;
      this._accessTokenFactory = accessTokenFactory;
      this._logMessageContent = logMessageContent;
      this._webSocketConstructor = webSocketConstructor;
      this._httpClient = httpClient;
      this.onreceive = null;
      this.onclose = null;
      this._headers = headers;
    }
    async connect(url, transferFormat) {
      Arg.isRequired(url, "url");
      Arg.isRequired(transferFormat, "transferFormat");
      Arg.isIn(transferFormat, TransferFormat, "transferFormat");
      this._logger.log(LogLevel.Trace, "(WebSockets transport) Connecting.");
      let token;
      if (this._accessTokenFactory) {
        token = await this._accessTokenFactory();
      }
      return new Promise((resolve, reject) => {
        url = url.replace(/^http/, "ws");
        let webSocket;
        const cookies = this._httpClient.getCookieString(url);
        let opened = false;
        if (Platform.isNode || Platform.isReactNative) {
          const headers = {};
          const [name, value] = getUserAgentHeader();
          headers[name] = value;
          if (token) {
            headers[HeaderNames.Authorization] = `Bearer ${token}`;
          }
          if (cookies) {
            headers[HeaderNames.Cookie] = cookies;
          }
          webSocket = new this._webSocketConstructor(url, void 0, {
            headers: { ...headers, ...this._headers }
          });
        } else {
          if (token) {
            url += (url.indexOf("?") < 0 ? "?" : "&") + `access_token=${encodeURIComponent(token)}`;
          }
        }
        if (!webSocket) {
          webSocket = new this._webSocketConstructor(url);
        }
        if (transferFormat === TransferFormat.Binary) {
          webSocket.binaryType = "arraybuffer";
        }
        webSocket.onopen = (_event) => {
          this._logger.log(LogLevel.Information, `WebSocket connected to ${url}.`);
          this._webSocket = webSocket;
          opened = true;
          resolve();
        };
        webSocket.onerror = (event) => {
          let error = null;
          if (typeof ErrorEvent !== "undefined" && event instanceof ErrorEvent) {
            error = event.error;
          } else {
            error = "There was an error with the transport";
          }
          this._logger.log(LogLevel.Information, `(WebSockets transport) ${error}.`);
        };
        webSocket.onmessage = (message) => {
          this._logger.log(LogLevel.Trace, `(WebSockets transport) data received. ${getDataDetail(message.data, this._logMessageContent)}.`);
          if (this.onreceive) {
            try {
              this.onreceive(message.data);
            } catch (error) {
              this._close(error);
              return;
            }
          }
        };
        webSocket.onclose = (event) => {
          if (opened) {
            this._close(event);
          } else {
            let error = null;
            if (typeof ErrorEvent !== "undefined" && event instanceof ErrorEvent) {
              error = event.error;
            } else {
              error = "WebSocket failed to connect. The connection could not be found on the server, either the endpoint may not be a SignalR endpoint, the connection ID is not present on the server, or there is a proxy blocking WebSockets. If you have multiple servers check that sticky sessions are enabled.";
            }
            reject(new Error(error));
          }
        };
      });
    }
    send(data2) {
      if (this._webSocket && this._webSocket.readyState === this._webSocketConstructor.OPEN) {
        this._logger.log(LogLevel.Trace, `(WebSockets transport) sending data. ${getDataDetail(data2, this._logMessageContent)}.`);
        this._webSocket.send(data2);
        return Promise.resolve();
      }
      return Promise.reject("WebSocket is not in the OPEN state");
    }
    stop() {
      if (this._webSocket) {
        this._close(void 0);
      }
      return Promise.resolve();
    }
    _close(event) {
      if (this._webSocket) {
        this._webSocket.onclose = () => {
        };
        this._webSocket.onmessage = () => {
        };
        this._webSocket.onerror = () => {
        };
        this._webSocket.close();
        this._webSocket = void 0;
      }
      this._logger.log(LogLevel.Trace, "(WebSockets transport) socket closed.");
      if (this.onclose) {
        if (this._isCloseEvent(event) && (event.wasClean === false || event.code !== 1e3)) {
          this.onclose(new Error(`WebSocket closed with status code: ${event.code} (${event.reason || "no reason given"}).`));
        } else if (event instanceof Error) {
          this.onclose(event);
        } else {
          this.onclose();
        }
      }
    }
    _isCloseEvent(event) {
      return event && typeof event.wasClean === "boolean" && typeof event.code === "number";
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/HttpConnection.js
  var MAX_REDIRECTS = 100;
  var HttpConnection = class {
    constructor(url, options = {}) {
      this._stopPromiseResolver = () => {
      };
      this.features = {};
      this._negotiateVersion = 1;
      Arg.isRequired(url, "url");
      this._logger = createLogger(options.logger);
      this.baseUrl = this._resolveUrl(url);
      options = options || {};
      options.logMessageContent = options.logMessageContent === void 0 ? false : options.logMessageContent;
      if (typeof options.withCredentials === "boolean" || options.withCredentials === void 0) {
        options.withCredentials = options.withCredentials === void 0 ? true : options.withCredentials;
      } else {
        throw new Error("withCredentials option was not a 'boolean' or 'undefined' value");
      }
      options.timeout = options.timeout === void 0 ? 100 * 1e3 : options.timeout;
      let webSocketModule = null;
      let eventSourceModule = null;
      if (Platform.isNode && typeof __require !== "undefined") {
        const requireFunc = typeof __webpack_require__ === "function" ? __non_webpack_require__ : __require;
        webSocketModule = requireFunc("ws");
        eventSourceModule = requireFunc("eventsource");
      }
      if (!Platform.isNode && typeof WebSocket !== "undefined" && !options.WebSocket) {
        options.WebSocket = WebSocket;
      } else if (Platform.isNode && !options.WebSocket) {
        if (webSocketModule) {
          options.WebSocket = webSocketModule;
        }
      }
      if (!Platform.isNode && typeof EventSource !== "undefined" && !options.EventSource) {
        options.EventSource = EventSource;
      } else if (Platform.isNode && !options.EventSource) {
        if (typeof eventSourceModule !== "undefined") {
          options.EventSource = eventSourceModule;
        }
      }
      this._httpClient = new AccessTokenHttpClient(options.httpClient || new DefaultHttpClient(this._logger), options.accessTokenFactory);
      this._connectionState = "Disconnected";
      this._connectionStarted = false;
      this._options = options;
      this.onreceive = null;
      this.onclose = null;
    }
    async start(transferFormat) {
      transferFormat = transferFormat || TransferFormat.Binary;
      Arg.isIn(transferFormat, TransferFormat, "transferFormat");
      this._logger.log(LogLevel.Debug, `Starting connection with transfer format '${TransferFormat[transferFormat]}'.`);
      if (this._connectionState !== "Disconnected") {
        return Promise.reject(new Error("Cannot start an HttpConnection that is not in the 'Disconnected' state."));
      }
      this._connectionState = "Connecting";
      this._startInternalPromise = this._startInternal(transferFormat);
      await this._startInternalPromise;
      if (this._connectionState === "Disconnecting") {
        const message = "Failed to start the HttpConnection before stop() was called.";
        this._logger.log(LogLevel.Error, message);
        await this._stopPromise;
        return Promise.reject(new AbortError(message));
      } else if (this._connectionState !== "Connected") {
        const message = "HttpConnection.startInternal completed gracefully but didn't enter the connection into the connected state!";
        this._logger.log(LogLevel.Error, message);
        return Promise.reject(new AbortError(message));
      }
      this._connectionStarted = true;
    }
    send(data2) {
      if (this._connectionState !== "Connected") {
        return Promise.reject(new Error("Cannot send data if the connection is not in the 'Connected' State."));
      }
      if (!this._sendQueue) {
        this._sendQueue = new TransportSendQueue(this.transport);
      }
      return this._sendQueue.send(data2);
    }
    async stop(error) {
      if (this._connectionState === "Disconnected") {
        this._logger.log(LogLevel.Debug, `Call to HttpConnection.stop(${error}) ignored because the connection is already in the disconnected state.`);
        return Promise.resolve();
      }
      if (this._connectionState === "Disconnecting") {
        this._logger.log(LogLevel.Debug, `Call to HttpConnection.stop(${error}) ignored because the connection is already in the disconnecting state.`);
        return this._stopPromise;
      }
      this._connectionState = "Disconnecting";
      this._stopPromise = new Promise((resolve) => {
        this._stopPromiseResolver = resolve;
      });
      await this._stopInternal(error);
      await this._stopPromise;
    }
    async _stopInternal(error) {
      this._stopError = error;
      try {
        await this._startInternalPromise;
      } catch (e2) {
      }
      if (this.transport) {
        try {
          await this.transport.stop();
        } catch (e2) {
          this._logger.log(LogLevel.Error, `HttpConnection.transport.stop() threw error '${e2}'.`);
          this._stopConnection();
        }
        this.transport = void 0;
      } else {
        this._logger.log(LogLevel.Debug, "HttpConnection.transport is undefined in HttpConnection.stop() because start() failed.");
      }
    }
    async _startInternal(transferFormat) {
      let url = this.baseUrl;
      this._accessTokenFactory = this._options.accessTokenFactory;
      this._httpClient._accessTokenFactory = this._accessTokenFactory;
      try {
        if (this._options.skipNegotiation) {
          if (this._options.transport === HttpTransportType.WebSockets) {
            this.transport = this._constructTransport(HttpTransportType.WebSockets);
            await this._startTransport(url, transferFormat);
          } else {
            throw new Error("Negotiation can only be skipped when using the WebSocket transport directly.");
          }
        } else {
          let negotiateResponse = null;
          let redirects = 0;
          do {
            negotiateResponse = await this._getNegotiationResponse(url);
            if (this._connectionState === "Disconnecting" || this._connectionState === "Disconnected") {
              throw new AbortError("The connection was stopped during negotiation.");
            }
            if (negotiateResponse.error) {
              throw new Error(negotiateResponse.error);
            }
            if (negotiateResponse.ProtocolVersion) {
              throw new Error("Detected a connection attempt to an ASP.NET SignalR Server. This client only supports connecting to an ASP.NET Core SignalR Server. See https://aka.ms/signalr-core-differences for details.");
            }
            if (negotiateResponse.url) {
              url = negotiateResponse.url;
            }
            if (negotiateResponse.accessToken) {
              const accessToken = negotiateResponse.accessToken;
              this._accessTokenFactory = () => accessToken;
              this._httpClient._accessToken = accessToken;
              this._httpClient._accessTokenFactory = void 0;
            }
            redirects++;
          } while (negotiateResponse.url && redirects < MAX_REDIRECTS);
          if (redirects === MAX_REDIRECTS && negotiateResponse.url) {
            throw new Error("Negotiate redirection limit exceeded.");
          }
          await this._createTransport(url, this._options.transport, negotiateResponse, transferFormat);
        }
        if (this.transport instanceof LongPollingTransport) {
          this.features.inherentKeepAlive = true;
        }
        if (this._connectionState === "Connecting") {
          this._logger.log(LogLevel.Debug, "The HttpConnection connected successfully.");
          this._connectionState = "Connected";
        }
      } catch (e2) {
        this._logger.log(LogLevel.Error, "Failed to start the connection: " + e2);
        this._connectionState = "Disconnected";
        this.transport = void 0;
        this._stopPromiseResolver();
        return Promise.reject(e2);
      }
    }
    async _getNegotiationResponse(url) {
      const headers = {};
      const [name, value] = getUserAgentHeader();
      headers[name] = value;
      const negotiateUrl = this._resolveNegotiateUrl(url);
      this._logger.log(LogLevel.Debug, `Sending negotiation request: ${negotiateUrl}.`);
      try {
        const response = await this._httpClient.post(negotiateUrl, {
          content: "",
          headers: { ...headers, ...this._options.headers },
          timeout: this._options.timeout,
          withCredentials: this._options.withCredentials
        });
        if (response.statusCode !== 200) {
          return Promise.reject(new Error(`Unexpected status code returned from negotiate '${response.statusCode}'`));
        }
        const negotiateResponse = JSON.parse(response.content);
        if (!negotiateResponse.negotiateVersion || negotiateResponse.negotiateVersion < 1) {
          negotiateResponse.connectionToken = negotiateResponse.connectionId;
        }
        if (negotiateResponse.useStatefulReconnect && this._options._useStatefulReconnect !== true) {
          return Promise.reject(new FailedToNegotiateWithServerError("Client didn't negotiate Stateful Reconnect but the server did."));
        }
        return negotiateResponse;
      } catch (e2) {
        let errorMessage = "Failed to complete negotiation with the server: " + e2;
        if (e2 instanceof HttpError) {
          if (e2.statusCode === 404) {
            errorMessage = errorMessage + " Either this is not a SignalR endpoint or there is a proxy blocking the connection.";
          }
        }
        this._logger.log(LogLevel.Error, errorMessage);
        return Promise.reject(new FailedToNegotiateWithServerError(errorMessage));
      }
    }
    _createConnectUrl(url, connectionToken) {
      if (!connectionToken) {
        return url;
      }
      return url + (url.indexOf("?") === -1 ? "?" : "&") + `id=${connectionToken}`;
    }
    async _createTransport(url, requestedTransport, negotiateResponse, requestedTransferFormat) {
      let connectUrl = this._createConnectUrl(url, negotiateResponse.connectionToken);
      if (this._isITransport(requestedTransport)) {
        this._logger.log(LogLevel.Debug, "Connection was provided an instance of ITransport, using that directly.");
        this.transport = requestedTransport;
        await this._startTransport(connectUrl, requestedTransferFormat);
        this.connectionId = negotiateResponse.connectionId;
        return;
      }
      const transportExceptions = [];
      const transports = negotiateResponse.availableTransports || [];
      let negotiate = negotiateResponse;
      for (const endpoint of transports) {
        const transportOrError = this._resolveTransportOrError(endpoint, requestedTransport, requestedTransferFormat, (negotiate === null || negotiate === void 0 ? void 0 : negotiate.useStatefulReconnect) === true);
        if (transportOrError instanceof Error) {
          transportExceptions.push(`${endpoint.transport} failed:`);
          transportExceptions.push(transportOrError);
        } else if (this._isITransport(transportOrError)) {
          this.transport = transportOrError;
          if (!negotiate) {
            try {
              negotiate = await this._getNegotiationResponse(url);
            } catch (ex) {
              return Promise.reject(ex);
            }
            connectUrl = this._createConnectUrl(url, negotiate.connectionToken);
          }
          try {
            await this._startTransport(connectUrl, requestedTransferFormat);
            this.connectionId = negotiate.connectionId;
            return;
          } catch (ex) {
            this._logger.log(LogLevel.Error, `Failed to start the transport '${endpoint.transport}': ${ex}`);
            negotiate = void 0;
            transportExceptions.push(new FailedToStartTransportError(`${endpoint.transport} failed: ${ex}`, HttpTransportType[endpoint.transport]));
            if (this._connectionState !== "Connecting") {
              const message = "Failed to select transport before stop() was called.";
              this._logger.log(LogLevel.Debug, message);
              return Promise.reject(new AbortError(message));
            }
          }
        }
      }
      if (transportExceptions.length > 0) {
        return Promise.reject(new AggregateErrors(`Unable to connect to the server with any of the available transports. ${transportExceptions.join(" ")}`, transportExceptions));
      }
      return Promise.reject(new Error("None of the transports supported by the client are supported by the server."));
    }
    _constructTransport(transport) {
      switch (transport) {
        case HttpTransportType.WebSockets:
          if (!this._options.WebSocket) {
            throw new Error("'WebSocket' is not supported in your environment.");
          }
          return new WebSocketTransport(this._httpClient, this._accessTokenFactory, this._logger, this._options.logMessageContent, this._options.WebSocket, this._options.headers || {});
        case HttpTransportType.ServerSentEvents:
          if (!this._options.EventSource) {
            throw new Error("'EventSource' is not supported in your environment.");
          }
          return new ServerSentEventsTransport(this._httpClient, this._httpClient._accessToken, this._logger, this._options);
        case HttpTransportType.LongPolling:
          return new LongPollingTransport(this._httpClient, this._logger, this._options);
        default:
          throw new Error(`Unknown transport: ${transport}.`);
      }
    }
    _startTransport(url, transferFormat) {
      this.transport.onreceive = this.onreceive;
      if (this.features.reconnect) {
        this.transport.onclose = async (e2) => {
          let callStop = false;
          if (this.features.reconnect) {
            try {
              this.features.disconnected();
              await this.transport.connect(url, transferFormat);
              await this.features.resend();
            } catch {
              callStop = true;
            }
          } else {
            this._stopConnection(e2);
            return;
          }
          if (callStop) {
            this._stopConnection(e2);
          }
        };
      } else {
        this.transport.onclose = (e2) => this._stopConnection(e2);
      }
      return this.transport.connect(url, transferFormat);
    }
    _resolveTransportOrError(endpoint, requestedTransport, requestedTransferFormat, useStatefulReconnect) {
      const transport = HttpTransportType[endpoint.transport];
      if (transport === null || transport === void 0) {
        this._logger.log(LogLevel.Debug, `Skipping transport '${endpoint.transport}' because it is not supported by this client.`);
        return new Error(`Skipping transport '${endpoint.transport}' because it is not supported by this client.`);
      } else {
        if (transportMatches(requestedTransport, transport)) {
          const transferFormats = endpoint.transferFormats.map((s) => TransferFormat[s]);
          if (transferFormats.indexOf(requestedTransferFormat) >= 0) {
            if (transport === HttpTransportType.WebSockets && !this._options.WebSocket || transport === HttpTransportType.ServerSentEvents && !this._options.EventSource) {
              this._logger.log(LogLevel.Debug, `Skipping transport '${HttpTransportType[transport]}' because it is not supported in your environment.'`);
              return new UnsupportedTransportError(`'${HttpTransportType[transport]}' is not supported in your environment.`, transport);
            } else {
              this._logger.log(LogLevel.Debug, `Selecting transport '${HttpTransportType[transport]}'.`);
              try {
                this.features.reconnect = transport === HttpTransportType.WebSockets ? useStatefulReconnect : void 0;
                return this._constructTransport(transport);
              } catch (ex) {
                return ex;
              }
            }
          } else {
            this._logger.log(LogLevel.Debug, `Skipping transport '${HttpTransportType[transport]}' because it does not support the requested transfer format '${TransferFormat[requestedTransferFormat]}'.`);
            return new Error(`'${HttpTransportType[transport]}' does not support ${TransferFormat[requestedTransferFormat]}.`);
          }
        } else {
          this._logger.log(LogLevel.Debug, `Skipping transport '${HttpTransportType[transport]}' because it was disabled by the client.`);
          return new DisabledTransportError(`'${HttpTransportType[transport]}' is disabled by the client.`, transport);
        }
      }
    }
    _isITransport(transport) {
      return transport && typeof transport === "object" && "connect" in transport;
    }
    _stopConnection(error) {
      this._logger.log(LogLevel.Debug, `HttpConnection.stopConnection(${error}) called while in state ${this._connectionState}.`);
      this.transport = void 0;
      error = this._stopError || error;
      this._stopError = void 0;
      if (this._connectionState === "Disconnected") {
        this._logger.log(LogLevel.Debug, `Call to HttpConnection.stopConnection(${error}) was ignored because the connection is already in the disconnected state.`);
        return;
      }
      if (this._connectionState === "Connecting") {
        this._logger.log(LogLevel.Warning, `Call to HttpConnection.stopConnection(${error}) was ignored because the connection is still in the connecting state.`);
        throw new Error(`HttpConnection.stopConnection(${error}) was called while the connection is still in the connecting state.`);
      }
      if (this._connectionState === "Disconnecting") {
        this._stopPromiseResolver();
      }
      if (error) {
        this._logger.log(LogLevel.Error, `Connection disconnected with error '${error}'.`);
      } else {
        this._logger.log(LogLevel.Information, "Connection disconnected.");
      }
      if (this._sendQueue) {
        this._sendQueue.stop().catch((e2) => {
          this._logger.log(LogLevel.Error, `TransportSendQueue.stop() threw error '${e2}'.`);
        });
        this._sendQueue = void 0;
      }
      this.connectionId = void 0;
      this._connectionState = "Disconnected";
      if (this._connectionStarted) {
        this._connectionStarted = false;
        try {
          if (this.onclose) {
            this.onclose(error);
          }
        } catch (e2) {
          this._logger.log(LogLevel.Error, `HttpConnection.onclose(${error}) threw error '${e2}'.`);
        }
      }
    }
    _resolveUrl(url) {
      if (url.lastIndexOf("https://", 0) === 0 || url.lastIndexOf("http://", 0) === 0) {
        return url;
      }
      if (!Platform.isBrowser) {
        throw new Error(`Cannot resolve '${url}'.`);
      }
      const aTag = window.document.createElement("a");
      aTag.href = url;
      this._logger.log(LogLevel.Information, `Normalizing '${url}' to '${aTag.href}'.`);
      return aTag.href;
    }
    _resolveNegotiateUrl(url) {
      const negotiateUrl = new URL(url);
      if (negotiateUrl.pathname.endsWith("/")) {
        negotiateUrl.pathname += "negotiate";
      } else {
        negotiateUrl.pathname += "/negotiate";
      }
      const searchParams = new URLSearchParams(negotiateUrl.searchParams);
      if (!searchParams.has("negotiateVersion")) {
        searchParams.append("negotiateVersion", this._negotiateVersion.toString());
      }
      if (searchParams.has("useStatefulReconnect")) {
        if (searchParams.get("useStatefulReconnect") === "true") {
          this._options._useStatefulReconnect = true;
        }
      } else if (this._options._useStatefulReconnect === true) {
        searchParams.append("useStatefulReconnect", "true");
      }
      negotiateUrl.search = searchParams.toString();
      return negotiateUrl.toString();
    }
  };
  function transportMatches(requestedTransport, actualTransport) {
    return !requestedTransport || (actualTransport & requestedTransport) !== 0;
  }
  var TransportSendQueue = class _TransportSendQueue {
    constructor(_transport) {
      this._transport = _transport;
      this._buffer = [];
      this._executing = true;
      this._sendBufferedData = new PromiseSource();
      this._transportResult = new PromiseSource();
      this._sendLoopPromise = this._sendLoop();
    }
    send(data2) {
      this._bufferData(data2);
      if (!this._transportResult) {
        this._transportResult = new PromiseSource();
      }
      return this._transportResult.promise;
    }
    stop() {
      this._executing = false;
      this._sendBufferedData.resolve();
      return this._sendLoopPromise;
    }
    _bufferData(data2) {
      if (this._buffer.length && typeof this._buffer[0] !== typeof data2) {
        throw new Error(`Expected data to be of type ${typeof this._buffer} but was of type ${typeof data2}`);
      }
      this._buffer.push(data2);
      this._sendBufferedData.resolve();
    }
    async _sendLoop() {
      while (true) {
        await this._sendBufferedData.promise;
        if (!this._executing) {
          if (this._transportResult) {
            this._transportResult.reject("Connection stopped.");
          }
          break;
        }
        this._sendBufferedData = new PromiseSource();
        const transportResult = this._transportResult;
        this._transportResult = void 0;
        const data2 = typeof this._buffer[0] === "string" ? this._buffer.join("") : _TransportSendQueue._concatBuffers(this._buffer);
        this._buffer.length = 0;
        try {
          await this._transport.send(data2);
          transportResult.resolve();
        } catch (error) {
          transportResult.reject(error);
        }
      }
    }
    static _concatBuffers(arrayBuffers) {
      const totalLength = arrayBuffers.map((b) => b.byteLength).reduce((a, b) => a + b);
      const result = new Uint8Array(totalLength);
      let offset = 0;
      for (const item of arrayBuffers) {
        result.set(new Uint8Array(item), offset);
        offset += item.byteLength;
      }
      return result.buffer;
    }
  };
  var PromiseSource = class {
    constructor() {
      this.promise = new Promise((resolve, reject) => [this._resolver, this._rejecter] = [resolve, reject]);
    }
    resolve() {
      this._resolver();
    }
    reject(reason) {
      this._rejecter(reason);
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/JsonHubProtocol.js
  var JSON_HUB_PROTOCOL_NAME = "json";
  var JsonHubProtocol = class {
    constructor() {
      this.name = JSON_HUB_PROTOCOL_NAME;
      this.version = 2;
      this.transferFormat = TransferFormat.Text;
    }
    /** Creates an array of {@link @microsoft/signalr.HubMessage} objects from the specified serialized representation.
     *
     * @param {string} input A string containing the serialized representation.
     * @param {ILogger} logger A logger that will be used to log messages that occur during parsing.
     */
    parseMessages(input, logger) {
      if (typeof input !== "string") {
        throw new Error("Invalid input for JSON hub protocol. Expected a string.");
      }
      if (!input) {
        return [];
      }
      if (logger === null) {
        logger = NullLogger.instance;
      }
      const messages = TextMessageFormat.parse(input);
      const hubMessages = [];
      for (const message of messages) {
        const parsedMessage = JSON.parse(message);
        if (typeof parsedMessage.type !== "number") {
          throw new Error("Invalid payload.");
        }
        switch (parsedMessage.type) {
          case MessageType.Invocation:
            this._isInvocationMessage(parsedMessage);
            break;
          case MessageType.StreamItem:
            this._isStreamItemMessage(parsedMessage);
            break;
          case MessageType.Completion:
            this._isCompletionMessage(parsedMessage);
            break;
          case MessageType.Ping:
            break;
          case MessageType.Close:
            break;
          case MessageType.Ack:
            this._isAckMessage(parsedMessage);
            break;
          case MessageType.Sequence:
            this._isSequenceMessage(parsedMessage);
            break;
          default:
            logger.log(LogLevel.Information, "Unknown message type '" + parsedMessage.type + "' ignored.");
            continue;
        }
        hubMessages.push(parsedMessage);
      }
      return hubMessages;
    }
    /** Writes the specified {@link @microsoft/signalr.HubMessage} to a string and returns it.
     *
     * @param {HubMessage} message The message to write.
     * @returns {string} A string containing the serialized representation of the message.
     */
    writeMessage(message) {
      return TextMessageFormat.write(JSON.stringify(message));
    }
    _isInvocationMessage(message) {
      this._assertNotEmptyString(message.target, "Invalid payload for Invocation message.");
      if (message.invocationId !== void 0) {
        this._assertNotEmptyString(message.invocationId, "Invalid payload for Invocation message.");
      }
    }
    _isStreamItemMessage(message) {
      this._assertNotEmptyString(message.invocationId, "Invalid payload for StreamItem message.");
      if (message.item === void 0) {
        throw new Error("Invalid payload for StreamItem message.");
      }
    }
    _isCompletionMessage(message) {
      if (message.result && message.error) {
        throw new Error("Invalid payload for Completion message.");
      }
      if (!message.result && message.error) {
        this._assertNotEmptyString(message.error, "Invalid payload for Completion message.");
      }
      this._assertNotEmptyString(message.invocationId, "Invalid payload for Completion message.");
    }
    _isAckMessage(message) {
      if (typeof message.sequenceId !== "number") {
        throw new Error("Invalid SequenceId for Ack message.");
      }
    }
    _isSequenceMessage(message) {
      if (typeof message.sequenceId !== "number") {
        throw new Error("Invalid SequenceId for Sequence message.");
      }
    }
    _assertNotEmptyString(value, errorMessage) {
      if (typeof value !== "string" || value === "") {
        throw new Error(errorMessage);
      }
    }
  };

  // node_modules/@microsoft/signalr/dist/esm/HubConnectionBuilder.js
  var LogLevelNameMapping = {
    trace: LogLevel.Trace,
    debug: LogLevel.Debug,
    info: LogLevel.Information,
    information: LogLevel.Information,
    warn: LogLevel.Warning,
    warning: LogLevel.Warning,
    error: LogLevel.Error,
    critical: LogLevel.Critical,
    none: LogLevel.None
  };
  function parseLogLevel(name) {
    const mapping = LogLevelNameMapping[name.toLowerCase()];
    if (typeof mapping !== "undefined") {
      return mapping;
    } else {
      throw new Error(`Unknown log level: ${name}`);
    }
  }
  var HubConnectionBuilder = class {
    configureLogging(logging) {
      Arg.isRequired(logging, "logging");
      if (isLogger(logging)) {
        this.logger = logging;
      } else if (typeof logging === "string") {
        const logLevel = parseLogLevel(logging);
        this.logger = new ConsoleLogger(logLevel);
      } else {
        this.logger = new ConsoleLogger(logging);
      }
      return this;
    }
    withUrl(url, transportTypeOrOptions) {
      Arg.isRequired(url, "url");
      Arg.isNotEmpty(url, "url");
      this.url = url;
      if (typeof transportTypeOrOptions === "object") {
        this.httpConnectionOptions = { ...this.httpConnectionOptions, ...transportTypeOrOptions };
      } else {
        this.httpConnectionOptions = {
          ...this.httpConnectionOptions,
          transport: transportTypeOrOptions
        };
      }
      return this;
    }
    /** Configures the {@link @microsoft/signalr.HubConnection} to use the specified Hub Protocol.
     *
     * @param {IHubProtocol} protocol The {@link @microsoft/signalr.IHubProtocol} implementation to use.
     */
    withHubProtocol(protocol) {
      Arg.isRequired(protocol, "protocol");
      this.protocol = protocol;
      return this;
    }
    withAutomaticReconnect(retryDelaysOrReconnectPolicy) {
      if (this.reconnectPolicy) {
        throw new Error("A reconnectPolicy has already been set.");
      }
      if (!retryDelaysOrReconnectPolicy) {
        this.reconnectPolicy = new DefaultReconnectPolicy();
      } else if (Array.isArray(retryDelaysOrReconnectPolicy)) {
        this.reconnectPolicy = new DefaultReconnectPolicy(retryDelaysOrReconnectPolicy);
      } else {
        this.reconnectPolicy = retryDelaysOrReconnectPolicy;
      }
      return this;
    }
    /** Configures {@link @microsoft/signalr.HubConnection.serverTimeoutInMilliseconds} for the {@link @microsoft/signalr.HubConnection}.
     *
     * @returns The {@link @microsoft/signalr.HubConnectionBuilder} instance, for chaining.
     */
    withServerTimeout(milliseconds) {
      Arg.isRequired(milliseconds, "milliseconds");
      this._serverTimeoutInMilliseconds = milliseconds;
      return this;
    }
    /** Configures {@link @microsoft/signalr.HubConnection.keepAliveIntervalInMilliseconds} for the {@link @microsoft/signalr.HubConnection}.
     *
     * @returns The {@link @microsoft/signalr.HubConnectionBuilder} instance, for chaining.
     */
    withKeepAliveInterval(milliseconds) {
      Arg.isRequired(milliseconds, "milliseconds");
      this._keepAliveIntervalInMilliseconds = milliseconds;
      return this;
    }
    /** Enables and configures options for the Stateful Reconnect feature.
     *
     * @returns The {@link @microsoft/signalr.HubConnectionBuilder} instance, for chaining.
     */
    withStatefulReconnect(options) {
      if (this.httpConnectionOptions === void 0) {
        this.httpConnectionOptions = {};
      }
      this.httpConnectionOptions._useStatefulReconnect = true;
      this._statefulReconnectBufferSize = options === null || options === void 0 ? void 0 : options.bufferSize;
      return this;
    }
    /** Creates a {@link @microsoft/signalr.HubConnection} from the configuration options specified in this builder.
     *
     * @returns {HubConnection} The configured {@link @microsoft/signalr.HubConnection}.
     */
    build() {
      const httpConnectionOptions = this.httpConnectionOptions || {};
      if (httpConnectionOptions.logger === void 0) {
        httpConnectionOptions.logger = this.logger;
      }
      if (!this.url) {
        throw new Error("The 'HubConnectionBuilder.withUrl' method must be called before building the connection.");
      }
      const connection = new HttpConnection(this.url, httpConnectionOptions);
      return HubConnection.create(connection, this.logger || NullLogger.instance, this.protocol || new JsonHubProtocol(), this.reconnectPolicy, this._serverTimeoutInMilliseconds, this._keepAliveIntervalInMilliseconds, this._statefulReconnectBufferSize);
    }
  };
  function isLogger(logger) {
    return logger.log !== void 0;
  }

  // src/hooks/useNfcHub.ts
  var HUB_URL = `${__import_meta_env__.VITE_API_URL}/hubs/nfc`;
  function useNfcHub(onScanResult, onSpoolUpdated, onReaderStatus) {
    const [isConnected, setIsConnected] = (0, import_react29.useState)(false);
    const connectionRef = (0, import_react29.useRef)(null);
    const scanCallbackRef = (0, import_react29.useRef)(onScanResult);
    const spoolCallbackRef = (0, import_react29.useRef)(onSpoolUpdated);
    const readerCallbackRef = (0, import_react29.useRef)(onReaderStatus);
    (0, import_react29.useEffect)(() => {
      scanCallbackRef.current = onScanResult;
    }, [onScanResult]);
    (0, import_react29.useEffect)(() => {
      spoolCallbackRef.current = onSpoolUpdated;
    }, [onSpoolUpdated]);
    (0, import_react29.useEffect)(() => {
      readerCallbackRef.current = onReaderStatus;
    }, [onReaderStatus]);
    (0, import_react29.useEffect)(() => {
      const connection = new HubConnectionBuilder().withUrl(HUB_URL).withAutomaticReconnect().build();
      connectionRef.current = connection;
      let cancelled = false;
      const scanHandler = (result) => scanCallbackRef.current(result);
      const spoolHandler = (spool) => spoolCallbackRef.current?.(spool);
      const readerHandler = (status) => readerCallbackRef.current?.(status);
      connection.on("ScanResult", scanHandler);
      connection.on("SpoolUpdated", spoolHandler);
      connection.on("ReaderStatus", readerHandler);
      connection.onreconnecting(() => setIsConnected(false));
      connection.onreconnected(() => setIsConnected(true));
      connection.onclose(() => setIsConnected(false));
      async function startWithRetry() {
        while (!cancelled) {
          try {
            await connection.start();
            if (!cancelled) setIsConnected(true);
            return;
          } catch {
            if (cancelled) return;
            await new Promise((resolve) => setTimeout(resolve, 5e3));
          }
        }
      }
      startWithRetry();
      return () => {
        cancelled = true;
        connection.off("ScanResult", scanHandler);
        connection.off("SpoolUpdated", spoolHandler);
        connection.off("ReaderStatus", readerHandler);
        connection.stop();
      };
    }, []);
    function selectReader(name) {
      connectionRef.current?.invoke("SelectReader", name).catch(console.error);
    }
    function disconnectReader() {
      connectionRef.current?.invoke("DisconnectReader").catch(console.error);
    }
    return { isConnected, selectReader, disconnectReader };
  }

  // src/components/scan/AndroidScanner/AndroidScanner.tsx
  var import_react30 = __toESM(require_react_shim(), 1);

  // src/components/scan/ScanResult/ScanResult.module.css
  var ScanResult_default = {
    unknown: "ScanResult_unknown",
    error: "ScanResult_error",
    row: "ScanResult_row",
    rowTop: "ScanResult_rowTop",
    iconWrap: "ScanResult_iconWrap",
    iconWrapWarning: "ScanResult_iconWrapWarning",
    iconWrapError: "ScanResult_iconWrapError",
    unknownMsg: "ScanResult_unknownMsg",
    body: "ScanResult_body",
    errorTitle: "ScanResult_errorTitle",
    errorDetail: "ScanResult_errorDetail",
    retryBtn: "ScanResult_retryBtn",
    warnIcon: "ScanResult_warnIcon",
    errorIcon: "ScanResult_errorIcon"
  };

  // src/components/scan/ScanResult/ScanResult.tsx
  var import_jsx_runtime37 = __toESM(require_react_shim(), 1);
  function ScanResult({ status, errorMessage, onRetry }) {
    const { t: t2 } = useTranslation();
    if (status === "unknown") {
      return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("div", { className: ScanResult_default.unknown, children: /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: ScanResult_default.row, children: [
        /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("div", { className: `${ScanResult_default.iconWrap} ${ScanResult_default.iconWrapWarning}`, children: /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("svg", { width: "14", height: "14", className: ScanResult_default.warnIcon, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: [
          /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("circle", { cx: "12", cy: "12", r: "10" }),
          /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("line", { x1: "12", y1: "8", x2: "12", y2: "12" }),
          /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("line", { x1: "12", y1: "16", x2: "12.01", y2: "16" })
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("p", { className: ScanResult_default.unknownMsg, children: t2("scan.unknownTagRedirecting") })
      ] }) });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("div", { className: ScanResult_default.error, children: /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: ScanResult_default.rowTop, children: [
      /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("div", { className: `${ScanResult_default.iconWrap} ${ScanResult_default.iconWrapError}`, children: /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("svg", { width: "14", height: "14", className: ScanResult_default.errorIcon, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: [
        /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
        /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
      ] }) }),
      /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: ScanResult_default.body, children: [
        /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("p", { className: ScanResult_default.errorTitle, children: t2("scan.scanFailed") }),
        errorMessage && /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("p", { className: ScanResult_default.errorDetail, children: errorMessage }),
        onRetry && /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("button", { onClick: onRetry, className: ScanResult_default.retryBtn, children: t2("scan.tryAgain") })
      ] })
    ] }) });
  }

  // src/components/scan/AndroidScanner/AndroidScanner.module.css
  var AndroidScanner_default = {
    ping: "AndroidScanner_ping",
    ping2: "AndroidScanner_ping2",
    spin: "AndroidScanner_spin",
    wrap: "AndroidScanner_wrap",
    nfcWrap: "AndroidScanner_nfcWrap",
    pingRing1: "AndroidScanner_pingRing1",
    pingRing2: "AndroidScanner_pingRing2",
    pingRing3: "AndroidScanner_pingRing3",
    nfcCircle: "AndroidScanner_nfcCircle",
    nfcCircleActive: "AndroidScanner_nfcCircleActive",
    nfcIcon: "AndroidScanner_nfcIcon",
    nfcIconActive: "AndroidScanner_nfcIconActive",
    heading: "AndroidScanner_heading",
    title: "AndroidScanner_title",
    subtitle: "AndroidScanner_subtitle",
    fullWidth: "AndroidScanner_fullWidth",
    successWrap: "AndroidScanner_successWrap",
    successCheckIcon: "AndroidScanner_successCheckIcon",
    successIcon: "AndroidScanner_successIcon",
    btnPrimary: "AndroidScanner_btnPrimary",
    btnSecondary: "AndroidScanner_btnSecondary",
    btnGroup: "AndroidScanner_btnGroup",
    disconnected: "AndroidScanner_disconnected"
  };

  // src/components/scan/AndroidScanner/AndroidScanner.tsx
  var import_jsx_runtime38 = __toESM(require_react_shim(), 1);
  function NfcIcon2({ active }) {
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: AndroidScanner_default.nfcWrap, children: [
      active && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(import_jsx_runtime38.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: AndroidScanner_default.pingRing1 }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: AndroidScanner_default.pingRing2 }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: AndroidScanner_default.pingRing3 })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: `${AndroidScanner_default.nfcCircle}${active ? ` ${AndroidScanner_default.nfcCircleActive}` : ""}`, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
        "svg",
        {
          width: "64",
          height: "64",
          className: active ? AndroidScanner_default.nfcIconActive : AndroidScanner_default.nfcIcon,
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          strokeWidth: "1.5",
          strokeLinecap: "round",
          strokeLinejoin: "round",
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("line", { x1: "5", y1: "7", x2: "5", y2: "17" }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M8 9.5a4 4 0 0 1 0 5" }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M11 8a7 7 0 0 1 0 8" }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M14 6.5a9.5 9.5 0 0 1 0 11" })
          ]
        }
      ) })
    ] });
  }
  function AndroidScanner({ isHubConnected, onSpoolFound, onUnknownTag }) {
    const { t: t2 } = useTranslation();
    const [scanState, setScanState] = (0, import_react30.useState)("idle");
    const [errorMessage, setErrorMessage] = (0, import_react30.useState)(null);
    const abortRef = (0, import_react30.useRef)(null);
    (0, import_react30.useEffect)(() => {
      return () => abortRef.current?.abort();
    }, []);
    function cancel() {
      abortRef.current?.abort();
      abortRef.current = null;
      setScanState("idle");
      setErrorMessage(null);
    }
    async function startScan() {
      setScanState("scanning");
      setErrorMessage(null);
      const abort = new AbortController();
      abortRef.current = abort;
      try {
        const reader = new NDEFReader();
        await reader.scan({ signal: abort.signal });
        reader.addEventListener("error", () => {
          if (abort.signal.aborted) return;
          setErrorMessage(t2("scan.errorReadTag"));
          setScanState("error");
        });
        reader.addEventListener("reading", async (event) => {
          if (abort.signal.aborted) return;
          abort.abort();
          const tagUid = event.serialNumber;
          try {
            const result = await scanTag(tagUid);
            if (result.status === "unknown") {
              if (onUnknownTag) {
                onUnknownTag(tagUid);
                return;
              }
              setScanState("unknown");
            } else if (result.spool) {
              setScanState("idle");
              onSpoolFound(result.spool);
            }
          } catch {
            setErrorMessage(t2("scan.errorLookup"));
            setScanState("error");
          }
        });
      } catch (err) {
        if (abort.signal.aborted) return;
        const msg = err instanceof Error && err.name === "NotAllowedError" ? t2("scan.errorNotAllowed") : t2("scan.errorNotAvailable");
        setErrorMessage(msg);
        setScanState("error");
      }
    }
    async function startRegister() {
      setScanState("write-ready");
      setErrorMessage(null);
      const abort = new AbortController();
      abortRef.current = abort;
      try {
        const ndef = new NDEFReader();
        ndef.addEventListener("reading", async (event) => {
          if (abort.signal.aborted) return;
          setScanState("writing");
          const tagUid = event.serialNumber;
          const url = `${window.location.origin}/scan?tagUid=${encodeURIComponent(tagUid)}`;
          try {
            await ndef.write(
              { records: [{ recordType: "url", data: url }] },
              { overwrite: true, signal: abort.signal }
            );
            abort.abort();
            setScanState("written");
          } catch {
            if (abort.signal.aborted) return;
            setErrorMessage(t2("scan.errorWriteTag"));
            setScanState("error");
          }
        });
        await ndef.scan({ signal: abort.signal });
      } catch (err) {
        if (abort.signal.aborted) return;
        const msg = err instanceof Error && err.name === "NotAllowedError" ? t2("scan.errorNotAllowed") : t2("scan.errorNotAvailable");
        setErrorMessage(msg);
        setScanState("error");
      }
    }
    const isActive = scanState === "scanning" || scanState === "write-ready" || scanState === "writing";
    const showResult = scanState === "unknown" || scanState === "error";
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: AndroidScanner_default.wrap, children: [
      scanState !== "written" && !showResult && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: AndroidScanner_default.heading, children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(NfcIcon2, { active: isActive }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("h1", { className: AndroidScanner_default.title, children: scanState === "write-ready" || scanState === "writing" ? t2("scan.registerNewTag") : t2("scan.scanNfcTag") }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("p", { className: AndroidScanner_default.subtitle, children: scanState === "writing" ? t2("scan.writingUrl") : scanState === "write-ready" ? t2("scan.holdPhoneBlank") : scanState === "scanning" ? t2("scan.holdPhoneNear") : t2("scan.tapDescription") })
        ] })
      ] }),
      scanState === "written" && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: AndroidScanner_default.successWrap, children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: AndroidScanner_default.successIcon, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { width: "40", height: "40", className: AndroidScanner_default.successCheckIcon, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("polyline", { points: "20 6 9 17 4 12" }) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("h1", { className: AndroidScanner_default.title, children: t2("scan.tagRegistered") }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("p", { className: AndroidScanner_default.subtitle, children: t2("scan.tagRegisteredDesc") })
        ] })
      ] }),
      showResult && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: AndroidScanner_default.fullWidth, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(ScanResult, { status: scanState, errorMessage, onRetry: cancel }) }),
      scanState === "idle" && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: AndroidScanner_default.btnGroup, children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("button", { onClick: startScan, className: AndroidScanner_default.btnPrimary, children: t2("scan.startScanning") }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("button", { onClick: startRegister, className: AndroidScanner_default.btnSecondary, children: t2("scan.newTag") })
      ] }),
      (scanState === "scanning" || scanState === "write-ready") && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("button", { onClick: cancel, className: AndroidScanner_default.btnSecondary, children: t2("common.cancel") }),
      scanState === "written" && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: AndroidScanner_default.btnGroup, children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("button", { onClick: startScan, className: AndroidScanner_default.btnPrimary, children: t2("scan.startScanning") }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("button", { onClick: startRegister, className: AndroidScanner_default.btnSecondary, children: t2("scan.registerAnother") })
      ] }),
      !isHubConnected && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("p", { className: AndroidScanner_default.disconnected, children: t2("scan.liveDisconnected") })
    ] });
  }

  // src/components/scan/IphoneScanner/IphoneScanner.tsx
  var import_react31 = __toESM(require_react_shim(), 1);

  // src/components/scan/IphoneScanner/IphoneScanner.module.css
  var IphoneScanner_default = {
    ping: "IphoneScanner_ping",
    card: "IphoneScanner_card",
    tapWrap: "IphoneScanner_tapWrap",
    nfcWrap: "IphoneScanner_nfcWrap",
    pingRing1: "IphoneScanner_pingRing1",
    pingRing2: "IphoneScanner_pingRing2",
    nfcCircle: "IphoneScanner_nfcCircle",
    backBtn: "IphoneScanner_backBtn",
    title: "IphoneScanner_title",
    subtitle: "IphoneScanner_subtitle",
    btnPrimary: "IphoneScanner_btnPrimary",
    setupWrap: "IphoneScanner_setupWrap",
    setupTitle: "IphoneScanner_setupTitle",
    setupSubtitle: "IphoneScanner_setupSubtitle",
    steps: "IphoneScanner_steps",
    step: "IphoneScanner_step",
    stepNum: "IphoneScanner_stepNum",
    stepText: "IphoneScanner_stepText",
    urlBox: "IphoneScanner_urlBox",
    urlLabel: "IphoneScanner_urlLabel",
    uidInput: "IphoneScanner_uidInput",
    urlText: "IphoneScanner_urlText",
    copyBtn: "IphoneScanner_copyBtn",
    copyBtnDisabled: "IphoneScanner_copyBtnDisabled",
    uidHint: "IphoneScanner_uidHint",
    copyBtnCopied: "IphoneScanner_copyBtnCopied"
  };

  // src/components/scan/IphoneScanner/IphoneScanner.tsx
  var import_jsx_runtime39 = __toESM(require_react_shim(), 1);
  function IphoneScanner() {
    const { t: t2 } = useTranslation();
    const [view, setView] = (0, import_react31.useState)("tap");
    const [copied, setCopied] = (0, import_react31.useState)(false);
    const [tagUid, setTagUid] = (0, import_react31.useState)("");
    const scanUrl = tagUid.trim() ? `${window.location.origin}/scan?tagUid=${encodeURIComponent(tagUid.trim())}` : `${window.location.origin}/scan?tagUid=[NFC Tag Serial Number]`;
    async function copyUrl() {
      try {
        if (navigator.clipboard) {
          await navigator.clipboard.writeText(scanUrl);
        } else {
          const el = document.createElement("textarea");
          el.value = scanUrl;
          el.style.cssText = "position:fixed;opacity:0;pointer-events:none";
          document.body.appendChild(el);
          el.focus();
          el.select();
          document.execCommand("copy");
          document.body.removeChild(el);
        }
        setCopied(true);
        setTimeout(() => setCopied(false), 2e3);
      } catch {
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { className: IphoneScanner_default.card, children: [
      view === "tap" && /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { className: IphoneScanner_default.tapWrap, children: [
        /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { className: IphoneScanner_default.nfcWrap, children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("div", { className: IphoneScanner_default.pingRing1 }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("div", { className: IphoneScanner_default.pingRing2 }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("div", { className: IphoneScanner_default.nfcCircle, children: /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)(
            "svg",
            {
              viewBox: "0 0 24 24",
              fill: "none",
              stroke: "currentColor",
              strokeWidth: "1.5",
              strokeLinecap: "round",
              strokeLinejoin: "round",
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("line", { x1: "5", y1: "7", x2: "5", y2: "17" }),
                /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M8 9.5a4 4 0 0 1 0 5" }),
                /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M11 8a7 7 0 0 1 0 8" }),
                /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M14 6.5a9.5 9.5 0 0 1 0 11" })
              ]
            }
          ) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("h1", { className: IphoneScanner_default.title, children: t2("scan.holdNearTag") }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("p", { className: IphoneScanner_default.subtitle, children: t2("scan.tapIphone") })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("button", { onClick: () => setView("setup"), className: IphoneScanner_default.btnPrimary, children: t2("scan.newTag") })
      ] }),
      view === "setup" && /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { className: IphoneScanner_default.setupWrap, children: [
        /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("button", { onClick: () => setView("tap"), className: IphoneScanner_default.backBtn, children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("polyline", { points: "15 18 9 12 15 6" }) }),
          t2("scan.back")
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("h2", { className: IphoneScanner_default.setupTitle, children: t2("scan.registerNewTag") }),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("ol", { className: IphoneScanner_default.steps, children: [1, 2, 3, 4, 5, 6].map((n) => /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("li", { className: IphoneScanner_default.step, children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: IphoneScanner_default.stepNum, children: n }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: IphoneScanner_default.stepText, children: t2(`scan.iphoneStep${n}`) })
        ] }, n)) }),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { className: IphoneScanner_default.urlBox, children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("p", { className: IphoneScanner_default.urlLabel, children: t2("scan.urlToPaste") }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
            "input",
            {
              className: IphoneScanner_default.uidInput,
              type: "text",
              placeholder: t2("scan.iphoneUidPlaceholder"),
              value: tagUid,
              onChange: (e2) => setTagUid(e2.target.value)
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("p", { className: IphoneScanner_default.urlText, children: scanUrl }),
          !tagUid.trim() && /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("p", { className: IphoneScanner_default.uidHint, children: t2("scan.iphoneUidHint") }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
            "button",
            {
              onClick: copyUrl,
              disabled: !tagUid.trim(),
              className: `${IphoneScanner_default.copyBtn}${copied ? ` ${IphoneScanner_default.copyBtnCopied}` : ""}${!tagUid.trim() ? ` ${IphoneScanner_default.copyBtnDisabled}` : ""}`,
              children: copied ? t2("scan.copied") : t2("scan.copyUrl")
            }
          )
        ] })
      ] })
    ] });
  }

  // src/components/scan/DesktopScanner/DesktopScanner.tsx
  var import_react33 = __toESM(require_react_shim(), 1);

  // src/hooks/useAgentNfc.ts
  var import_react32 = __toESM(require_react_shim(), 1);
  var AGENT_WS = "ws://localhost:8765/events";
  var AGENT_HTTP = "http://localhost:8765";
  var SKIP_PROMPT_KEY = "spoolhub.agent.skipInstallPrompt";
  function useAgentNfc(onTagFound) {
    const [state, setState] = (0, import_react32.useState)("checking");
    const [readerName, setReaderName] = (0, import_react32.useState)(null);
    const wsRef = (0, import_react32.useRef)(null);
    const onTagFoundRef = (0, import_react32.useRef)(onTagFound);
    const mountedRef = (0, import_react32.useRef)(true);
    (0, import_react32.useEffect)(() => {
      onTagFoundRef.current = onTagFound;
    }, [onTagFound]);
    const openWebSocket = (0, import_react32.useCallback)(() => {
      if (wsRef.current) {
        wsRef.current.onclose = null;
        wsRef.current.onerror = null;
        wsRef.current.close();
        wsRef.current = null;
      }
      if (!mountedRef.current) return;
      setState("connecting");
      const ws = new WebSocket(AGENT_WS);
      wsRef.current = ws;
      ws.onopen = async () => {
        if (!mountedRef.current) return;
        try {
          const res = await fetch(`${AGENT_HTTP}/readers`);
          const data2 = await res.json();
          if (!mountedRef.current) return;
          if (data2.connected && data2.activeReader) {
            setReaderName(data2.activeReader);
            setState("ready");
          } else {
            setReaderName(null);
            setState("no-reader");
          }
        } catch {
          if (mountedRef.current) setState("no-reader");
        }
      };
      ws.onmessage = (e2) => {
        if (!mountedRef.current) return;
        try {
          const msg = JSON.parse(e2.data);
          if (msg.event === "reader_status") {
            if (msg.connected && msg.reader) {
              setReaderName(msg.reader);
              setState("ready");
            } else {
              setReaderName(null);
              setState("no-reader");
            }
          } else if (msg.event === "tag_scanned" && msg.uid) {
            onTagFoundRef.current(msg.uid);
          }
        } catch {
        }
      };
      ws.onerror = () => {
        if (!mountedRef.current) return;
        setState("agent-offline");
        setReaderName(null);
      };
      ws.onclose = () => {
        if (!mountedRef.current) return;
        setState("agent-offline");
        setReaderName(null);
      };
    }, []);
    const checkAgent = (0, import_react32.useCallback)(async () => {
      if (!mountedRef.current) return;
      setState("checking");
      try {
        const res = await fetch(`${AGENT_HTTP}/health`, {
          signal: AbortSignal.timeout(2e3)
        });
        if (!res.ok) throw new Error("not ok");
        if (mountedRef.current) openWebSocket();
      } catch {
        if (!mountedRef.current) return;
        const skip = localStorage.getItem(SKIP_PROMPT_KEY) === "true";
        setState(skip ? "agent-offline" : "install-prompt");
      }
    }, [openWebSocket]);
    (0, import_react32.useEffect)(() => {
      mountedRef.current = true;
      checkAgent();
      return () => {
        mountedRef.current = false;
        if (wsRef.current) {
          wsRef.current.onclose = null;
          wsRef.current.onerror = null;
          wsRef.current.close();
        }
      };
    }, [checkAgent]);
    const reload = (0, import_react32.useCallback)(() => {
      checkAgent();
    }, [checkAgent]);
    const dismissInstallPrompt = (0, import_react32.useCallback)((skipForever) => {
      if (skipForever) localStorage.setItem(SKIP_PROMPT_KEY, "true");
      setState("agent-offline");
    }, []);
    const disconnect = (0, import_react32.useCallback)(async () => {
      if (wsRef.current) {
        wsRef.current.onclose = null;
        wsRef.current.onerror = null;
        wsRef.current.close();
        wsRef.current = null;
      }
      try {
        await fetch(`${AGENT_HTTP}/disconnect`, { method: "POST" });
      } catch {
      }
      setReaderName(null);
      setState("no-reader");
    }, []);
    return { state, readerName, reload, dismissInstallPrompt, disconnect };
  }

  // src/components/icons/UsbOffIcon.tsx
  var import_jsx_runtime40 = __toESM(require_react_shim(), 1);
  function UsbOffIcon({ className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)(
      "svg",
      {
        className,
        "aria-hidden": true,
        viewBox: "0 0 24 24",
        fill: "none",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(
            "g",
            {
              fill: "currentColor",
              transform: "translate(12,12) scale(0.05051) rotate(-90) translate(-237.624,-114.046)",
              children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("path", { d: "M 462.836,114.054 L 412.799,85.158 L 412.799,105.771 L 157.046,105.771 L 206.844,53.159 C 211.082,49.762 216.627,47.379 222.331,47.247 C 245.406,47.247 259.109,47.241 264.153,47.231 C 267.572,56.972 276.756,64.003 287.674,64.003 C 301.486,64.003 312.695,52.795 312.695,38.978 C 312.695,25.155 301.487,13.951 287.674,13.951 C 276.756,13.951 267.572,20.978 264.153,30.711 L 222.821,30.704 C 211.619,30.704 199.881,36.85 192.41,44.055 C 192.614,43.841 192.826,43.613 192.398,44.059 C 192.24,44.237 139.564,99.873 139.564,99.873 C 135.335,103.265 129.793,105.633 124.093,105.769 L 95.161,105.769 C 91.326,86.656 74.448,72.256 54.202,72.256 C 31.119,72.256 12.408,90.967 12.408,114.043 C 12.408,137.126 31.119,155.838 54.202,155.838 C 74.452,155.838 91.33,141.426 95.165,122.297 L 123.59,122.297 C 123.663,122.297 123.736,122.301 123.81,122.297 L 186.681,122.297 C 192.37,122.442 197.905,124.813 202.13,128.209 C 202.13,128.209 254.794,183.841 254.957,184.021 C 255.379,184.468 255.169,184.235 254.961,184.025 C 262.432,191.229 274.175,197.371 285.379,197.371 L 325.211,197.362 L 325.211,214.139 L 375.261,214.139 L 375.261,164.094 L 325.211,164.094 L 325.211,180.849 C 325.211,180.849 314.72,180.83 284.891,180.83 C 279.186,180.699 273.635,178.319 269.399,174.922 L 219.59,122.3 L 412.799,122.3 L 412.799,142.946 L 462.836,114.054 z" })
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("line", { x1: "3", y1: "21", x2: "21", y2: "3", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round" })
        ]
      }
    );
  }

  // src/components/icons/ReloadIcon.tsx
  var import_jsx_runtime41 = __toESM(require_react_shim(), 1);
  function ReloadIcon({ className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsxs)(
      "svg",
      {
        className,
        "aria-hidden": true,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        strokeWidth: "2.5",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime41.jsx)("path", { d: "M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8" }),
          /* @__PURE__ */ (0, import_jsx_runtime41.jsx)("path", { d: "M3 3v5h5" })
        ]
      }
    );
  }

  // src/components/icons/InfoCircleIcon.tsx
  var import_jsx_runtime42 = __toESM(require_react_shim(), 1);
  function InfoCircleIcon({ className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime42.jsxs)(
      "svg",
      {
        className,
        "aria-hidden": true,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        strokeWidth: "1.5",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime42.jsx)("circle", { cx: "12", cy: "12", r: "10" }),
          /* @__PURE__ */ (0, import_jsx_runtime42.jsx)("line", { x1: "12", y1: "16", x2: "12", y2: "12" }),
          /* @__PURE__ */ (0, import_jsx_runtime42.jsx)("line", { x1: "12", y1: "8", x2: "12.01", y2: "8", strokeWidth: "2" })
        ]
      }
    );
  }

  // src/components/scan/DesktopScanner/DesktopScanner.module.css
  var DesktopScanner_default = {
    spin: "DesktopScanner_spin",
    ping: "DesktopScanner_ping",
    card: "DesktopScanner_card",
    wrap: "DesktopScanner_wrap",
    readerCircle: "DesktopScanner_readerCircle",
    readerIcon: "DesktopScanner_readerIcon",
    nfcWrap: "DesktopScanner_nfcWrap",
    pingRing1: "DesktopScanner_pingRing1",
    pingRing2: "DesktopScanner_pingRing2",
    pingRing3: "DesktopScanner_pingRing3",
    nfcCircle: "DesktopScanner_nfcCircle",
    nfcIcon: "DesktopScanner_nfcIcon",
    heading: "DesktopScanner_heading",
    title: "DesktopScanner_title",
    subtitle: "DesktopScanner_subtitle",
    deviceLabel: "DesktopScanner_deviceLabel",
    notice: "DesktopScanner_notice",
    noticeTitle: "DesktopScanner_noticeTitle",
    searchingRow: "DesktopScanner_searchingRow",
    spinner: "DesktopScanner_spinner",
    noticeButtons: "DesktopScanner_noticeButtons",
    tipList: "DesktopScanner_tipList",
    helpText: "DesktopScanner_helpText",
    helpLink: "DesktopScanner_helpLink",
    supportedWrap: "DesktopScanner_supportedWrap",
    supportedLabel: "DesktopScanner_supportedLabel",
    readerGrid: "DesktopScanner_readerGrid",
    readerChip: "DesktopScanner_readerChip",
    chipName: "DesktopScanner_chipName",
    btnTroubleshoot: "DesktopScanner_btnTroubleshoot",
    btnReload: "DesktopScanner_btnReload",
    reloadIcon: "DesktopScanner_reloadIcon",
    btnSecondary: "DesktopScanner_btnSecondary",
    fullWidth: "DesktopScanner_fullWidth",
    nfcSection: "DesktopScanner_nfcSection",
    statusRow: "DesktopScanner_statusRow",
    statusDot: "DesktopScanner_statusDot",
    statusText: "DesktopScanner_statusText",
    waitingArea: "DesktopScanner_waitingArea",
    waitingTitle: "DesktopScanner_waitingTitle",
    waitingSubtitle: "DesktopScanner_waitingSubtitle",
    deviceRow: "DesktopScanner_deviceRow",
    deviceInfoIcon: "DesktopScanner_deviceInfoIcon",
    deviceInfoBody: "DesktopScanner_deviceInfoBody",
    deviceName: "DesktopScanner_deviceName",
    deviceTags: "DesktopScanner_deviceTags",
    lastTagSection: "DesktopScanner_lastTagSection",
    lastTagLabel: "DesktopScanner_lastTagLabel",
    lastTagValue: "DesktopScanner_lastTagValue",
    btnDisconnect: "DesktopScanner_btnDisconnect",
    disconnectIcon: "DesktopScanner_disconnectIcon",
    installDialog: "DesktopScanner_installDialog",
    installTitle: "DesktopScanner_installTitle",
    installDesc: "DesktopScanner_installDesc",
    installActions: "DesktopScanner_installActions",
    btnDownload: "DesktopScanner_btnDownload",
    btnCancel: "DesktopScanner_btnCancel",
    dlProgress: "DesktopScanner_dlProgress",
    dlBar: "DesktopScanner_dlBar",
    dlFill: "DesktopScanner_dlFill",
    dlLabel: "DesktopScanner_dlLabel",
    dlError: "DesktopScanner_dlError"
  };

  // src/components/scan/DesktopScanner/DesktopScanner.tsx
  var import_jsx_runtime43 = __toESM(require_react_shim(), 1);
  var SUPPORTED_READERS = ["ACR122U", "SCL3711", "OmniKey", "Feitian", "NXP", "SpringCard", "Bit4ID"];
  var AGENT_RELEASES_URL = "https://github.com/Coding252/spoolhub/releases/latest";
  var STORAGE_KEY = "spoolhub.lastTag";
  function loadLastTag() {
    try {
      const raw = sessionStorage.getItem(STORAGE_KEY);
      if (!raw) return null;
      const parsed = JSON.parse(raw);
      return { uid: parsed.uid, scannedAt: new Date(parsed.scannedAt) };
    } catch {
      return null;
    }
  }
  function saveLastTag(tag) {
    sessionStorage.setItem(STORAGE_KEY, JSON.stringify({ uid: tag.uid, scannedAt: tag.scannedAt.toISOString() }));
  }
  function useRelativeTime(date, t2) {
    const [label, setLabel] = (0, import_react33.useState)("");
    (0, import_react33.useEffect)(() => {
      if (!date) return;
      const d = date;
      function update() {
        const now = /* @__PURE__ */ new Date();
        const diffMs = now.getTime() - d.getTime();
        const diffSec = Math.floor(diffMs / 1e3);
        const diffMin = Math.floor(diffSec / 60);
        const diffHr = Math.floor(diffMin / 60);
        const diffDays = Math.floor(diffHr / 24);
        if (diffSec < 60) setLabel(t2("scan.timeJustNow"));
        else if (diffMin < 60) setLabel(t2("scan.timeMinAgo", { count: diffMin }));
        else if (diffHr < 24) setLabel(t2("scan.timeHoursAgo", { count: diffHr }));
        else if (diffDays === 1) setLabel(t2("scan.timeYesterday"));
        else if (diffDays < 7) {
          const dayName = d.toLocaleDateString("en", { weekday: "short" });
          setLabel(t2("scan.timeDayName", { day: dayName }));
        } else {
          const dateStr = d.toLocaleDateString("en", { month: "short", day: "numeric" });
          setLabel(t2("scan.timeDate", { date: dateStr }));
        }
      }
      update();
      const id = setInterval(update, 15e3);
      return () => clearInterval(id);
    }, [date, t2]);
    return label;
  }
  function NfcPulse() {
    return /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.nfcWrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.pingRing1 }),
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.pingRing2 }),
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.pingRing3 }),
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.nfcCircle, children: /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(NfcIcon, { className: DesktopScanner_default.nfcIcon }) })
    ] });
  }
  function SupportedReaders() {
    const { t: t2 } = useTranslation();
    return /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.supportedWrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.supportedLabel, children: t2("scan.supportedReaders") }),
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.readerGrid, children: SUPPORTED_READERS.map((name) => /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.readerChip, children: /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("span", { className: DesktopScanner_default.chipName, children: name }) }, name)) })
    ] });
  }
  function DesktopScanner({ onSpoolFound, onUnknownTag }) {
    const { t: t2 } = useTranslation();
    const [scanPhase, setScanPhase] = (0, import_react33.useState)("polling");
    const [scanError, setScanError] = (0, import_react33.useState)(null);
    const [dlPhase, setDlPhase] = (0, import_react33.useState)("idle");
    const [lastTag, setLastTag] = (0, import_react33.useState)(loadLastTag);
    const pollRef = (0, import_react33.useRef)(null);
    const lastTagTime = useRelativeTime(lastTag?.scannedAt ?? null, t2);
    const handleTagFound = (0, import_react33.useCallback)(async (uid) => {
      const tag = { uid, scannedAt: /* @__PURE__ */ new Date() };
      setLastTag(tag);
      saveLastTag(tag);
      setScanPhase("looking-up");
      try {
        const result = await scanTag(uid);
        if (result.status === "unknown") {
          if (onUnknownTag) onUnknownTag(uid);
          else setScanPhase("unknown");
        } else if (result.spool) {
          setScanPhase("polling");
          onSpoolFound(result.spool);
        }
      } catch {
        setScanError(t2("scan.errorLookup"));
        setScanPhase("error");
      }
    }, [onSpoolFound, onUnknownTag, t2]);
    const { state, readerName, reload, dismissInstallPrompt, disconnect } = useAgentNfc(handleTagFound);
    (0, import_react33.useEffect)(() => {
      if (state === "ready" || state === "no-reader") {
        if (pollRef.current) {
          clearInterval(pollRef.current);
          pollRef.current = null;
        }
      }
    }, [state]);
    (0, import_react33.useEffect)(() => () => {
      if (pollRef.current) clearInterval(pollRef.current);
    }, []);
    const handleDownload = (0, import_react33.useCallback)(() => {
      window.open(AGENT_RELEASES_URL, "_blank", "noopener,noreferrer");
      setDlPhase("waiting");
      pollRef.current = setInterval(() => reload(), 2e3);
    }, [reload]);
    function retryScan() {
      setScanPhase("polling");
      setScanError(null);
    }
    if (state === "ready") {
      const showResult = scanPhase === "unknown" || scanPhase === "error";
      const isLookingUp = scanPhase === "looking-up";
      return /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.card, children: [
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.nfcSection, children: !showResult ? /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)(import_jsx_runtime43.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.statusRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("span", { className: DesktopScanner_default.statusDot }),
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("span", { className: DesktopScanner_default.statusText, children: t2("scan.readerConnectedStatus") })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(NfcPulse, {}),
          /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.waitingArea, children: [
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.waitingTitle, children: isLookingUp ? t2("scan.lookingUp") : t2("scan.waitingForTag") }),
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.waitingSubtitle, children: t2("scan.holdSpool25cm") })
          ] })
        ] }) : /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.fullWidth, children: /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(
          ScanResult,
          {
            status: scanPhase,
            errorMessage: scanError,
            onRetry: retryScan
          }
        ) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.deviceRow, children: [
          /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(InfoCircleIcon, { className: DesktopScanner_default.deviceInfoIcon }),
          /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.deviceInfoBody, children: [
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.deviceName, children: t2("scan.deviceInfo", { name: readerName ?? "\u2014" }) }),
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.deviceTags, children: t2("scan.supportedTagsDesc") })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("button", { className: DesktopScanner_default.btnDisconnect, onClick: disconnect, children: [
          /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(UsbOffIcon, { className: DesktopScanner_default.disconnectIcon }),
          t2("scan.disconnect")
        ] }),
        lastTag && /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.lastTagSection, children: [
          /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.lastTagLabel, children: t2("scan.lastTagRead") }),
          /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("p", { className: DesktopScanner_default.lastTagValue, children: [
            lastTag.uid,
            " \u2022 ",
            lastTagTime
          ] })
        ] })
      ] });
    }
    if (state === "install-prompt") {
      return /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.card, children: /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.wrap, children: [
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.readerCircle, children: /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(UsbOffIcon, { className: DesktopScanner_default.readerIcon }) }),
        /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.installDialog, children: [
          /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.installTitle, children: t2("scan.agentRequired") }),
          /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.installDesc, children: t2("scan.agentRequiredDesc") }),
          dlPhase === "idle" && /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(import_jsx_runtime43.Fragment, { children: /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.installActions, children: [
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("button", { onClick: handleDownload, className: DesktopScanner_default.btnDownload, children: t2("scan.downloadAgent") }),
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("button", { onClick: () => dismissInstallPrompt(false), className: DesktopScanner_default.btnCancel, children: t2("scan.cancel") })
          ] }) }),
          dlPhase === "waiting" && /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.searchingRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.spinner }),
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.noticeTitle, children: t2("scan.waitingForAgent") })
          ] }),
          dlPhase === "error" && /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.installActions, children: [
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.dlError, children: t2("scan.downloadFailed") }),
            /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("button", { onClick: handleDownload, className: DesktopScanner_default.btnDownload, children: t2("scan.retry") })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(SupportedReaders, {})
      ] }) });
    }
    const isSpinning = state === "checking" || state === "connecting";
    const noticeTitle = state === "no-reader" ? t2("scan.agentNoReader") : state === "agent-offline" ? t2("scan.agentOffline") : t2("scan.agentConnecting");
    const tips = state === "agent-offline" ? [t2("scan.agentTip1"), t2("scan.agentTip2")] : [t2("scan.tip1"), t2("scan.tip2"), t2("scan.tip3")];
    return /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.card, children: /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.wrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.readerCircle, children: /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(UsbOffIcon, { className: DesktopScanner_default.readerIcon }) }),
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("h1", { className: DesktopScanner_default.title, children: t2("scan.usbNfcReader") }),
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.notice, children: isSpinning ? /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("div", { className: DesktopScanner_default.searchingRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.spinner }),
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.noticeTitle, children: noticeTitle })
      ] }) : /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)(import_jsx_runtime43.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("p", { className: DesktopScanner_default.noticeTitle, children: noticeTitle }),
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("div", { className: DesktopScanner_default.noticeButtons, children: /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)("button", { onClick: reload, className: DesktopScanner_default.btnReload, children: [
          /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(ReloadIcon, { className: DesktopScanner_default.reloadIcon }),
          t2("scan.reload")
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("ul", { className: DesktopScanner_default.tipList, children: tips.map((tip) => /* @__PURE__ */ (0, import_jsx_runtime43.jsx)("li", { children: tip }, tip)) })
      ] }) }),
      /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(SupportedReaders, {})
    ] }) });
  }

  // src/components/ScanView/ScanView.module.css
  var ScanView_default = {
    wrap: "ScanView_wrap",
    spinner: "ScanView_spinner",
    spinnerRing: "ScanView_spinnerRing",
    spin: "ScanView_spin",
    spinnerLabel: "ScanView_spinnerLabel",
    errorBox: "ScanView_errorBox",
    errorMsg: "ScanView_errorMsg",
    retryBtn: "ScanView_retryBtn"
  };

  // src/components/ScanView/ScanView.tsx
  var import_jsx_runtime44 = __toESM(require_react_shim(), 1);
  function detectPlatform() {
    if ("NDEFReader" in window) return "android";
    if (/iPhone|iPad/i.test(navigator.userAgent)) return "ios";
    return "pc";
  }
  function ScanView({ onUnknownTag } = {}) {
    const [searchParams] = useSearchParams();
    const navigate = useNavigate();
    const { t: t2 } = useTranslation();
    const tagUidParam = searchParams.get("tagUid");
    const platform = detectPlatform();
    const [phase, setPhase] = (0, import_react34.useState)(() => tagUidParam ? "processing" : "idle");
    const [errorMessage, setErrorMessage] = (0, import_react34.useState)(null);
    const [foundSpool, setFoundSpool] = (0, import_react34.useState)(null);
    const handleSpoolFound = (0, import_react34.useCallback)((spool) => {
      if (spool.isActive) {
        navigate(`/spools/${spool.id}`);
        return;
      }
      setFoundSpool(spool);
      setPhase("idle");
    }, [navigate]);
    const handleScanResult = (0, import_react34.useCallback)((result) => {
      if (platform === "pc") return;
      if (result.status === "found" && result.spool) {
        handleSpoolFound(result.spool);
      } else if (result.status === "unknown") {
        if (onUnknownTag && result.tagUid) {
          onUnknownTag(result.tagUid);
          return;
        }
        setPhase("error");
        setErrorMessage(t2("scan.unknownTag"));
      }
    }, [platform, handleSpoolFound, onUnknownTag, t2]);
    const { isConnected } = useNfcHub(handleScanResult);
    (0, import_react34.useEffect)(() => {
      if (!tagUidParam) return;
      scanTag(tagUidParam).then((result) => {
        if (result.status === "unknown") {
          const uid = result.tagUid || tagUidParam;
          if (onUnknownTag) {
            onUnknownTag(uid);
          } else {
            navigate(`/spools/add/nfctag?tagUid=${encodeURIComponent(uid)}`, { replace: true });
          }
        } else if (result.spool) {
          handleSpoolFound(result.spool);
        }
      }).catch(() => {
        setErrorMessage(t2("scan.lookupError"));
        setPhase("error");
      });
    }, [tagUidParam]);
    function reset() {
      setPhase("idle");
      setErrorMessage(null);
    }
    return /* @__PURE__ */ (0, import_jsx_runtime44.jsxs)("div", { className: ScanView_default.wrap, children: [
      phase === "processing" && /* @__PURE__ */ (0, import_jsx_runtime44.jsxs)("div", { className: ScanView_default.spinner, children: [
        /* @__PURE__ */ (0, import_jsx_runtime44.jsx)("div", { className: ScanView_default.spinnerRing }),
        /* @__PURE__ */ (0, import_jsx_runtime44.jsx)("p", { className: ScanView_default.spinnerLabel, children: t2("scan.lookingUp") })
      ] }),
      phase === "error" && /* @__PURE__ */ (0, import_jsx_runtime44.jsxs)("div", { className: ScanView_default.errorBox, children: [
        /* @__PURE__ */ (0, import_jsx_runtime44.jsx)("p", { className: ScanView_default.errorMsg, children: errorMessage }),
        /* @__PURE__ */ (0, import_jsx_runtime44.jsx)("button", { onClick: reset, className: ScanView_default.retryBtn, children: t2("scan.tryAgain") })
      ] }),
      phase === "idle" && /* @__PURE__ */ (0, import_jsx_runtime44.jsxs)(import_jsx_runtime44.Fragment, { children: [
        platform === "android" && /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(AndroidScanner, { isHubConnected: isConnected, onSpoolFound: handleSpoolFound, onUnknownTag }),
        platform === "ios" && /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(IphoneScanner, {}),
        platform === "pc" && /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(
          DesktopScanner,
          {
            isHubConnected: isConnected,
            onSpoolFound: handleSpoolFound,
            onUnknownTag
          }
        )
      ] }),
      foundSpool && /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(
        NfcScanModal,
        {
          spool: foundSpool,
          onClose: () => setFoundSpool(null)
        }
      )
    ] });
  }

  // src/components/Sidebar/Sidebar.tsx
  var import_react35 = __toESM(require_react_shim(), 1);

  // src/components/Sidebar/Sidebar.module.css
  var Sidebar_default = {
    overlay: "Sidebar_overlay",
    aside: "Sidebar_aside",
    asideOpen: "Sidebar_asideOpen",
    logoRow: "Sidebar_logoRow",
    closeBtn: "Sidebar_closeBtn",
    nav: "Sidebar_nav",
    link: "Sidebar_link",
    linkActive: "Sidebar_linkActive",
    groupRow: "Sidebar_groupRow",
    groupRowActive: "Sidebar_groupRowActive",
    groupLink: "Sidebar_groupLink",
    chevronBtn: "Sidebar_chevronBtn",
    chevron: "Sidebar_chevron",
    chevronOpen: "Sidebar_chevronOpen",
    submenu: "Sidebar_submenu"
  };

  // src/components/Sidebar/Sidebar.tsx
  var import_jsx_runtime45 = __toESM(require_react_shim(), 1);
  function navClass({ isActive }) {
    return isActive ? `${Sidebar_default.link} ${Sidebar_default.linkActive}` : Sidebar_default.link;
  }
  function Sidebar({ isOpen, onClose }) {
    const { t: t2 } = useTranslation();
    const { pathname } = useLocation();
    const filamentGroupActive = pathname === "/spools" || pathname.startsWith("/spools/") || pathname.startsWith("/filaments") || pathname.startsWith("/spool-profiles") || pathname === "/locations";
    const [state, setState] = (0, import_react35.useState)({ prevActive: filamentGroupActive, open: filamentGroupActive });
    if (state.prevActive !== filamentGroupActive) {
      setState({ prevActive: filamentGroupActive, open: filamentGroupActive });
    }
    const showFilament = state.open;
    return /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(import_jsx_runtime45.Fragment, { children: [
      isOpen && /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("div", { className: Sidebar_default.overlay, onClick: onClose, "aria-hidden": "true" }),
      /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("aside", { className: `${Sidebar_default.aside}${isOpen ? ` ${Sidebar_default.asideOpen}` : ""}`, children: [
        /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("div", { className: Sidebar_default.logoRow, children: [
          /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(NavLink, { to: "/", end: true, onClick: onClose, children: /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(SpoolHubLogo, { height: 52 }) }),
          /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("button", { onClick: onClose, className: Sidebar_default.closeBtn, "aria-label": t2("nav.closeSidebar"), children: /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
            /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
          ] }) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("nav", { className: Sidebar_default.nav, children: [
          /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/", end: true, onClick: onClose, className: navClass, children: [
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polyline", { points: "9 22 9 12 15 12 15 22" })
            ] }),
            t2("nav.home")
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/scan", onClick: onClose, className: navClass, children: [
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M3 7V5a2 2 0 0 1 2-2h2" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M17 3h2a2 2 0 0 1 2 2v2" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M21 17v2a2 2 0 0 1-2 2h-2" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M7 21H5a2 2 0 0 1-2-2v-2" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "7", y1: "12", x2: "17", y2: "12" })
            ] }),
            t2("nav.scan")
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("div", { className: `${Sidebar_default.groupRow}${filamentGroupActive ? ` ${Sidebar_default.groupRowActive}` : ""}`, children: [
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/spools", onClick: onClose, className: Sidebar_default.groupLink, children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5 flex-shrink-0", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polygon", { points: "12 2 2 7 12 12 22 7 12 2" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polyline", { points: "2 17 12 22 22 17" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polyline", { points: "2 12 12 17 22 12" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("span", { children: t2("nav.filamentInventory") })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(
              "button",
              {
                onClick: () => setState((s) => ({ ...s, open: !s.open })),
                "aria-label": t2("nav.toggleFilamentSubmenu"),
                className: Sidebar_default.chevronBtn,
                children: /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("svg", { className: `${Sidebar_default.chevron}${showFilament ? ` ${Sidebar_default.chevronOpen}` : ""}`, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polyline", { points: "6 9 12 15 18 9" }) })
              }
            )
          ] }),
          showFilament && /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("div", { className: Sidebar_default.submenu, children: [
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/spools", end: true, onClick: onClose, className: navClass, children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polyline", { points: "21 8 21 21 3 21 3 8" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("rect", { x: "1", y: "3", width: "22", height: "5" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "10", y1: "12", x2: "14", y2: "12" })
              ] }),
              t2("nav.spoolStock")
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/spool-profiles", onClick: onClose, className: navClass, children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z" }) }),
              t2("nav.spoolProfiles")
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/locations", onClick: onClose, className: navClass, children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("circle", { cx: "12", cy: "10", r: "3" })
              ] }),
              t2("nav.locations")
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/spools/active", onClick: onClose, className: navClass, children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("circle", { cx: "12", cy: "12", r: "9" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("circle", { cx: "12", cy: "12", r: "3" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "12", y1: "3", x2: "12", y2: "9" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "12", y1: "15", x2: "12", y2: "21" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "3", y1: "12", x2: "9", y2: "12" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "15", y1: "12", x2: "21", y2: "12" })
              ] }),
              t2("nav.activeSpools")
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/spools/low", onClick: onClose, className: navClass, children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "12", y1: "9", x2: "12", y2: "13" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "12", y1: "17", x2: "12.01", y2: "17" })
              ] }),
              t2("nav.lowSpools")
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/filaments", onClick: onClose, className: navClass, children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M4 19.5A2.5 2.5 0 0 1 6.5 17H20" }),
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z" })
              ] }),
              t2("nav.filamentLibrary")
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/activity", onClick: onClose, className: navClass, children: [
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polyline", { points: "14 2 14 8 20 8" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "16", y1: "13", x2: "8", y2: "13" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("line", { x1: "16", y1: "17", x2: "8", y2: "17" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polyline", { points: "10 9 9 9 8 9" })
            ] }),
            "Activity Log"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/printers", onClick: onClose, className: navClass, children: [
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("polyline", { points: "6 9 6 2 18 2 18 9" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("rect", { x: "6", y: "14", width: "12", height: "8" })
            ] }),
            t2("nav.printers")
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(NavLink, { to: "/settings", onClick: onClose, className: navClass, children: [
            /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)("svg", { className: "w-5 h-5", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("circle", { cx: "12", cy: "12", r: "3" }),
              /* @__PURE__ */ (0, import_jsx_runtime45.jsx)("path", { d: "M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" })
            ] }),
            t2("nav.settings")
          ] })
        ] })
      ] })
    ] });
  }

  // src/components/SpoolCard/SpoolCard.module.css
  var SpoolCard_default = {
    card: "SpoolCard_card",
    top: "SpoolCard_top",
    iconWrap: "SpoolCard_iconWrap",
    info: "SpoolCard_info",
    nameRow: "SpoolCard_nameRow",
    brand: "SpoolCard_brand",
    material: "SpoolCard_material",
    badges: "SpoolCard_badges",
    badgeActive: "SpoolCard_badgeActive",
    tags: "SpoolCard_tags",
    materialBadge: "SpoolCard_materialBadge",
    barWrap: "SpoolCard_barWrap",
    bar: "SpoolCard_bar",
    barFill: "SpoolCard_barFill",
    weightRow: "SpoolCard_weightRow",
    weightLabel: "SpoolCard_weightLabel",
    weightValue: "SpoolCard_weightValue",
    weightInitial: "SpoolCard_weightInitial",
    pct: "SpoolCard_pct",
    pctLow: "SpoolCard_pctLow",
    pctMedium: "SpoolCard_pctMedium",
    pctGood: "SpoolCard_pctGood",
    specs: "SpoolCard_specs",
    specChip: "SpoolCard_specChip",
    meta: "SpoolCard_meta",
    metaItem: "SpoolCard_metaItem",
    minW0: "SpoolCard_minW0",
    nfcIcon: "SpoolCard_nfcIcon",
    iconFlexShrink: "SpoolCard_iconFlexShrink",
    locationText: "SpoolCard_locationText"
  };

  // src/components/SpoolCard/SpoolCard.tsx
  var import_jsx_runtime46 = __toESM(require_react_shim(), 1);
  function SpoolCard({ spool, onClick }) {
    const { t: t2 } = useTranslation();
    const pct = Math.min(100, Math.round(spool.currentWeightG / spool.initialWeightG * 100));
    const isLow = spool.currentWeightG < spool.lowStockThresholdG;
    const lastUsed = spool.lastScannedAt ? new Date(spool.lastScannedAt).toLocaleString(void 0, { month: "short", day: "numeric", year: "numeric" }) : t2("spools.never", "never");
    const createdAt = new Date(spool.createdAt).toLocaleString(void 0, { month: "short", day: "numeric", year: "numeric" });
    const barColor = isLow || pct < 25 ? "#ef4444" : pct < 50 ? "#eab308" : "#22c55e";
    const pctClass = isLow || pct < 25 ? SpoolCard_default.pctLow : pct < 50 ? SpoolCard_default.pctMedium : SpoolCard_default.pctGood;
    const colorName = spool.colorName;
    const handleClick = (e2) => {
      if (onClick) {
        e2.preventDefault();
        onClick(spool);
      }
    };
    return /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)(Link, { to: `/spools/${spool.id}`, className: SpoolCard_default.card, onClick: handleClick, children: [
      /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("div", { className: SpoolCard_default.top, children: [
        /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("div", { className: SpoolCard_default.iconWrap, children: /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(SpoolIcon, { color: spool.colorHex, size: 72 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("div", { className: SpoolCard_default.info, children: [
          /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("div", { className: SpoolCard_default.nameRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("div", { className: SpoolCard_default.minW0, children: [
              /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("p", { className: SpoolCard_default.brand, children: spool.brand }),
              /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("p", { className: SpoolCard_default.material, children: [
                spool.material,
                " ",
                colorName
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("div", { className: SpoolCard_default.badges, children: [
              spool.isActive && /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("span", { className: SpoolCard_default.badgeActive, children: t2("spools.active") }),
              spool.hasNfcTag && /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("svg", { "aria-label": t2("spools.nfcTagLinked"), width: "20", height: "20", className: SpoolCard_default.nfcIcon, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("line", { x1: "5", y1: "7", x2: "5", y2: "17" }),
                /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M8 9.5a4 4 0 0 1 0 5" }),
                /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M11 8a7 7 0 0 1 0 8" }),
                /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M14 6.5a9.5 9.5 0 0 1 0 11" })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("div", { className: SpoolCard_default.tags, children: /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("span", { className: SpoolCard_default.materialBadge, children: [
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("svg", { width: "12", height: "12", className: SpoolCard_default.iconFlexShrink, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("polyline", { points: "20 6 9 17 4 12" }) }),
            spool.material
          ] }) })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("div", { className: SpoolCard_default.barWrap, children: /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("div", { className: SpoolCard_default.bar, children: /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("div", { className: SpoolCard_default.barFill, style: { width: `${pct}%`, backgroundColor: barColor, boxShadow: `0 0 8px ${barColor}66` } }) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("div", { className: SpoolCard_default.weightRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("p", { className: SpoolCard_default.weightLabel, children: [
          /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("span", { className: SpoolCard_default.weightValue, children: [
            Math.round(spool.currentWeightG),
            "g"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("span", { className: SpoolCard_default.weightInitial, children: [
            "/",
            Math.round(spool.initialWeightG),
            "g"
          ] }),
          " ",
          t2("spools.remaining")
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("p", { className: `${SpoolCard_default.pct} ${pctClass}`, children: [
          (isLow || pct < 25) && /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("svg", { width: "14", height: "14", className: SpoolCard_default.iconFlexShrink, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("line", { x1: "12", y1: "9", x2: "12", y2: "13" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("line", { x1: "12", y1: "17", x2: "12.01", y2: "17" })
          ] }),
          pct,
          "%"
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("div", { className: SpoolCard_default.meta, children: [
        /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("span", { className: SpoolCard_default.metaItem, children: [
          /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("svg", { width: "12", height: "12", className: SpoolCard_default.iconFlexShrink, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("rect", { x: "3", y: "4", width: "18", height: "18", rx: "2", ry: "2" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("line", { x1: "16", y1: "2", x2: "16", y2: "6" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("line", { x1: "8", y1: "2", x2: "8", y2: "6" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("line", { x1: "3", y1: "10", x2: "21", y2: "10" })
          ] }),
          createdAt
        ] }),
        spool.printerName ? /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("span", { className: SpoolCard_default.metaItem, children: [
          /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("svg", { width: "12", height: "12", className: SpoolCard_default.iconFlexShrink, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M6 9V3h12v6" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M6 9H2v10h4" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M18 9h4v10h-4" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M6 14h12v5H6z" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M9 17h6" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("circle", { cx: "12", cy: "5", r: "1" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("span", { className: SpoolCard_default.locationText, children: [
            spool.printerName,
            spool.amsSlot ? ` \u2022 Slot ${spool.amsSlot}` : ""
          ] })
        ] }) : spool.stockLocation ? /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("span", { className: SpoolCard_default.metaItem, children: [
          /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("svg", { width: "10", height: "10", className: SpoolCard_default.iconFlexShrink, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("path", { d: "M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("circle", { cx: "12", cy: "10", r: "3" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("span", { className: SpoolCard_default.locationText, children: spool.stockLocation })
        ] }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("span", { className: SpoolCard_default.metaItem, children: [
          /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)("svg", { width: "12", height: "12", className: SpoolCard_default.iconFlexShrink, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("circle", { cx: "12", cy: "12", r: "10" }),
            /* @__PURE__ */ (0, import_jsx_runtime46.jsx)("polyline", { points: "12 6 12 12 16 14" })
          ] }),
          lastUsed
        ] })
      ] })
    ] });
  }

  // src/components/SpoolDetail/SpoolDetail.tsx
  var import_react36 = __toESM(require_react_shim(), 1);
  var import_react_dom3 = __toESM(require_react_dom_shim(), 1);

  // src/components/SpoolDetail/SpoolDetail.module.css
  var SpoolDetail_default = {
    shake: "SpoolDetail_shake",
    printerLink: "SpoolDetail_printerLink",
    historyBtn: "SpoolDetail_historyBtn",
    viewCard: "SpoolDetail_viewCard",
    infoRow: "SpoolDetail_infoRow",
    card: "SpoolDetail_card",
    formInput: "SpoolDetail_formInput",
    barTrack: "SpoolDetail_barTrack",
    barError: "SpoolDetail_barError",
    barWarn: "SpoolDetail_barWarn",
    barOk: "SpoolDetail_barOk",
    pctError: "SpoolDetail_pctError",
    pctWarn: "SpoolDetail_pctWarn",
    pctOk: "SpoolDetail_pctOk",
    priceBorder: "SpoolDetail_priceBorder",
    priceValue: "SpoolDetail_priceValue",
    secondary: "SpoolDetail_secondary",
    locationIcon: "SpoolDetail_locationIcon",
    printerImgWrap: "SpoolDetail_printerImgWrap",
    trayText: "SpoolDetail_trayText",
    historyContent: "SpoolDetail_historyContent",
    editSep: "SpoolDetail_editSep",
    editGrid: "SpoolDetail_editGrid",
    fNotes: "SpoolDetail_fNotes",
    fNfc: "SpoolDetail_fNfc",
    nfcValue: "SpoolDetail_nfcValue",
    fCurWeight: "SpoolDetail_fCurWeight",
    fInitWeight: "SpoolDetail_fInitWeight",
    fSpoolWeight: "SpoolDetail_fSpoolWeight",
    fPrice: "SpoolDetail_fPrice",
    fActive: "SpoolDetail_fActive",
    fLowStock: "SpoolDetail_fLowStock",
    fPrinter: "SpoolDetail_fPrinter",
    fDiameter: "SpoolDetail_fDiameter",
    priceLabel: "SpoolDetail_priceLabel",
    priceInput: "SpoolDetail_priceInput",
    priceUnit: "SpoolDetail_priceUnit",
    printerLabel: "SpoolDetail_printerLabel",
    printerLabelError: "SpoolDetail_printerLabelError",
    printerSelect: "SpoolDetail_printerSelect",
    printerSelectError: "SpoolDetail_printerSelectError",
    colorHeader: "SpoolDetail_colorHeader",
    colorHeaderOverlay: "SpoolDetail_colorHeaderOverlay",
    viewHeaderRow: "SpoolDetail_viewHeaderRow",
    viewHeaderInfo: "SpoolDetail_viewHeaderInfo",
    viewHeaderMeta: "SpoolDetail_viewHeaderMeta",
    viewHeaderBrand: "SpoolDetail_viewHeaderBrand",
    viewHeaderTitle: "SpoolDetail_viewHeaderTitle",
    viewHeaderHex: "SpoolDetail_viewHeaderHex",
    viewHeaderBadges: "SpoolDetail_viewHeaderBadges",
    viewHeaderIcon: "SpoolDetail_viewHeaderIcon",
    viewStatusBadge: "SpoolDetail_viewStatusBadge",
    viewNfcBadge: "SpoolDetail_viewNfcBadge",
    viewNfcIcon: "SpoolDetail_viewNfcIcon",
    modal: "SpoolDetail_modal",
    modalHeader: "SpoolDetail_modalHeader"
  };

  // src/components/SpoolDetail/SpoolDetail.tsx
  var import_jsx_runtime47 = __toESM(require_react_shim(), 1);
  function SectionTitle({ children }) {
    return /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-xs font-semibold text-[var(--text-secondary)] uppercase tracking-widest mb-3", children });
  }
  function InfoRow({ label, value }) {
    return /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: `flex items-baseline justify-between py-2 border-b last:border-0 ${SpoolDetail_default.infoRow}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: "text-xs text-[var(--text-secondary)]", children: label }),
      /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: "text-sm font-semibold text-[var(--text-primary)] text-right", children: value })
    ] });
  }
  function ChevronIcon({ open }) {
    return /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("svg", { className: `w-4 h-4 transition-transform duration-200 ${open ? "rotate-180" : ""}`, fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", viewBox: "0 0 24 24", children: /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("polyline", { points: "6 9 12 15 18 9" }) });
  }
  function fmt(date, neverStr) {
    if (!date) return neverStr;
    return new Date(date).toLocaleString(void 0, { month: "short", day: "numeric", year: "numeric", hour: "2-digit", minute: "2-digit" });
  }
  function fmtDate(date) {
    return new Date(date).toLocaleString(void 0, { month: "short", day: "numeric", year: "numeric" });
  }
  function SpoolDetail({ spool, jobs, onUpdate, onDelete }) {
    const navigate = useNavigate();
    const { t: t2 } = useTranslation();
    const [editing, setEditing] = (0, import_react36.useState)(false);
    const [historyOpen, setHistoryOpen] = (0, import_react36.useState)(false);
    const [confirmDelete, setConfirmDelete] = (0, import_react36.useState)(false);
    const [saving, setSaving] = (0, import_react36.useState)(false);
    const [deleting, setDeleting] = (0, import_react36.useState)(false);
    const [form, setForm] = (0, import_react36.useState)({});
    const [selectedPrinterId, setSelectedPrinterId] = (0, import_react36.useState)(null);
    const [selectedAmsSlot, setSelectedAmsSlot] = (0, import_react36.useState)(null);
    const [printers, setPrinters] = (0, import_react36.useState)([]);
    const [allSpools, setAllSpools] = (0, import_react36.useState)([]);
    const [conflictSpool, setConflictSpool] = (0, import_react36.useState)(null);
    const [spoolToEvict, setSpoolToEvict] = (0, import_react36.useState)(null);
    const [deactivateConfirm, setDeactivateConfirm] = (0, import_react36.useState)(false);
    const [printerError, setPrinterError] = (0, import_react36.useState)(false);
    const [printerShakeKey, setPrinterShakeKey] = (0, import_react36.useState)(0);
    const [stockLocationError, setStockLocationError] = (0, import_react36.useState)(false);
    const [stockLocationShakeKey, setStockLocationShakeKey] = (0, import_react36.useState)(0);
    const [currency, setCurrency] = (0, import_react36.useState)("USD");
    const [locations, setLocations] = (0, import_react36.useState)([]);
    (0, import_react36.useEffect)(() => {
      printersApi.getAll().then(setPrinters).catch(() => {
      });
      spoolsApi.getAll().then(setAllSpools).catch(() => {
      });
      settingsApi.getApp().then((s) => {
        if (s.currency) setCurrency(s.currency);
      }).catch(() => {
      });
      locationsApi.getAll().then((locs) => setLocations(locs.map((l) => l.name))).catch(() => {
      });
    }, []);
    const trayOccupants = (0, import_react36.useMemo)(() => {
      if (!selectedPrinterId) return {};
      const result = {};
      for (const s of allSpools) {
        if (s.printerId === selectedPrinterId && s.amsSlot != null && s.id !== spool.id) {
          result[s.amsSlot] = { colorHex: s.colorHex, colorName: s.colorName, brand: s.brand, material: s.material };
        }
      }
      return result;
    }, [allSpools, selectedPrinterId, spool.id]);
    const printerTrayMap = (0, import_react36.useMemo)(() => {
      if (!spool.printerId) return {};
      const map = {};
      for (const s of allSpools) {
        if (s.printerId === spool.printerId && s.amsSlot != null) {
          map[s.amsSlot] = s;
        }
      }
      return map;
    }, [allSpools, spool.printerId]);
    function startEdit() {
      setForm({
        currentWeightG: spool.currentWeightG,
        initialWeightG: spool.initialWeightG,
        spoolWeightG: spool.spoolWeightG,
        lowStockThresholdG: spool.lowStockThresholdG,
        notes: spool.notes ?? "",
        stockLocation: spool.stockLocation ?? "",
        price: spool.price ?? void 0,
        isActive: spool.isActive,
        density: spool.density ?? void 0,
        diameterTolerance: spool.diameterTolerance ?? void 0,
        extruderMin: spool.extruderMin ?? void 0,
        extruderMax: spool.extruderMax ?? void 0,
        bedMin: spool.bedMin ?? void 0,
        bedMax: spool.bedMax ?? void 0
      });
      setSelectedPrinterId(spool.printerId);
      setSelectedAmsSlot(spool.amsSlot);
      setEditing(true);
      setConfirmDelete(false);
    }
    function cancelEdit() {
      setEditing(false);
      setForm({});
      setSelectedPrinterId(null);
      setSelectedAmsSlot(null);
      setPrinterError(false);
      setStockLocationError(false);
      setDeactivateConfirm(false);
      setConflictSpool(null);
      setSpoolToEvict(null);
    }
    function handleBusyTrayClick(slot) {
      const occupant = allSpools.find(
        (s) => s.printerId === selectedPrinterId && s.amsSlot === slot && s.id !== spool.id
      );
      if (occupant) setConflictSpool(occupant);
    }
    async function doSave(stockLocationOverride) {
      setSaving(true);
      try {
        if (spoolToEvict) {
          await spoolsApi.deactivate(spoolToEvict.id);
        }
        const { isActive: newIsActive, ...otherFields } = form;
        const deactivating = newIsActive === false && spool.isActive;
        let updated = spool;
        if (newIsActive === true && !spool.isActive) {
          updated = await spoolsApi.activate(spool.id);
        } else if (deactivating) {
          updated = await spoolsApi.deactivate(spool.id);
        }
        const notesBefore = (spool.notes ?? "").trim();
        const notesAfter = (otherFields.notes ?? "").trim();
        const locationBefore = (spool.stockLocation ?? "").trim();
        const locationAfter = stockLocationOverride !== void 0 ? stockLocationOverride.trim() : (otherFields.stockLocation ?? "").trim();
        const priceBefore = spool.price ?? null;
        const priceAfter = otherFields.price ?? null;
        const hasChanges = Math.abs((otherFields.currentWeightG ?? spool.currentWeightG) - spool.currentWeightG) > 0.01 || Math.abs((otherFields.initialWeightG ?? spool.initialWeightG) - spool.initialWeightG) > 0.01 || Math.abs((otherFields.spoolWeightG ?? spool.spoolWeightG) - spool.spoolWeightG) > 0.01 || Math.abs((otherFields.lowStockThresholdG ?? spool.lowStockThresholdG) - spool.lowStockThresholdG) > 0.01 || notesAfter !== notesBefore || locationAfter !== locationBefore || priceAfter !== priceBefore || (otherFields.extruderMin ?? spool.extruderMin ?? 0) !== (spool.extruderMin ?? 0) || (otherFields.extruderMax ?? spool.extruderMax ?? 0) !== (spool.extruderMax ?? 0) || (otherFields.bedMin ?? spool.bedMin ?? 0) !== (spool.bedMin ?? 0) || (otherFields.bedMax ?? spool.bedMax ?? 0) !== (spool.bedMax ?? 0) || (otherFields.density ?? spool.density ?? 0) !== (spool.density ?? 0) || (otherFields.diameterTolerance ?? spool.diameterTolerance ?? 0) !== (spool.diameterTolerance ?? 0);
        if (hasChanges) {
          const payload = { ...otherFields, notes: notesAfter || void 0, stockLocation: locationAfter || void 0, price: priceAfter };
          updated = await spoolsApi.update(spool.id, payload);
        }
        if (!deactivating && (selectedPrinterId !== spool.printerId || selectedAmsSlot !== spool.amsSlot)) {
          updated = await spoolsApi.assignPrinter(spool.id, { printerId: selectedPrinterId, amsSlot: selectedAmsSlot });
        }
        onUpdate(updated);
        setEditing(false);
        setConflictSpool(null);
        setSpoolToEvict(null);
        window.dispatchEvent(new CustomEvent("spools-updated"));
        navigate(-1);
      } finally {
        setSaving(false);
      }
    }
    async function handleSave() {
      if (form.isActive && !selectedPrinterId) {
        setPrinterError(true);
        setPrinterShakeKey((k) => k + 1);
        return;
      }
      if (spool.isActive && !form.isActive && !form.stockLocation?.trim()) {
        setStockLocationError(true);
        setStockLocationShakeKey((k) => k + 1);
        return;
      }
      await doSave();
    }
    async function handleDelete() {
      setDeleting(true);
      try {
        await spoolsApi.delete(spool.id);
        window.dispatchEvent(new CustomEvent("spools-updated", { detail: { deletedId: spool.id } }));
        onDelete();
      } finally {
        setDeleting(false);
      }
    }
    const hasPrintSettings = spool.extruderMin != null || spool.extruderMax != null || spool.bedMin != null || spool.bedMax != null;
    const selectedPrinterObj = printers.find((p) => p.id === selectedPrinterId) ?? null;
    const selectedPrinterImg = selectedPrinterObj ? getPrinterImage(selectedPrinterObj.brand, selectedPrinterObj.model) : "/printers/generic.svg";
    return /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)(import_jsx_runtime47.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "max-w-4xl mx-auto space-y-4", children: [
        !editing && /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: `border divide-y divide-[var(--border-subtle)] ${SpoolDetail_default.viewCard}`, children: [
          /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: `p-5 ${SpoolDetail_default.colorHeader}`, children: [
            /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("div", { className: SpoolDetail_default.colorHeaderOverlay, style: { backgroundColor: spool.colorHex ?? void 0 } }),
            /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: SpoolDetail_default.viewHeaderRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SpoolIcon, { color: spool.colorHex, size: 56, className: SpoolDetail_default.viewHeaderIcon }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: SpoolDetail_default.viewHeaderInfo, children: [
                /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: SpoolDetail_default.viewHeaderMeta, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(BrandLogo, { brand: spool.brand, size: 14 }),
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: SpoolDetail_default.viewHeaderBrand, children: spool.brand })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: SpoolDetail_default.viewHeaderTitle, children: spool.colorName }),
                spool.colorHex && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: SpoolDetail_default.viewHeaderHex, children: spool.colorHex })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: SpoolDetail_default.viewHeaderBadges, children: [
                /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(MaterialTag, { material: spool.material }),
                spool.isActive && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: SpoolDetail_default.viewStatusBadge, children: t2("spoolDetail.statusActive") }),
                spool.isArchived && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: SpoolDetail_default.viewStatusBadge, children: t2("spoolDetail.statusArchived") }),
                spool.hasNfcTag && /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("span", { className: SpoolDetail_default.viewNfcBadge, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(NfcIcon, { className: SpoolDetail_default.viewNfcIcon }),
                  t2("addSpool.nfcTag")
                ] })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "grid grid-cols-1 md:grid-cols-2 divide-y md:divide-y-0 md:divide-x divide-[var(--border-subtle)]", children: [
            /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "p-5", children: [
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SectionTitle, { children: t2("spoolDetail.sectionWeight") }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelCurrent"), value: `${Math.round(spool.currentWeightG)} g` }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelInitial"), value: `${Math.round(spool.initialWeightG)} g` }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelSpoolTare"), value: `${Math.round(spool.spoolWeightG)} g` }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelLowStockAt"), value: `${Math.round(spool.lowStockThresholdG)} g` })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: `p-5 border-b ${SpoolDetail_default.priceBorder}`, children: [
                /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SectionTitle, { children: t2("spoolForm.sectionPrice") }),
                spool.price != null ? /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("p", { className: `text-sm font-semibold ${SpoolDetail_default.priceValue}`, children: [
                  getCurrencySymbol(currency),
                  spool.price.toFixed(2)
                ] }) : /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: `text-sm italic ${SpoolDetail_default.secondary}`, children: t2("spoolDetail.noPrice") })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("div", { className: "p-5", children: (() => {
                const printer = printers.find((p) => p.id === spool.printerId);
                if (!spool.printerId || !printer) return /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)(import_jsx_runtime47.Fragment, { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SectionTitle, { children: t2("spoolDetail.sectionStockLocation") }),
                  spool.stockLocation ? /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "flex items-center gap-1.5", children: [
                    /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("svg", { width: "12", height: "12", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", className: SpoolDetail_default.locationIcon, children: [
                      /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("path", { d: "M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" }),
                      /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("circle", { cx: "12", cy: "10", r: "3" })
                    ] }),
                    /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: `text-sm font-semibold ${SpoolDetail_default.secondary}`, children: spool.stockLocation })
                  ] }) : /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-sm text-[var(--text-secondary)] italic", children: t2("spoolDetail.noLocation") })
                ] });
                const imgSrc = getPrinterImage(printer.brand, printer.model);
                return /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)(import_jsx_runtime47.Fragment, { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SectionTitle, { children: t2("spoolDetail.sectionPrinter") }),
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)(
                    Link,
                    {
                      to: `/printers/${printer.id}`,
                      className: `flex items-center gap-3 -m-2 p-2 rounded-xl no-underline transition-colors ${SpoolDetail_default.printerLink}`,
                      children: [
                        /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("div", { className: `flex-shrink-0 w-24 h-24 rounded-xl flex items-center justify-center overflow-hidden ${SpoolDetail_default.printerImgWrap}`, children: /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
                          "img",
                          {
                            src: imgSrc,
                            alt: `${printer.brand} ${printer.model}`,
                            className: "w-full h-full object-contain p-1.5",
                            onError: (e2) => {
                              e2.currentTarget.src = "/printers/generic.svg";
                            }
                          }
                        ) }),
                        /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "min-w-0", children: [
                          /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-xs text-[var(--text-secondary)]", children: printer.brand }),
                          /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-sm font-semibold text-[var(--text-primary)] truncate", children: printer.name }),
                          /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-xs text-[var(--text-secondary)] mt-0.5", children: printer.model }),
                          printer.hasAms && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("div", { className: "flex items-center gap-1.5 mt-2", children: [1, 2, 3, 4].map((t3) => {
                            const occupant = printerTrayMap[t3];
                            const isCurrent = t3 === spool.amsSlot;
                            return /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
                              "div",
                              {
                                title: occupant ? `${occupant.colorName} \u2014 ${occupant.brand} ${occupant.material}` : `Tray ${t3} \u2014 empty`,
                                className: `relative w-9 h-9 rounded-lg flex items-center justify-center text-xs font-bold transition-colors ${occupant ? isCurrent ? "ring-2 ring-cyan-400 ring-offset-1 ring-offset-white dark:ring-offset-[#21262d]" : "" : "border-2 border-dashed border-[var(--border)] text-[var(--text-secondary)]"}`,
                                style: occupant ? { backgroundColor: occupant.colorHex } : void 0,
                                children: /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: occupant ? SpoolDetail_default.trayText : void 0, children: t3 })
                              },
                              t3
                            );
                          }) })
                        ] })
                      ]
                    }
                  )
                ] });
              })() }),
              " "
            ] }),
            " "
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "grid grid-cols-1 md:grid-cols-3 divide-y md:divide-y-0 md:divide-x divide-[var(--border-subtle)]", children: [
            /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "p-5", children: [
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SectionTitle, { children: t2("spoolDetail.sectionUsage") }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelAdded"), value: fmtDate(spool.createdAt) }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelLastUsed"), value: fmt(spool.lastScannedAt, t2("spools.never")) }),
              spool.nfcTagUid && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelNfcTag"), value: /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: "font-mono", children: spool.nfcTagUid }) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "p-5", children: [
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SectionTitle, { children: t2("spoolDetail.sectionMaterial") }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelType"), value: spool.material }),
              spool.density != null && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolDetail.labelDensity"), value: `${spool.density} g/cm\xB3` }),
              spool.diameterTolerance != null && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(InfoRow, { label: t2("spoolForm.diameterTolerance"), value: `\xB1${spool.diameterTolerance} mm` })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "p-5", children: [
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SectionTitle, { children: t2("spoolDetail.sectionPrintSettings") }),
              hasPrintSettings ? /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "space-y-2", children: [
                (spool.extruderMin != null || spool.extruderMax != null) && /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-xs text-[var(--text-secondary)] mb-0.5", children: t2("spoolForm.extruderRange") }),
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("p", { className: "text-sm font-medium text-[var(--text-primary)]", children: [
                    spool.extruderMin ?? "?",
                    "\u2013",
                    spool.extruderMax ?? "?",
                    " \xB0C"
                  ] })
                ] }),
                (spool.bedMin != null || spool.bedMax != null) && /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-xs text-[var(--text-secondary)] mb-0.5", children: t2("spoolForm.bedRange") }),
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("p", { className: "text-sm font-medium text-[var(--text-primary)]", children: [
                    spool.bedMin ?? "?",
                    "\u2013",
                    spool.bedMax ?? "?",
                    " \xB0C"
                  ] })
                ] })
              ] }) : /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-xs text-[var(--text-secondary)]", children: t2("common.notSet") })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "p-5", children: [
            /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(SectionTitle, { children: t2("spoolDetail.sectionNotes") }),
            spool.notes ? /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: `text-sm ${SpoolDetail_default.secondary}`, children: spool.notes }) : /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: `text-sm italic ${SpoolDetail_default.secondary}`, children: t2("spoolDetail.noNotes") })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)(
              "button",
              {
                onClick: () => setHistoryOpen((o) => !o),
                className: `w-full flex items-center justify-between px-5 py-4 transition-colors ${SpoolDetail_default.historyBtn}`,
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "flex items-center gap-3", children: [
                    /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: "text-xs font-semibold text-[var(--text-secondary)] uppercase tracking-widest", children: t2("spoolDetail.sectionPrintHistory") }),
                    /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("span", { className: "text-xs text-[var(--text-secondary)] font-mono", children: t2("spoolDetail.jobs", { count: jobs.length }) })
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(ChevronIcon, { open: historyOpen })
                ]
              }
            ),
            historyOpen && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("div", { className: `border-t ${SpoolDetail_default.historyContent}`, children: /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(PrintHistoryList, { jobs }) })
          ] })
        ] }),
        editing && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
          SpoolEditor,
          {
            brand: spool.brand,
            material: spool.material,
            colorName: spool.colorName,
            colorHex: spool.colorHex,
            tagUid: spool.nfcTagUid ?? void 0,
            extruderMin: form.extruderMin ?? spool.extruderMin,
            extruderMax: form.extruderMax ?? spool.extruderMax,
            bedMin: form.bedMin ?? spool.bedMin,
            bedMax: form.bedMax ?? spool.bedMax,
            density: form.density ?? spool.density,
            diameterTolerance: form.diameterTolerance ?? spool.diameterTolerance,
            currentWeightG: String(form.currentWeightG ?? spool.currentWeightG),
            setCurrentWeightG: (v) => setForm((f) => ({ ...f, currentWeightG: parseFloat(v) || 0 })),
            initialWeightG: String(form.initialWeightG ?? spool.initialWeightG),
            setInitialWeightG: (v) => setForm((f) => ({ ...f, initialWeightG: parseFloat(v) || 0 })),
            spoolWeightG: String(form.spoolWeightG ?? spool.spoolWeightG),
            setSpoolWeightG: (v) => setForm((f) => ({ ...f, spoolWeightG: parseFloat(v) || 0 })),
            lowStockThresholdG: String(form.lowStockThresholdG ?? spool.lowStockThresholdG),
            setLowStockThresholdG: (v) => setForm((f) => ({ ...f, lowStockThresholdG: parseFloat(v) || 0 })),
            weightError: "",
            setWeightError: () => {
            },
            notes: form.notes ?? "",
            setNotes: (v) => setForm((f) => ({ ...f, notes: v })),
            stockLocation: form.stockLocation ?? "",
            setStockLocation: (v) => setForm((f) => ({ ...f, stockLocation: v })),
            stockLocationOptions: locations,
            price: String(form.price ?? ""),
            setPrice: (v) => setForm((f) => ({ ...f, price: v ? parseFloat(v) : void 0 })),
            currency,
            isActive: form.isActive ?? spool.isActive,
            setIsActive: (v) => {
              if (!v && spool.isActive && (form.isActive ?? spool.isActive)) {
                setDeactivateConfirm(true);
                return;
              }
              setForm((f) => ({ ...f, isActive: v }));
            },
            printers,
            printerId: selectedPrinterId,
            setPrinterId: (v) => {
              setSelectedPrinterId(v);
              if (v) setPrinterError(false);
              if (!v) setSelectedAmsSlot(null);
              setSpoolToEvict(null);
            },
            amsSlot: selectedAmsSlot,
            onAmsSlotChange: (slot) => {
              setSelectedAmsSlot(slot);
              if (!slot) setSpoolToEvict(null);
            },
            occupiedSlots: trayOccupants,
            currentSpoolColor: spool.colorHex,
            printerError,
            printerShakeKey,
            onPrinterShakeEnd: () => setPrinterError(false),
            onBusyTrayClick: handleBusyTrayClick,
            stockLocationError,
            stockLocationShakeKey,
            onStockLocationShakeEnd: () => setStockLocationError(false),
            submitting: saving,
            submitError: null,
            onSubmit: (e2) => {
              e2.preventDefault();
              handleSave();
            },
            onCancel: cancelEdit,
            onExtruderMinChange: (v) => setForm((f) => ({ ...f, extruderMin: parseInt(v) || void 0 })),
            onExtruderMaxChange: (v) => setForm((f) => ({ ...f, extruderMax: parseInt(v) || void 0 })),
            onBedMinChange: (v) => setForm((f) => ({ ...f, bedMin: parseInt(v) || void 0 })),
            onBedMaxChange: (v) => setForm((f) => ({ ...f, bedMax: parseInt(v) || void 0 })),
            onDensityChange: (v) => setForm((f) => ({ ...f, density: parseFloat(v) || void 0 })),
            onDiameterToleranceChange: (v) => setForm((f) => ({ ...f, diameterTolerance: parseFloat(v) || void 0 }))
          }
        ),
        !editing && !confirmDelete && /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "grid grid-cols-2 gap-3", children: [
          /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
            "button",
            {
              onClick: startEdit,
              className: "w-full py-3 rounded-xl bg-cyan-500 hover:bg-cyan-400 text-sm font-semibold text-white transition-colors",
              children: t2("common.edit")
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
            "button",
            {
              onClick: () => setConfirmDelete(true),
              className: "w-full py-3 rounded-xl bg-red-500 hover:bg-red-600 text-sm font-semibold text-white transition-colors",
              children: t2("common.delete")
            }
          )
        ] }),
        confirmDelete && /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "grid grid-cols-2 gap-3", children: [
          /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("button", { onClick: () => setConfirmDelete(false), className: "w-full py-3 rounded-xl border border-[var(--border)] text-sm font-semibold text-[var(--text-primary)] hover:bg-[var(--bg-secondary)] transition-colors", children: t2("common.cancel") }),
          /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("button", { onClick: handleDelete, disabled: deleting, className: "w-full py-3 rounded-xl bg-red-500 hover:bg-red-600 text-sm font-semibold text-white disabled:opacity-50 transition-colors", children: deleting ? t2("common.deleting") : t2("spoolDetail.confirmDelete") })
        ] })
      ] }),
      conflictSpool && /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
        AmsConflictModal,
        {
          printerImgSrc: selectedPrinterImg,
          printerBrand: selectedPrinterObj?.brand,
          printerModel: selectedPrinterObj?.model,
          traySlot: conflictSpool.amsSlot ?? void 0,
          occupantSpool: conflictSpool,
          onCancel: () => {
            if (conflictSpool.amsSlot == null) setSelectedPrinterId(null);
            setConflictSpool(null);
          },
          onConfirm: () => {
            setSpoolToEvict(conflictSpool);
            if (conflictSpool.amsSlot != null) setSelectedAmsSlot(conflictSpool.amsSlot);
            setConflictSpool(null);
          }
        }
      ),
      deactivateConfirm && (0, import_react_dom3.createPortal)(
        /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("div", { className: "fixed inset-0 z-[9999] flex items-center justify-center bg-black/60 backdrop-blur-sm p-4", onClick: () => setDeactivateConfirm(false), children: /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: `rounded-2xl shadow-2xl w-full max-w-sm overflow-hidden ${SpoolDetail_default.modal}`, onClick: (e2) => e2.stopPropagation(), children: [
          /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("div", { className: `flex items-center justify-center p-6 border-b ${SpoolDetail_default.modalHeader}`, children: /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
            "img",
            {
              src: selectedPrinterImg,
              alt: "",
              className: "w-32 h-32 object-contain",
              onError: (e2) => {
                e2.currentTarget.src = "/printers/generic.svg";
              }
            }
          ) }),
          /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "p-5", children: [
            selectedPrinterObj && /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("p", { className: "text-xs text-[var(--text-secondary)] mb-0.5", children: [
              selectedPrinterObj.brand,
              " \xB7 ",
              selectedPrinterObj.model
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-base font-semibold text-[var(--text-primary)] mb-1", children: t2("spoolDetail.deactivateTitle") }),
            /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("p", { className: "text-sm text-[var(--text-secondary)] mb-4", children: selectedAmsSlot != null ? t2("spoolDetail.deactivateBodyWithTray", { slot: selectedAmsSlot }) : t2("spoolDetail.deactivateBody") }),
            /* @__PURE__ */ (0, import_jsx_runtime47.jsxs)("div", { className: "grid grid-cols-2 gap-3", children: [
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)("button", { onClick: () => setDeactivateConfirm(false), className: "py-2.5 rounded-xl border border-[var(--border)] text-sm font-semibold text-[var(--text-primary)] hover:bg-[var(--bg-secondary)] transition-colors", children: t2("common.cancel") }),
              /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
                "button",
                {
                  onClick: () => {
                    setDeactivateConfirm(false);
                    setForm((f) => ({ ...f, isActive: false }));
                  },
                  className: "py-2.5 rounded-xl bg-red-500 hover:bg-red-600 text-sm font-semibold text-white transition-colors",
                  children: saving ? t2("common.saving") : t2("spoolDetail.deactivate")
                }
              )
            ] })
          ] })
        ] }) }),
        document.body
      )
    ] });
  }

  // src/components/SpoolDetailsForm/SpoolDetailsForm.tsx
  var import_react37 = __toESM(require_react_shim(), 1);

  // src/components/SpoolDetailsForm/SpoolDetailsForm.module.css
  var SpoolDetailsForm_default = {
    shake: "SpoolDetailsForm_shake",
    form: "SpoolDetailsForm_form",
    card: "SpoolDetailsForm_card",
    section: "SpoolDetailsForm_section",
    sectionHeader: "SpoolDetailsForm_sectionHeader",
    header: "SpoolDetailsForm_header",
    headerColorOverlay: "SpoolDetailsForm_headerColorOverlay",
    headerRow: "SpoolDetailsForm_headerRow",
    headerInfo: "SpoolDetailsForm_headerInfo",
    headerBadges: "SpoolDetailsForm_headerBadges",
    headerMeta: "SpoolDetailsForm_headerMeta",
    headerBrand: "SpoolDetailsForm_headerBrand",
    headerTitle: "SpoolDetailsForm_headerTitle",
    headerSub: "SpoolDetailsForm_headerSub",
    headerHex: "SpoolDetailsForm_headerHex",
    brandLogo: "SpoolDetailsForm_brandLogo",
    brandFallback: "SpoolDetailsForm_brandFallback",
    mainGrid: "SpoolDetailsForm_mainGrid",
    statsArea: "SpoolDetailsForm_statsArea",
    rightColWrap: "SpoolDetailsForm_rightColWrap",
    stockLocWrap: "SpoolDetailsForm_stockLocWrap",
    activeBox: "SpoolDetailsForm_activeBox",
    activeRow: "SpoolDetailsForm_activeRow",
    activeBoxOn: "SpoolDetailsForm_activeBoxOn",
    priceBox: "SpoolDetailsForm_priceBox",
    box: "SpoolDetailsForm_box",
    boxLabel: "SpoolDetailsForm_boxLabel",
    boxInput: "SpoolDetailsForm_boxInput",
    boxInputError: "SpoolDetailsForm_boxInputError",
    boxInputValid: "SpoolDetailsForm_boxInputValid",
    boxError: "SpoolDetailsForm_boxError",
    boxLabelRow: "SpoolDetailsForm_boxLabelRow",
    editLabel: "SpoolDetailsForm_editLabel",
    boxEditBtn: "SpoolDetailsForm_boxEditBtn",
    boxReadonly: "SpoolDetailsForm_boxReadonly",
    boxLocError: "SpoolDetailsForm_boxLocError",
    boxLabelError: "SpoolDetailsForm_boxLabelError",
    minW0: "SpoolDetailsForm_minW0",
    toggleLabel: "SpoolDetailsForm_toggleLabel",
    toggleSub: "SpoolDetailsForm_toggleSub",
    toggle: "SpoolDetailsForm_toggle",
    toggleOn: "SpoolDetailsForm_toggleOn",
    toggleThumb: "SpoolDetailsForm_toggleThumb",
    toggleThumbOn: "SpoolDetailsForm_toggleThumbOn",
    newLocRow: "SpoolDetailsForm_newLocRow",
    newLocInput: "SpoolDetailsForm_newLocInput",
    newLocSave: "SpoolDetailsForm_newLocSave",
    newLocCancel: "SpoolDetailsForm_newLocCancel",
    printerBox: "SpoolDetailsForm_printerBox",
    printerBoxError: "SpoolDetailsForm_printerBoxError",
    printerLabel: "SpoolDetailsForm_printerLabel",
    printerLabelError: "SpoolDetailsForm_printerLabelError",
    printerSelectWrap: "SpoolDetailsForm_printerSelectWrap",
    printerSelect: "SpoolDetailsForm_printerSelect",
    placeholderSelect: "SpoolDetailsForm_placeholderSelect",
    printerSelectError: "SpoolDetailsForm_printerSelectError",
    printerSelectChevron: "SpoolDetailsForm_printerSelectChevron",
    printerSelectChevronError: "SpoolDetailsForm_printerSelectChevronError",
    printerDetail: "SpoolDetailsForm_printerDetail",
    printerImg: "SpoolDetailsForm_printerImg",
    printerImgSrc: "SpoolDetailsForm_printerImgSrc",
    printerMeta: "SpoolDetailsForm_printerMeta",
    printerName: "SpoolDetailsForm_printerName",
    printerBrandLabel: "SpoolDetailsForm_printerBrandLabel",
    amsLabel: "SpoolDetailsForm_amsLabel",
    amsLabelError: "SpoolDetailsForm_amsLabelError",
    amsTray: "SpoolDetailsForm_amsTray",
    amsSlotError: "SpoolDetailsForm_amsSlotError",
    amsBtn: "SpoolDetailsForm_amsBtn",
    amsBtnSelected: "SpoolDetailsForm_amsBtnSelected",
    trayText: "SpoolDetailsForm_trayText",
    directSpool: "SpoolDetailsForm_directSpool",
    nfcBadge: "SpoolDetailsForm_nfcBadge",
    nfcBadgeText: "SpoolDetailsForm_nfcBadgeText",
    nfcIcon: "SpoolDetailsForm_nfcIcon",
    specBox: "SpoolDetailsForm_specBox",
    specLabel: "SpoolDetailsForm_specLabel",
    specValue: "SpoolDetailsForm_specValue",
    sectionToggleRow: "SpoolDetailsForm_sectionToggleRow",
    chevron: "SpoolDetailsForm_chevron",
    chevronOpen: "SpoolDetailsForm_chevronOpen",
    specGrid3: "SpoolDetailsForm_specGrid3",
    boxInputWrap: "SpoolDetailsForm_boxInputWrap",
    boxUnit: "SpoolDetailsForm_boxUnit",
    textarea: "SpoolDetailsForm_textarea",
    submitError: "SpoolDetailsForm_submitError",
    footerRow: "SpoolDetailsForm_footerRow",
    btnCancel: "SpoolDetailsForm_btnCancel",
    btnSave: "SpoolDetailsForm_btnSave"
  };

  // src/components/SpoolDetailsForm/SpoolDetailsForm.tsx
  var import_jsx_runtime48 = __toESM(require_react_shim(), 1);
  var BRAND_DOMAINS4 = {
    "Bambu Lab": "bambulab.com",
    "eSUN 3D": "esun3d.com",
    "Prusament": "prusament.com",
    "Polymaker": "polymaker.com",
    "Hatchbox": "hatchbox3d.com",
    "SUNLU": "sunlu.com",
    "Creality": "creality.com",
    "ELEGOO": "elegoo.com",
    "Overture": "overture3d.com",
    "PolyLite": "polymaker.com",
    "colorFabb": "colorfabb.com",
    "Fillamentum": "fillamentum.com",
    "Fiberlogy": "fiberlogy.com",
    "extrudr": "extrudr.com",
    "Das Filament": "dasfilament.de",
    "Protopasta": "proto-pasta.com",
    "MatterHackers": "matterhackers.com",
    "NinjaTek": "ninjatek.com",
    "Atomic Filament": "atomicfilament.com",
    "Inland": "microcenter.com",
    "JAYO": "jayofilament.com",
    "TINMORRY": "tinmorry.com",
    "AzureFilm": "azurefilm.hr",
    "Spectrum": "spectrumfilaments.com",
    "Devil Design": "devildesign.pl",
    "Anycubic": "anycubic.com"
  };
  function BrandFavicon2({ brand }) {
    const [error, setError] = (0, import_react37.useState)(false);
    const domain = BRAND_DOMAINS4[brand];
    if (domain && !error) {
      return /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
        "img",
        {
          src: `https://www.google.com/s2/favicons?sz=64&domain=${domain}`,
          alt: brand,
          className: SpoolDetailsForm_default.brandLogo,
          onError: () => setError(true)
        }
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: SpoolDetailsForm_default.brandFallback, children: brand[0] });
  }
  function SpecBox2({ label, value }) {
    if (!value) return null;
    return /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.specBox, children: [
      /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.specLabel, children: label }),
      /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.specValue, children: value })
    ] });
  }
  var PenIcon2 = () => /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("svg", { width: "14", height: "14", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
    /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("path", { d: "M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7" }),
    /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("path", { d: "M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z" })
  ] });
  var LockIcon2 = () => /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("svg", { width: "14", height: "14", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
    /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("rect", { x: "3", y: "11", width: "18", height: "11", rx: "2", ry: "2" }),
    /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("path", { d: "M7 11V7a5 5 0 0 1 10 0v4" })
  ] });
  function EditableWeightBox2({ label, value, onChange, editing, onToggleEdit, errorText }) {
    return /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.box, children: [
      /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.boxLabelRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: SpoolDetailsForm_default.editLabel, children: label }),
        /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("button", { type: "button", className: SpoolDetailsForm_default.boxEditBtn, onClick: onToggleEdit, title: editing ? "Lock" : "Edit", children: editing ? /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(LockIcon2, {}) : /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(PenIcon2, {}) })
      ] }),
      editing ? /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("input", { type: "number", value, onChange: (e2) => onChange(e2.target.value), className: SpoolDetailsForm_default.boxInput, autoFocus: true }) : /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.boxReadonly, children: value !== "" ? `${value} g` : "\u2014" }),
      errorText && /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.boxError, children: errorText })
    ] });
  }
  function PrinterAssignSection2({
    printers,
    printerId,
    onPrinterChange,
    amsSlot,
    onAmsSlotChange,
    occupiedSlots,
    currentSpoolColor,
    disabled,
    printerError,
    printerShakeKey,
    onPrinterShakeEnd,
    amsSlotError,
    onBusyTrayClick
  }) {
    const { t: t2 } = useTranslation();
    const selected = printers.find((p) => p.id === printerId);
    const showAms = selected?.hasAms && onAmsSlotChange !== null;
    return /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)(
      "div",
      {
        className: `${SpoolDetailsForm_default.printerBox}${printerError ? ` ${SpoolDetailsForm_default.printerBoxError} ${SpoolDetailsForm_default.shake}` : ""}`,
        onAnimationEnd: onPrinterShakeEnd,
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("label", { className: `${SpoolDetailsForm_default.printerLabel}${printerError ? ` ${SpoolDetailsForm_default.printerLabelError}` : ""}`, children: printerError ? t2("spoolForm.assignPrinterWarning") : t2("spoolForm.assignedPrinter") }),
          /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)(
            "select",
            {
              value: printerId ?? "",
              onChange: (e2) => onPrinterChange(e2.target.value || null),
              disabled,
              className: `${SpoolDetailsForm_default.printerSelect}${printerError ? ` ${SpoolDetailsForm_default.printerSelectError}` : ""}`,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("option", { value: "", children: t2("spoolForm.noPrinter") }),
                printers.map((p) => /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("option", { value: p.id, children: [
                  p.name,
                  " \xB7 ",
                  p.brand,
                  " ",
                  p.model
                ] }, p.id))
              ]
            }
          ),
          selected && /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.printerDetail, children: [
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("div", { className: SpoolDetailsForm_default.printerImg, children: /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
              "img",
              {
                src: getPrinterImage(selected.brand, selected.model),
                alt: `${selected.brand} ${selected.model}`,
                className: SpoolDetailsForm_default.printerImgSrc,
                onError: (e2) => {
                  e2.currentTarget.src = "/printers/generic.svg";
                }
              }
            ) }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.printerMeta, children: [
              /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.printerName, children: selected.name }),
              /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.printerBrandLabel, children: selected.brand }),
              showAms ? /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)(import_jsx_runtime48.Fragment, { children: [
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: `${SpoolDetailsForm_default.amsLabel}${amsSlotError ? ` ${SpoolDetailsForm_default.amsLabelError}` : ""}`, children: amsSlotError ? t2("spoolForm.selectTrayWarning") : t2("spoolForm.amsTray") }),
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("div", { className: `${SpoolDetailsForm_default.amsTray}${amsSlotError ? ` ${SpoolDetailsForm_default.amsSlotError}` : ""}`, children: [1, 2, 3, 4].map((slotNum) => {
                  const occupant = occupiedSlots[slotNum];
                  const isSel = amsSlot === slotNum;
                  const color = isSel ? currentSpoolColor : occupant?.colorHex;
                  return /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
                    "button",
                    {
                      type: "button",
                      title: isSel ? "This spool" : occupant ? `${occupant.colorName} \u2014 ${occupant.brand} ${occupant.material}` : `Tray ${slotNum} \u2014 empty`,
                      onClick: () => {
                        if (isSel) {
                          onAmsSlotChange(null);
                          return;
                        }
                        if (occupiedSlots[slotNum] && onBusyTrayClick) {
                          onBusyTrayClick(slotNum);
                          return;
                        }
                        onAmsSlotChange(slotNum);
                      },
                      disabled,
                      style: color ? { backgroundColor: color } : void 0,
                      className: `${SpoolDetailsForm_default.amsBtn}${isSel ? ` ${SpoolDetailsForm_default.amsBtnSelected}` : ""}`,
                      children: /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: color ? SpoolDetailsForm_default.trayText : void 0, children: slotNum })
                    },
                    slotNum
                  );
                }) })
              ] }) : /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: SpoolDetailsForm_default.directSpool, children: t2("spoolForm.directSpool") })
            ] })
          ] })
        ]
      },
      printerShakeKey
    );
  }
  function SpoolDetailsForm({
    filament: f,
    tagUid,
    currentWeightG,
    setCurrentWeightG,
    currentWeightShake,
    currentWeightStatus,
    initialWeightG,
    setInitialWeightG,
    spoolWeightG,
    setSpoolWeightG,
    lowStockThresholdG,
    setLowStockThresholdG,
    notes,
    setNotes,
    stockLocation,
    setStockLocation,
    stockLocationOptions = [],
    price,
    setPrice,
    currency = "USD",
    weightError,
    setWeightError,
    isActive,
    setIsActive,
    printers,
    printerId,
    setPrinterId,
    amsSlot,
    onAmsSlotChange,
    occupiedSlots,
    currentSpoolColor,
    printerError,
    printerShakeKey,
    onPrinterShakeEnd,
    amsSlotError,
    onBusyTrayClick,
    stockLocationError,
    stockLocationShakeKey,
    onStockLocationShakeEnd,
    submitting,
    submitError,
    onSubmit,
    onCancel
  }) {
    const { t: t2 } = useTranslation();
    const [editInitial, setEditInitial] = (0, import_react37.useState)(false);
    const [editSpool, setEditSpool] = (0, import_react37.useState)(false);
    const [editLowStock, setEditLowStock] = (0, import_react37.useState)(false);
    const [printOpen, setPrintOpen] = (0, import_react37.useState)(false);
    const [materialOpen, setMaterialOpen] = (0, import_react37.useState)(false);
    const [addingLoc, setAddingLoc] = (0, import_react37.useState)(false);
    const [newLocName, setNewLocName] = (0, import_react37.useState)("");
    const [savingLoc, setSavingLoc] = (0, import_react37.useState)(false);
    const [extraLocations, setExtraLocations] = (0, import_react37.useState)([]);
    const newLocInputRef = (0, import_react37.useRef)(null);
    const allLocOptions = [.../* @__PURE__ */ new Set([...stockLocationOptions, ...extraLocations])];
    async function handleSaveLoc() {
      const name = newLocName.trim();
      if (!name || savingLoc) return;
      setSavingLoc(true);
      try {
        await locationsApi.add({ name });
        setExtraLocations((prev) => [...prev, name]);
        setStockLocation(name);
        setAddingLoc(false);
        setNewLocName("");
      } finally {
        setSavingLoc(false);
      }
    }
    const hasPrintSettings = f.extruderMin != null || f.extruderMax != null || f.bedMin != null || f.bedMax != null;
    const cwInputClass = `${SpoolDetailsForm_default.boxInput}${currentWeightStatus === "error" ? ` ${SpoolDetailsForm_default.boxInputError}` : currentWeightStatus === "valid" ? ` ${SpoolDetailsForm_default.boxInputValid}` : ""}`;
    return /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("form", { onSubmit, noValidate: true, className: SpoolDetailsForm_default.form, children: [
      /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.card, children: [
        /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.header, children: [
          /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("div", { className: SpoolDetailsForm_default.headerColorOverlay, style: { backgroundColor: f.colorHex ?? void 0 } }),
          /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.headerRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpoolIcon, { color: f.colorHex ?? "#888", size: 100 }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.headerInfo, children: [
              /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.headerMeta, children: [
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(BrandFavicon2, { brand: f.brand }),
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: SpoolDetailsForm_default.headerBrand, children: f.brand })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.headerTitle, children: f.colorName ?? f.filamentName }),
              /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("div", { className: SpoolDetailsForm_default.headerSub, children: /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.headerHex, children: f.colorHex }) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.headerBadges, children: [
              /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(MaterialTag, { material: f.material }),
              tagUid && /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.nfcBadge, children: [
                /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("svg", { width: "14", height: "14", className: SpoolDetailsForm_default.nfcIcon, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("line", { x1: "5", y1: "7", x2: "5", y2: "17" }),
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("path", { d: "M8 9.5a4 4 0 0 1 0 5" }),
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("path", { d: "M11 8a7 7 0 0 1 0 8" }),
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("path", { d: "M14 6.5a9.5 9.5 0 0 1 0 11" })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: SpoolDetailsForm_default.nfcBadgeText, children: t2("addSpool.nfcTag") })
              ] })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.section, children: [
          /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.sectionHeader, children: t2("spoolForm.sectionSpoolStats") }),
          /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.mainGrid, children: [
            /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.statsArea, children: [
              /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: `${SpoolDetailsForm_default.box}${currentWeightShake ? ` ${SpoolDetailsForm_default.shake}` : ""}`, children: [
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("label", { className: SpoolDetailsForm_default.boxLabel, children: t2("spoolDetail.currentWeightG") }),
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
                  "input",
                  {
                    type: "number",
                    value: currentWeightG,
                    onChange: (e2) => setCurrentWeightG(e2.target.value),
                    className: cwInputClass
                  }
                )
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
                EditableWeightBox2,
                {
                  label: t2("spoolDetail.initialWeightG"),
                  value: initialWeightG,
                  onChange: (v) => {
                    setInitialWeightG(v);
                    setWeightError("");
                  },
                  editing: editInitial,
                  onToggleEdit: () => setEditInitial((e2) => !e2),
                  errorText: weightError || void 0
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: `${SpoolDetailsForm_default.activeBox}${isActive ? ` ${SpoolDetailsForm_default.activeBoxOn}` : ""}`, children: [
                /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.toggleLabel, children: t2("spoolForm.active") }),
                /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.activeRow, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.toggleSub, children: t2("spoolForm.loadedSpool") }),
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
                    "button",
                    {
                      type: "button",
                      onClick: () => setIsActive(!isActive),
                      className: `${SpoolDetailsForm_default.toggle}${isActive ? ` ${SpoolDetailsForm_default.toggleOn}` : ""}`,
                      children: /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: `${SpoolDetailsForm_default.toggleThumb}${isActive ? ` ${SpoolDetailsForm_default.toggleThumbOn}` : ""}` })
                    }
                  )
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
                EditableWeightBox2,
                {
                  label: t2("spoolDetail.spoolWeightG"),
                  value: spoolWeightG,
                  onChange: setSpoolWeightG,
                  editing: editSpool,
                  onToggleEdit: () => setEditSpool((e2) => !e2)
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
                EditableWeightBox2,
                {
                  label: t2("spoolDetail.lowStockAtG"),
                  value: lowStockThresholdG,
                  onChange: setLowStockThresholdG,
                  editing: editLowStock,
                  onToggleEdit: () => setEditLowStock((e2) => !e2)
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.priceBox, children: [
                /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("label", { className: SpoolDetailsForm_default.boxLabel, children: [
                  t2("spoolForm.price"),
                  " (",
                  getCurrencySymbol(currency),
                  ")"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.boxInputWrap, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
                    "input",
                    {
                      type: "number",
                      min: 0,
                      step: 0.01,
                      value: price,
                      onChange: (e2) => setPrice(e2.target.value),
                      placeholder: "0.00",
                      className: SpoolDetailsForm_default.boxInput
                    }
                  ),
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: SpoolDetailsForm_default.boxUnit, children: getCurrencySymbol(currency) })
                ] })
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("div", { className: SpoolDetailsForm_default.rightColWrap, children: setPrinterId && isActive ? /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
              PrinterAssignSection2,
              {
                printers: printers ?? [],
                printerId: printerId ?? null,
                onPrinterChange: (v) => {
                  setPrinterId(v);
                  if (!v) onAmsSlotChange?.(null);
                },
                amsSlot: amsSlot ?? null,
                onAmsSlotChange: onAmsSlotChange ?? null,
                occupiedSlots: occupiedSlots ?? {},
                currentSpoolColor,
                disabled: submitting,
                printerError,
                printerShakeKey,
                onPrinterShakeEnd,
                amsSlotError,
                onBusyTrayClick
              }
            ) : !isActive ? /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)(
              "div",
              {
                className: `${SpoolDetailsForm_default.printerBox}${stockLocationError ? ` ${SpoolDetailsForm_default.printerBoxError} ${SpoolDetailsForm_default.shake}` : ""}`,
                onAnimationEnd: onStockLocationShakeEnd,
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("label", { className: `${SpoolDetailsForm_default.printerLabel}${stockLocationError ? ` ${SpoolDetailsForm_default.printerLabelError}` : ""}`, children: stockLocationError ? t2("spoolForm.stockLocationRequired") : t2("spoolForm.stockLocation") }),
                  addingLoc ? /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.newLocRow, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
                      "input",
                      {
                        ref: newLocInputRef,
                        type: "text",
                        value: newLocName,
                        onChange: (e2) => setNewLocName(e2.target.value),
                        onKeyDown: (e2) => {
                          if (e2.key === "Enter") {
                            e2.preventDefault();
                            handleSaveLoc();
                          }
                          if (e2.key === "Escape") {
                            setAddingLoc(false);
                            setNewLocName("");
                          }
                        },
                        placeholder: t2("locations.namePlaceholder"),
                        className: SpoolDetailsForm_default.newLocInput,
                        autoFocus: true
                      }
                    ),
                    /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("button", { type: "button", className: SpoolDetailsForm_default.newLocSave, onClick: handleSaveLoc, disabled: savingLoc || !newLocName.trim(), children: /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("polyline", { points: "20 6 9 17 4 12" }) }) }),
                    /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("button", { type: "button", className: SpoolDetailsForm_default.newLocCancel, onClick: () => {
                      setAddingLoc(false);
                      setNewLocName("");
                    }, children: /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: [
                      /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
                      /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
                    ] }) })
                  ] }) : /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)(
                    "select",
                    {
                      value: stockLocation,
                      onChange: (e2) => {
                        if (e2.target.value === "__new__") {
                          setAddingLoc(true);
                          setNewLocName("");
                        } else setStockLocation(e2.target.value);
                      },
                      className: `${SpoolDetailsForm_default.printerSelect}${stockLocationError ? ` ${SpoolDetailsForm_default.printerSelectError}` : ""}`,
                      children: [
                        /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("option", { value: "", children: t2("spoolForm.selectStorage") }),
                        allLocOptions.map((loc) => /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("option", { value: loc, children: loc }, loc)),
                        /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("option", { value: "__new__", children: [
                          "\uFF0B ",
                          t2("locations.addLocation")
                        ] })
                      ]
                    }
                  )
                ]
              },
              stockLocationShakeKey
            ) : null })
          ] })
        ] }),
        hasPrintSettings && /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.section, children: [
          /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("button", { type: "button", className: SpoolDetailsForm_default.sectionToggleRow, onClick: () => setPrintOpen((o) => !o), children: [
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.sectionHeader, children: t2("spoolForm.sectionPrintSettings") }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
              "svg",
              {
                className: `${SpoolDetailsForm_default.chevron}${printOpen ? ` ${SpoolDetailsForm_default.chevronOpen}` : ""}`,
                width: "16",
                height: "16",
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                children: /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("polyline", { points: "6 9 12 15 18 9" })
              }
            )
          ] }),
          printOpen && /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.specGrid3, children: [
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.extruderMin"), value: f.extruderMin != null ? `${f.extruderMin} \xB0C` : null }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.extruderMax"), value: f.extruderMax != null ? `${f.extruderMax} \xB0C` : null }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.extruderRange"), value: f.extruderMin != null && f.extruderMax != null ? `${f.extruderMin}\u2013${f.extruderMax} \xB0C` : null }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.bedMin"), value: f.bedMin != null ? `${f.bedMin} \xB0C` : null }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.bedMax"), value: f.bedMax != null ? `${f.bedMax} \xB0C` : null }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.bedRange"), value: f.bedMin != null && f.bedMax != null ? `${f.bedMin}\u2013${f.bedMax} \xB0C` : null })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.section, children: [
          /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("button", { type: "button", className: SpoolDetailsForm_default.sectionToggleRow, onClick: () => setMaterialOpen((o) => !o), children: [
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.sectionHeader, children: t2("spoolForm.sectionMaterialProperties") }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
              "svg",
              {
                className: `${SpoolDetailsForm_default.chevron}${materialOpen ? ` ${SpoolDetailsForm_default.chevronOpen}` : ""}`,
                width: "16",
                height: "16",
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                children: /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("polyline", { points: "6 9 12 15 18 9" })
              }
            )
          ] }),
          materialOpen && /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.specGrid3, children: [
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.material"), value: f.material }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.density"), value: f.density != null ? `${f.density} g/cm\xB3` : null }),
            /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(SpecBox2, { label: t2("spoolForm.diameterTolerance"), value: f.diameterTolerance != null ? `\xB1${f.diameterTolerance} mm` : null })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.section, children: [
          /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.sectionHeader, children: t2("spoolForm.sectionNotes") }),
          /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(
            "textarea",
            {
              rows: 3,
              value: notes,
              onChange: (e2) => setNotes(e2.target.value),
              placeholder: t2("common.notesPlaceholder"),
              className: SpoolDetailsForm_default.textarea
            }
          )
        ] })
      ] }),
      submitError && /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("p", { className: SpoolDetailsForm_default.submitError, children: submitError }),
      /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)("div", { className: SpoolDetailsForm_default.footerRow, children: [
        /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("button", { type: "button", onClick: onCancel, className: SpoolDetailsForm_default.btnCancel, children: t2("common.cancel") }),
        /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("button", { type: "submit", disabled: submitting, className: SpoolDetailsForm_default.btnSave, children: submitting ? t2("common.saving") : t2("common.save") })
      ] })
    ] });
  }

  // src/components/SpoolFilterDropdown/SpoolFilterDropdown.tsx
  var import_react38 = __toESM(require_react_shim(), 1);

  // src/types/spoolFilters.ts
  var DEFAULT_FILTERS = {
    materials: [],
    brands: [],
    stockLevels: [],
    colors: [],
    activeOnly: false,
    lowStockOnly: false,
    archivedOnly: false,
    neverScanned: false
  };

  // src/components/SpoolFilterDropdown/SpoolFilterDropdown.module.css
  var SpoolFilterDropdown_default = {
    wrap: "SpoolFilterDropdown_wrap",
    trigger: "SpoolFilterDropdown_trigger",
    triggerActive: "SpoolFilterDropdown_triggerActive",
    triggerBadge: "SpoolFilterDropdown_triggerBadge",
    checkbox: "SpoolFilterDropdown_checkbox",
    dropdown: "SpoolFilterDropdown_dropdown",
    dropdownHeader: "SpoolFilterDropdown_dropdownHeader",
    dropdownHeaderLabel: "SpoolFilterDropdown_dropdownHeaderLabel",
    clearBtn: "SpoolFilterDropdown_clearBtn",
    scroll: "SpoolFilterDropdown_scroll",
    section: "SpoolFilterDropdown_section",
    sectionLabel: "SpoolFilterDropdown_sectionLabel",
    flagRow: "SpoolFilterDropdown_flagRow",
    flagRowLabel: "SpoolFilterDropdown_flagRowLabel",
    stockRow: "SpoolFilterDropdown_stockRow",
    stockDot: "SpoolFilterDropdown_stockDot",
    stockLabel: "SpoolFilterDropdown_stockLabel",
    pillGroup: "SpoolFilterDropdown_pillGroup",
    pill: "SpoolFilterDropdown_pill",
    pillActive: "SpoolFilterDropdown_pillActive",
    colorGrid: "SpoolFilterDropdown_colorGrid",
    colorSwatch: "SpoolFilterDropdown_colorSwatch",
    colorSwatchActive: "SpoolFilterDropdown_colorSwatchActive"
  };

  // src/components/SpoolFilterDropdown/SpoolFilterDropdown.tsx
  var import_jsx_runtime49 = __toESM(require_react_shim(), 1);
  var STOCK_LEVEL_KEYS = [
    { key: "full", tKey: "filters.full", color: "#22c55e" },
    { key: "good", tKey: "filters.good", color: "#34d399" },
    { key: "low", tKey: "filters.low", color: "#facc15" },
    { key: "critical", tKey: "filters.critical", color: "#ef4444" }
  ];
  function SpoolFilterDropdown({
    allMaterials,
    allBrands,
    allColors,
    filters,
    onChange
  }) {
    const { t: t2 } = useTranslation();
    const [open, setOpen] = (0, import_react38.useState)(false);
    const ref = (0, import_react38.useRef)(null);
    const activeCount = filters.materials.length + filters.brands.length + filters.stockLevels.length + filters.colors.length + (filters.activeOnly ? 1 : 0) + (filters.lowStockOnly ? 1 : 0) + (filters.archivedOnly ? 1 : 0) + (filters.neverScanned ? 1 : 0);
    (0, import_react38.useEffect)(() => {
      function handleClickOutside(e2) {
        if (ref.current && !ref.current.contains(e2.target)) setOpen(false);
      }
      document.addEventListener("mousedown", handleClickOutside);
      return () => document.removeEventListener("mousedown", handleClickOutside);
    }, []);
    function toggleItem(arr, item) {
      return arr.includes(item) ? arr.filter((x) => x !== item) : [...arr, item];
    }
    function toggleFlag(key) {
      onChange({ ...filters, [key]: !filters[key] });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { ref, className: SpoolFilterDropdown_default.wrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)(
        "button",
        {
          onClick: () => setOpen((o) => !o),
          className: `${SpoolFilterDropdown_default.trigger}${activeCount > 0 ? ` ${SpoolFilterDropdown_default.triggerActive}` : ""}`,
          "aria-label": t2("filters.filterSpools"),
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("polygon", { points: "22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3" }) }),
            activeCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("span", { className: SpoolFilterDropdown_default.triggerBadge, children: activeCount })
          ]
        }
      ),
      open && /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { className: SpoolFilterDropdown_default.dropdown, children: [
        /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { className: SpoolFilterDropdown_default.dropdownHeader, children: [
          /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("span", { className: SpoolFilterDropdown_default.dropdownHeaderLabel, children: t2("filters.header") }),
          activeCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("button", { onClick: () => onChange({ ...DEFAULT_FILTERS }), className: SpoolFilterDropdown_default.clearBtn, children: t2("filters.clearAll") })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { className: SpoolFilterDropdown_default.scroll, children: [
          /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { className: SpoolFilterDropdown_default.section, children: [
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("p", { className: SpoolFilterDropdown_default.sectionLabel, children: t2("filters.status") }),
            ["activeOnly", "lowStockOnly", "archivedOnly", "neverScanned"].map((key) => /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("label", { className: SpoolFilterDropdown_default.flagRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("input", { type: "checkbox", checked: !!filters[key], onChange: () => toggleFlag(key), className: SpoolFilterDropdown_default.checkbox }),
              /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("span", { className: SpoolFilterDropdown_default.flagRowLabel, children: t2(`filters.${key}`) })
            ] }, key))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { className: SpoolFilterDropdown_default.section, children: [
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("p", { className: SpoolFilterDropdown_default.sectionLabel, children: t2("filters.stockLevel") }),
            STOCK_LEVEL_KEYS.map(({ key, tKey, color }) => /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("label", { className: SpoolFilterDropdown_default.stockRow, children: [
              /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(
                "input",
                {
                  type: "checkbox",
                  checked: filters.stockLevels.includes(key),
                  onChange: () => onChange({ ...filters, stockLevels: toggleItem(filters.stockLevels, key) }),
                  className: SpoolFilterDropdown_default.checkbox
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("span", { className: SpoolFilterDropdown_default.stockDot, style: { backgroundColor: color } }),
              /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("span", { className: SpoolFilterDropdown_default.stockLabel, children: t2(tKey) })
            ] }, key))
          ] }),
          allMaterials.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { className: SpoolFilterDropdown_default.section, children: [
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("p", { className: SpoolFilterDropdown_default.sectionLabel, children: t2("filters.material") }),
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("div", { className: SpoolFilterDropdown_default.pillGroup, children: allMaterials.map((mat) => /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(
              "button",
              {
                onClick: () => onChange({ ...filters, materials: toggleItem(filters.materials, mat) }),
                className: `${SpoolFilterDropdown_default.pill}${filters.materials.includes(mat) ? ` ${SpoolFilterDropdown_default.pillActive}` : ""}`,
                children: mat
              },
              mat
            )) })
          ] }),
          allBrands.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { className: SpoolFilterDropdown_default.section, children: [
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("p", { className: SpoolFilterDropdown_default.sectionLabel, children: t2("filters.brand") }),
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("div", { className: SpoolFilterDropdown_default.pillGroup, children: allBrands.map((brand) => /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(
              "button",
              {
                onClick: () => onChange({ ...filters, brands: toggleItem(filters.brands, brand) }),
                className: `${SpoolFilterDropdown_default.pill}${filters.brands.includes(brand) ? ` ${SpoolFilterDropdown_default.pillActive}` : ""}`,
                children: brand
              },
              brand
            )) })
          ] }),
          allColors.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)("div", { className: SpoolFilterDropdown_default.section, children: [
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("p", { className: SpoolFilterDropdown_default.sectionLabel, children: t2("filters.color") }),
            /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("div", { className: SpoolFilterDropdown_default.colorGrid, children: allColors.map(({ hex, name }) => /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(
              "button",
              {
                onClick: () => onChange({ ...filters, colors: toggleItem(filters.colors, hex) }),
                title: name,
                "aria-label": name,
                className: `${SpoolFilterDropdown_default.colorSwatch}${filters.colors.includes(hex) ? ` ${SpoolFilterDropdown_default.colorSwatchActive}` : ""}`,
                style: { backgroundColor: hex }
              },
              hex
            )) })
          ] })
        ] })
      ] })
    ] });
  }

  // src/components/SpoolSearchBar/SpoolSearchBar.module.css
  var SpoolSearchBar_default = {
    wrap: "SpoolSearchBar_wrap",
    icon: "SpoolSearchBar_icon",
    input: "SpoolSearchBar_input",
    clearBtn: "SpoolSearchBar_clearBtn"
  };

  // src/components/SpoolSearchBar/SpoolSearchBar.tsx
  var import_jsx_runtime50 = __toESM(require_react_shim(), 1);
  function SpoolSearchBar({ value, onChange, placeholder }) {
    const { t: t2 } = useTranslation();
    const resolvedPlaceholder = placeholder ?? t2("search.placeholder");
    return /* @__PURE__ */ (0, import_jsx_runtime50.jsxs)("div", { className: SpoolSearchBar_default.wrap, children: [
      /* @__PURE__ */ (0, import_jsx_runtime50.jsxs)("svg", { className: SpoolSearchBar_default.icon, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
        /* @__PURE__ */ (0, import_jsx_runtime50.jsx)("circle", { cx: "11", cy: "11", r: "8" }),
        /* @__PURE__ */ (0, import_jsx_runtime50.jsx)("line", { x1: "21", y1: "21", x2: "16.65", y2: "16.65" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(
        "input",
        {
          type: "text",
          placeholder: resolvedPlaceholder,
          value,
          onChange: (e2) => onChange(e2.target.value),
          className: SpoolSearchBar_default.input
        }
      ),
      value && /* @__PURE__ */ (0, import_jsx_runtime50.jsx)("button", { onClick: () => onChange(""), className: SpoolSearchBar_default.clearBtn, "aria-label": t2("search.clear"), children: /* @__PURE__ */ (0, import_jsx_runtime50.jsxs)("svg", { className: "w-4 h-4", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
        /* @__PURE__ */ (0, import_jsx_runtime50.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
        /* @__PURE__ */ (0, import_jsx_runtime50.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
      ] }) })
    ] });
  }

  // src/components/UnassignedTagPanel/UnassignedTagPanel.tsx
  var import_react39 = __toESM(require_react_shim(), 1);

  // src/components/UnassignedTagPanel/UnassignedTagPanel.module.css
  var UnassignedTagPanel_default = {
    panel: "UnassignedTagPanel_panel",
    header: "UnassignedTagPanel_header",
    iconWrap: "UnassignedTagPanel_iconWrap",
    title: "UnassignedTagPanel_title",
    uid: "UnassignedTagPanel_uid",
    desc: "UnassignedTagPanel_desc",
    loading: "UnassignedTagPanel_loading",
    spinner: "UnassignedTagPanel_spinner",
    spin: "UnassignedTagPanel_spin",
    empty: "UnassignedTagPanel_empty",
    spoolList: "UnassignedTagPanel_spoolList",
    spoolCardWrap: "UnassignedTagPanel_spoolCardWrap",
    divider: "UnassignedTagPanel_divider",
    createBtn: "UnassignedTagPanel_createBtn"
  };

  // src/components/UnassignedTagPanel/UnassignedTagPanel.tsx
  var import_jsx_runtime51 = __toESM(require_react_shim(), 1);
  function UnassignedTagPanel({ tagUid, onAssign, onCreateNew }) {
    const { t: t2 } = useTranslation();
    const [spools, setSpools] = (0, import_react39.useState)([]);
    const [loading, setLoading] = (0, import_react39.useState)(true);
    (0, import_react39.useEffect)(() => {
      spoolsApi.getAll().then((data2) => {
        const unassigned = data2.filter((s) => !s.hasNfcTag && !s.isArchived);
        setSpools(unassigned);
      }).catch(() => {
      }).finally(() => setLoading(false));
    }, []);
    return /* @__PURE__ */ (0, import_jsx_runtime51.jsxs)("div", { className: UnassignedTagPanel_default.panel, children: [
      /* @__PURE__ */ (0, import_jsx_runtime51.jsxs)("div", { className: UnassignedTagPanel_default.header, children: [
        /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("div", { className: UnassignedTagPanel_default.iconWrap, children: /* @__PURE__ */ (0, import_jsx_runtime51.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
          /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("line", { x1: "5", y1: "7", x2: "5", y2: "17" }),
          /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("path", { d: "M8 9.5a4 4 0 0 1 0 5" }),
          /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("path", { d: "M11 8a7 7 0 0 1 0 8" }),
          /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("path", { d: "M14 6.5a9.5 9.5 0 0 1 0 11" })
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime51.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("p", { className: UnassignedTagPanel_default.title, children: t2("addSpool.unassignTagTitle") }),
          /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("p", { className: UnassignedTagPanel_default.uid, children: t2("addSpool.unassignTagUid", { uid: tagUid }) })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("p", { className: UnassignedTagPanel_default.desc, children: t2("addSpool.unassignTagDesc") }),
      loading ? /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("div", { className: UnassignedTagPanel_default.loading, children: /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("div", { className: UnassignedTagPanel_default.spinner }) }) : spools.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("p", { className: UnassignedTagPanel_default.empty, children: t2("addSpool.unassignTagNoSpools") }) : /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("div", { className: UnassignedTagPanel_default.spoolList, children: spools.map((spool) => /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("div", { className: UnassignedTagPanel_default.spoolCardWrap, onClick: () => onAssign(spool.id), children: /* @__PURE__ */ (0, import_jsx_runtime51.jsx)(SpoolCard, { spool }) }, spool.id)) }),
      /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("div", { className: UnassignedTagPanel_default.divider }),
      /* @__PURE__ */ (0, import_jsx_runtime51.jsxs)("button", { className: UnassignedTagPanel_default.createBtn, onClick: onCreateNew, children: [
        /* @__PURE__ */ (0, import_jsx_runtime51.jsxs)("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", children: [
          /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("line", { x1: "12", y1: "5", x2: "12", y2: "19" }),
          /* @__PURE__ */ (0, import_jsx_runtime51.jsx)("line", { x1: "5", y1: "12", x2: "19", y2: "12" })
        ] }),
        t2("addSpool.unassignTagCreateNew")
      ] })
    ] });
  }

  // src/components/scan/SpoolScanPopup/SpoolScanPopup.tsx
  var import_react40 = __toESM(require_react_shim(), 1);

  // src/components/scan/SpoolScanPopup/SpoolScanPopup.module.css
  var SpoolScanPopup_default = {
    spin: "SpoolScanPopup_spin",
    overlay: "SpoolScanPopup_overlay",
    modal: "SpoolScanPopup_modal",
    closeRow: "SpoolScanPopup_closeRow",
    closeBtn: "SpoolScanPopup_closeBtn",
    header: "SpoolScanPopup_header",
    headerInfo: "SpoolScanPopup_headerInfo",
    brandRow: "SpoolScanPopup_brandRow",
    brandLeft: "SpoolScanPopup_brandLeft",
    brandName: "SpoolScanPopup_brandName",
    colorName: "SpoolScanPopup_colorName",
    colorHex: "SpoolScanPopup_colorHex",
    brandLogo: "SpoolScanPopup_brandLogo",
    brandFallback: "SpoolScanPopup_brandFallback",
    progressWrap: "SpoolScanPopup_progressWrap",
    progressRow: "SpoolScanPopup_progressRow",
    progressBar: "SpoolScanPopup_progressBar",
    progressFill: "SpoolScanPopup_progressFill",
    divider: "SpoolScanPopup_divider",
    actions: "SpoolScanPopup_actions",
    errorMsg: "SpoolScanPopup_errorMsg",
    statusMsg: "SpoolScanPopup_statusMsg",
    spinnerWrap: "SpoolScanPopup_spinnerWrap",
    spinnerRing: "SpoolScanPopup_spinnerRing",
    btnPrimary: "SpoolScanPopup_btnPrimary",
    btnDanger: "SpoolScanPopup_btnDanger",
    btnSecondary: "SpoolScanPopup_btnSecondary"
  };

  // src/components/scan/SpoolScanPopup/SpoolScanPopup.tsx
  var import_jsx_runtime52 = __toESM(require_react_shim(), 1);
  var BRAND_DOMAINS5 = {
    "Bambu Lab": "bambulab.com",
    "eSUN 3D": "esun3d.com",
    "Prusament": "prusament.com",
    "Polymaker": "polymaker.com",
    "Hatchbox": "hatchbox3d.com",
    "SUNLU": "sunlu.com",
    "Creality": "creality.com",
    "ELEGOO": "elegoo.com",
    "Overture": "overture3d.com",
    "PolyLite": "polymaker.com",
    "colorFabb": "colorfabb.com",
    "Fillamentum": "fillamentum.com",
    "Fiberlogy": "fiberlogy.com",
    "extrudr": "extrudr.com",
    "Das Filament": "dasfilament.de",
    "Protopasta": "proto-pasta.com",
    "MatterHackers": "matterhackers.com",
    "NinjaTek": "ninjatek.com",
    "Atomic Filament": "atomicfilament.com",
    "Inland": "microcenter.com",
    "JAYO": "jayofilament.com",
    "TINMORRY": "tinmorry.com",
    "AzureFilm": "azurefilm.hr",
    "Spectrum": "spectrumfilaments.com",
    "Devil Design": "devildesign.pl",
    "Anycubic": "anycubic.com"
  };
  function BrandFavicon3({ brand }) {
    const [error, setError] = (0, import_react40.useState)(false);
    const domain = BRAND_DOMAINS5[brand];
    if (domain && !error) {
      return /* @__PURE__ */ (0, import_jsx_runtime52.jsx)(
        "img",
        {
          src: `https://www.google.com/s2/favicons?sz=64&domain=${domain}`,
          alt: brand,
          className: SpoolScanPopup_default.brandLogo,
          onError: () => setError(true)
        }
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("span", { className: SpoolScanPopup_default.brandFallback, children: brand[0] });
  }
  function SpoolScanPopup({ spool, onClose }) {
    const navigate = useNavigate();
    const { t: t2 } = useTranslation();
    const [loading, setLoading] = (0, import_react40.useState)(false);
    const [apiError, setApiError] = (0, import_react40.useState)(null);
    const [current, setCurrent] = (0, import_react40.useState)(spool);
    const [fetching, setFetching] = (0, import_react40.useState)(true);
    (0, import_react40.useEffect)(() => {
      apiClient.get(`/api/spools/getbyid/${spool.id}`).then((r) => setCurrent(r.data)).catch(() => {
      }).finally(() => setFetching(false));
    }, [spool.id]);
    const totalG = Math.max(1, current.initialWeightG);
    const remainingG = Math.max(0, current.currentWeightG);
    const pct = Math.round(remainingG / totalG * 100);
    const barColor = pct > 30 ? "#22c55e" : pct > 15 ? "#eab308" : "#ef4444";
    async function handleActivate() {
      setLoading(true);
      setApiError(null);
      try {
        await spoolsApi.activate(current.id);
        window.dispatchEvent(new CustomEvent("spools-updated"));
        navigate(`/spools/${current.id}`);
      } catch {
        setApiError(t2("scan.activateError"));
        setLoading(false);
      }
    }
    async function handleDeactivate() {
      setLoading(true);
      setApiError(null);
      try {
        await spoolsApi.deactivate(current.id);
        window.dispatchEvent(new CustomEvent("spools-updated"));
        navigate(`/spools/${current.id}`);
      } catch {
        setApiError(t2("scan.deactivateError"));
        setLoading(false);
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("div", { className: SpoolScanPopup_default.overlay, children: /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("div", { className: SpoolScanPopup_default.modal, children: [
      /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("div", { className: SpoolScanPopup_default.closeRow, children: /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("button", { onClick: onClose, className: SpoolScanPopup_default.closeBtn, children: /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: [
        /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
        /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
      ] }) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("div", { className: SpoolScanPopup_default.header, children: [
        /* @__PURE__ */ (0, import_jsx_runtime52.jsx)(SpoolIcon, { color: current.colorHex || "#94a3b8", size: 64 }),
        /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("div", { className: SpoolScanPopup_default.headerInfo, children: [
          /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("div", { className: SpoolScanPopup_default.brandRow, children: [
            /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("div", { className: SpoolScanPopup_default.brandLeft, children: [
              /* @__PURE__ */ (0, import_jsx_runtime52.jsx)(BrandFavicon3, { brand: current.brand }),
              /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("p", { className: SpoolScanPopup_default.brandName, children: current.brand })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime52.jsx)(MaterialTag, { material: current.material })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("h2", { className: SpoolScanPopup_default.colorName, children: current.colorName }),
          /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("p", { className: SpoolScanPopup_default.colorHex, children: current.colorHex })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("div", { className: SpoolScanPopup_default.progressWrap, children: [
        /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("div", { className: SpoolScanPopup_default.progressRow, children: [
          /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("span", { children: [
            remainingG,
            "g / ",
            totalG,
            "g"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("span", { children: [
            pct,
            "%"
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("div", { className: SpoolScanPopup_default.progressBar, children: /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("div", { className: SpoolScanPopup_default.progressFill, style: { width: `${pct}%`, backgroundColor: barColor } }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("div", { className: SpoolScanPopup_default.divider }),
      /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)("div", { className: SpoolScanPopup_default.actions, children: [
        apiError && /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("p", { className: SpoolScanPopup_default.errorMsg, children: apiError }),
        fetching ? /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("div", { className: SpoolScanPopup_default.spinnerWrap, children: /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("div", { className: SpoolScanPopup_default.spinnerRing }) }) : current.isActive ? /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)(import_jsx_runtime52.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("p", { className: SpoolScanPopup_default.statusMsg, children: t2("scan.alreadyActive") }),
          /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("button", { onClick: handleDeactivate, disabled: loading, className: SpoolScanPopup_default.btnDanger, children: loading ? t2("scan.deactivating") : t2("scan.deactivate") })
        ] }) : /* @__PURE__ */ (0, import_jsx_runtime52.jsxs)(import_jsx_runtime52.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("p", { className: SpoolScanPopup_default.statusMsg, children: t2("scan.notActive") }),
          /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("button", { onClick: handleActivate, disabled: loading, className: SpoolScanPopup_default.btnPrimary, children: loading ? t2("scan.activating") : t2("scan.activate") })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("button", { onClick: () => navigate(`/spools/${current.id}`), className: SpoolScanPopup_default.btnSecondary, children: t2("scan.viewDetails") })
      ] })
    ] }) });
  }

  // src/components/icons/InfoIcon.tsx
  var import_jsx_runtime53 = __toESM(require_react_shim(), 1);
  function InfoIcon({ className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime53.jsxs)("svg", { className, "aria-hidden": true, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
      /* @__PURE__ */ (0, import_jsx_runtime53.jsx)("circle", { cx: "12", cy: "12", r: "10" }),
      /* @__PURE__ */ (0, import_jsx_runtime53.jsx)("line", { x1: "12", y1: "8", x2: "12", y2: "8", strokeWidth: "2.5" }),
      /* @__PURE__ */ (0, import_jsx_runtime53.jsx)("line", { x1: "12", y1: "12", x2: "12", y2: "16" })
    ] });
  }

  // src/components/icons/UpdatesIcon.tsx
  var import_jsx_runtime54 = __toESM(require_react_shim(), 1);
  function UpdatesIcon({ className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime54.jsxs)("svg", { className, "aria-hidden": true, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
      /* @__PURE__ */ (0, import_jsx_runtime54.jsx)("polyline", { points: "23 4 23 10 17 10" }),
      /* @__PURE__ */ (0, import_jsx_runtime54.jsx)("polyline", { points: "1 20 1 14 7 14" }),
      /* @__PURE__ */ (0, import_jsx_runtime54.jsx)("path", { d: "M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20.49 15" })
    ] });
  }
  return __toCommonJS(index_exports);
})();
/*! Bundled license information:

use-sync-external-store/cjs/use-sync-external-store-shim.development.js:
  (**
   * @license React
   * use-sync-external-store-shim.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-router/dist/development/chunk-QUQL4437.mjs:
react-router/dist/development/index.mjs:
  (**
   * react-router v7.16.0
   *
   * Copyright (c) Remix Software Inc.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE.md file in the root directory of this source tree.
   *
   * @license MIT
   *)
*/
window.SpoolHub=SpoolHub.__dsMainNs?Object.assign({},SpoolHub,SpoolHub.__dsMainNs,{__dsMainNs:undefined}):SpoolHub;
