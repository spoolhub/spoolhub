PrintHistoryList from web. Use via `window.SpoolHub.PrintHistoryList` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Preview

```jsx
() => <PrintHistoryList jobs={[]} showSpool={false} />
```
