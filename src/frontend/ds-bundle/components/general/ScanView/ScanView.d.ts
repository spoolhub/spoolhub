import * as React from 'react';

/**
 * ScanView — from web@0.0.0.
 */
export interface ScanViewProps {
  [key: string]: unknown;
}

export declare const ScanView: React.ComponentType<ScanViewProps>;
