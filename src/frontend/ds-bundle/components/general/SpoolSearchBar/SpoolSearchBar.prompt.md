SpoolSearchBar from web. Use via `window.SpoolHub.SpoolSearchBar` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Preview

```jsx
() => <SpoolSearchBar value={""} onChange={null} placeholder={"Search spools..."} />
```
