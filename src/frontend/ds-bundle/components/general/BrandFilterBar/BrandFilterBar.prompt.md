BrandFilterBar from web. Use via `window.SpoolHub.BrandFilterBar` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Preview

```jsx
() => <BrandFilterBar brands={["Bambu Lab","eSUN","Prusa"]} selected={""} onChange={null} />
```
