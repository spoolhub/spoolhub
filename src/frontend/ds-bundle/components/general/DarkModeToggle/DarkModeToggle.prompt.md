DarkModeToggle from web. Use via `window.SpoolHub.DarkModeToggle` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Preview

```jsx
() => <DarkModeToggle isDark={false} onToggle={null} />
```
