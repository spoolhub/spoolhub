import * as React from 'react';

/**
 * DarkModeToggle — from web@0.0.0.
 */
export interface DarkModeToggleProps {
  [key: string]: unknown;
}

export declare const DarkModeToggle: React.ComponentType<DarkModeToggleProps>;
