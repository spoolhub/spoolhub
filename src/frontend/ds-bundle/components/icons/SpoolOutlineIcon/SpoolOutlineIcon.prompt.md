SpoolOutlineIcon from web. Use via `window.SpoolHub.SpoolOutlineIcon` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Preview

```jsx
() => <SpoolOutlineIcon className={"w-8 h-8"} />
```
