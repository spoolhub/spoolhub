ActiveSpoolIcon from web. Use via `window.SpoolHub.ActiveSpoolIcon` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Preview

```jsx
() => <ActiveSpoolIcon className={"w-8 h-8"} />
```
